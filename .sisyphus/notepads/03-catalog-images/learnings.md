## 2026-01-16 Task: catalog tests

- Catalog filters are exercised via data-testid selectors on search, brand, grid, and cards.
- Vitest config lives in `vitest.config.ts` and is invoked with `pnpm run test:unit --config vitest.config.ts`.
