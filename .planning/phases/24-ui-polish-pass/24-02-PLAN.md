---
phase: 24-ui-polish-pass
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "Top-of-page vault stats are easy to scan and use the same stat grid styling as the Stats tab"
    - "Primary navigation shows clear selected + focus states"
  artifacts:
    - path: src/App.tsx
      provides: "Hero header with tab navigation and vault stats"
      contains: "className=\"stats\""
  key_links:
    - from: src/App.tsx
      to: src/style.css
      via: "stats grid class hook"
      pattern: "className=\"stats-grid\""
---

<objective>
Make the hero header stats and nav hierarchy more consistent by reusing shared layout helpers, without changing any stable selectors.

Purpose: Support POLISH-01 by making key stats more readable and consistent with the rest of the UI.
Output: Updated `src/App.tsx` markup/class usage only (no selector changes).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-ui-polish-pass/24-RESEARCH.md
@src/App.tsx
@src/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reuse stats grid helper for the hero stats list</name>
  <files>src/App.tsx</files>
  <action>
In the hero stats section (`<section className="stats" ...>`), update the `<dl>` to use the shared stats grid helper:

- Add `className="stats-grid"` to the `<dl>`.
- Keep existing `id` values on the `<dd>` elements (`#enjoyment`, `#currency`, etc.) unchanged.
- Keep the `id="vault-stats-title"` and its `aria-labelledby` wiring unchanged.
  </action>
  <verify>pnpm run test:unit</verify>
  <done>Hero vault stats render with the same grid density and typography as other stats surfaces; existing ids remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Keep navigation selectors stable while enabling CSS selected styling</name>
  <files>src/App.tsx</files>
  <action>
Confirm the top nav buttons expose the state needed for styling, without changing selectors:

- Ensure `aria-selected` remains set on `.page-nav-link` buttons (it already exists); do not rename `id={`${tab.id}-tab`}`.
- Do not add/remove `data-testid` attributes.
- If any nav element currently blocks CSS selectors (e.g. missing `aria-selected` on some buttons), fix by adding ARIA only (no new ids).
  </action>
  <verify>pnpm run typecheck</verify>
  <done>CSS can style the selected tab via `.page-nav-link[aria-selected="true"]`, and all nav ids/testids are unchanged.</done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit`
</verification>

<success_criteria>
- Hero stats list uses `.stats-grid` and remains readable at typical widths (POLISH-01)
- Nav remains ARIA-correct and styleable without selector changes (POLISH-02/03)
</success_criteria>

<output>
After completion, create `.planning/phases/24-ui-polish-pass/24-02-SUMMARY.md`
</output>
