---
phase: 24-ui-polish-pass
plan: 03
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - src/ui/tabs/CollectionTab.tsx
  - src/ui/tabs/CareerTab.tsx
autonomous: true

must_haves:
  truths:
    - "Vault purchase CTAs read as primary actions and supporting metadata is visually de-emphasized"
    - "Career action button reads as a primary CTA when available"
  artifacts:
    - path: src/ui/tabs/CollectionTab.tsx
      provides: "Vault purchase list + upgrades panels"
      contains: "data-testid=\"collection-setup\""
    - path: src/ui/tabs/CareerTab.tsx
      provides: "Therapist career panel"
      contains: "data-testid=\"career-panel\""
  key_links:
    - from: src/ui/tabs/CareerTab.tsx
      to: src/style.css
      via: "primary button styling via default button styles"
      pattern: "data-testid=\"career-action\""
---

<objective>
Polish the early-game tabs (Vault + Career) to improve hierarchy and reduce clutter using low-risk class/markup tweaks.

Purpose: Deliver POLISH-01/02/04 in the highest-traffic surfaces without changing stable selectors.
Output: Updated `CollectionTab.tsx` and `CareerTab.tsx` with improved hierarchy and CTA emphasis.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-ui-polish-pass/24-RESEARCH.md
@src/ui/tabs/CollectionTab.tsx
@src/ui/tabs/CareerTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: De-emphasize secondary metadata in Vault cards without changing selectors</name>
  <files>src/ui/tabs/CollectionTab.tsx</files>
  <action>
Improve scanability by pushing non-primary metadata into the background:

- For the right-hand values in `.card-header` blocks (e.g. “{owned} owned”, “Level {level}”, “Unlocked/Locked”, “Active/Inactive”), add `className="muted"` so names/descriptions read as primary.
- Apply this pattern to the highest-volume lists:
  - Watch item cards in `#collection-list`
  - Upgrade cards in `#upgrade-list`
  - Events cards
  - Set bonus cards

Constraints:
- Do not change any existing `id` or `data-testid` attributes.
- Do not reorder buttons inside `.card-actions` for the watch cards (keep Buy buttons and testids stable).
  </action>
  <verify>pnpm run test:unit</verify>
  <done>Primary labels (names/descriptions) visually dominate; supporting counts/status labels are muted; all ids/testids unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Make the Career primary action read as a primary CTA</name>
  <files>src/ui/tabs/CareerTab.tsx</files>
  <action>
Adjust the “Run session” button to be the primary CTA when available:

- On the button with `data-testid="career-action"`, remove `className="secondary"` so it uses the default (primary) button styling.
- Keep the `data-testid` and disabled logic unchanged.
- Do not change the cooldown/status calculation logic.
  </action>
  <verify>pnpm run typecheck</verify>
  <done>The Career tab’s main action is visually prominent (primary styling) and remains correctly disabled/enabled.</done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit`
</verification>

<success_criteria>
- Vault and Career surfaces have clearer hierarchy (POLISH-01)
- Card metadata is less visually noisy while preserving structure (POLISH-02)
- Primary CTA styling is consistent with global micro-interactions (POLISH-04)
</success_criteria>

<output>
After completion, create `.planning/phases/24-ui-polish-pass/24-03-SUMMARY.md`
</output>
