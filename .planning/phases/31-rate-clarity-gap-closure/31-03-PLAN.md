---
phase: 31-rate-clarity-gap-closure
plan: 03
type: execute
wave: 3
depends_on:
  - 31-01
  - 31-02
files_modified:
  - tests/career-first-economy.unit.test.ts
  - tests/rate-breakdowns.unit.test.ts
  - tests/therapist.unit.test.tsx
  - tests/upgrades-preview.unit.test.tsx
  - .planning/REQUIREMENTS.md
autonomous: true
must_haves:
  truths:
    - "Automated tests assert that event multipliers affect both dollars/sec and cash accrual (sim ticks)."
    - "Automated tests assert that step() enjoyment accrual matches (enjoyment/sec after upgrades) * eventMultiplier over dt."
    - "Automated tests assert that buying an upgrade changes enjoyment/sec and the Upgrades preview reflects the delta."
    - "Planning traceability table no longer has duplicate WATCH-03 rows."
  artifacts:
    - path: tests/upgrades-preview.unit.test.tsx
      provides: "Unit coverage for upgrade enjoyment delta + preview"
      min_lines: 40
    - path: tests/therapist.unit.test.tsx
      provides: "Sim accrual tests: event->cash and upgrade->enjoyment (step() deltas)"
    - path: .planning/REQUIREMENTS.md
      provides: "Single WATCH-03 traceability entry"
  key_links:
    - from: tests/therapist.unit.test.tsx
      to: src/game/sim.ts
      via: "step() accrual math under events"
      pattern: "step\(.*\)"
    - from: tests/upgrades-preview.unit.test.tsx
      to: src/ui/tabs/UpgradesTab.tsx
      via: "renders upgrade preview deltas"
      pattern: "UpgradesTab"
---

<objective>
Lock in the Phase 31 fixes with automated tests (events affect cash + accrual; upgrades affect enjoyment + preview), and clean up REQUIREMENTS traceability tech debt.

Purpose: Prevent regressions and ensure the milestone audit gaps remain closed.
Output: Updated/new unit tests + REQUIREMENTS traceability cleanup.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v3.0-MILESTONE-AUDIT.md
@.planning/REQUIREMENTS.md

@tests/career-first-economy.unit.test.ts
@tests/rate-breakdowns.unit.test.ts
@tests/therapist.unit.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update existing unit tests to match new event-adjusted cash behavior</name>
  <files>
tests/career-first-economy.unit.test.ts
tests/rate-breakdowns.unit.test.ts
  </files>
  <action>
- Update tests/career-first-economy.unit.test.ts:
  - Keep the invariant: watches do not affect cash rate.
  - Update assertions to reflect the new behavior: when an event is active, the effective cash rate is multiplied by the event multiplier.
  - Recommended: compute eventMultiplier via getEventIncomeMultiplier(stateWithWatches, nowMs) and assert:
    - getEffectiveCashRateCentsPerSec(stateWithWatches, eventMultiplier) === getTotalCashRateCentsPerSec(baseState) * eventMultiplier (within tolerance)
    - getTotalCashRateCentsPerSec(stateWithWatches) still matches baseState (watches ignored)
- Update tests/rate-breakdowns.unit.test.ts:
  - Extend/adjust coverage so cash breakdown can accept eventMultiplier and totals match the effective cash rate.
  - When eventMultiplier != 1, assert the cash breakdown includes an explicit event multiplier term (mirrors enjoyment breakdown):
    - breakdown.multiplierTerms contains term with id === "event"
    - that term.multiplier === eventMultiplier
  </action>
  <verify>pnpm run test:unit</verify>
  <done>
- Existing unit tests reflect the new event-adjusted cash rules and pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add sim-level accrual tests (event->cash with rounding, upgrade->enjoyment under events)</name>
  <files>
tests/therapist.unit.test.tsx
  </files>
  <action>
- In tests/therapist.unit.test.tsx, add TWO new sim-level tests:

  - Update imports from ../src/game/state as needed (keep test file style consistent):
    - applyEventState
    - getCollectionValueCents
    - getEventIncomeMultiplier
    - getEnjoymentRateCentsPerSec

  A) Event multiplier affects cash accrual (rounding-safe):
  1) Seed a state with a known therapist cash rate (e.g., therapistCareer.level = 2).
  2) Seed an active event (state.eventStates[eventId] with activeUntilMs > nowMs and incomeMultiplier = 1.15 or 2.0).
  3) Compute eventMultiplier deterministically the same way the sim does:
     - withEvents = applyEventState(seededState, nowMs, getCollectionValueCents(seededState))
     - eventMultiplier = getEventIncomeMultiplier(withEvents, nowMs)
  4) Call nextState = step(seededState, 1_000, nowMs).
  5) Assert the currency delta matches the expected value without brittle exact-equality:
     - expected = getTotalCashRateCentsPerSec(withEvents) * eventMultiplier
     - actualDelta = nextState.currencyCents - seededState.currencyCents
     - Use a tolerance/range to allow rounding/truncation (e.g. expect(actualDelta).toBeCloseTo(expected, 8) OR expect(actualDelta).toBeGreaterThanOrEqual(expected - 1) and <= expected + 1).

  B) step() enjoyment accrual matches (enjoyment/sec after upgrades) * eventMultiplier:
  1) Seed a state with enough owned watches so enjoyment/sec > 0.
  2) Create two otherwise-identical states:
     - withoutUpgrade (upgrade level 0)
     - withUpgrade (set upgrades["polishing-tools"] = 1)
  3) Seed the same active event on both states (as in test A) and compute eventMultiplier using applyEventState + getEventIncomeMultiplier.
  4) Assert upgrades actually change enjoyment/sec in selectors (guardrail for the test's premise):
     - getEnjoymentRateCentsPerSec(withUpgrade) !== getEnjoymentRateCentsPerSec(withoutUpgrade)
  5) Call step(withUpgrade, 1_000, nowMs) and assert:
     - actualEnjoymentDelta = next.enjoymentCents - withUpgrade.enjoymentCents
     - expectedEnjoymentDelta = getEnjoymentRateCentsPerSec(withEventsWithUpgrade) * eventMultiplier
     - Compare using toBeCloseTo/tolerance to avoid float brittleness.

- Keep both tests deterministic by using fixed nowMs, fixed event state, and dtMs = 1_000.
  </action>
  <verify>pnpm run test:unit</verify>
  <done>
- There is an automated test that covers event multiplier -> cash accrual using step(), without brittle equality.
- There is an automated test that covers step() enjoyment accrual matching (selector enjoyment/sec after upgrades) * eventMultiplier.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add an upgrade preview test and fix REQUIREMENTS WATCH-03 duplicate</name>
  <files>
tests/upgrades-preview.unit.test.tsx
.planning/REQUIREMENTS.md
  </files>
  <action>
- Create tests/upgrades-preview.unit.test.tsx (Testing Library style, matching existing conventions):
  - Seed a state with at least one owned watch model so enjoyment/sec is > 0.
  - Seed enough currency to buy the first upgrade.
  - Render UpgradesTab as active with real upgrades/workshopUpgrades/maisonUpgrades lists.
  - Assert an upgrade card shows an Enjoyment delta chip that is non-zero (e.g., text contains "Enjoyment" and a "+").
  - Assert the preview details show different before vs after enjoyment rates.
  - Assert no cash delta chip is shown for that upgrade (cash should remain career-driven).
- In .planning/REQUIREMENTS.md traceability table:
  - Collapse the duplicated WATCH-03 rows (Pending + Complete) into a single Complete row.
  - Keep the table sorted and consistent with surrounding entries.
  </action>
  <verify>pnpm run test:unit</verify>
  <done>
- Upgrade purchase has automated coverage proving it changes enjoyment/sec and the preview reflects the delta.
- REQUIREMENTS traceability contains only one WATCH-03 row with Status = Complete.
  </done>
</task>

</tasks>

<verification>
- pnpm run typecheck
- pnpm run test:unit
</verification>

<success_criteria>
1. Tests cover: (a) event multiplier affects cash/sec + cash accrual, (b) buying an upgrade changes enjoyment/sec and the preview reflects it.
2. REQUIREMENTS traceability has no duplicate WATCH-03 entries.
</success_criteria>

<output>
After completion, create `.planning/phases/31-rate-clarity-gap-closure/31-03-SUMMARY.md`
</output>
