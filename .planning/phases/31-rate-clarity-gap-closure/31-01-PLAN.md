---
phase: 31-rate-clarity-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/sim.ts
  - src/game/selectors/index.ts
  - src/App.tsx
  - src/ui/tabs/StatsTab.tsx
autonomous: true
must_haves:
  truths:
    - "When an event is active, dollars/sec shown in the header and Stats matches the cash actually accrued by the sim."
    - "Event multiplier applies consistently to both enjoyment accrual and cash accrual."
  artifacts:
    - path: src/game/sim.ts
      provides: "Cash accrual uses event multiplier during step()"
      contains: "earnedCents"
    - path: src/game/selectors/index.ts
      provides: "Selector helpers for effective cash rate + cash breakdown exposes event multiplier term"
    - path: src/App.tsx
      provides: "Header stats dollars/sec uses eventMultiplier"
    - path: src/ui/tabs/StatsTab.tsx
      provides: "Cash rate breakdown reflects eventMultiplier"
  key_links:
    - from: src/game/sim.ts
      to: src/game/selectors/index.ts
      via: "effective cash rate helper"
      pattern: "get(E|e)ffectiveCashRateCentsPerSec"
    - from: src/App.tsx
      to: src/game/selectors/index.ts
      via: "eventMultiplier applied to dollars/sec display"
      pattern: "getEventIncomeMultiplier\(state, nowMs\)"
---

<objective>
Make events multiply cash accrual and displayed dollars/sec, so the cash rate UI matches what the sim actually accrues.

Purpose: Close the milestone audit gap where events multiply enjoyment only, leaving dollars/sec and cash accrual inconsistent.
Output: Sim cash accrual uses eventMultiplier; App header + Stats cash breakdown show the same effective cash rate.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v3.0-MILESTONE-AUDIT.md

@src/game/sim.ts
@src/game/selectors/index.ts
@src/App.tsx
@src/ui/tabs/StatsTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply event multiplier to cash accrual via a shared selector helper</name>
  <files>
src/game/selectors/index.ts
src/game/sim.ts
  </files>
  <action>
- In src/game/selectors/index.ts, add a small helper for event-adjusted cash rate.
  - Create: getEffectiveCashRateCentsPerSec(state: GameState, eventMultiplier = 1): number.
  - Implementation: getTotalCashRateCentsPerSec(state) * eventMultiplier.
  - Keep cash career-driven: do NOT include UPGRADES/workshop/maison/etc in getTotalCashRateCentsPerSec.
- In src/game/sim.ts:
  - Keep computing eventMultiplier via getEventIncomeMultiplier(withEvents, nowMs).
  - Change cash accrual to use getEffectiveCashRateCentsPerSec(withEvents, eventMultiplier) instead of getTotalCashRateCentsPerSec(withEvents).
  - Preserve purity rules: do not introduce Date.now usage in selectors/actions; sim already accepts nowMs.
  </action>
  <verify>pnpm run typecheck</verify>
  <done>
- Sim cash accrual is multiplied by the active event multiplier.
- getTotalCashRateCentsPerSec remains career-only (salary-driven).
  </done>
</task>

<task type="auto">
  <name>Task 2: Make dollars/sec display + breakdown use event-adjusted cash rate</name>
  <files>
src/game/selectors/index.ts
src/App.tsx
src/ui/tabs/StatsTab.tsx
  </files>
  <action>
- In src/game/selectors/index.ts:
  - Update getCashRateBreakdown to accept eventMultiplier (default 1), mirroring getEnjoymentRateBreakdown's explicit multiplier terms:
    - Signature: getCashRateBreakdown(state: GameState, eventMultiplier = 1)
    - Update CashRateBreakdown shape to include multiplierTerms + eventMultiplier:
      - Add: multiplierTerms: RateBreakdownMultiplierTerm[]
      - Add: eventMultiplier: number
    - multiplierTerms MUST include an explicit event term (id: "event", label: "Event", multiplier: eventMultiplier).
      - It is acceptable to always include the event term (even when multiplier === 1).
    - totalCentsPerSec MUST be the effective rate (career salary * eventMultiplier):
      - totalCentsPerSec === getEffectiveCashRateCentsPerSec(state, eventMultiplier)
    - Keep cash career-driven: careerAddends stays salary-only; do not add watch/item income; do not incorporate upgrades into cash.
- In src/App.tsx:
  - In the header stats memo, compute cashRate using getEffectiveCashRateCentsPerSec(state, eventMultiplier).
  - Keep enjoymentRate behavior unchanged (still eventMultiplier applied).
- In src/ui/tabs/StatsTab.tsx:
  - Pass currentEventMultiplier into getCashRateBreakdown(state, currentEventMultiplier).
  - Render the cash breakdown event multiplier term when currentEventMultiplier !== 1 (same conditional behavior as enjoyment breakdown, if applicable).
  - Ensure the summary value displayed in the cash breakdown matches header dollars/sec for the same state.
  </action>
  <verify>pnpm run test:unit</verify>
  <done>
- Dollars/sec in App header and Stats cash breakdown reflect the active event multiplier.
- Stats cash breakdown total matches the same effective cash rate used by the sim.
- Stats cash breakdown includes an explicit multiplier term with id "event" (and uses it when currentEventMultiplier !== 1).
  </done>
</task>

</tasks>

<verification>
- pnpm run typecheck
- pnpm run test:unit
</verification>

<success_criteria>
1. With an active event, cash accrual (sim) and dollars/sec display (header + Stats) use the same event-adjusted cash rate.
2. Event multiplier now applies consistently to both enjoyment and cash.
</success_criteria>

<output>
After completion, create `.planning/phases/31-rate-clarity-gap-closure/31-01-SUMMARY.md`
</output>
