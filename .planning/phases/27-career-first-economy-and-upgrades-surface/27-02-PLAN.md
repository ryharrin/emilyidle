---
phase: 27-career-first-economy-and-upgrades-surface
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - src/game/selectors/index.ts
  - src/game/actions/index.ts
  - src/game/sim.ts
  - src/ui/tabs/StatsTab.tsx
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "Cash / sec is derived from career salary (no passive cash from watch ownership)"
    - "Events do not affect cash generation"
    - "First therapist session after a reset costs 0 enjoyment; later sessions cost enjoyment"
  artifacts:
    - path: src/game/selectors/index.ts
      provides: "Career-driven cash rate + session policy selectors"
      contains: "getTotalCashRateCentsPerSec"
    - path: src/game/actions/index.ts
      provides: "Therapist session action honors free-first rule and track policies"
      contains: "performTherapistSession"
    - path: src/game/sim.ts
      provides: "Simulation tick uses career cash rate without event multiplier"
      contains: "getTotalCashRateCentsPerSec"
    - path: src/ui/tabs/StatsTab.tsx
      provides: "Cash rate breakdown reflects career-only cash"
      contains: "cash-rate-breakdown"
  key_links:
    - from: src/game/sim.ts
      to: src/game/selectors/index.ts
      via: "cash income computation"
      pattern: "getTotalCashRateCentsPerSec"
    - from: src/ui/tabs/StatsTab.tsx
      to: src/game/selectors/index.ts
      via: "getCashRateBreakdown"
      pattern: "getCashRateBreakdown"
---

<objective>
Make the cash economy career-first and implement track-aware therapist session rules, including the free-first-session policy.

Purpose: Phase 27 requires that cash comes from career progression and upgrades are evaluated against that reality. This plan rewires simulation + selectors to remove passive watch cash and ensures therapist sessions follow the new cost rules.
Output: Updated cash-rate computations, updated stats breakdown wiring, and updated therapist session gating/action logic.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-career-first-economy-and-upgrades-surface/27-CONTEXT.md
@src/game/selectors/index.ts
@src/game/actions/index.ts
@src/game/sim.ts
@src/App.tsx
@src/ui/tabs/StatsTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire cash generation to be career-only (no events multiplier)</name>
  <files>
src/game/selectors/index.ts
src/game/sim.ts
src/App.tsx
src/ui/tabs/StatsTab.tsx
  </files>
  <action>
Implement ECON-01/ECON-02 cash rules:

- Change `getTotalCashRateCentsPerSec` to return only the career salary cash/sec.
  - Remove the vault/item income component from the cash rate.
  - Remove the `eventMultiplier` parameter from cash-rate APIs OR ensure it is ignored (choose one and apply consistently).

- Update `getCashRateBreakdown` to reflect the new model:
  - No vault addends/softcap math in the cash breakdown.
  - Present the career salary as the primary (and only) cash/sec term.
  - Ensure the displayed total equals the rate used by `step()`.

- Update `src/game/sim.ts` so the cash income uses the new cash-rate API and is unaffected by events.
  - Keep event multiplier for enjoyment only.

- Update `src/App.tsx` stats computation so cash rate display uses the career-only cash rate, unaffected by event multiplier.

- Update `src/ui/tabs/StatsTab.tsx` calls so the cash breakdown does not depend on `currentEventMultiplier` anymore.

Do not remove watch income fields or refactor item definitions in this plan; the goal is to stop using them for cash generation.
  </action>
  <verify>pnpm run typecheck && pnpm run test:unit</verify>
  <done>Cash / sec is non-zero due to career salary and does not change when an event multiplier changes.</done>
</task>

<task type="auto">
  <name>Task 2: Implement track-aware therapist sessions with free-first-session policy</name>
  <files>
src/game/selectors/index.ts
src/game/actions/index.ts
  </files>
  <action>
Implement CAREER-02 session rules and track policies:

- Add selectors to determine:
  - Whether the active track supports sessions.
  - The session enjoyment cost/payout/cooldown for the current career + track.
  - A user-facing session cost label that reflects the free-first rule.

- Update `canPerformTherapistSession`:
  - Return false if the active track does not support sessions.
  - If `freeSessionAvailable` is true, ignore the enjoyment cost check (still respect cooldown).
  - Otherwise, require `state.enjoymentCents >= cost`.

- Update `performTherapistSession`:
  - If `freeSessionAvailable` is true, spend 0 enjoyment and set it to false after the session.
  - Otherwise, spend the computed enjoyment cost.
  - Cooldown varies by track (use the Phase 27 context direction: track-based cooldown).

- Ensure resets set `freeSessionAvailable: true` for the first session after reset.
  - At minimum, do this on `prestigeNostalgia` reset (existing reset hook).

Keep actions/selectors pure (no Date.now inside selectors; accept `nowMs` args as existing patterns).
  </action>
  <verify>pnpm run typecheck && pnpm run test:unit</verify>
  <done>First session after a reset costs 0 enjoyment and subsequent sessions cost enjoyment; non-session tracks cannot run sessions.</done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit`
</verification>

<success_criteria>
- ECON-01: cash/sec comes from career salary only.
- ECON-02: owning watches does not generate passive cash.
- CAREER-02: first session free after reset; later sessions spend enjoyment; rule enforced by selectors/actions.
</success_criteria>

<output>
After completion, create `.planning/phases/27-career-first-economy-and-upgrades-surface/27-02-SUMMARY.md`
</output>
