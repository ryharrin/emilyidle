---
phase: 27-career-first-economy-and-upgrades-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/data/career.ts
  - src/game/model/types.ts
  - src/game/model/state.ts
  - src/game/persistence.ts
autonomous: true

must_haves:
  truths:
    - "Career progression supports 3 tracks with one active track at a time"
    - "Career state persists across reloads and resets cleanly on prestige"
    - "Career has a points-and-nodes representation suitable for a tree UI"
  artifacts:
    - path: src/game/data/career.ts
      provides: "Career track + node definitions"
      contains: "export const CAREER_TRACKS"
    - path: src/game/model/types.ts
      provides: "TherapistCareerState includes track + points + nodes + session-free marker"
      contains: "TherapistCareerState"
    - path: src/game/model/state.ts
      provides: "Initial career state + prestige reset wiring"
      contains: "therapistCareer"
    - path: src/game/persistence.ts
      provides: "Save decode/encode includes career fields"
      contains: "therapistCareer"
  key_links:
    - from: src/game/data/career.ts
      to: src/game/model/types.ts
      via: "track/node ids"
      pattern: "CareerTrackId"
    - from: src/game/persistence.ts
      to: src/game/model/state.ts
      via: "createStateFromSave"
      pattern: "createStateFromSave"
---

<objective>
Add the career domain primitives (tracks + progression tree representation) and persist them in saves.

Purpose: Phase 27 needs career-first cash and specialization depth. This plan establishes the underlying data model and persistence so later plans can implement cash rules, sessions, and UI without guesswork.
Output: Career track/node definitions, extended GameState types, state initialization/reset wiring, and save compatibility.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-career-first-economy-and-upgrades-surface/27-CONTEXT.md
@src/game/model/types.ts
@src/game/model/state.ts
@src/game/persistence.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define career tracks + progression nodes (data-only)</name>
  <files>
src/game/data/career.ts
  </files>
  <action>
Create a new career data module with:

- `CareerTrackId` (3 tracks): `private-practice`, `va-hospital`, `research-teaching`.
- `CareerNodeId` string ids for progression nodes.
- `CAREER_TRACKS`: metadata per track including whether sessions exist.
- `CAREER_NODES`: a deterministic list representing a tree (each node has: id, trackId (or "core"), label, description, costPoints, and prerequisites).

Constraints from Phase 27 context:
- One active track at a time (switchable).
- Branching starts after a few levels (implement as a `TRACK_CHOICE_UNLOCK_LEVEL` constant; default to 3).
- Respec is free (tree representation must support full refund).

Keep this file pure/static (no selectors, no state mutation, no Date.now).
  </action>
  <verify>pnpm run typecheck</verify>
  <done>`src/game/data/career.ts` exports stable ids and deterministic track/node definitions suitable for a UI tree.</done>
</task>

<task type="auto">
  <name>Task 2: Extend GameState career fields and persist them in saves</name>
  <files>
src/game/model/types.ts
src/game/model/state.ts
src/game/persistence.ts
  </files>
  <action>
Update the domain model to store specialization and session-free state:

- Extend `TherapistCareerState` (in `src/game/model/types.ts`) to include:
  - `activeTrackId: CareerTrackId | null`
  - `pointsAvailable: number`
  - `spentNodes: Record<CareerNodeId, boolean>` (or an array of node ids; prefer a record for O(1) reads)
  - `freeSessionAvailable: boolean` (used by Phase 27 rule: first session after reset costs 0 enjoyment)

- Update `GameState` and `PersistedGameState` shapes accordingly.

- Update `src/game/model/state.ts`:
  - `createInitialState` initializes the new fields with safe defaults.
  - `createStateFromSave` sanitizes missing/invalid fields (backwards-compatible).

- Update `src/game/persistence.ts` sanitize/decode logic to accept and clamp the new fields.

Do NOT change gameplay behavior in this plan beyond state shape/persistence (no cash-rate math here).
  </action>
  <verify>pnpm run typecheck && pnpm run test:unit</verify>
  <done>Career state is present in fresh state, loads from older saves without crashing, and round-trips through export/import.</done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit`
</verification>

<success_criteria>
- Career tracks and nodes exist as deterministic data.
- GameState can represent: active track, spent nodes, available points, and free-session marker.
- Save load remains backwards compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/27-career-first-economy-and-upgrades-surface/27-01-SUMMARY.md`
</output>
