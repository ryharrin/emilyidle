---
phase: 27-career-first-economy-and-upgrades-surface
plan: 03
type: execute
wave: 3
depends_on: ["27-02"]
files_modified:
  - src/App.tsx
  - src/ui/tabs/CareerTab.tsx
  - src/ui/components/CareerTree.tsx
  - src/style.css
autonomous: true

must_haves:
  truths:
    - "Career is visible and usable from the beginning of a fresh save"
    - "User can choose an active track and spend/refund points in a progression tree"
    - "Session cost rule is visible pre-commit (including the free-first-session note)"
  artifacts:
    - path: src/App.tsx
      provides: "Career tab is visible on fresh save"
      contains: "career: true"
    - path: src/ui/tabs/CareerTab.tsx
      provides: "Career track + tree UI and session action surface"
      contains: "data-testid=\"career-panel\""
    - path: src/ui/components/CareerTree.tsx
      provides: "Reusable progression tree renderer"
      contains: "export function CareerTree"
  key_links:
    - from: src/ui/tabs/CareerTab.tsx
      to: src/game/state.ts
      via: "selectors/actions for track + nodes + sessions"
      pattern: "getTherapist"
---

<objective>
Make the career system visible from the start and provide a progression-tree UI with track switching, free respec, and clear session cost messaging.

Purpose: CAREER-01 requires early usability + depth; CAREER-02 requires visible rules before committing a session. This plan builds the player-facing Career surface.
Output: Career tab visible on fresh saves, progression tree UI, and updated session panel messaging.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-career-first-economy-and-upgrades-surface/27-CONTEXT.md
@src/App.tsx
@src/ui/tabs/CareerTab.tsx
@src/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make Career visible from the beginning</name>
  <files>
src/App.tsx
  </files>
  <action>
Update tab visibility rules so Career is available immediately on a fresh save.

- In the `tabVisibility` memo, change `career` visibility from milestone-gated to always true.
- Ensure hideable tab settings still work (career should remain hideable if configured).

Also update any copy that now contradicts the career-first economy:
- In coachmarks that claim watches "earn cash", adjust wording so watches are enjoyment/memories and career is cash.

Do not change the default landing tab in this phase (Phase 26 governs Catalog-first landing).
  </action>
  <verify>pnpm run typecheck</verify>
  <done>Career tab appears in the top navigation on a fresh save, without needing to unlock `collector-shelf`.</done>
</task>

<task type="auto">
  <name>Task 2: Implement track + progression tree UI and session cost messaging</name>
  <files>
src/ui/tabs/CareerTab.tsx
src/ui/components/CareerTree.tsx
src/style.css
  </files>
  <action>
Implement the Career panel UX from Phase 27 context:

- Add a progression tree UI:
  - Render nodes from the new career data definitions.
  - Show available points.
  - Clicking a node spends points if prerequisites met; add a disabled/locked visual state when not.
  - Include a "Respec" control that refunds all points and clears spent nodes (free respec).

- Add track selection UI:
  - Show 3 tracks.
  - Allow switching active track (one at a time).
  - Gate track selection until `TRACK_CHOICE_UNLOCK_LEVEL` (default 3). Before that, show the tracks but disabled with a "Unlocks at level X" hint.

- Sessions panel:
  - Only show an enabled "Run session" button when the active track supports sessions.
  - Pre-commit messaging: inline copy that explicitly states when the next session costs 0 enjoyment (free-first) vs the normal cost.
  - Keep the existing disabled reasons pattern (cooldown vs need enjoyment) but make it accurate under the free-first rule.

Implementation constraints:
- Prefer reusing existing card/panel styles; keep selectors stable (keep `data-testid="career-panel"`, `data-testid="career-action"`, and `data-testid="career-status"`).
- Add minimal CSS for the tree layout in `src/style.css` (responsive; stack on narrow screens).
  </action>
  <verify>pnpm run typecheck && pnpm run test:unit</verify>
  <done>Career tab supports: track selection, point spending, free respec, and clear session cost messaging before clicking Run session.</done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit`
</verification>

<success_criteria>
- CAREER-01: career is usable early and offers track depth.
- CAREER-02: free-first session rule is visible before commit and accurately reflected in the session panel.
</success_criteria>

<output>
After completion, create `.planning/phases/27-career-first-economy-and-upgrades-surface/27-03-SUMMARY.md`
</output>
