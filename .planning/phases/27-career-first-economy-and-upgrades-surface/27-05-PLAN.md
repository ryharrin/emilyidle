---
phase: 27-career-first-economy-and-upgrades-surface
plan: 05
type: execute
wave: 5
depends_on: ["27-04"]
files_modified:
  - tests/career-first-economy.unit.test.ts
  - tests/career-upgrades.spec.ts
autonomous: true

must_haves:
  truths:
    - "Automated tests confirm cash is career-only and session free-first rule works"
    - "E2E covers Career visibility and Upgrades tab presence with stable selectors"
  artifacts:
    - path: tests/career-first-economy.unit.test.ts
      provides: "Unit coverage for cash rate + session rules"
      contains: "career-first"
    - path: tests/career-upgrades.spec.ts
      provides: "Playwright coverage for Career + Upgrades navigation"
      contains: "upgrades"
  key_links:
    - from: tests/career-upgrades.spec.ts
      to: src/App.tsx
      via: "tab navigation"
      pattern: "role=tab"
---

<objective>
Add unit and e2e coverage for the Phase 27 economy shift and new surfaces (Career and Upgrades).

Purpose: Phase 27 is a high-impact economy change; tests prevent regressions (cash accidentally coming from watch ownership, broken session rules, missing navigation).
Output: Unit tests for selectors/actions and Playwright flows for navigation and upgrade previews.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/game/selectors/index.ts
@src/game/actions/index.ts
@src/ui/tabs/CareerTab.tsx
@src/ui/tabs/UpgradesTab.tsx
@tests
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unit coverage for career-first cash + session rules</name>
  <files>
tests/career-first-economy.unit.test.ts
  </files>
  <action>
Create a new Vitest unit file that asserts:

- `getTotalCashRateCentsPerSec` is unaffected by owned watches and by event multiplier changes.
- `getCashRateBreakdown.totalCentsPerSec` matches `getTotalCashRateCentsPerSec`.
- `canPerformTherapistSession` + `performTherapistSession` honor:
  - free-first-session after reset (`freeSessionAvailable` toggles off after first session)
  - non-session tracks cannot run sessions
  - cooldown blocks sessions

Keep tests stable and deterministic; avoid reliance on Date.now by passing explicit `nowMs` values.
  </action>
  <verify>pnpm run test:unit</verify>
  <done>Unit tests cover ECON-01/02 and CAREER-02 invariants with clear failures when broken.</done>
</task>

<task type="auto">
  <name>Task 2: Add Playwright coverage for Career visibility + Upgrades surface</name>
  <files>
tests/career-upgrades.spec.ts
  </files>
  <action>
Create a new Playwright spec that verifies:

1. Fresh app load shows a Career tab in the top nav.
2. Career tab panel renders (`data-testid="career-panel"`).
3. Upgrades tab exists and opens (`data-testid="upgrades-panel"`).
4. Upgrade cards show preview delta chips (assert at least one upgrade card contains a "Before"/"After" or delta marker).

Keep selectors stable:
- Prefer `role="tab"` + tab label text or existing `data-testid` hooks.
- Do not introduce brittle CSS selectors.
  </action>
  <verify>pnpm run test:e2e</verify>
  <done>Playwright validates navigation to Career and Upgrades and basic presence of preview UI.</done>
</task>

</tasks>

<verification>
- `pnpm run test:unit`
- `pnpm run test:e2e`
</verification>

<success_criteria>
- Automated checks cover the economy shift and core UI surfaces.
- Tests use stable selectors and resist layout refactors.
</success_criteria>

<output>
After completion, create `.planning/phases/27-career-first-economy-and-upgrades-surface/27-05-SUMMARY.md`
</output>
