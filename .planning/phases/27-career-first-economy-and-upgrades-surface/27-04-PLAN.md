---
phase: 27-career-first-economy-and-upgrades-surface
plan: 04
type: execute
wave: 4
depends_on: ["27-03"]
files_modified:
  - src/App.tsx
  - src/ui/tabs/UpgradesTab.tsx
  - src/ui/tabs/CollectionTab.tsx
  - src/style.css
autonomous: true

must_haves:
  truths:
    - "Upgrades are accessible from a dedicated top-level tab"
    - "Upgrades are grouped by system (cash/career, workshop, maison)"
    - "Each upgrade preview shows the before/after effect on cash/enjoyment rates before purchase"
  artifacts:
    - path: src/ui/tabs/UpgradesTab.tsx
      provides: "Dedicated upgrades surface with preview deltas"
      contains: "data-testid=\"upgrades-panel\""
    - path: src/App.tsx
      provides: "Navigation includes upgrades tab"
      contains: "{ id: \"upgrades\""
    - path: src/ui/tabs/CollectionTab.tsx
      provides: "Vault no longer duplicates the primary upgrades surface"
      contains: "upgrade-list"
  key_links:
    - from: src/ui/tabs/UpgradesTab.tsx
      to: src/game/actions/index.ts
      via: "buy* actions used for previews + purchases"
      pattern: "buyUpgrade\("
---

<objective>
Add a dedicated Upgrades tab that consolidates upgrade browsing/purchasing and previews each upgrade's effect on rates.

Purpose: NAV-01 and CLAR-05 require upgrades to be separate from the catalog purchase flow and transparent in their impact. This plan creates the player-facing Upgrades surface and removes redundant upgrade browsing from the Vault.
Output: New Upgrades tab, consolidated upgrade cards with before/after rate previews, and updated Vault sidebar to avoid duplication.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-career-first-economy-and-upgrades-surface/27-CONTEXT.md
@src/App.tsx
@src/ui/tabs/CollectionTab.tsx
@src/ui/tabs/WorkshopTab.tsx
@src/ui/tabs/MaisonTab.tsx
@src/ui/tabs/StatsTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a top-level Upgrades tab and render a new Upgrades panel</name>
  <files>
src/App.tsx
src/ui/tabs/UpgradesTab.tsx
  </files>
  <action>
Create a new top-level tab:

- Add `{ id: "upgrades", label: "Upgrades" }` to `TAB_DEFINITIONS` in `src/App.tsx`.
- Extend `combinedTabVisibility` to include `upgrades: true` (and make it hideable only if desired; default: not hideable).
- Mount a new `<UpgradesTab />` panel similar to the existing tabs.

Implement `src/ui/tabs/UpgradesTab.tsx`:

- Panel root: `<section id="upgrades" ...>` with `data-testid="upgrades-panel"`.
- Receive `state`, `onPurchase`, and any shared props needed (no need for `onNavigate`).
- Render 3 groups:
  1. Career/Cash upgrades (from `getUpgrades()`)
  2. Workshop upgrades (from `getWorkshopUpgrades()`)
  3. Maison upgrades (from `getMaisonUpgrades()`)

For each upgrade card:
- Show current owned state/level.
- Show price/cost and a buy button.
- Add pre-purchase preview: compute `before` and `after` rates and show delta chips.
  - Use pure actions for preview: `nextState = buyUpgrade(state, id)` (or workshop/maison equivalents) and compare:
    - cash/sec: `getTotalCashRateCentsPerSec(state)` vs `getTotalCashRateCentsPerSec(nextState)`
    - enjoyment/sec: `getEnjoymentRateCentsPerSec(state)` vs `getEnjoymentRateCentsPerSec(nextState)`
  - If a delta is 0, omit that chip.
- Add an expand/collapse `<details>` for deeper breakdown (Claude discretion) while keeping consistent with existing `details.card` styling used in Stats.

Keep `id` / `data-testid` stable and do not remove existing workshop/maison upgrade UIs yet.
  </action>
  <verify>pnpm run typecheck</verify>
  <done>Upgrades tab exists in navigation and shows consolidated upgrade groups with before/after rate preview chips.</done>
</task>

<task type="auto">
  <name>Task 2: Remove duplicate upgrade browsing from Vault sidebar (Collection tab)</name>
  <files>
src/ui/tabs/CollectionTab.tsx
src/style.css
  </files>
  <action>
Update `src/ui/tabs/CollectionTab.tsx`:

- Remove (or collapse behind a single link) the Vault sidebar "Upgrades" panel that renders `div#upgrade-list`.
- Replace it with a small callout card that points the player to the new Upgrades tab.
  - Keep the DOM id `upgrade-list` unused after removal to avoid stale anchors.
  - Do not break existing milestones/achievements panels.

If any CSS is needed for the new callout, add minimal styles in `src/style.css` consistent with existing panel/card language.
  </action>
  <verify>pnpm run typecheck && pnpm run test:unit</verify>
  <done>Vault no longer serves as the primary upgrades browsing surface; upgrades are accessed from the dedicated tab.</done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit`
</verification>

<success_criteria>
- NAV-01: upgrades accessible from a dedicated surface separate from catalog buying.
- CLAR-05: each upgrade shows its effect on cash/enjoyment rates before purchase.
</success_criteria>

<output>
After completion, create `.planning/phases/27-career-first-economy-and-upgrades-surface/27-04-SUMMARY.md`
</output>
