---
phase: 43-new-watch-mini-games
plan: 02
type: execute
wave: 2
depends_on:
  - 43-01
files_modified:
  - src/game/model/types.ts
  - src/game/data/items.ts
  - src/game/actions/interactions.ts
  - src/game/state.ts
  - src/App.tsx
  - src/ui/components/DateMiniGameModal.tsx
  - src/style.css
  - src/ui/help/helpContent.ts
  - tests/catalog.unit.test.tsx
autonomous: true

must_haves:
  truths:
    - "Set-date mini-game exists and can be launched from the interaction menu for eligible watch tiers."
    - "Set-date ends in an unambiguous Miss/Good/Perfect outcome and applies a reward + cooldown."
  artifacts:
    - path: src/ui/components/DateMiniGameModal.tsx
      provides: "Set-date mini-game modal"
      contains: "date-modal"
    - path: src/game/actions/interactions.ts
      provides: "Pure reward application for date interaction"
      contains: "applyDateReward"
  key_links:
    - from: src/App.tsx
      to: src/ui/components/DateMiniGameModal.tsx
      via: "renders date modal + onComplete"
      pattern: "<DateMiniGameModal"
    - from: src/App.tsx
      to: src/game/state.ts
      via: "applyDateReward"
      pattern: "applyDateReward"
---

<objective>
Add a set-date mini-game and wire it into the interaction menu for tiers where date-setting makes sense.

Purpose: Deliver WATCH-03 (Phase 43) as a new interaction type.
Output: DateMiniGameModal + domain reward action + menu option gating.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/43-new-watch-mini-games/43-CONTEXT.md

@src/game/data/items.ts
@src/game/actions/interactions.ts
@src/game/state.ts
@src/App.tsx
@src/ui/components/InteractionMenuModal.tsx
@src/style.css
@tests/catalog.unit.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a tier-level flag for date-setting eligibility</name>
  <files>
src/game/model/types.ts
src/game/data/items.ts
  </files>
  <action>
Add a simple, tier-level eligibility signal for date-setting:

- Extend `WatchItemDefinition` with either:
  - `supportsDateSetting?: boolean` (preferred for minimal churn), or
  - `supportedInteractions?: WatchInteractionId[]` (more flexible, more churn).

Set initial values per `43-CONTEXT.md` decision proposal (adjust as needed during implementation):
- classic: true
- chronograph: true
- tourbillon: decide (default: false to keep scope small)
- starter: false
  </action>
  <verify>pnpm -s run typecheck</verify>
  <done>
- Eligibility is represented in data without touching save schema.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement DateMiniGameModal + pure reward application</name>
  <files>
src/ui/components/DateMiniGameModal.tsx
src/style.css
src/game/actions/interactions.ts
src/game/state.ts
src/App.tsx
src/ui/help/helpContent.ts
tests/catalog.unit.test.tsx
  </files>
  <action>
Implement the set-date interaction end-to-end:

- UI (`DateMiniGameModal.tsx`)
  - Use the same modal conventions and stable selectors:
    - `data-testid="date-modal"`
    - `data-testid="date-action"` for the main action
    - `data-testid="date-outcome"` for the results block
    - `data-testid="date-done"` / `data-testid="date-close"`
  - Mechanic: a date wheel/strip moves; player taps/clicks to stop on the target.
  - Outcomes: Miss/Good/Perfect (based on distance to target).
  - Communicate the reward clearly on result.

- Domain (`src/game/actions/interactions.ts`)
  - Add `applyDateReward(state, itemId, nowMs, outcome)`:
    - Reuse the same `isInteractionAvailable` + cooldown setting flow.
    - Choose a simple placeholder reward (cash burst or enjoyment burst); Phase 44 will unify scaling.
  - Export via `src/game/state.ts`.

- Wiring (`src/App.tsx`)
  - Add a new interaction kind for date-setting.
  - Add menu option "Set date" when `supportsDateSetting` is true.
  - On completion, call `applyDateReward`.

- Help (`helpContent.ts`)
  - Add one bullet describing date-setting and its reward.

- Tests (`tests/catalog.unit.test.tsx`)
  - Add/extend a unit test that:
    - seeds a save with an eligible tier owned
    - opens interaction menu
    - launches set-date
    - completes the action
    - asserts a reward was applied (localStorage delta) and the modal closes.
  </action>
  <verify>pnpm -s run test:unit -- tests/catalog.unit.test.tsx</verify>
  <done>
- Date mini-game is playable, has clear feedback, applies reward, and respects cooldown/ownership.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit -- tests/catalog.unit.test.tsx`
</verification>

<success_criteria>
- WATCH-03 satisfied: eligible watches can play a set-date mini-game with interactive controls and clear feedback.
</success_criteria>

<output>
After completion, create `.planning/phases/43-new-watch-mini-games/43-02-SUMMARY.md`
</output>

