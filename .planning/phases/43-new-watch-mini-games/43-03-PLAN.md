---
phase: 43-new-watch-mini-games
plan: 03
type: execute
wave: 3
depends_on:
  - 43-02
files_modified:
  - src/game/actions/interactions.ts
  - src/game/state.ts
  - src/App.tsx
  - src/ui/components/StrapMiniGameModal.tsx
  - src/style.css
  - src/ui/help/helpContent.ts
  - tests/catalog.unit.test.tsx
autonomous: true

must_haves:
  truths:
    - "Strap-change mini-game exists and is available via the interaction menu."
    - "Strap-change provides clear visual feedback during play and a completed result state."
    - "Completing strap-change applies a reward + cooldown using existing interaction rules."
  artifacts:
    - path: src/ui/components/StrapMiniGameModal.tsx
      provides: "Strap-change mini-game modal"
      contains: "strap-modal"
    - path: src/game/actions/interactions.ts
      provides: "Pure reward application for strap interaction"
      contains: "applyStrapChangeReward"
---

<objective>
Add a strap-change mini-game with clear visual feedback and wire it into the interaction menu.

Purpose: Deliver WATCH-04 (Phase 43).
Output: StrapMiniGameModal + domain reward action + menu option.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/43-new-watch-mini-games/43-CONTEXT.md

@src/App.tsx
@src/ui/components/InteractionMenuModal.tsx
@src/style.css
@tests/catalog.unit.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement StrapMiniGameModal with touch-friendly controls</name>
  <files>
src/ui/components/StrapMiniGameModal.tsx
src/style.css
  </files>
  <action>
Create `StrapMiniGameModal.tsx` using existing modal patterns and stable selectors:

- `data-testid="strap-modal"`
- `data-testid="strap-action"` for the main action
- `data-testid="strap-outcome"` for results
- `data-testid="strap-done"` / `data-testid="strap-close"`

Mechanic proposal (keep it simple, visual, and deterministic):
- The player must “align” a strap pin into a lug slot using 2–3 taps (or choose the correct strap type to match the watch vibe).
- Provide obvious in-progress feedback (highlight, progress indicator).
- End in Miss/Good/Perfect tiers and show the resulting reward.

Respect reduced motion and keep touch targets >= 44px.
  </action>
  <verify>pnpm -s run typecheck</verify>
  <done>
- Strap mini-game can be played end-to-end and communicates a completed result state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire strap-change into menu and apply reward via a pure action</name>
  <files>
src/game/actions/interactions.ts
src/game/state.ts
src/App.tsx
src/ui/help/helpContent.ts
tests/catalog.unit.test.tsx
  </files>
  <action>
Implement the strap-change interaction plumbing:

- Add `applyStrapChangeReward(state, itemId, nowMs, outcome)` in `src/game/actions/interactions.ts`.
  - Use the existing availability rules and cooldown setter.
  - Choose a placeholder reward type (enjoyment or cash) consistent with the intended theme; Phase 44 will scale/tune.
- Export via `src/game/state.ts`.
- Add a new interaction kind in `src/App.tsx` to render `<StrapMiniGameModal>` and call `applyStrapChangeReward` on completion.
- Add a strap option to the interaction menu for all tiers (unless explicitly excluded).
- Update help content to mention strap-change as an interaction.
- Add/extend a unit test in `tests/catalog.unit.test.tsx` to cover opening and completing the strap mini-game via the menu.
  </action>
  <verify>pnpm -s run test:unit -- tests/catalog.unit.test.tsx</verify>
  <done>
- Strap-change is available via the interaction menu, applies a reward, and triggers cooldown.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit -- tests/catalog.unit.test.tsx`
</verification>

<success_criteria>
- WATCH-04 satisfied: strap-change interaction exists, has visual feedback, and completes with a clear reward.
</success_criteria>

<output>
After completion, create `.planning/phases/43-new-watch-mini-games/43-03-SUMMARY.md`
</output>

