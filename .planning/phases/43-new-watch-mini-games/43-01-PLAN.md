---
phase: 43-new-watch-mini-games
plan: 1
type: execute
wave: 1
files_modified:
  - src/ui/components/QuartzMiniGameModal.tsx
  - tests/quartz-outcome.unit.test.ts

must_haves:
  truths:
    - "Result tiers reflect a wider sweet spot: taps that land near the center now yield Good instead of Miss while perfect stays reserved for the closest hits."
    - "Players still see the distinct Miss/Good/Perfect messages + cash rewards after completing the quartz mini-game."
    - "Reduced-motion runs, touch targets, and modal stability remain unchanged despite the relaxed margins."
  artifacts:
    - path: "src/ui/components/QuartzMiniGameModal.tsx"
      provides: "Quartz outcome math + modal messaging"
    - path: "tests/quartz-outcome.unit.test.ts"
      provides: "Regression coverage for the widened thresholds"
  key_links:
    - from: "src/ui/components/QuartzMiniGameModal.tsx"
      to: "tests/quartz-outcome.unit.test.ts"
      via: "getOutcomeTier"
      pattern: "getOutcomeTier"

<objective>
Make the quartz time-setting mini-game more forgiving: widen the tolerance around the center, keep the deeper penalty for wild swings, and lock the new math behind regression tests.

Purpose: Reduce misses on the starter quartz interaction while preserving the feeling of landing a tight perfect, improving the player experience for time-setting interactions.
Output: Adjusted tier thresholds, explanatory copy, and unit tests that guard the lenient window.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/ui/components/QuartzMiniGameModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the quartz outcome thresholds</name>
  <files>
src/ui/components/QuartzMiniGameModal.tsx
  </files>
  <action>
- Increase the perfect and good distance margins so taps that are close to the dial center count as Good instead of Miss while still reserving Perfect for the tightest shots.
- Keep the helper math deterministic (progress, performance, preference, tier) and export the key helpers for new regression tests.
- Ensure the cash payout logic continues to read the tier and nothing else needs adjustment.
  </action>
  <verify>
pnpm test:unit -- tests/quartz-outcome.unit.test.ts
  </verify>
  <done>
- The outcome helper uses the new margin constants and still produces Miss/Good/Perfect in predictable ranges.
- `getOutcomeTier`/`getPerformance` remain deterministic and reusable by other tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clarify the usermessage for the expanded sweet spot</name>
  <files>
src/ui/components/QuartzMiniGameModal.tsx
  </files>
  <action>
- Update the helper copy so players know the Good zone is wide and they should aim when the hand nears the marker; keep the Miss/Perfect messaging unchanged in the result view.
- Ensure reduced-motion, focus trapping, and button sizing remain untouched.
  </action>
  <verify>
pnpm test:unit -- tests/quartz-outcome.unit.test.ts
  </verify>
  <done>
- The launch copy references the wide window and still prompts players to tap when the hand meets the anchor.
- The result view still shows `Cash +` text and the Miss/Good/Perfect badge without visual regressions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Guard the new margins with regression tests</name>
  <files>
tests/quartz-outcome.unit.test.ts
  </files>
  <action>
- Add a Vitest file that imports `getOutcomeTier` and ensures values near the new edges produce the expected tiers.
- Confirm the existing performance calculation keeps `1` near center and falls toward `0` as you drift toward Miss.
- Run the new unit test alongside existing quartz sims if needed.
  </action>
  <verify>
pnpm test:unit -- tests/quartz-outcome.unit.test.ts
  </verify>
  <done>
- The test suite fails if Good/Miss boundaries drift too far from the specified margins.
- Performance calibration stays smooth (monotonic drop as distance increases).
  </done>
</task>

</tasks>

<verification>
- `pnpm test:unit -- tests/quartz-outcome.unit.test.ts`
</verification>

<success_criteria>
- Good outcomes now cover a wider range around the center while Perfect still enforces a precise hit.
- Players read the modal copy, understand the target window, and still see the Miss/Good/Perfect messaging that drives payouts.
- The new unit test defends the margins and fails if the thresholds slide again.
</success_criteria>

<output>
After completion, create `.planning/phases/43-new-watch-mini-games/43-01-SUMMARY.md`
</output>
