---
phase: 43-new-watch-mini-games
plan: 01
type: execute
wave: 1
depends_on:
  - 42-02
files_modified:
  - src/App.tsx
  - src/ui/components/InteractionMenuModal.tsx
  - src/ui/tabs/CatalogTab.tsx
  - src/style.css
  - src/ui/help/helpContent.ts
  - tests/catalog.unit.test.tsx
autonomous: true

must_haves:
  truths:
    - "Clicking an interact button opens an Interaction Menu modal."
    - "From the menu, players can launch the existing winding/automatic/quartz mini-games."
    - "Menu options respect ownership + cooldown gating and communicate why an option is disabled."
  artifacts:
    - path: src/ui/components/InteractionMenuModal.tsx
      provides: "Accessible interaction picker modal with stable test ids"
      contains: "interaction-menu"
    - path: src/App.tsx
      provides: "Interaction selection state + wiring to existing mini-game modals"
      contains: "setActiveInteraction"
    - path: tests/catalog.unit.test.tsx
      provides: "Unit coverage for interaction menu opening + selecting an interaction"
      contains: "interaction-menu"
  key_links:
    - from: src/ui/tabs/CatalogTab.tsx
      to: src/App.tsx
      via: "onInteract(tierId) routes into menu"
      pattern: "onInteract\\("
    - from: src/App.tsx
      to: src/ui/components/InteractionMenuModal.tsx
      via: "renders menu modal"
      pattern: "<InteractionMenuModal"
---

<objective>
Add an interaction selection menu so a single watch card can offer multiple mini-games (existing and new) without adding multiple action buttons.

Purpose: Prepare Phase 43 for set-date + strap-change while preserving a clean catalog card UI.
Output: InteractionMenuModal + updated wiring/tests so existing mini-games remain reachable.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-new-watch-mini-games/43-CONTEXT.md

@src/App.tsx
@src/ui/tabs/CatalogTab.tsx
@src/ui/components/WindingMiniGameModal.tsx
@src/ui/components/AutomaticMiniGameModal.tsx
@src/ui/components/QuartzMiniGameModal.tsx
@src/style.css
@src/ui/help/helpContent.ts
@tests/catalog.unit.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create an accessible InteractionMenuModal component</name>
  <files>
src/ui/components/InteractionMenuModal.tsx
src/style.css
  </files>
  <action>
Create `src/ui/components/InteractionMenuModal.tsx` using the repo’s existing modal patterns:

- Container:
  - `role="dialog"`, `aria-modal="true"`, similar structure to `WindingMiniGameModal`.
  - Stable test id: `data-testid="interaction-menu"`.
- API:
  - `open: boolean`
  - `itemLabel: string`
  - `options: Array<{ id: string; label: string; description?: string; disabledReason?: string }>`
  - `onSelect(optionId: string): void`
  - `onClose(): void`
- UI:
  - Render a stack of option buttons with clear labels.
  - Disabled options remain visible and show a one-line disabled reason.
  - Include a close button with `data-testid="interaction-menu-close"`.

Add minimal CSS in `src/style.css` for option list layout and touch-friendly targets (>= 44px).
  </action>
  <verify>pnpm -s run typecheck</verify>
  <done>
- InteractionMenuModal renders and is keyboard navigable with clear disabled messaging.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Catalog interact buttons to open the menu and launch existing mini-games</name>
  <files>
src/App.tsx
src/ui/tabs/CatalogTab.tsx
src/ui/help/helpContent.ts
tests/catalog.unit.test.tsx
  </files>
  <action>
Update the interaction flow so the menu is the entry point:

- `src/App.tsx`
  - Extend interaction state to support a `kind: "menu"` (or a separate `activeInteractionMenu`) in addition to the existing mini-game kinds.
  - Change `handleInteract(itemId)` to open the menu for that item instead of immediately opening a specific mini-game.
  - Provide menu options based on the watch movement:
    - manual → Wind crown
    - automatic → Charge rotor
    - quartz → Set time
  - When the user selects an option:
    - close menu
    - open the corresponding mini-game modal (existing behavior)
  - Disable menu options when:
    - watch not owned, or
    - cooldown active (reuse existing cooldown selectors)

- `src/ui/tabs/CatalogTab.tsx`
  - Keep existing `data-testid="vault-interact-<tier>"` stable.
  - Keep the interact button visible only when owned (existing behavior from v3.2).
  - The button should now open the interaction menu (not the mini-game directly).

- `src/ui/help/helpContent.ts`
  - Update the Interactions & mini-games help text to mention the interaction menu.

- `tests/catalog.unit.test.tsx`
  - Update the existing winding/quartz interaction unit tests:
    - click `vault-interact-*`
    - assert `interaction-menu` appears
    - click the relevant option
    - assert the expected mini-game modal opens
  </action>
  <verify>pnpm -s run test:unit -- tests/catalog.unit.test.tsx</verify>
  <done>
- Existing mini-games remain reachable via the new interaction menu.
- Unit tests reflect the new flow without changing stable selectors.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit -- tests/catalog.unit.test.tsx`
</verification>

<success_criteria>
- Interaction menu exists and launches the existing mini-games from a single watch card entry point.
</success_criteria>

<output>
After completion, create `.planning/phases/43-new-watch-mini-games/43-01-SUMMARY.md`
</output>

