---
phase: 44-interaction-feedback-and-rewards
plan: 01
type: execute
wave: 1
depends_on:
  - 43-03
files_modified:
  - src/game/actions/interactions.ts
  - src/game/state.ts
  - src/App.tsx
  - tests/interactions.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "Interaction rewards scale with watch tier (starter/classic/chronograph/tourbillon)."
    - "Interaction rewards scale with precision (performance) and preserve Miss < Good < Perfect."
    - "Reward application remains pure and unit-tested."
  artifacts:
    - path: src/game/actions/interactions.ts
      provides: "Centralized reward resolver + per-interaction apply* functions"
      contains: "INTERACTION_BASE_COOLDOWN_MS"
    - path: tests/interactions.unit.test.ts
      provides: "Regression coverage for reward scaling"
      contains: "applyWindingReward"
  key_links:
    - from: src/App.tsx
      to: src/game/state.ts
      via: "interaction completion -> apply reward action"
      pattern: "apply.*Reward\\("
---

<objective>
Make interaction rewards feel worth doing by scaling them with watch tier and precision, and protect the rules with unit tests.

Purpose: Deliver WATCH-06 and set the stage for consistent results UI (WATCH-05) in 44-02.
Output: Domain-level reward scaling driven by tier + performance.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/44-interaction-feedback-and-rewards/44-CONTEXT.md

@src/game/actions/interactions.ts
@src/game/selectors/interactions.ts
@src/game/data/items.ts
@src/App.tsx
@tests/interactions.unit.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce a shared reward resolver that supports tier + performance scaling</name>
  <files>src/game/actions/interactions.ts</files>
  <action>
Refactor rewards so they can scale by watch tier and precision:

- Introduce a small internal resolver (name is illustrative):
  - `resolveInteractionReward(itemId, interactionId, tier, performance) -> { enjoymentDeltaCents, currencyDeltaCents, reserveDelta }`
- Use `WatchItemDefinition` values to scale rewards in a predictable way (examples):
  - Base scale derived from `item.enjoymentCentsPerSec` or `item.collectionValueCents`.
  - Apply a bounded multiplier for performance (e.g. 0.8–1.2 within a tier).
  - Ensure results are clamped and deterministic.

Keep the public API simple:
- `applyWindingReward(...)`, `applyAutomaticReward(...)`, `applyQuartzReward(...)`, and any Phase 43 new actions should call the resolver and then set cooldown.

Important:
- Do not introduce randomness.
- Do not move cooldown rules; keep `INTERACTION_BASE_COOLDOWN_MS` as the source of truth.
  </action>
  <verify>pnpm -s run typecheck</verify>
  <done>
- A single reward resolver drives all interactions, enabling tier/performance scaling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Plumb performance through App -> action calls and add unit coverage</name>
  <files>
src/App.tsx
src/game/actions/interactions.ts
src/game/state.ts
tests/interactions.unit.test.ts
  </files>
  <action>
Wire performance through to reward application:

- Update `apply*Reward` signatures to accept a performance scalar (0..1) in addition to tier:
  - Option A: accept `{ tier, performance }` object
  - Option B: accept `(tier, performance)` parameters
- Update `src/App.tsx` to pass through the modal’s `outcome.performance` to the action.
- Export updated actions via `src/game/state.ts`.

Update `tests/interactions.unit.test.ts` to cover:
- Better performance yields better rewards (within the same tier).
- Higher tier yields higher rewards at the same performance.
- Rewards remain monotonic across Miss/Good/Perfect.
  </action>
  <verify>pnpm -s run test:unit -- tests/interactions.unit.test.ts</verify>
  <done>
- Reward scaling is test-protected and cannot silently drift.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit -- tests/interactions.unit.test.ts`
</verification>

<success_criteria>
- WATCH-06 satisfied: rewards scale with both tier and precision/performance, and the scaling is unit-tested.
</success_criteria>

<output>
After completion, create `.planning/phases/44-interaction-feedback-and-rewards/44-01-SUMMARY.md`
</output>

