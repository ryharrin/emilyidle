---
phase: 44-interaction-feedback-and-rewards
plan: 02
type: execute
wave: 2
depends_on:
  - 44-01
files_modified:
  - src/ui/components/WindingMiniGameModal.tsx
  - src/ui/components/AutomaticMiniGameModal.tsx
  - src/ui/components/QuartzMiniGameModal.tsx
  - src/ui/components/DateMiniGameModal.tsx
  - src/ui/components/StrapMiniGameModal.tsx
  - src/style.css
  - src/ui/help/helpContent.ts
  - tests/ui-screenshots.spec.ts
autonomous: true

must_haves:
  truths:
    - "Every interaction ends in a clear Miss/Good/Perfect state with visible feedback."
    - "Every interaction shows the reward result and why (tier + key performance cue)."
  artifacts:
    - path: src/style.css
      provides: "Consistent outcome styling across interaction modals"
      contains: "winding-outcome"
    - path: tests/ui-screenshots.spec.ts
      provides: "Visual artifact captures for modals"
      contains: "winding"
---

<objective>
Make interaction outcomes and rewards feel consistent across mini-games via shared UI vocabulary, clear success/failure states, and readable result cards.

Purpose: Deliver WATCH-05 after the reward rules are centralized/scaled in 44-01.
Output: Consistent result UI across winding/automatic/quartz/date/strap.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/44-interaction-feedback-and-rewards/44-CONTEXT.md

@src/ui/components/WindingMiniGameModal.tsx
@src/ui/components/AutomaticMiniGameModal.tsx
@src/ui/components/QuartzMiniGameModal.tsx
@src/style.css
@tests/ui-screenshots.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize result UI patterns across all interaction modals</name>
  <files>
src/ui/components/WindingMiniGameModal.tsx
src/ui/components/AutomaticMiniGameModal.tsx
src/ui/components/QuartzMiniGameModal.tsx
src/ui/components/DateMiniGameModal.tsx
src/ui/components/StrapMiniGameModal.tsx
src/style.css
  </files>
  <action>
Align the “result” section across all interaction modals:

- Every modal must:
  - show a clear Miss/Good/Perfect title on completion
  - show the reward result in a consistent format (e.g. `Reward: +X enjoyment` / `Reward: +$Y cash` / `Power reserve +Z%`)
  - show the cooldown/next-available message (if applicable)
- Use consistent styling classes (consider a shared `.interaction-outcome` base class).
- Ensure the main action button remains touch-friendly (>= 44px) and visually obvious.

Avoid introducing a new component library; keep changes within existing CSS primitives.
  </action>
  <verify>pnpm -s run typecheck</verify>
  <done>
- Outcomes are visually consistent and unambiguous across all interaction modals.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update help copy and capture refreshed modal screenshots</name>
  <files>
src/ui/help/helpContent.ts
tests/ui-screenshots.spec.ts
  </files>
  <action>
- Update help text for Interactions & mini-games to reflect:
  - all available interaction types (including date + strap)
  - rewards vary by tier and precision

- Extend `tests/ui-screenshots.spec.ts` to capture at least one interaction modal screenshot after this polish pass (desktop and mobile), so changes are reviewable.
  </action>
  <verify>pnpm -s run test:e2e -- tests/ui-screenshots.spec.ts</verify>
  <done>
- Help copy matches reality and screenshots provide a visual diff anchor.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:e2e -- tests/ui-screenshots.spec.ts`
</verification>

<success_criteria>
- WATCH-05 satisfied: every mini-game ends in a clear success/failure tier and shows reward results consistently.
</success_criteria>

<output>
After completion, create `.planning/phases/44-interaction-feedback-and-rewards/44-02-SUMMARY.md`
</output>

