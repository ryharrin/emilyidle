---
phase: 41-stability-and-regression-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/localstorage-keys.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "localStorage key strings used by the app remain unchanged"
    - "Legacy save key remains referenced so older installs can still migrate"
  artifacts:
    - path: tests/localstorage-keys.unit.test.ts
      provides: "Regression guardrail for localStorage key string contracts"
      contains: "emily-idle:save"
  key_links:
    - from: tests/localstorage-keys.unit.test.ts
      to: src/game/persistence.ts
      via: strict string assertions
      pattern: "emily-idle:save"
    - from: tests/localstorage-keys.unit.test.ts
      to: src/App.tsx
      via: strict string assertions
      pattern: "emily-idle:navigation"
---

<objective>
Add a strict unit-level contract test that fails loudly if any localStorage keys are renamed or removed.

Purpose: Satisfy TEC-02 by freezing key strings that existing players and tests depend on.
Output: A single Vitest contract test that reads source files and asserts key strings remain present.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-stability-and-regression-guardrails/41-RESEARCH.md

@src/game/persistence.ts
@src/App.tsx
@src/ui/help/HelpModal.tsx
@src/ui/components/careerMap/CareerMapCanvas.tsx
@src/ui/tabs/career/CareerUpgradesCanvas.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add strict localStorage key string contract test</name>
  <files>tests/localstorage-keys.unit.test.ts</files>
  <action>
Create a new Vitest unit test that reads the relevant source files from disk and asserts the exact key strings remain present.

- Create `tests/localstorage-keys.unit.test.ts`.
- Read these files via `node:fs/promises` + `new URL(..., import.meta.url)`:
  - `src/game/persistence.ts`
  - `src/App.tsx`
  - `src/ui/help/HelpModal.tsx`
  - `src/ui/components/careerMap/CareerMapCanvas.tsx`
  - `src/ui/tabs/career/CareerUpgradesCanvas.tsx`
- Assert (strictly) that these strings are still present in source:
  - `emily-idle:save`
  - `watch-idle:save`
  - `emily-idle:settings`
  - `emily-idle:audio`
  - `emily-idle:navigation`
  - `emily-idle:help`
  - `emily-idle:career-map-viewport:v1`
  - `emily-idle:career-upgrades-viewport:v1`

Keep the test intentionally strict (string includes or regex that matches the literal). This plan must not modify production key usage or add any new keys.
  </action>
  <verify>pnpm run test:unit -- tests/localstorage-keys.unit.test.ts</verify>
  <done>The unit test fails if any of the listed key strings are renamed/removed from the source files.</done>
</task>

</tasks>

<verification>
- `pnpm run test:unit -- tests/localstorage-keys.unit.test.ts`
</verification>

<success_criteria>
- TEC-02 is protected by a strict unit-level contract test covering all `emily-idle:*` keys currently used in production code plus the legacy save key.
</success_criteria>

<output>
After completion, create `.planning/phases/41-stability-and-regression-guardrails/41-01-SUMMARY.md`
</output>
