---
phase: 41-stability-and-regression-guardrails
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/catalog-image-url-contract.unit.test.ts
  - tests/catalog-image-rendering.spec.ts
autonomous: true

must_haves:
  truths:
    - "Catalog image URL mapping remains base-aware via import.meta.env.BASE_URL"
    - "Catalog images render in the UI under the deployed /emilyidle/ base path"
  artifacts:
    - path: tests/catalog-image-url-contract.unit.test.ts
      provides: "Unit contract that catalog image URLs remain base-aware"
      contains: "import.meta.env.BASE_URL"
    - path: tests/catalog-image-rendering.spec.ts
      provides: "E2E contract that catalog images actually load (naturalWidth > 0)"
      contains: "/emilyidle/"
  key_links:
    - from: src/game/catalog.ts
      to: import.meta.env.BASE_URL
      via: LOCAL_CATALOG_ROOT
      pattern: "BASE_URL\}catalog/"
    - from: tests/catalog-image-rendering.spec.ts
      to: public/catalog
      via: img element loading
      pattern: "img\[src\*='\/catalog\/'\]"
---

<objective>
Add catalog image guardrails that cover both the URL-mapping contract and real UI rendering under the deployed base path.

Purpose: Satisfy TEC-04 by preventing regressions where images exist on disk but break due to base URL or rendering changes.
Output: (1) a unit contract test for base-aware URL mapping, and (2) a Playwright rendering test that asserts images actually load in the DOM.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-stability-and-regression-guardrails/41-RESEARCH.md

@src/game/catalog.ts
@vite.config.ts
@tests/catalog-images.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unit contract test for base-aware catalog image URL mapping</name>
  <files>tests/catalog-image-url-contract.unit.test.ts</files>
  <action>
Create `tests/catalog-image-url-contract.unit.test.ts` that reads `src/game/catalog.ts` from disk and asserts the critical base-aware mapping is still present.

Assertions (use regex/string checks against the file text):
- `LOCAL_CATALOG_ROOT` uses `import.meta.env.BASE_URL` and appends `catalog/`.
- `getCatalogImageUrl` returns a string built from `LOCAL_CATALOG_ROOT` (not a hard-coded `/catalog/` or `http://` prefix) when the entry image is a Wikimedia URL.
- `LOCAL_CATALOG_OVERRIDES` exists (do not validate its full contents; just ensure it’s present so percent-encoding fixes don’t get removed accidentally).

Do not modify `src/game/catalog.ts`; this is test-only contract enforcement.
  </action>
  <verify>pnpm run test:unit -- tests/catalog-image-url-contract.unit.test.ts</verify>
  <done>The test fails if base-awareness (`import.meta.env.BASE_URL`) or the override mechanism is removed.</done>
</task>

<task type="auto">
  <name>Task 2: Add Playwright test ensuring catalog images load in the UI under /emilyidle/</name>
  <files>tests/catalog-image-rendering.spec.ts</files>
  <action>
Create `tests/catalog-image-rendering.spec.ts` as a rendering-focused complement to `tests/catalog-images.spec.ts`.

Test behavior:
- Seed a minimal v2 save + settings (similar to `tests/catalog-images.spec.ts`) so the UI renders deterministically.
- `page.goto("/emilyidle/")` (intentionally use the deployed base path).
- Navigate to the Catalog tab if present and wait for `catalog-grid` to be visible.
- Locate the first ~10 images matching `img[src*="/catalog/"]`.
- For each, wait for it to finish loading and assert `naturalWidth > 0` (evaluate on the element).

Constraints:
- Keep this test resilient: it should not depend on a specific catalog entry id, only on the existence of some catalog images.
- Do not expand this into a full asset enumeration; that is already covered by `tests/catalog-images.spec.ts`.
  </action>
  <verify>pnpm run test:e2e -- tests/catalog-image-rendering.spec.ts</verify>
  <done>The test fails if catalog images render with broken/empty loads under the /emilyidle base path.</done>
</task>

</tasks>

<verification>
- `pnpm run test:unit -- tests/catalog-image-url-contract.unit.test.ts`
- `pnpm run test:e2e -- tests/catalog-image-rendering.spec.ts`
</verification>

<success_criteria>
- TEC-04 is protected both at the mapping layer (unit contract) and at the DOM rendering layer (Playwright under `/emilyidle/`).
</success_criteria>

<output>
After completion, create `.planning/phases/41-stability-and-regression-guardrails/41-04-SUMMARY.md`
</output>
