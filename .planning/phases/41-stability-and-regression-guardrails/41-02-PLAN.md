---
phase: 41-stability-and-regression-guardrails
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/persistence-compat.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "A v2 save payload round-trips through encode/decode without changing version or required fields"
    - "A legacy-key-only install (watch-idle:save) loads successfully and migrates to emily-idle:save"
  artifacts:
    - path: tests/persistence-compat.unit.test.ts
      provides: "Regression guardrails for save payload shape and legacy key migration"
      contains: "watch-idle:save"
  key_links:
    - from: tests/persistence-compat.unit.test.ts
      to: src/game/persistence.ts
      via: encode/decode + localStorage load
      pattern: "loadSaveFromLocalStorage"
---

<objective>
Add unit-level save compatibility tests that lock down the v2 save payload contract and the legacy-key migration path.

Purpose: Satisfy TEC-01 by proving the existing save format still loads and migrates without data loss for the fields we can validate deterministically.
Output: A Vitest file that covers (1) v2 payload shape and (2) legacy `watch-idle:save` migration to `emily-idle:save`.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-stability-and-regression-guardrails/41-RESEARCH.md

@src/game/persistence.ts
@src/game/model/state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add save format + legacy migration unit tests</name>
  <files>tests/persistence-compat.unit.test.ts</files>
  <action>
Create `tests/persistence-compat.unit.test.ts` with deterministic contract tests around `src/game/persistence.ts`.

Implement these test cases:

1) "encodeSaveString produces a v2 payload with required fields"
   - Import `createInitialState` from `src/game/state.ts` (facade) or `src/game/model/state.ts`.
   - Call `encodeSaveString(state, lastSimulatedAtMs, savedAt)` with a fixed date (`new Date(0)`) and a fixed `lastSimulatedAtMs`.
   - `JSON.parse` and assert:
     - `version === 2`
     - `savedAt === new Date(0).toISOString()`
     - `lastSimulatedAtMs` is the provided number
     - `state` exists and is an object

2) "decodeSaveString accepts v1 payloads and normalizes them to v2"
   - Create a minimal v1 JSON payload with:
     - `version: 1`
     - valid `savedAt` string
     - valid `lastSimulatedAtMs`
     - `state: { currencyCents: 123 }` (intentionally minimal; relies on sanitizer defaults)
   - Call `decodeSaveString(raw)` and assert `ok: true` and `save.version === 2`.

3) "loadSaveFromLocalStorage migrates legacy watch-idle:save to emily-idle:save"
   - In the test, `localStorage.clear()`.
   - Set only `localStorage.setItem("watch-idle:save", rawV2)` where `rawV2` is a v2 payload with a small state (include `currencyCents` and one other field you can assert survives sanitizer, e.g. `enjoymentCents`).
   - Call `loadSaveFromLocalStorage()`.
   - Assert:
     - it returns `{ ok: true }`
     - `localStorage.getItem("emily-idle:save") === rawV2`
     - `localStorage.getItem("watch-idle:save") === null`

Constraints:
- Do not change save format, version, or key strings.
- If any test requires changing sanitizer behavior, stop and instead adjust the test input to match existing sanitizer expectations.
  </action>
  <verify>pnpm run test:unit -- tests/persistence-compat.unit.test.ts</verify>
  <done>Unit tests fail on any regression in v2 payload shape or legacy key migration behavior.</done>
</task>

</tasks>

<verification>
- `pnpm run test:unit -- tests/persistence-compat.unit.test.ts`
</verification>

<success_criteria>
- TEC-01 is protected by deterministic unit tests proving v2 payload shape and v1/legacy migration still work.
</success_criteria>

<output>
After completion, create `.planning/phases/41-stability-and-regression-guardrails/41-02-SUMMARY.md`
</output>
