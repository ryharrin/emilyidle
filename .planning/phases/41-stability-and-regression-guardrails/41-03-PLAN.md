---
phase: 41-stability-and-regression-guardrails
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/selectors-contract.spec.ts
autonomous: true

must_haves:
  truths:
    - "Key consolidation-era UI anchors keep their data-testid values and remain discoverable via Playwright"
    - "Core navigation entry points (Help, Settings clear-save, Catalog shop surface) remain reachable"
  artifacts:
    - path: tests/selectors-contract.spec.ts
      provides: "E2E selector contract coverage for consolidated Catalog/Settings/Help flows"
      contains: "catalog-grid"
  key_links:
    - from: tests/selectors-contract.spec.ts
      to: src/ui/tabs/CatalogTab.tsx
      via: data-testid selectors
      pattern: "catalog-collection-context|catalog-upgrade-context"
---

<objective>
Add a Playwright selector contract test that verifies a curated set of high-value `data-testid` anchors still exist after consolidation.

Purpose: Satisfy TEC-03 by preventing accidental renames/removals of selectors that existing E2E and automation depend on.
Output: One Playwright spec that navigates through the consolidated flows and asserts selector presence/visibility.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-stability-and-regression-guardrails/41-RESEARCH.md

@tests/catalog-buy-buttons.spec.ts
@tests/catalog-disabled-explanations.spec.ts
@tests/settings-clear-save.spec.ts
@tests/help.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add curated selector contract E2E test</name>
  <files>tests/selectors-contract.spec.ts</files>
  <action>
Create `tests/selectors-contract.spec.ts` that seeds a stable save and verifies critical selectors are present.

Test structure (single test is fine; keep it fast):

1) Seed localStorage via `page.addInitScript`:
   - Set `emily-idle:save` with a v2 payload.
   - Include `unlockedMilestones: ["collector-shelf", "showcase"]` so the Catalog surface is available.
   - Keep `hiddenTabs: []` in `emily-idle:settings` if you set settings.

2) Verify Help entry point selectors:
   - `page.getByTestId("help-open")` visible
   - click -> `page.getByTestId("help-modal")` visible
   - close via `help-close` and assert modal is gone

3) Verify Collection -> CTA -> Catalog context selectors:
   - Navigate to Collection tab via role `tab` name "Collection".
   - Assert `next-unlock-cta-career` is visible and click it.
   - Assert both are visible:
     - `catalog-collection-context`
     - `catalog-upgrade-context`

4) Verify Catalog shop selectors:
   - Ensure Catalog tab is active (click role tab name "Catalog" if needed).
   - Assert:
     - `catalog-grid` is visible
     - at least one `catalog-card` exists
     - `catalog-shop` is present (visible or in DOM)
     - `catalog-filters` is visible
     - `catalog-search` is visible

5) Verify the Phase 38 disabled-explanations selector pattern using one known model id:
   - Use `CLASSIC_MODEL_ID = "rolex-rolex-gmt-master-ii-ref-126713grnr"`.
   - Fill catalog search with a substring like "126713GRNR".
   - Assert these exist/are visible:
     - `catalog-gate-${CLASSIC_MODEL_ID}`
     - `catalog-lock-${CLASSIC_MODEL_ID}`
     - `catalog-why-${CLASSIC_MODEL_ID}`
   - Click `catalog-why-${CLASSIC_MODEL_ID}` and assert `catalog-explain-${CLASSIC_MODEL_ID}` becomes visible.

6) Verify Settings clear-save selectors:
   - Click Settings tab (role tab name "Settings").
   - Assert these are visible:
     - `settings-clear-save`
   - Click it and assert `settings-clear-save-confirm` and `settings-clear-save-cancel` become visible.

Constraints:
- Do not add new selectors or rename existing ones.
- Keep selector assertions targeted to the curated list above (avoid asserting broad UI structure).
  </action>
  <verify>pnpm run test:e2e -- tests/selectors-contract.spec.ts</verify>
  <done>The test fails if any curated selector is renamed, removed, or no longer reachable in the consolidated flow.</done>
</task>

</tasks>

<verification>
- `pnpm run test:e2e -- tests/selectors-contract.spec.ts`
</verification>

<success_criteria>
- TEC-03 is protected by a dedicated selector contract Playwright test covering Catalog/Settings/Help high-value anchors.
</success_criteria>

<output>
After completion, create `.planning/phases/41-stability-and-regression-guardrails/41-03-SUMMARY.md`
</output>
