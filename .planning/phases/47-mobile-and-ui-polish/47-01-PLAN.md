---
phase: 47-mobile-and-ui-polish
plan: 01
type: execute
wave: 1
depends_on:
  - 46-02
files_modified:
  - src/App.tsx
  - src/style.css
  - tests/tabs.spec.ts
autonomous: true

must_haves:
  truths:
    - "On small screens, the primary tab bar scrolls horizontally with scroll-snap."
    - "The tab bar remains sticky while content scrolls."
  artifacts:
    - path: src/style.css
      provides: "Mobile tab bar scroll + sticky styling"
      contains: ".page-nav"
    - path: tests/tabs.spec.ts
      provides: "E2E sanity for tab navigation behavior"
      contains: "tab"
---

<objective>
Make primary navigation mobile-friendly: a sticky, horizontally scrollable tab bar with snap behavior.

Purpose: Deliver MOBILE-01 and MOBILE-02.
Output: Tab bar UX that remains usable on 375px widths without wrapping into multiple rows.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/47-mobile-and-ui-polish/47-CONTEXT.md

@src/App.tsx
@src/style.css
@tests/tabs.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the primary tablist horizontally scrollable with snap</name>
  <files>src/style.css</files>
  <action>
Implement scroll + snap for `.page-nav-tabs` and `.page-nav-link`:

- `.page-nav-tabs`:
  - `overflow-x: auto;`
  - `scroll-snap-type: x mandatory;`
  - `-webkit-overflow-scrolling: touch;`
  - prevent wrapping (e.g. `white-space: nowrap` or flex-nowrap)
- `.page-nav-link`:
  - `scroll-snap-align: start;`
  - ensure min touch target height (>= 44px)
  - maintain visible selected state via `[aria-selected="true"]`

Avoid changing `id`/`data-testid` selectors.
  </action>
  <verify>pnpm -s run test:unit</verify>
  <done>
- On narrow widths the tab bar scrolls instead of wrapping.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make the nav sticky and verify keyboard navigation still works</name>
  <files>
src/style.css
src/App.tsx
tests/tabs.spec.ts
  </files>
  <action>
- Add sticky positioning to the top nav container (likely `.page-nav` or its wrapper):
  - `position: sticky; top: 0; z-index` appropriate
  - theme-aware background so content doesn’t bleed through

- If markup needs a wrapper to make sticky safe, do the smallest possible change in `src/App.tsx`.

- Update/extend `tests/tabs.spec.ts`:
  - verify switching tabs still works
  - optionally verify that the tablist stays visible while scrolling (basic assertion; avoid pixel-perfect checks)
  </action>
  <verify>pnpm -s run test:e2e -- tests/tabs.spec.ts</verify>
  <done>
- Nav remains accessible and usable on mobile and still passes tab navigation tests.
  </done>
</task>

</tasks>

<verification>
- `pnpm run test:unit`
- `pnpm run test:e2e -- tests/tabs.spec.ts`
</verification>

<success_criteria>
- MOBILE-01 and MOBILE-02 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/47-mobile-and-ui-polish/47-01-SUMMARY.md`
</output>

