---
phase: 47-mobile-and-ui-polish
plan: 06
type: execute
wave: 6
depends_on:
  - 47-05
files_modified:
  - src/ui/tabs/StatsTab.tsx
  - src/game/selectors/index.ts
  - src/style.css
  - tests/rate-breakdowns.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "Stats breakdown groups modifiers and shows subtotals (especially on mobile)."
    - "Breakdown data remains consistent with the underlying selector calculations."
  artifacts:
    - path: src/ui/tabs/StatsTab.tsx
      provides: "Grouped breakdown rendering"
      contains: "Breakdown"
    - path: tests/rate-breakdowns.unit.test.ts
      provides: "Unit coverage for breakdown terms/subtotals"
      contains: "breakdown"
---

<objective>
Make the Stats tab easier to read on mobile by grouping breakdown terms and showing subtotals instead of a long flat list.

Purpose: Deliver MOBILE-08 and support overall mobile readability.
Output: Grouped breakdown UI + unit-verified subtotal math.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@src/ui/tabs/StatsTab.tsx
@src/game/selectors/index.ts
@tests/rate-breakdowns.unit.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add grouping/subtotal structure to breakdown selectors (minimal)</name>
  <files>
src/game/selectors/index.ts
tests/rate-breakdowns.unit.test.ts
  </files>
  <action>
Add a minimal grouping structure for breakdown terms (without changing existing math):

- Either:
  - add `groupId` fields to breakdown terms, OR
  - add a wrapper return type that groups terms into sections (e.g. Prestige / Upgrades / Sets / Events / Other).

Update unit tests to assert:
- subtotal math matches the product/sum of underlying terms,
- grouping does not drop or duplicate terms.
  </action>
  <verify>pnpm -s run test:unit -- tests/rate-breakdowns.unit.test.ts</verify>
  <done>
- Breakdown grouping is unit-tested and math stays correct.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update StatsTab to render grouped breakdown with subtotals</name>
  <files>
src/ui/tabs/StatsTab.tsx
src/style.css
  </files>
  <action>
Update StatsTab rendering:

- Render breakdown grouped by section with a subtotal line per section.
- Keep the UI compact for mobile: collapsible groups are acceptable if needed.
- Update/add CSS using the existing primitives (`.panel`, `.card`, `.stats-grid`) for readability.
  </action>
  <verify>pnpm -s run typecheck</verify>
  <done>
- Stats breakdown is easier to scan on mobile without losing information.
  </done>
</task>

</tasks>

<verification>
- `pnpm run test:unit -- tests/rate-breakdowns.unit.test.ts`
- `pnpm run typecheck`
</verification>

<success_criteria>
- MOBILE-08 satisfied: grouped modifiers + subtotals reduce long-list fatigue on mobile.
</success_criteria>

<output>
After completion, create `.planning/phases/47-mobile-and-ui-polish/47-06-SUMMARY.md`
</output>

