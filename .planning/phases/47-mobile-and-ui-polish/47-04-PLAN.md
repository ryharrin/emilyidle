---
phase: 47-mobile-and-ui-polish
plan: 04
type: execute
wave: 4
depends_on:
  - 47-03
files_modified:
  - src/ui/tabs/CatalogTab.tsx
  - src/ui/tabs/CollectionTab.tsx
  - src/style.css
autonomous: true

must_haves:
  truths:
    - "Catalog/Collection content is grouped into sections with an in-page subnav on mobile."
    - "Subnav is touch-friendly and does not break existing deep-link anchors (e.g. catalog-shop)."
  artifacts:
    - path: src/ui/tabs/CatalogTab.tsx
      provides: "Section anchors + grouped layout"
      contains: "catalog-shop"
---

<objective>
Reduce mega-scroll fatigue on mobile by grouping Catalog/Collection into sections and providing an in-page subnav to jump between them.

Purpose: Deliver MOBILE-04.
Output: Sectioned layout with stable anchors and touch-friendly subnav.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@src/ui/tabs/CatalogTab.tsx
@src/ui/tabs/CollectionTab.tsx
@src/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add section anchors and a subnav for Catalog</name>
  <files>
src/ui/tabs/CatalogTab.tsx
src/style.css
  </files>
  <action>
Implement a lightweight section grouping:

- Define section anchors (keep existing `catalog-shop` anchor stable):
  - `catalog-shop` (purchase grid)
  - `catalog-filters`
  - `catalog-tier-bonuses` (if present)
  - `catalog-owned-actions` (wear/dismantle/interactions)

- Add a small in-page subnav (likely a horizontal chip list) near the top of the tab:
  - Buttons/links scroll to the section anchors.
  - Use `scrollIntoView` behavior that respects reduced motion.

Keep the markup minimal and avoid changing existing `data-testid` used by tests.
  </action>
  <verify>pnpm -s run typecheck</verify>
  <done>
- Catalog is sectioned and has a working in-page subnav on mobile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add section grouping/subnav for Collection where appropriate</name>
  <files>
src/ui/tabs/CollectionTab.tsx
src/style.css
  </files>
  <action>
Apply the same pattern to Collection (only where it reduces scrolling):

- Group long “panels” into named sections.
- Provide jump links to the most important sections (equipment, next actions, etc.).

Do not introduce deep IA changes; this is polish and navigation assistance.
  </action>
  <verify>pnpm -s run typecheck</verify>
  <done>
- Collection is easier to navigate on mobile with minimal UI disruption.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
</verification>

<success_criteria>
- MOBILE-04 satisfied and existing anchors remain valid.
</success_criteria>

<output>
After completion, create `.planning/phases/47-mobile-and-ui-polish/47-04-SUMMARY.md`
</output>

