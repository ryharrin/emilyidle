---
phase: 47-mobile-and-ui-polish
plan: 03
type: execute
wave: 3
depends_on:
  - 47-02
files_modified:
  - src/ui/help/HelpModal.tsx
  - src/style.css
  - tests/help.spec.ts
autonomous: true

must_haves:
  truths:
    - "Help modal supports searching by keyword and updating the visible sections list."
    - "Help modal header remains sticky on mobile and Close remains reachable."
  artifacts:
    - path: src/ui/help/HelpModal.tsx
      provides: "Help UI with search input"
      contains: "help-modal"
    - path: tests/help.spec.ts
      provides: "E2E coverage for help open/close and (new) search"
      contains: "help"
---

<objective>
Add search to the Help modal and make the modal header sticky for mobile usability.

Purpose: Deliver MOBILE-05 and MOBILE-06.
Output: Searchable help glossary that remains usable on small screens.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@src/ui/help/HelpModal.tsx
@src/style.css
@tests/help.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement help search filtering + sticky header</name>
  <files>
src/ui/help/HelpModal.tsx
src/style.css
  </files>
  <action>
Add a simple search UX:

- In `HelpModal.tsx`:
  - Add an input in the header: `data-testid="help-search"`.
  - Filter the section list by title/body matches (case-insensitive).
  - If the active section no longer matches, auto-select the first matching section.
  - Provide an empty state when no results match.

- In `src/style.css`:
  - Make `.help-modal-header` sticky within the modal card (so Close/Search remain visible).
  - Improve chips/list layout on narrow widths (wrap or scroll; no overflow offscreen).
  </action>
  <verify>pnpm -s run typecheck</verify>
  <done>
- Help modal search works and header remains usable on mobile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Playwright help spec to cover searching</name>
  <files>tests/help.spec.ts</files>
  <action>
Extend `tests/help.spec.ts` to:
- open the help modal
- type into `help-search`
- assert the section list changes (e.g. fewer buttons visible)
- select a result and confirm the active section updates
  </action>
  <verify>pnpm -s run test:e2e -- tests/help.spec.ts</verify>
  <done>
- E2E coverage protects help search from regressions.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:e2e -- tests/help.spec.ts`
</verification>

<success_criteria>
- MOBILE-05 and MOBILE-06 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/47-mobile-and-ui-polish/47-03-SUMMARY.md`
</output>

