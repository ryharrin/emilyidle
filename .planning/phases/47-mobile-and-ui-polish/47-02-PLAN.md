---
phase: 47-mobile-and-ui-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 47-01
files_modified:
  - src/ui/tabs/SaveTab.tsx
  - src/style.css
autonomous: true

must_haves:
  truths:
    - "Settings fieldsets and checkbox groups look intentionally styled on mobile."
    - "All settings controls meet the 44px touch target standard."
  artifacts:
    - path: src/ui/tabs/SaveTab.tsx
      provides: "Settings UI markup"
      contains: "Settings"
    - path: src/style.css
      provides: "Fieldset/legend/checkbox styling"
      contains: "fieldset"
---

<objective>
Polish the Settings tab UI for mobile by restyling fieldsets/legends and checkbox groups to match the app’s design system.

Purpose: Deliver MOBILE-03 and support MOBILE-09.
Output: Mobile-friendly settings controls with consistent spacing and focus styling.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@src/ui/tabs/SaveTab.tsx
@src/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle fieldsets and checkbox groups in Settings</name>
  <files>
src/ui/tabs/SaveTab.tsx
src/style.css
  </files>
  <action>
- Add small semantic wrappers/classes in `SaveTab.tsx` only if needed (avoid selector churn).
- Implement CSS for:
  - `fieldset` / `legend` layout and spacing
  - checkbox rows (label spacing, touch target height, focus-visible)
  - group separators or card-like grouping using existing `.panel` / `.card` primitives

Ensure:
- keyboard focus remains visible
- controls remain usable in dark/light themes
  </action>
  <verify>pnpm -s run typecheck</verify>
  <done>
- Settings looks polished and touch-friendly on mobile widths.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
</verification>

<success_criteria>
- MOBILE-03 satisfied and Settings touch targets meet MOBILE-09.
</success_criteria>

<output>
After completion, create `.planning/phases/47-mobile-and-ui-polish/47-02-SUMMARY.md`
</output>

