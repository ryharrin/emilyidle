---
phase: 52-ux-redesign-spec
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/components/PageTabRail.tsx
  - src/ui/components/StatsHeader.tsx
  - src/ui/tabs/CatalogTab.tsx
  - src/style.css
  - tests/catalog.unit.test.tsx
  - tests/mobile-responsive.unit.test.tsx
autonomous: true

must_haves:
  truths:
    - "Primary actions are visually dominant in Catalog cards and key panels."
    - "Tab overflow discoverability and active-state emphasis are clearer without breaking existing tab IDs."
    - "Dense text/metric hierarchy is improved in StatsHeader and card metadata."
  artifacts:
    - path: "src/ui/tabs/CatalogTab.tsx"
      provides: "Primary-vs-secondary action hierarchy scaffolding"
    - path: "src/ui/components/PageTabRail.tsx"
      provides: "Stronger active and overflow cues"
    - path: "src/ui/components/StatsHeader.tsx"
      provides: "Improved metric hierarchy"
---

<objective>
Ship visual hierarchy foundations: make primary actions unmistakable, increase navigation discoverability, and improve readability in high-density surfaces.
</objective>

<context>
@.planning/phases/52-ux-redesign-spec/52-CONTEXT.md
@.planning/phases/52-ux-redesign-spec/52-01-DESIGN.md
@src/ui/tabs/CatalogTab.tsx
@src/ui/components/PageTabRail.tsx
@src/ui/components/StatsHeader.tsx
@src/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply primary/secondary action hierarchy in catalog cards</name>
  <files>
src/ui/tabs/CatalogTab.tsx
src/style.css
  </files>
  <action>
- Promote one primary CTA per card (`Buy` or `Interact`) with stronger contrast/weight.
- Demote `Favorite`, `Compare`, and tertiary actions to secondary visual tier.
- Keep `data-testid` anchors intact; add new wrappers/classes only.
  </action>
  <verify>pnpm test:unit -- tests/catalog.unit.test.tsx</verify>
  <done>
Catalog cards communicate action priority without removing existing capabilities.
  </done>
</task>

<task type="auto">
  <name>Task 2: Improve tab rail active state and overflow discoverability</name>
  <files>
src/ui/components/PageTabRail.tsx
src/style.css
  </files>
  <action>
- Strengthen active tab styling and add subtle edge/overflow cue treatment.
- Preserve tab role/id contracts and keyboard behavior.
- Ensure visual improvements degrade gracefully in reduced-motion mode.
  </action>
  <verify>pnpm test:e2e -- tests/selectors-contract.spec.ts</verify>
  <done>
Tabs are easier to scan, and hidden overflow is more discoverable.
  </done>
</task>

<task type="auto">
  <name>Task 3: Improve dense metric typography hierarchy</name>
  <files>
src/ui/components/StatsHeader.tsx
src/style.css
tests/mobile-responsive.unit.test.tsx
  </files>
  <action>
- Increase contrast/weight separation between labels and critical numeric values.
- Normalize minimum text sizes and spacing in dense metric blocks.
- Verify no responsive regressions in existing mobile tests.
  </action>
  <verify>pnpm test:unit -- tests/mobile-responsive.unit.test.tsx</verify>
  <done>
High-priority values are easier to parse in one glance.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/catalog.unit.test.tsx tests/mobile-responsive.unit.test.tsx`
- `pnpm test:e2e -- tests/selectors-contract.spec.ts`
</verification>

<output>
After completion, create `.planning/phases/52-ux-redesign-spec/52-01-SUMMARY.md`
</output>
