---
phase: 52-ux-redesign-spec
plan: 2
type: execute
wave: 2
depends_on:
  - "52-01"
files_modified:
  - src/ui/tabs/CatalogTab.tsx
  - src/ui/components/CatalogCardDetailsSheet.tsx
  - src/ui/tabs/SaveTab.tsx
  - src/style.css
  - tests/catalog.unit.test.tsx
  - tests/mobile-responsive.unit.test.tsx
  - tests/modal-interactions.spec.ts
autonomous: true

must_haves:
  truths:
    - "Mobile catalog defaults to compact, scan-friendly presentation with optional expansion."
    - "High-frequency controls meet practical 44px interaction targets in catalog/settings."
    - "Low-frequency card actions are discoverable via details/bottom-sheet flow rather than always visible."
  artifacts:
    - path: "src/ui/tabs/CatalogTab.tsx"
      provides: "Compact catalog rows + density toggle + sticky quick-action rail"
    - path: "src/ui/components/CatalogCardDetailsSheet.tsx"
      provides: "Secondary/tertiary action host for compact mode"
    - path: "src/ui/tabs/SaveTab.tsx"
      provides: "Improved settings/toggle tap geometry"
---

<objective>
Reduce mobile friction in high-density screens by introducing compact catalog defaults, better tap geometry, and staged access to low-frequency actions.
</objective>

<context>
@.planning/phases/52-ux-redesign-spec/52-CONTEXT.md
@.planning/phases/52-ux-redesign-spec/52-01-DESIGN.md
@src/ui/tabs/CatalogTab.tsx
@src/ui/components/CatalogCardDetailsSheet.tsx
@src/ui/tabs/SaveTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement catalog compact mode and sticky quick-action row</name>
  <files>
src/ui/tabs/CatalogTab.tsx
src/style.css
tests/catalog.unit.test.tsx
  </files>
  <action>
- Add a `compact`/`expanded` density toggle (default `compact` on mobile viewports).
- Add sticky quick-action controls (`Filters`, `Sort`, `Density`) in catalog mobile flow.
- Keep existing filtering/sorting/test IDs stable and additive.
  </action>
  <verify>pnpm test:unit -- tests/catalog.unit.test.tsx</verify>
  <done>
Mobile catalog scan cost is reduced while full detail remains available.
  </done>
</task>

<task type="auto">
  <name>Task 2: Move secondary actions into details sheet in compact mode</name>
  <files>
src/ui/components/CatalogCardDetailsSheet.tsx
src/ui/tabs/CatalogTab.tsx
src/style.css
  </files>
  <action>
- In compact mode, keep primary CTA inline and route low-frequency actions through details sheet.
- Ensure compare/favorite/details flows remain one-tap reachable.
- Preserve modal focus/scroll-lock behavior.
  </action>
  <verify>pnpm test:e2e -- tests/modal-interactions.spec.ts</verify>
  <done>
Compact cards remain actionable without visual overload.
  </done>
</task>

<task type="auto">
  <name>Task 3: Improve settings tap targets and spacing</name>
  <files>
src/ui/tabs/SaveTab.tsx
src/style.css
tests/mobile-responsive.unit.test.tsx
  </files>
  <action>
- Increase vertical spacing and input/toggle hit area in settings groups.
- Keep existing section grouping and storage behavior unchanged.
- Validate mobile layout consistency in responsive tests.
  </action>
  <verify>pnpm test:unit -- tests/mobile-responsive.unit.test.tsx</verify>
  <done>
Settings interactions are easier and less error-prone on touch devices.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/catalog.unit.test.tsx tests/mobile-responsive.unit.test.tsx`
- `pnpm test:e2e -- tests/modal-interactions.spec.ts`
</verification>

<output>
After completion, create `.planning/phases/52-ux-redesign-spec/52-02-SUMMARY.md`
</output>
