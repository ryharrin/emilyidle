---
phase: 50-catalog-collection-depth
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/components/catalog/WatchComparePanel.tsx
  - src/ui/components/catalog/catalogCompare.css
  - src/ui/tabs/CatalogTab.tsx
  - tests/catalog.unit.test.tsx
  - tests/catalog-tier-sections.spec.ts
  - tests/catalog-compare.spec.ts
autonomous: true

must_haves:
  truths:
    - "Players can pick two watches and compare stats, price, tier, and movement side-by-side."
    - "Catalog ownership tabs show readiness cues when quick actions are available."
    - "Compare interactions are additive and do not break existing filter/tab semantics."
  artifacts:
    - path: "src/ui/components/catalog/WatchComparePanel.tsx"
      provides: "Side-by-side comparison UI for two selected watches"
    - path: "src/ui/tabs/CatalogTab.tsx"
      provides: "Catalog compare state + tab-ready indicator wiring"
    - path: "tests/catalog-compare.spec.ts"
      provides: "E2E validation of compare flow and readiness cues"
  key_links:
    - from: "src/ui/tabs/CatalogTab.tsx"
      to: "src/ui/components/catalog/WatchComparePanel.tsx"
      via: "selected watch IDs and computed compare payload"
      pattern: "WatchComparePanel"
    - from: "src/ui/tabs/CatalogTab.tsx"
      to: "catalog owned/unowned tab buttons"
      via: "tab-ready badge rendering"
      pattern: "catalog-tab-ready-unowned|catalog-tab-ready-owned"
---

<objective>
Add Catalog compare tooling and tab-level quick-action readiness indicators.

Purpose: Complete `COMPARE-01` and `TAB-04` with additive, test-safe catalog UX.
Output: Compare panel component + CatalogTab state wiring + regression coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/50-catalog-collection-depth/50-CONTEXT.md
@src/ui/tabs/CatalogTab.tsx
@src/ui/components/PerWatchStatsTable.tsx
@tests/catalog-tier-sections.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build reusable side-by-side compare panel</name>
  <files>
src/ui/components/catalog/WatchComparePanel.tsx
src/ui/components/catalog/catalogCompare.css
  </files>
  <action>
Create a compare panel component that accepts two selected watch payloads and renders aligned columns.

Required fields per side:
- watch identity (brand/model),
- tier badge + movement,
- price/purchase gate state,
- enjoyment/sec and cash/sec values.

Include additive controls:
- clear slot,
- clear all compare,
- optional swap order.

Add stable anchors such as `compare-slot-a`, `compare-slot-b`, and `compare-clear-all`.
  </action>
  <verify>pnpm typecheck</verify>
  <done>
- Compare panel renders two-sided watch data with stable test anchors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate compare state and catalog tab readiness badges</name>
  <files>
src/ui/tabs/CatalogTab.tsx
  </files>
  <action>
Wire compare interactions into catalog cards and ownership tabs.

Implementation requirements:
- Add compare selection state capped at two entries.
- Add compare toggles on cards (`data-testid="catalog-compare-toggle-{entryId}"`).
- Mount `WatchComparePanel` in Catalog with graceful empty-state copy.

Implement `TAB-04` readiness indicators for catalog-owned sub-tabs:
- Unowned tab badge when at least one visible unowned watch is buy-ready.
- Owned tab badge when at least one visible owned watch has immediate quick action (interaction or dismantle).
- Render additive badge anchors: `catalog-tab-ready-unowned`, `catalog-tab-ready-owned`.

Do not rename existing `catalog-unowned-tab` / `catalog-owned-tab` role+id contracts.
  </action>
  <verify>pnpm test:unit -- tests/catalog.unit.test.tsx</verify>
  <done>
- Catalog supports side-by-side compare and readiness badges without regressing tab semantics.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add compare/readiness regression coverage</name>
  <files>
tests/catalog-tier-sections.spec.ts
tests/catalog-compare.spec.ts
  </files>
  <action>
Add/extend e2e checks for:
- selecting two watches into compare,
- compare panel rendering key fields side-by-side,
- catalog-owned tab readiness badge visibility under seeded states.

Keep assertions role/testid-driven rather than text-fragile.
  </action>
  <verify>pnpm test:e2e -- tests/catalog-tier-sections.spec.ts tests/catalog-compare.spec.ts</verify>
  <done>
- Compare and readiness behavior are covered by Playwright regressions.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/catalog.unit.test.tsx`
- `pnpm test:e2e -- tests/catalog-tier-sections.spec.ts tests/catalog-compare.spec.ts`
</verification>

<success_criteria>
- `COMPARE-01`: users can compare two watches side-by-side with stats, price, tier, and movement.
- `TAB-04`: catalog owned/unowned tabs show quick-action readiness indicators.
</success_criteria>

<output>
After completion, create `.planning/phases/50-catalog-collection-depth/50-03-SUMMARY.md`
</output>
