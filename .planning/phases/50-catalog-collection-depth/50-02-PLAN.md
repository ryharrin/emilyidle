---
phase: 50-catalog-collection-depth
plan: 2
type: execute
wave: 2
depends_on:
  - 50-01
files_modified:
  - src/ui/components/CollectionInsightsPanel.tsx
  - src/ui/components/CollectionTierSegments.tsx
  - src/ui/components/collectionDepth.css
  - src/ui/tabs/CollectionTab.tsx
  - tests/collection.unit.test.tsx
  - tests/collection-loop.spec.ts
autonomous: true

must_haves:
  truths:
    - "Collection shows explicit set bonus progress as X/Y completion with remaining requirements."
    - "Collection shows a next prestige preview with clear threshold and reward context."
    - "Collection analytics reveal most valuable watch and brand/era/tier breakdowns."
    - "Collection navigation includes Starter/Mid/Lux segmented anchors reachable from sticky subnav."
  artifacts:
    - path: "src/ui/components/CollectionInsightsPanel.tsx"
      provides: "Reusable panel for set bonus progress, prestige preview, and analytics summaries"
    - path: "src/ui/components/CollectionTierSegments.tsx"
      provides: "Tier-segmented Collection sections with stable anchor IDs"
    - path: "src/ui/tabs/CollectionTab.tsx"
      provides: "Integrated Collection depth layout and segmented subnav wiring"
  key_links:
    - from: "src/ui/tabs/CollectionTab.tsx"
      to: "src/game/state.ts"
      via: "collection insight selectors consumed by Collection components"
      pattern: "getSetBonusProgressRows|getNextPrestigePreview|getCollectionAnalyticsSnapshot"
    - from: "src/ui/tabs/CollectionTab.tsx"
      to: "src/ui/components/CollectionSectionNav.tsx"
      via: "Starter/Mid/Lux anchor link definitions"
      pattern: "collection-segment-starter|collection-segment-mid|collection-segment-lux"
---

<objective>
Integrate Collection depth surfaces for set bonus progress, prestige motivation, analytics visibility, and tier-segmented navigation.

Purpose: Complete `SETBONUS-01`, `PRESTIGE-01`, `COLLECT-01`, and `VAULT-02` on the Collection tab.
Output: Modular Collection depth components + CollectionTab wiring + regression coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/50-catalog-collection-depth/50-CONTEXT.md
@.planning/phases/50-catalog-collection-depth/50-01-PLAN.md

@src/ui/tabs/CollectionTab.tsx
@src/ui/components/CollectionSectionNav.tsx
@tests/collection.unit.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build modular Collection depth components</name>
  <files>
src/ui/components/CollectionInsightsPanel.tsx
src/ui/components/CollectionTierSegments.tsx
src/ui/components/collectionDepth.css
  </files>
  <action>
Create reusable UI components that keep CollectionTab from absorbing additional large inline blocks.

Requirements:
- `CollectionInsightsPanel` renders:
  - set bonus progress cards (`X / Y met`, next requirement, active/inactive state),
  - next prestige preview summary (nearest target, remaining amount, effect summary),
  - analytics cards (most valuable watch, brand distribution, era distribution, tier totals).
- `CollectionTierSegments` renders three segmented sections with stable IDs:
  - `collection-segment-starter`
  - `collection-segment-mid`
  - `collection-segment-lux`
- Add additive `data-testid` anchors for each panel/segment.

Use component-scoped CSS (`collectionDepth.css`) imported by the new components; avoid touching monolithic styling.
  </action>
  <verify>pnpm typecheck</verify>
  <done>
- New Collection depth components exist with stable IDs/testids and no selector math duplicated in JSX.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire CollectionTab to depth selectors and segmented nav</name>
  <files>
src/ui/tabs/CollectionTab.tsx
  </files>
  <action>
Integrate the new components into CollectionTab and feed them from selector outputs created in `50-01`.

Implementation requirements:
- Replace/augment inline set-bonus and prestige snippets with `CollectionInsightsPanel`.
- Extend section nav link definitions to include Starter/Mid/Lux segment links.
- Mount `CollectionTierSegments` so sticky nav can jump to each tier segment section.
- Preserve existing IDs/testids and coachmark behavior; this is additive, not a rewrite.
- Keep existing explain buttons (especially tier badge education) functional.
  </action>
  <verify>pnpm test:unit -- tests/collection.unit.test.tsx</verify>
  <done>
- Collection tab surfaces progress, prestige preview, analytics, and segment anchors in one coherent flow.
- Sticky section navigation can reach the new tier segments.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend Collection unit and e2e coverage</name>
  <files>
tests/collection.unit.test.tsx
tests/collection-loop.spec.ts
  </files>
  <action>
Expand tests to guard new behavior:
- Unit: set bonus progress text/ratios and analytics panel rendering.
- E2E: segmented nav links jump to Starter/Mid/Lux sections and the new insights panel is visible.

Use role/testid assertions; avoid brittle full-copy snapshots.
  </action>
  <verify>pnpm test:e2e -- tests/collection-loop.spec.ts</verify>
  <done>
- Collection depth features are covered by automated tests for regression safety.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/collection.unit.test.tsx`
- `pnpm test:e2e -- tests/collection-loop.spec.ts`
</verification>

<success_criteria>
- `SETBONUS-01`: every set bonus shows completion progress and remaining requirements.
- `PRESTIGE-01`: nearest prestige unlock is clearly previewed on Collection.
- `COLLECT-01`: Collection analytics panel shows most valuable watch and breakdowns.
- `VAULT-02`: Collection is segmented by Starter/Mid/Lux via subnav anchors.
</success_criteria>

<output>
After completion, create `.planning/phases/50-catalog-collection-depth/50-02-SUMMARY.md`
</output>
