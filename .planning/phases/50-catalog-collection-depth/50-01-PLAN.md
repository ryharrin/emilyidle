---
phase: 50-catalog-collection-depth
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/selectors/collectionInsights.ts
  - src/game/selectors/index.ts
  - src/game/state.ts
  - tests/collection-analytics.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "Set bonus completion and remaining requirements are computed deterministically from collection state."
    - "Next prestige preview exposes the nearest threshold, remaining gap, and reward summary."
    - "Collection analytics expose most valuable watch plus brand/era/tier breakdowns for UI consumption."
  artifacts:
    - path: "src/game/selectors/collectionInsights.ts"
      provides: "Typed selector helpers for set bonus progress, prestige preview, and collection analytics"
    - path: "src/game/state.ts"
      provides: "Facade exports so UI can consume collection insight helpers without deep imports"
    - path: "tests/collection-analytics.unit.test.ts"
      provides: "Unit coverage for deterministic selector outputs"
  key_links:
    - from: "src/game/state.ts"
      to: "src/game/selectors/collectionInsights.ts"
      via: "selector export surface"
      pattern: "getSetBonusProgressRows|getNextPrestigePreview|getCollectionAnalyticsSnapshot"
    - from: "tests/collection-analytics.unit.test.ts"
      to: "src/game/selectors/collectionInsights.ts"
      via: "seeded-state selector assertions"
      pattern: "getSetBonusProgressRows|getNextPrestigePreview"
---

<objective>
Create selector foundations for Collection depth: set bonus progress, prestige previews, and analytics view models.

Purpose: `SETBONUS-01`, `PRESTIGE-01`, and `COLLECT-01` need deterministic domain outputs before UI wiring.
Output: New selector module + state facade exports + focused unit tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/50-catalog-collection-depth/50-CONTEXT.md
@.planning/phases/50-catalog-collection-depth/50-RESEARCH.md

@src/game/selectors/index.ts
@src/game/state.ts
@src/ui/tabs/CollectionTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add collection insight selector module</name>
  <files>
src/game/selectors/collectionInsights.ts
  </files>
  <action>
Create a new selector module with typed outputs for phase-50 collection depth surfaces.

Implement at minimum:
- `getSetBonusProgressRows(state)` returning one row per set bonus with:
  - requirement totals (`requiredCount`, `metCount`, `remainingCount`),
  - progress ratio (0..1),
  - per-item requirement entries (`itemId`, `currentCount`, `requiredCount`, `met`),
  - `active` and concise `nextNeedLabel` for UI copy.
- `getNextPrestigePreview(state)` returning the nearest locked prestige target among workshop/maison/nostalgia with:
  - `id`, `label`, `current`, `threshold`, `remaining`, `ratio`, `effectSummary`.
- `getCollectionAnalyticsSnapshot(state)` returning:
  - most valuable owned model,
  - brand distribution,
  - era distribution,
  - tier distribution and totals.

Use existing model/catalog/selectors data. Keep the module pure and deterministic (`Date.now()` and browser APIs are forbidden).
  </action>
  <verify>pnpm typecheck</verify>
  <done>
- Selector module exists with typed, deterministic outputs for set bonus/progress/prestige/analytics.
- All ratios and counts are clamped/safe for empty or partial collection states.
  </done>
</task>

<task type="auto">
  <name>Task 2: Export selector helpers and add unit coverage</name>
  <files>
src/game/selectors/index.ts
src/game/state.ts
tests/collection-analytics.unit.test.ts
  </files>
  <action>
Expose the new helpers through the selector barrel and state facade so UI imports remain consistent with project conventions.

Add `tests/collection-analytics.unit.test.ts` to validate:
- set bonus progress counts and active-state transitions,
- nearest prestige target selection and remaining-gap calculations,
- analytics breakdown stability (most valuable model + brand/era/tier totals).

Seed tests with representative collection mixes (starter/mid/lux) and assert deterministic sorting/output.
  </action>
  <verify>pnpm test:unit -- tests/collection-analytics.unit.test.ts</verify>
  <done>
- New helpers are available from `src/game/state.ts`.
- Unit tests lock selector behavior for downstream UI plans.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/collection-analytics.unit.test.ts`
</verification>

<success_criteria>
- `SETBONUS-01` foundation: selector outputs expose collection progress toward each set bonus.
- `PRESTIGE-01` foundation: selector outputs expose next prestige threshold and reward summary.
- `COLLECT-01` foundation: selector outputs provide collection analytics snapshots for UI panels.
</success_criteria>

<output>
After completion, create `.planning/phases/50-catalog-collection-depth/50-01-SUMMARY.md`
</output>
