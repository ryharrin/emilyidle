---
phase: 28-wear-one-bonus
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/model/types.ts
  - src/game/model/state.ts
  - src/game/actions/index.ts
  - src/game/persistence.ts
autonomous: true
must_haves:
  truths:
    - "Game state supports an optional worn watch id (wear none allowed)."
    - "Setting a worn watch always replaces the previous worn watch (no stacking)."
    - "Worn watch selection persists across refresh; invalid/unknown ids load as wear none."
  artifacts:
    - path: src/game/model/types.ts
      provides: "Worn watch id type/state shape"
    - path: src/game/model/state.ts
      provides: "State initialization + reducer support for wornWatchId"
    - path: src/game/actions/index.ts
      provides: "Action(s) to set/clear worn watch id"
    - path: src/game/persistence.ts
      provides: "Save/load sanitization for wornWatchId"
  key_links:
    - from: src/game/persistence.ts
      to: src/game/model/state.ts
      via: "restore/sanitize pipeline"
      pattern: "wornWatchId"
    - from: src/game/actions/index.ts
      to: src/game/model/state.ts
      via: "dispatch -> reducer"
      pattern: "wornWatchId"
---

<objective>
Add first-class “worn watch” state with strict persistence.

Purpose: Establish a single-slot equip primitive (wear one or none) that is safe under strict persistence sanitization.
Output: State fields + actions + persistence roundtrip for `wornWatchId`.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/28-wear-one-bonus/28-CONTEXT.md
@.planning/phases/28-wear-one-bonus/28-RESEARCH.md

@src/game/model/types.ts
@src/game/model/state.ts
@src/game/actions/index.ts
@src/game/persistence.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add wornWatchId to model state (nullable)</name>
  <files>src/game/model/types.ts, src/game/model/state.ts</files>
  <action>
Add an optional worn watch field to the persisted game state, allowing “wear none”:
- Prefer `wornWatchId: string | null` (or existing WatchId type if one exists) and keep it nullable.
- Initialize to `null` in the default/initial state.
- Ensure any reducer/state-update logic treats this as a single slot (overwrite the value; no arrays/sets; no stacking).

Guardrails:
- Keep types strict; no `any`.
- Do not add derived logic here (bonuses/selectors belong in later plans).
  </action>
  <verify>pnpm run typecheck</verify>
  <done>
State compiles with strict TS; initial state includes `wornWatchId: null`; reducer/state container supports updating the field without introducing stacking.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add an action to wear/clear a watch with validation</name>
  <files>src/game/actions/index.ts, src/game/model/state.ts</files>
  <action>
Add a single action API used by UI and tests:
- Implement something like `setWornWatchId(id: WatchId | null)` or `wearWatch(id: WatchId | null)`.
- It must always replace the previous value (assignment).
- Validate inputs defensively:
  - If `id` is non-null but not a known watch id or not owned, coerce to `null`.
  - Keep validation simple and local (use existing owned inventory data already in state; do NOT reach into UI).

Notes:
- Unowned watches will hide controls in UI, but validation must still exist to protect persistence and future callers.
  </action>
  <verify>pnpm run typecheck</verify>
  <done>
There is exactly one public action to set/clear worn watch; setting a new id overwrites the prior worn id; invalid/unowned id results in `null`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Thread wornWatchId through strict persistence sanitization</name>
  <files>src/game/persistence.ts</files>
  <action>
Update save/load to include `wornWatchId` and preserve strictness:
- Include `wornWatchId` in the saved payload.
- On load/restore, sanitize:
  - If missing -> default `null`.
  - If not a string/null -> default `null`.
  - If string but unknown watch id -> default `null`.
  - (Optional but preferred) If known id but not owned in restored state -> default `null`.

Important:
- Do not weaken “unknown keys dropped” behavior.
- Keep the sanitizer aligned with existing patterns in `src/game/persistence.ts`.
  </action>
  <verify>pnpm run typecheck</verify>
  <done>
Refreshing the app can preserve a valid worn watch id; malformed/unknown values load safely as wear none; strict persistence behavior remains intact.
  </done>
</task>
</tasks>

<verification>
- `pnpm run typecheck`
</verification>

<success_criteria>
- `wornWatchId` exists in model state, defaults to null, can be set/cleared via a single action, and roundtrips through persistence with strict sanitization.
</success_criteria>

<output>
After completion, create `.planning/phases/28-wear-one-bonus/28-01-SUMMARY.md`
</output>
