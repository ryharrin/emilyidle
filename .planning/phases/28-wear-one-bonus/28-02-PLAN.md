---
phase: 28-wear-one-bonus
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - src/game/data/items.ts
  - src/game/selectors/enjoyment.ts
  - src/game/selectors/index.ts
autonomous: true
must_haves:
  truths:
    - "Wearing a watch changes enjoyment rate via a distinct bucket-based multiplier."
    - "Enjoyment breakdown shows a single worn-watch bonus line only when a watch is worn."
    - "Switching worn watch updates multiplier and breakdown immediately."
  artifacts:
    - path: src/game/data/items.ts
      provides: "Watch bucket lookup for ids (starter/classic/chronograph/tourbillon)"
    - path: src/game/selectors/enjoyment.ts
      provides: "Selector for worn enjoyment multiplier + breakdown integration"
    - path: src/game/selectors/index.ts
      provides: "Exported selector(s) used by UI/tests"
  key_links:
    - from: src/game/selectors/enjoyment.ts
      to: src/game/data/items.ts
      via: "bucket lookup"
      pattern: "(starter|classic|chronograph|tourbillon)"
    - from: src/game/selectors/enjoyment.ts
      to: "enjoyment breakdown term list"
      via: "conditional term insertion"
      pattern: "worn"
---

<objective>
Implement the worn-watch enjoyment bonus and make it visible in the enjoyment breakdown.

Purpose: Deliver the “unique bonus” outcome (WATCH-03) in a way that is immediately visible and updates on switch.
Output: Bucket-based multiplier selector + enjoyment breakdown term.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/28-wear-one-bonus/28-CONTEXT.md
@.planning/phases/28-wear-one-bonus/28-RESEARCH.md

@src/game/data/items.ts
@src/game/selectors/enjoyment.ts
@src/game/selectors/index.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add bucket lookup for watch ids</name>
  <files>src/game/data/items.ts</files>
  <action>
Implement a stable way to map a watch id to one of the existing buckets:
- Buckets: `starter`, `classic`, `chronograph`, `tourbillon`.
- Reuse existing watch id definitions/buckets already present in the codebase (do not invent new ids).
- Export a helper like `getWatchBucket(id): "starter" | "classic" | "chronograph" | "tourbillon" | null`.

Rules:
- Return `null` if id is unknown or not a watch.
- Keep this function pure and deterministic.
  </action>
  <verify>pnpm run typecheck</verify>
  <done>
There is a single exported helper that maps known watch ids to the 4 buckets and returns null for unknown ids.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create worn enjoyment multiplier selector (single source of truth)</name>
  <files>src/game/selectors/enjoyment.ts, src/game/selectors/index.ts</files>
  <action>
Add a selector that computes the worn enjoyment multiplier from `state.wornWatchId`:
- If `wornWatchId` is null -> multiplier is `1`.
- If bucket is:
  - starter: `1.02`
  - classic: `1.05`
  - chronograph: `1.08`
  - tourbillon: `1.12`
- If bucket lookup returns null -> multiplier `1` (defensive).

Export this selector via `src/game/selectors/index.ts` so UI/tests can use it.

Important:
- This must be the only place the multiplier values live (no duplicated constants).
  </action>
  <verify>pnpm run typecheck</verify>
  <done>
Selector exists, exported, returns 1 when wear none, and distinct multipliers per bucket when worn.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire multiplier into enjoyment rate + enjoyment breakdown term list</name>
  <files>src/game/selectors/enjoyment.ts, src/game/selectors/index.ts</files>
  <action>
Wire the worn multiplier into:
1) The enjoyment rate math (so gameplay changes).
2) The enjoyment breakdown term list (so it's visible).

Specifically:
- Update `getEnjoymentRateBreakdown()` in `src/game/selectors/index.ts` to insert a multiplier term with `id: "worn-watch"` only when a valid worn watch is present.
- Ensure the breakdown's `effectiveCentsPerSec` incorporates the worn multiplier (and continues to respect any event multiplier behavior in the existing breakdown).

Breakdown rules (locked):
- Show the worn-watch bonus in the enjoyment breakdown only.
- Omit the term entirely when `wornWatchId` is null.
- Ensure the breakdown term has a stable identifier (prefer `id: "worn-watch"`) so UI wiring can target it reliably.
- Display as a one-line numeric effect (use either `x1.08` style or `+8%` style, but keep it consistent and easy to scan).
- Ensure switching watches updates the computed breakdown immediately.

Guardrails:
- Do not add worn-watch terms to other breakdowns.
- Keep ordering stable (place near other multipliers if the breakdown is ordered).
  </action>
  <verify>pnpm run typecheck</verify>
  <done>
Enjoyment rate incorporates the worn multiplier; breakdown shows exactly one worn-watch line when worn and none when wear none.
  </done>
</task>
</tasks>

<verification>
- `pnpm run typecheck`
</verification>

<success_criteria>
- Equipping a watch changes enjoyment rate and adds a single worn-watch breakdown line; clearing worn watch removes the line; switching worn watch updates multiplier immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/28-wear-one-bonus/28-02-SUMMARY.md`
</output>
