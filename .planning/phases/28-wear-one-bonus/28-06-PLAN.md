---
phase: 28-wear-one-bonus
plan: 06
type: execute
wave: 4
depends_on:
  - 28-03
  - 28-05
files_modified:
  - tests/wear-one-bonus.spec.ts
autonomous: true
must_haves:
  truths:
    - "E2E: User can wear a watch from Collection and see Equipped badge and Worn summary update."
    - "E2E: Switching worn watch updates enjoyment breakdown line immediately."
    - "E2E: ExplainButton for worn-watch bonus opens Help modal content."
  artifacts:
    - path: tests/wear-one-bonus.spec.ts
      provides: "Playwright coverage for equip UX + stats breakdown visibility"
  key_links:
    - from: tests/wear-one-bonus.spec.ts
      to: src/ui/tabs/CollectionTab.tsx
      via: "data-testid selectors"
      pattern: "worn-watch|watch-wear|watch-equipped"
    - from: tests/wear-one-bonus.spec.ts
      to: src/ui/tabs/StatsTab.tsx
      via: "breakdown assertions"
      pattern: "enjoyment"
---

<objective>
Add Playwright coverage for the wear-one flow and the visible bonus in Stats.

Purpose: Prevent regressions in the core user story and ensure UX is correct on the integrated app.
Output: A deterministic E2E test validating equip/switch/unequip and Stats breakdown + explanation.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/ui/tabs/CollectionTab.tsx
@src/ui/tabs/StatsTab.tsx
@src/ui/help/helpContent.ts
@tests/explanations.spec.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create wear-one Playwright spec using stable test ids</name>
  <files>tests/wear-one-bonus.spec.ts</files>
  <action>
Create a new Playwright spec that covers the full flow:

Setup:
- Seed the game state so the user owns at least 2 watches in different buckets (reuse existing test seeding patterns; do not click-through long progression).
- Ensure the app loads deterministically (use existing helpers in tests).

Flow assertions:
1) Go to Collection tab.
2) Confirm the Worn summary exists: `worn-watch-summary`.
3) Wear watch A via `watch-wear-{watchIdA}`:
   - `watch-equipped-{watchIdA}` appears.
   - Summary reflects worn watch (at minimum: not "None").
4) Click Change (`worn-watch-change`) to open modal:
   - Modal appears `worn-watch-picker-modal`.
   - Choose watch B via `worn-watch-option-{watchIdB}`:
     - Equipped badge moves from A to B (A removed, B present).
5) Go to Stats tab:
   - Confirm enjoyment breakdown includes a worn-watch line (assert by matching the label/value used by UI).
   - Confirm it changes when switching from A to B (value changes).
6) Click the Explain button for worn-watch bonus (if added):
   - Help modal opens with "Worn watch bonus" content visible.

Also test wear none:
- From Change modal select `worn-watch-option-none`.
- Confirm equipped badge is gone and Stats omits the worn-watch line.

Constraints:
- Use `data-testid` selectors whenever available; avoid brittle text-only selectors except for help title.
  </action>
  <verify>pnpm run test:e2e</verify>
  <done>
Playwright test passes and validates: wear one, switch updates immediately, wear none clears, and Stats breakdown + explanation behave as expected.
  </done>
</task>
</tasks>

<verification>
- `pnpm run test:e2e`
</verification>

<success_criteria>
- E2E suite covers the equipped UX and the visible worn-watch enjoyment bonus + explanation and passes on CI/local.
</success_criteria>

<output>
After completion, create `.planning/phases/28-wear-one-bonus/28-06-SUMMARY.md`
</output>
