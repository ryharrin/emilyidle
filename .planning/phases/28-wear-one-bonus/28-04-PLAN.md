---
phase: 28-wear-one-bonus
plan: 04
type: execute
wave: 3
depends_on:
  - 28-02
files_modified:
  - tests/rate-breakdowns.unit.test.ts
  - tests/persistence.unit.test.ts
autonomous: true
must_haves:
  truths:
    - "Unit tests prove worn watch multiplier values per bucket and default=1 when wear none."
    - "Unit tests prove enjoyment breakdown includes/omits worn-watch term correctly."
    - "Unit tests prove persistence roundtrips wornWatchId and sanitizes invalid values."
  artifacts:
    - path: tests/rate-breakdowns.unit.test.ts
      provides: "Enjoyment breakdown term coverage for worn watch"
    - path: tests/persistence.unit.test.ts
      provides: "Persistence sanitize/roundtrip coverage for wornWatchId"
  key_links:
    - from: tests/rate-breakdowns.unit.test.ts
      to: src/game/selectors/enjoyment.ts
      via: "selector invocation"
      pattern: "worn"
    - from: tests/persistence.unit.test.ts
      to: src/game/persistence.ts
      via: "save/load"
      pattern: "wornWatchId"
---

<objective>
Add unit test coverage for worn watch persistence and enjoyment breakdown.

Purpose: Lock in strict persistence behavior and prevent regressions in the visible bonus line.
Output: Deterministic Vitest coverage for wornWatchId and worn enjoyment bonus term.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@tests/rate-breakdowns.unit.test.ts
@src/game/selectors/enjoyment.ts
@src/game/persistence.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add enjoyment breakdown unit tests for worn-watch term</name>
  <files>tests/rate-breakdowns.unit.test.ts</files>
  <action>
Extend existing breakdown tests to cover worn watch:
- Case A: wear none -> enjoyment breakdown has no worn-watch term at all.
- Case B: wear starter/classic/chronograph/tourbillon -> enjoyment breakdown includes exactly one worn-watch term with the expected numeric effect (match the chosen display format, e.g. `x1.08`).
- Case C: switching worn watch changes the term's effect accordingly.

Guidance:
- Follow existing test patterns in this file for setting up state and reading breakdown terms.
- Assert both presence and absence explicitly (no partial matches).
  </action>
  <verify>pnpm run test:unit</verify>
  <done>
Unit tests reliably prove the worn-watch term appears only when worn and has correct values for each bucket.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add persistence unit tests for wornWatchId strict sanitization</name>
  <files>tests/persistence.unit.test.ts</files>
  <action>
Create a focused persistence test file that validates:
- Roundtrip: saving state with `wornWatchId` and reloading restores it.
- Missing key: load without `wornWatchId` defaults to null.
- Invalid value: non-string/non-null sanitizes to null.
- Unknown id: sanitizes to null.

Guidance:
- Use existing persistence helpers/exports used elsewhere in tests (do not reimplement persistence).
- Keep tests deterministic; do not depend on timers or UI.
  </action>
  <verify>pnpm run test:unit</verify>
  <done>
Persistence tests pass and demonstrate strict sanitization and safe defaulting for wornWatchId.
  </done>
</task>
</tasks>

<verification>
- `pnpm run test:unit`
</verification>

<success_criteria>
- Vitest suite covers worn watch persistence + enjoyment breakdown line behavior with clear, deterministic assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/28-wear-one-bonus/28-04-SUMMARY.md`
</output>
