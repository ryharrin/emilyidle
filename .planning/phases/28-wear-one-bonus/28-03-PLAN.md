---
phase: 28-wear-one-bonus
plan: 03
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - src/ui/tabs/CollectionTab.tsx
autonomous: true
must_haves:
  truths:
    - "User can wear exactly one owned watch from the Vault/Collection list via one-click Wear."
    - "The worn watch is clearly indicated with an Equipped badge on its card."
    - "A Worn watch summary card appears at the top; Change opens a picker modal of owned watches; wear none is allowed."
  artifacts:
    - path: src/ui/tabs/CollectionTab.tsx
      provides: "Wear controls + worn summary card + picker modal + testids"
  key_links:
    - from: src/ui/tabs/CollectionTab.tsx
      to: "setWornWatchId action"
      via: "onClick handler"
      pattern: "wornWatchId"
---

<objective>
Add the equip UX in Vault/Collection (wear one or none) with clear worn indication and stable test ids.

Purpose: Deliver the primary equip location/interaction and make the worn slot visible and controllable.
Output: Wear button on owned watch cards, Equipped badge, Worn watch summary card, and owned-watch picker modal.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/28-wear-one-bonus/28-CONTEXT.md
@.planning/phases/28-wear-one-bonus/28-RESEARCH.md

@src/ui/tabs/CollectionTab.tsx
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add Worn watch summary card at top of Collection tab</name>
  <files>src/ui/tabs/CollectionTab.tsx</files>
  <action>
Implement a "Worn watch" summary card at the top of the Vault/Collection list:
- When wear none: show "Worn watch: None" with a Change button (opens picker modal).
- When a watch is worn: show watch name + an "Equipped" state; Change button opens picker modal.
- Add stable test ids:
  - Summary card container: `data-testid="worn-watch-summary"`
  - Change button: `data-testid="worn-watch-change"`

Design constraints:
- Keep it visually consistent with existing tab cards.
- Do not add wear controls for unowned watches anywhere.
  </action>
  <verify>pnpm run typecheck</verify>
  <done>
Collection tab shows a Worn watch summary card at top with a Change button and stable test ids; wear none state is represented cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add one-click Wear control on owned watch cards + Equipped badge</name>
  <files>src/ui/tabs/CollectionTab.tsx</files>
  <action>
On each owned watch card in the Collection list:
- Render a one-click Wear button when the watch is owned and not currently worn.
- Hide Wear control for unowned watches (locked).
- When a watch is worn:
  - Show an "Equipped" badge on that card (locked).
  - Do not allow stacking; clicking Wear on a different watch immediately switches worn watch.

Add stable test ids:
- Wear button: `data-testid="watch-wear-{watchId}"`
- Equipped badge: `data-testid="watch-equipped-{watchId}"`

Implementation notes:
- Use existing action(s) introduced in Plan 28-01.
- Do not introduce additional equip slots.
  </action>
  <verify>pnpm run typecheck</verify>
  <done>
Owned watches expose Wear; unowned watches do not; only one card shows Equipped at a time; switching worn watch updates immediately with stable test ids.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Change picker modal (owned watches + wear none)</name>
  <files>src/ui/tabs/CollectionTab.tsx</files>
  <action>
Implement the Change flow via a modal (reuse existing modal overlay pattern, e.g. the nostalgia modal approach):
- Opened via the summary card's Change button.
- Shows a list of owned watches only (locked).
- Selecting a watch immediately wears it (one click) and closes the modal.
- Include a "Wear none" option that clears worn watch and closes modal.

Add stable test ids:
- Modal container: `data-testid="worn-watch-picker-modal"`
- Owned option button rows: `data-testid="worn-watch-option-{watchId}"`
- Wear none option: `data-testid="worn-watch-option-none"`
- Modal close button: `data-testid="worn-watch-picker-close"`

Guardrails:
- Keep focus/esc/close behavior aligned with existing modals.
  </action>
  <verify>pnpm run typecheck</verify>
  <done>
Change opens a modal listing owned watches plus a wear-none option; selecting an option applies immediately, closes modal, and UI reflects equipped state with stable test ids.
  </done>
</task>
</tasks>

<verification>
- `pnpm run typecheck`
</verification>

<success_criteria>
- User can equip exactly one owned watch from Collection; equipped badge is clear; summary card + Change modal exist; wear none works; test ids are stable and present.
</success_criteria>

<output>
After completion, create `.planning/phases/28-wear-one-bonus/28-03-SUMMARY.md`
</output>
