---
phase: 35-balance-and-help-clarity
plan: 03
type: execute
wave: 1
depends_on: []
autonomous: true
gap_closure: true
files_modified:
  - src/game/selectors/therapistSessions.ts
  - tests/career-salary-window.unit.test.ts
  - tests/therapist.unit.test.tsx

must_haves:
  truths:
    - "After starting the career (PhD program) and before track choice unlock (level < 3), the player can run sessions (at least the free first session)."
    - "Running a session before track choice unlock refreshes/extends the salary window, making the salary-window loop achievable before level 3."
    - "At/after track choice unlock (level >= 3), sessions remain unavailable until a track is selected (track choice remains meaningful)."
  artifacts:
    - path: src/game/selectors/therapistSessions.ts
      provides: "Selector-level pre-track fallback session policy (no state mutation) and correct supportsSessions gating."
    - path: tests/career-salary-window.unit.test.ts
      provides: "Regression coverage that salary window refresh works before track unlock."
    - path: tests/therapist.unit.test.tsx
      provides: "Regression coverage that sessions are supported pre-track and policy terms are non-zero."
  key_links:
    - from: src/game/selectors/therapistSessions.ts
      to: src/game/data/careerTracks.ts
      via: "TRACK_CHOICE_UNLOCK_LEVEL gate for pre-track policy"
      pattern: "TRACK_CHOICE_UNLOCK_LEVEL"
    - from: src/game/actions/index.ts
      to: src/game/selectors/therapistSessions.ts
      via: "performTherapistSession -> getTherapistSessionPolicy"
      pattern: "getTherapistSessionPolicy\(state\)"
    - from: src/game/actions/index.ts
      to: src/game/selectors/therapistSalary.ts
      via: "performTherapistSession extends salaryActiveUntilMs"
      pattern: "salaryActiveUntilMs"
---

<objective>
Fix the early-career loop regression where sessions are unavailable immediately after starting the career.

Purpose: Close the Phase 35 UAT major gap by allowing pre-track sessions (before level 3) without mutating `activeTrackId`, so the salary window refresh loop is achievable.
Output: Selector-level pre-track session policy fallback + unit regressions.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

@.planning/phases/35-balance-and-help-clarity/35-UAT.md
@.planning/debug/35-sessions-unavailable.md
@.planning/phases/35-balance-and-help-clarity/35-01-SUMMARY.md

@src/game/selectors/therapistSessions.ts
@src/game/actions/index.ts
@src/game/actions/therapistCareer.ts
@src/game/data/careerTracks.ts
@tests/career-salary-window.unit.test.ts
@tests/therapist.unit.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add selector-level pre-track fallback session policy (no state mutation)</name>
  <files>
src/game/selectors/therapistSessions.ts
  </files>
  <action>
  Goal: sessions should be runnable immediately after `enterPhdProgram()` (career started) even when `activeTrackId === null`, until track choice unlocks at level 3.

  Constraints:
  - Do NOT mutate `state.therapistCareer.activeTrackId` during career start (no changes to `enterPhdProgram`).
  - Keep retirement behavior: sessions must remain disabled in the `retirement` stage.
  - Keep track choice meaningful: at/after `TRACK_CHOICE_UNLOCK_LEVEL`, sessions should remain unavailable until a track is selected.

  Implementation (selector-only):
  - Import `TRACK_CHOICE_UNLOCK_LEVEL`.
  - Add a small helper to resolve an effective track id for session *policy computation*:
    - If `activeTrackId` is set: use it.
    - Else if `careerStartId !== null`, stage is not `retirement`, and `level < TRACK_CHOICE_UNLOCK_LEVEL`: use a stable fallback track id (recommended: `"private-practice"`) ONLY for policy calculations.
    - Else: return `null`.
  - Update `doesActiveCareerTrackSupportSessions()` and `getTherapistSessionPolicy()` to use the resolved effective track id so that when pre-track sessions are allowed:
    - `supportsSessions === true`
    - `enjoymentCostCents`, `cashPayoutCents`, `cooldownMs` are computed from the fallback track id (non-zero terms).
  - Ensure `getTherapistSessionCostLabel()` and `canPerformTherapistSession()` continue to work without a chosen track in the pre-track window.
  </action>
  <verify>pnpm run typecheck</verify>
  <done>
  - With `careerStartId !== null`, `activeTrackId === null`, and `level < TRACK_CHOICE_UNLOCK_LEVEL`, `getTherapistSessionPolicy(state).supportsSessions` is true and terms are non-zero.
  - With `careerStartId !== null`, `activeTrackId === null`, and `level >= TRACK_CHOICE_UNLOCK_LEVEL`, `getTherapistSessionPolicy(state).supportsSessions` is false.
  - Retirement stage still yields `supportsSessions === false`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit regressions for pre-track sessions and salary-window refresh</name>
  <files>
tests/career-salary-window.unit.test.ts
tests/therapist.unit.test.tsx
  </files>
  <action>
  Update/add unit coverage to lock in the intended Phase 35 loop:

  - In `tests/career-salary-window.unit.test.ts`, update the "expires and refreshes via sessions" test to prove refresh works BEFORE track unlock:
    - Start from `createInitialState()` -> `enterPhdProgram()`.
    - Do NOT set `primaryTrackId`/`activeTrackId`.
    - Ensure a session can be performed (e.g. free first session + `nextAvailableAtMs: 0`).
    - Call `performTherapistSession()` after expiry and assert `salaryActiveUntilMs` extends and `getEffectiveCashRateCentsPerSec` becomes > 0.

  - In `tests/therapist.unit.test.tsx`, add a focused regression that a pre-track career state (career started, `activeTrackId: null`, `level: 1` or `2`) returns a supported session policy with non-zero `cashPayoutCents` and `cooldownMs`.
  </action>
  <verify>pnpm run test:unit</verify>
  <done>
  - Unit tests prove sessions are supported pre-track and salary window refresh works before level 3.
  - No existing therapist/career tests regress.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit`
</verification>

<success_criteria>
- Phase 35 UAT gap closes: after starting career, sessions are no longer stuck on "Sessions unavailable" solely because `activeTrackId` is null pre-level-3.
- Salary window refresh loop is achievable before track choice unlock.
- Track choice remains meaningful at level 3+: sessions require selecting a track once unlocked.
</success_criteria>

<output>
After completion, create `.planning/phases/35-balance-and-help-clarity/35-03-SUMMARY.md`.
</output>
