---
phase: 35-balance-and-help-clarity
plan: 02
type: execute
wave: 2
depends_on:
  - 35-01
autonomous: true
files_modified:
  - src/ui/help/helpContent.ts
  - src/ui/tabs/career/CareerPanel.tsx
  - src/ui/components/CareerNextActionCard.tsx
  - src/ui/components/CareerStageChoices.tsx
  - src/ui/tabs/CollectionTab.tsx
  - src/ui/tabs/CatalogTab.tsx
  - src/ui/tabs/SaveTab.tsx
  - tests/explanations.spec.ts

must_haves:
  truths:
    - "A fresh-save player understands why cash/sec starts at 0 and exactly how to start their career (PhD enrollment) from where they see the CTA."
    - "A player understands the early-career loop: stipend + time-limited salary window, sessions refresh it, spending career points extends it, and sessions are burst payouts separate from cash/sec."
    - "A player can tell where to buy watches (Shop surface) vs where to browse reference/licensing info (Catalog archive), without thinking these are two separate purchase systems."
  artifacts:
    - path: src/ui/help/helpContent.ts
      provides: "Updated Help sections (career start, career progression loop, shop vs catalog, saving/clear-save note)."
    - path: src/ui/components/CareerNextActionCard.tsx
      provides: "ExplainButton adjacent to fresh-save start-career CTA, pointing to a dedicated Help section."
    - path: src/ui/components/CareerStageChoices.tsx
      provides: "ExplainButton on Career stages surface, pointing to a dedicated Help section."
    - path: tests/explanations.spec.ts
      provides: "Regression coverage that ExplainButtons open the intended Help sections (career start/stages, shop)."
  key_links:
    - from: src/ui/components/CareerNextActionCard.tsx
      to: src/ui/help/helpContent.ts
      via: "ExplainButton(sectionId=HELP_SECTION_IDS.careerStart)"
      pattern: "sectionId=\{HELP_SECTION_IDS\\.careerStart\}"
    - from: src/ui/components/CareerStageChoices.tsx
      to: src/ui/help/helpContent.ts
      via: "ExplainButton(sectionId=HELP_SECTION_IDS.careerStages)"
      pattern: "sectionId=\{HELP_SECTION_IDS\\.careerStages\}"
    - from: tests/explanations.spec.ts
      to: src/ui/help/ExplainButton.tsx
      via: "Playwright clicks data-testid explain-*"
      pattern: "getByTestId\(\"explain-"
---

<objective>
Update Help + in-context ExplainButtons so the post-35-01 Career onboarding and Shop/Catalog surfaces are self-explanatory.

Purpose: Close CAREER-CLAR-01 by making the loop and choices legible where the player encounters them.
Output: Updated help copy, clarified UI text, and regression tests for Help anchors.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@.planning/phases/35-balance-and-help-clarity/35-CONTEXT.md
@.planning/phases/35-balance-and-help-clarity/35-01-SUMMARY.md

@src/ui/help/helpContent.ts
@src/ui/help/ExplainButton.tsx
@src/ui/components/CareerNextActionCard.tsx
@src/ui/components/CareerStageChoices.tsx
@src/ui/tabs/career/CareerPanel.tsx
@src/ui/tabs/CollectionTab.tsx
@src/ui/tabs/CatalogTab.tsx
@src/ui/tabs/SaveTab.tsx
@tests/explanations.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Help content to match current Career + Shop/Catalog reality</name>
  <files>
src/ui/help/helpContent.ts
  </files>
  <action>
  - Update the Career help so it explicitly reflects the 35-01 behavior and uses the player-facing phrasing used in the UI.
  - Add TWO dedicated Help sections + ids so ExplainButtons can link without duplicating `data-testid` values:
    - `HELP_SECTION_IDS.careerStart` (e.g. id value `"career-start"`) titled "Starting your career".
    - `HELP_SECTION_IDS.careerStages` (e.g. id value `"career-stages"`) titled "Career stages & choices".
  - Ensure the existing `HELP_SECTION_IDS.careerProgression` section remains and is updated (do not remove/rename existing ids).

  Career help requirements (copy-level; do not change gameplay):
  - Fresh saves: cash/sec starts at 0 until the player explicitly starts Career by entering the PhD program.
  - PhD enrollment: begins a small stipend and unlocks progression (use non-numeric wording like "small stipend" unless the UI shows a stable number).
  - Salary window: salary is time-limited and expires after a few minutes; running sessions refreshes it; spending career points extends it; returning to the game refreshes it (sessions refresh is the primary mechanic).
  - Sessions: grant XP + burst cash; after the free first session they cost enjoyment; cooldowns apply; sessions are not averaged into cash/sec.
  - Track/stage choices: stages unlock at specific levels; only the next choice surface is shown; choices are permanent but respec is available.
  - Retirement stage: sessions are unavailable; the player should focus on shopping/progression and prestige loops.

  Shop/Catalog clarity requirements:
  - Update "Catalog-first" and/or "Catalog shopping" sections to clearly differentiate:
    - "Vault" tab = where you manage your vault (wear/interactions/progress) and contains an embedded "Shop" panel.
    - "Shop" = the buying surface (embedded in Vault and also reachable elsewhere as applicable).
    - "Catalog" tab = archive/reference + licensing details (not a separate purchase system).
  - Keep the "Catalog shopping" title intact unless you also update `tests/explanations.spec.ts` expectations.

  Saving clarity requirements:
  - Update the "Saving" section to mention Settings -> Clear save (local-only, confirmed, export first).
  </action>
  <verify>pnpm run typecheck</verify>
  <done>
  - `HELP_SECTION_IDS` includes `careerStart` and `careerStages`.
  - Help includes updated Career onboarding copy (start gate, stipend, salary window, session refresh/extension) and Shop vs Catalog clarification.
  - Existing help ids referenced by current UI/tests are preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add in-context ExplainButtons and tighten on-screen copy (no selector churn)</name>
  <files>
src/ui/components/CareerNextActionCard.tsx
src/ui/components/CareerStageChoices.tsx
src/ui/tabs/career/CareerPanel.tsx
src/ui/tabs/CollectionTab.tsx
src/ui/tabs/CatalogTab.tsx
src/ui/tabs/SaveTab.tsx
  </files>
  <action>
  Constraints:
  - Do NOT change existing `id` or `data-testid` values.
  - Avoid broad refactors; keep changes localized to copy and ExplainButton placement.

  Career start clarity:
  - In `src/ui/components/CareerNextActionCard.tsx`, when `cue.id === "start-career"`, add an `ExplainButton` adjacent to the "Enter program" CTA (or within the card header) that links to `HELP_SECTION_IDS.careerStart`.
  - Ensure the new ExplainButton has a distinct `data-testid` by using the dedicated section id (e.g. `explain-career-start`).

  Career stages clarity:
  - In `src/ui/components/CareerStageChoices.tsx` (StageTimeline card), add an `ExplainButton` near the "Career stages" header that links to `HELP_SECTION_IDS.careerStages`.
  - Keep the existing stage/test ids (`career-stages-card`, `career-stage-node-*`, etc.) unchanged.

  Copy sweep (targeted):
  - In `src/ui/tabs/CollectionTab.tsx`, add a short, non-interactive line of copy near the `#catalog-shop` section to clarify that "Shop" is the purchase flow and "Catalog" is the archive/reference view. (No new ids required.)
  - In `src/ui/tabs/CatalogTab.tsx`, adjust header/description copy to reduce Vault vs Catalog confusion, while keeping existing structure and selectors.
  - In `src/ui/tabs/SaveTab.tsx`, keep the existing Clear save confirmation flow; only adjust copy if needed to match help text (do not change test ids).

  </action>
  <verify>pnpm run test:unit</verify>
  <done>
  - Fresh saves show an ExplainButton near the start-career CTA that opens the "Starting your career" help section.
  - Career stages surface includes an ExplainButton that opens the "Career stages & choices" help section.
  - Vault/Shop/Catalog copy no longer implies separate purchase systems.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update/add minimal tests for Help anchors + key copy</name>
  <files>
tests/explanations.spec.ts
  </files>
  <action>
  - Extend `tests/explanations.spec.ts` with two new tests:
    1) "career start explain trigger opens starting-career help"
       - Use a default fresh save (no seeding required) or seed minimal state if the app defaults to an existing save in CI.
       - Navigate to the Career tab if needed.
       - Click `data-testid="explain-career-start"`.
       - Assert help modal opens and active section heading matches "Starting your career".
    2) "career stages explain trigger opens stages help"
       - Click `data-testid="explain-career-stages"`.
       - Assert help modal opens and active section heading matches "Career stages" (or the exact section title you added).

  - Keep existing tests intact:
    - Do not break the existing "catalog help opens shopping guidance" test (update expectations only if you intentionally changed the section title).
  - Prefer assertions that are stable to minor copy edits (match headings, not full paragraphs).
  </action>
  <verify>pnpm run test:e2e</verify>
  <done>
  - `pnpm run test:e2e` passes.
  - ExplainButtons for career start and career stages are covered by Playwright and open the correct help sections.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit`
- `pnpm run test:e2e`
</verification>

<success_criteria>
- CAREER-CLAR-01 is satisfied: Help + in-context ExplainButtons explain the early-career loop (explicit start, stipend, salary window, session refresh/extension) and stage choices clearly.
- Players are not misled about Catalog vs Vault vs Shop surfaces; copy aligns with current UI labels.
- No selector churn: existing ids/testids remain stable; only additive ExplainButtons use new section ids.
</success_criteria>

<output>
After completion, create `.planning/phases/35-balance-and-help-clarity/35-02-SUMMARY.md`.
</output>
