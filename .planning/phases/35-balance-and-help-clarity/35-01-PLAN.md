---
phase: 35-balance-and-help-clarity
plan: 01
type: execute
wave: 1
depends_on:
  - 34-03
autonomous: false
files_modified:
  - src/game/model/types.ts
  - src/game/model/state.ts
  - src/game/persistence.ts
  - src/game/actions/index.ts
  - src/game/actions/therapistCareer.ts
  - src/game/selectors/index.ts
  - src/game/selectors/therapistPolicy.ts
  - src/game/selectors/careerNextAction.ts
  - src/game/selectors/careerProgress.ts
  - src/game/data/career.ts
  - src/ui/tabs/career/CareerPanel.tsx
  - src/ui/components/CareerStageChoiceBlocks.tsx
  - tests/**/*.unit.test.ts*
  - tests/**/*.spec.ts
---

<objective>
Adjust early career onboarding and pacing:
- No passive cash on a fresh save until the player explicitly starts career (enter PhD program).
- Rename the "private practice" career track to a different path (avoid overlap with stage 5).
- Make career choice UI show only the next available stage choice, and ensure buttons are disabled until unlocked.
- Put Sessions at the top of Career.
- Slow down the leveling curve.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add PhD enrollment gating for early salary</name>
  <action>
  - Add a persisted field on `TherapistCareerState` (e.g. `phdEnrolled: boolean`).
  - Fresh save default: `phdEnrolled = false`.
  - Migration: existing saves should load as `phdEnrolled = true` when career has already started (level > 1, or track chosen, or session cooldown set, etc.).
  - Update cash-rate selectors so therapist salary is 0 while `phdEnrolled === false`.
  - Add an action to enroll in PhD (pure): flips `phdEnrolled` to true (one-way).
  - Update Career UI to show an explicit CTA when not enrolled.
  </action>
  <verify>pnpm run typecheck</verify>
</task>

<task type="auto">
  <name>Task 2: UI ordering + only-next-choice visibility</name>
  <action>
  - Move Sessions card to the top of the Career tab.
  - Update stage/track choice UI so only the next stage choice grid is visible at a time.
  - Ensure choice buttons are disabled when not unlocked.
  </action>
  <verify>pnpm run test:e2e</verify>
</task>

<task type="auto">
  <name>Task 3: Slow the leveling curve</name>
  <action>
  - Adjust XP requirements and/or XP gain rates to slow leveling.
  - Add unit assertions to protect the new curve from accidental reversion.
  </action>
  <verify>pnpm run test:unit</verify>
</task>

<task type="auto">
  <name>Task 4: Rename the "private practice" track (label + description)</name>
  <action>
  - Change the user-facing label/description for the track currently keyed by `id: "private-practice"`.
  - Keep the internal id for save compatibility.
  </action>
  <verify>pnpm run typecheck</verify>
</task>

</tasks>

<output>
After completion, create `.planning/phases/35-balance-and-help-clarity/35-01-SUMMARY.md`.
</output>
