---
phase: 56-full-ui-audit-remediation
plan: 1
type: execute
depends_on: []
files_modified:
  - src/ui/navigation/tabMeta.ts
  - src/ui/navigation/PageTabRail.tsx
  - src/App.tsx
  - src/style.css
  - tests/tabs.spec.ts
  - tests/mobile-navigation.spec.ts
---

<objective>
Fix global navigation discoverability and tab ordering so primary game loops are easy to find on desktop and mobile.
</objective>

<tasks>
- Reorder top-level tabs so `Catalog` is immediately left of `Collection` in all layouts and keyboard flows.
- Ensure horizontal tab rail keeps labels readable at narrow widths (<= 390px) with explicit overflow affordance.
- Keep active-tab visibility deterministic after tab switches, page restore, and settings tab-visibility toggles.
- Preserve existing tab IDs/test IDs while updating displayed order and grouping.
</tasks>

<acceptance_criteria>
1. Tab order is consistent across desktop/mobile and starts with `Career -> Catalog -> Collection`.
2. No tab label clipping occurs on Pixel 5 and iPhone 12 equivalent widths.
3. Keyboard and pointer navigation can reach every visible tab without hidden dead zones.
4. Existing navigation-related tests pass without selector rewrites outside this plan.
</acceptance_criteria>

<verification>
- `pnpm test:e2e --project=chromium -- tests/tabs.spec.ts tests/mobile-navigation.spec.ts`
- `pnpm test:e2e --project=chromium-mobile-pixel5 -- tests/tabs.spec.ts tests/mobile-navigation.spec.ts`
</verification>
