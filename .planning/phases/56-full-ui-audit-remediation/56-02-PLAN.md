---
phase: 56-full-ui-audit-remediation
plan: 2
type: execute
depends_on:
  - 56-01-PLAN.md
files_modified:
  - src/ui/components/ToastStack.tsx
  - src/ui/components/FloatingDelta.tsx
  - src/ui/components/ConfirmModal.tsx
  - src/ui/help/HelpModal.tsx
  - src/style.css
  - tests/achievements-toast.spec.ts
  - tests/modal-interactions.spec.ts
  - tests/settings-clear-save.spec.ts
---

<objective>
Enforce a single-primary-action hierarchy and prevent overlays/toasts from interrupting core actions.
</objective>

<tasks>
- Define shared action-priority tokens (`primary`, `secondary`, `tertiary`) and apply them to cross-tab CTA styling.
- Introduce overlay stacking/queue rules so at most one blocking modal and one non-blocking toast are visible.
- Reserve a no-occlusion zone for primary-action surfaces in desktop and mobile layouts.
- Standardize help/confirm modal placement and dismissal affordances to reduce accidental interruption.
</tasks>

<acceptance_criteria>
1. Each tab first viewport exposes one visually dominant CTA.
2. Toasts never overlap the primary-action button area in Career/Catalog/Collection/Workshop.
3. Modal close/cancel paths are consistent and reachable with keyboard and touch.
4. Existing accessibility roles for live regions and dialogs remain valid.
</acceptance_criteria>

<verification>
- `pnpm test:unit -- tests/achievement-toast.unit.test.tsx tests/notifications-preferences.unit.test.tsx`
- `pnpm test:e2e --project=chromium -- tests/achievements-toast.spec.ts tests/modal-interactions.spec.ts tests/settings-clear-save.spec.ts`
- `pnpm test:e2e --project=chromium-mobile-pixel5 -- tests/achievements-toast.spec.ts tests/modal-interactions.spec.ts`
</verification>
