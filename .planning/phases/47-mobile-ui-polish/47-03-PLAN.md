---
phase: 47-mobile-ui-polish
plan: 3
type: plan
wave: 3
depends_on:
  - "47-01"
  - "47-02"
files_modified:
  - tests/mobile-navigation.spec.ts
  - tests/touch-targets.spec.ts
  - tests/modal-interactions.spec.ts
  - tests/mobile-responsive.unit.test.tsx
autonomous: true

must_haves:
  truths:
    - "Mobile navigation supports horizontal scroll with snap behavior on iOS Safari and Android Chrome"
    - "Tab bar remains sticky while scrolling content on mobile viewports"
    - "All primary touch targets meet 44px minimum dimension"
    - "Modals handle reduced-motion preferences and focus trap correctly"
    - "Help modal search functionality works on mobile and supports keyboard navigation"
  artifacts:
    - path: "tests/mobile-navigation.spec.ts"
      provides: "Playwright E2E tests verifying tab scroll-snap, sticky bar, and viewport behavior across mobile devices"
    - path: "tests/touch-targets.spec.ts"
      provides: "Playwright E2E tests asserting 44px minimum touch target dimensions on primary actions"
    - path: "tests/modal-interactions.spec.ts"
      provides: "Playwright E2E tests for modal focus trap, scroll lock, and reduced-motion compliance"

<objective>
Implement comprehensive regression coverage for mobile navigation, touch targets, and modal interactions to ensure Phase 47 deliverables work correctly across mobile devices and meet accessibility requirements.
</objective>

<context>
- Requirements: MOBILE-01, MOBILE-02, MOBILE-05, MOBILE-06, MOBILE-07, MOBILE-09
- Phase 47-01: Tier badges implemented
- Phase 47-02: Catalog UI surfaces with mobile-responsive layout
- Research: CSS Scroll Snap patterns, sticky navigation, modal focus trap, touch targets 44px
- Stack: Playwright, Vitest, existing test patterns
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mobile navigation E2E tests</name>
  <files>
tests/mobile-navigation.spec.ts
  </files>
  <action>
Create Playwright E2E tests that:
- Open app on mobile viewport (iPhone 12, Pixel 5)
- Navigate between tabs using swipe gestures
- Assert horizontal scroll-snap behavior works on tab strip
- Assert tab bar stays sticky when scrolling content
- Test tab switching via click and verify active tab indicator
- Verify help modal can be opened and closed from mobile
  </action>
  <verify>
pnpm test:e2e -- tests/mobile-navigation.spec.ts
  </verify>
  <done>
Mobile navigation passes E2E tests on simulated mobile devices.
  </done>
</task>

<task type="auto">
  <name>Task 2: Touch target regression tests</name>
  <files>
tests/touch-targets.spec.ts
  </files>
  <action>
Create Playwright tests that verify:
- All tab buttons have minimum 44px height and width
- Catalog card purchase buttons meet 44px minimum
- Interaction modal buttons (wind, set time, change strap) meet minimum
- Help modal close button meets minimum
- Settings panel checkboxes and toggles meet minimum
- Collection interaction buttons meet minimum
Use Playwright's `locator.boundingBox()` to assert dimensions.
  </action>
  <verify>
pnpm test:e2e -- tests/touch-targets.spec.ts
  </verify>
  <done>
All primary touch targets verified at 44px minimum across key UI components.
  </done>
</task>

<task type="auto">
  <name>Task 3: Modal interaction and accessibility tests</name>
  <files>
tests/modal-interactions.spec.ts
  </files>
  <action>
Create Playwright E2E tests for modal behavior:
- Opening a modal locks body scroll
- Focus is trapped within modal (tabbing cycles through modal elements)
- Close button or Escape key closes modal and restores focus
- Reduced motion preference is respected (no animations when prefers-reduced-motion: reduce)
- Help modal search works and supports keyboard navigation (arrow keys, Enter, Escape)
- Modal content is scrollable on mobile viewports without breaking focus trap
  </action>
  <verify>
pnpm test:e2e -- tests/modal-interactions.spec.ts
  </verify>
  <done>
Modals pass accessibility and interaction tests including focus management and reduced-motion.
  </done>
</task>

<task type="auto">
  <name>Task 4: Responsive layout unit tests</name>
  <files>
tests/mobile-responsive.unit.test.tsx
  </files>
  <action>
Create Vitest unit tests for responsive layout logic:
- Test TierBadge component renders correctly at different screen sizes
- Test catalog grid adapts column count based on viewport width
- Test Collection summary layout stacks vertically on narrow viewports
- Test tab navigation shows/hides appropriately based on viewport
- Mock window.matchMedia to test responsive breakpoints
  </action>
  <verify>
pnpm test:unit -- tests/mobile-responsive.unit.test.tsx
  </verify>
  <done>
Responsive layout components have unit test coverage for breakpoint behavior.
  </done>
</task>

<task type="auto">
  <name>Task 5: Cross-browser compatibility verification</name>
  <files>
playwright.config.ts
  </files>
  <action>
- Verify Playwright config includes mobile viewport profiles (iPhone 12, Pixel 5)
- Run full E2E suite against mobile viewports to catch cross-browser issues
- Document any browser-specific behaviors in test comments
- Ensure CSS Scroll Snap works consistently across Safari and Chrome
  </action>
  <verify>
pnpm test:e2e
  </verify>
  <done>
All mobile tests pass across iOS Safari and Android Chrome viewports.
  </done>
</task>

</tasks>
