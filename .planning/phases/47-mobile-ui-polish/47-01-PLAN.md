---
phase: 47-mobile-ui-polish
plan: 1
type: plan
wave: 1
depends_on:
files_modified:
  - src/ui/components/TierBadge.tsx
  - src/style.css
  - src/game/data/watchModels.ts
  - src/game/catalog.ts
autonomous: true

must_haves:
  truths:
    - "Tier badges highlight low/mid/lux tiers with brand cues, helping players quickly identify watch categories at a glance."
    - "Each catalog entry carries a `tierBadge` with styling that reflects the tier's pricing/messaging and matches existing catalog UI patterns."
    - "Styling uses CSS variables so that badge colors (starter=green, mid=gold, lux=red) can be reused and centrally adjusted without touching component code for color changes."
    - "New TierBadge component anchors to existing `data-tier-badge-*` attributes used by catalog cards, ensuring no regression in filter/sort logic."
  artifacts:
    - path: "src/ui/components/TierBadge.tsx"
      provides: "TierBadge (props: tier, showLabel) - renders a small badge with brand-aware styling, anchoring to `data-tier-badge-*` for test stability."
    - path: "src/style.css"
      provides: "CSS variables for badge colors and responsive sizing."
    - path: "src/game/data/watchModels.ts"
      provides: "Tier metadata (tiers, badge text, colors) for Badge to render."
    - path: "src/game/catalog.ts"
      provides: "Catalog data helpers to map each watch model’s tier to badge information."

<objective>
Create a reusable TierBadge component that renders tier badges with color-coded branding and responsive sizing, ensuring catalog variety can be quickly understood at a glance.
</objective>

<context>
- Requirements: MOBILE-01, MOBILE-03, MOBILE-08, MOBILE-09 (navigation, responsiveness, touch targets)
- Requirements: CAT-05, CAT-06, CAT-07, CAT-08, CAT-09, CAT-10 (catalog variety across tiers, appropriate imagery)
- Phase 46 plans (47-01-03) will consume this badge component in their UI wiring
- Phase 45 delivered selector-backed per-watch stats with equipped contribution math that keeps cash anchored to therapist salary
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TierBadge component</name>
  <files>
src/ui/components/TierBadge.tsx
src/style.css
  </files>
  <action>
Create a TierBadge component with a `tier` prop (values: 'starter', 'mid', 'lux') and optional `showLabel` prop. The component:
- Renders a badge with CSS variables for colors (--tier-starter, --tier-mid, --tier-lux)
- Displays optional label text (e.g., "Starter", "Mid-tier", "Luxury") when `showLabel` is true
- Includes `data-tier-badge` attribute for test anchoring
- Uses responsive sizing with `rem` units
  </action>
  <verify>
pnpm test:unit -- tests/catalog.unit.test.tsx
  </verify>
  <done>
TierBadge component exists and renders tier badges with correct styling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tier metadata to watch models</name>
  <files>
src/game/data/watchModels.ts
  </files>
  <action>
Add a `tierBadge` field to each tier object in `WATCH_TIERS` with configuration including:
- Badge text label (e.g., "Starter", "Mid-tier", "Luxury")
- Badge description text for hover tooltips
- Color mapping (CSS variable names for badge styling)
Ensure this data is accessible to `PerWatchStatsTable` and any UI that needs to display tier information.
  </action>
  <verify>
pnpm typecheck
  </verify>
  <done>
Each watch model now has `tierBadge` metadata for UI rendering.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire tier badge to catalog renderer</name>
  <files>
src/game/catalog.ts
  </files>
  <action>
Update catalog rendering helpers to accept and pass through a `tierBadge` prop on watch model objects.
Expose a new helper function `getWatchModelTierBadge(modelId)` that maps a watch model to its tier’s badge configuration.
Update `getCatalogCardTierBadge(model)` to read from the watch model’s `tierBadge` field (if present) or compute from tier ID.
  </action>
  <verify>
pnpm test:unit -- tests/catalog.unit.test.tsx
  </verify>
  <done>
Catalog cards can now render tier badges and tooltips by consuming watch model tier metadata.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add CSS variables for badge colors</name>
  <files>
src/style.css
  </files>
  <action>
Add CSS custom properties for badge color theming:
- `--tier-starter`: Badge background color (green: #a3c5a0)
- `--tier-mid`: Badge background color (gold: #ffd700)
- `--tier-lux`: Badge background color (red: #e74c3c)
- `--tier-badge-text-light`: Badge text color (white for light backgrounds)

Apply these to `.tier-badge` component, and ensure responsive sizing (`rem` units for font-size, `rem` for padding).
Update badge foreground color to use white text on light badges for accessibility (WCAG AA minimum).
  </action>
  <verify>
pnpm lint
  </verify>
  <done>
CSS variables added, badge colors are themeable, and accessibility improved with high-contrast text.
  </done>
</task>

</tasks>
