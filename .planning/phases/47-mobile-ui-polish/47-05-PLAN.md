---
phase: 47-mobile-ui-polish
plan: 5
type: plan
wave: 5
depends_on:
  - "47-04"
files_modified:
  - src/ui/help/HelpModal.tsx
autonomous: true
must_haves:
  truths:
    - "HelpModal still allows focus to escape WebKit when Tab leaves the search input, so the trap is not yet reliable."
    - "The modal should still restore focus on close and keep background content inert while open."
  artifacts:
    - path: "src/ui/help/HelpModal.tsx"
      provides: "Manual Tab/Shift+Tab handling for the search input + close button to keep focus cycling inside the dialog."

<objective>
Prevent WebKit from letting Tab/Shift+Tab escape the HelpModal by intercepting the search input/close button key events so focus stays on defined controls.
</objective>

<context>
- WebKit (Safari/iOS) remains the only platform where pressing Tab from the search input still lands on the body despite the previous focus trap improvements.
- Manually handling Tab and Shift+Tab between the search input and close button avoids depending on sentinels that WebKit rejects.
- This follows the same focus discipline as other modals but keeps logic localized to HelpModal because the search field is the only tabbable control before the sentinel.
- We already inerted the background in the prior plan, so this is purely about keyboard cycling behavior.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keep HelpModal search input focusable cycle on WebKit</name>
  <files>
src/ui/help/HelpModal.tsx
  </files>
  <action>
  Add a Tab-aware key handler for the search input that prevents the default escape and focuses the first section button when Tab is pressed and section buttons exist. Hook a Shift+Tab handler on the close button that returns focus to the search input. Attach the handlers via `onKeyDown` so WebKit respects the sequence instead of relying on inert sentinels.
  </action>
  <verify>
pnpm test:e2e -- tests/help.spec.ts
  </verify>
  <done>
After adding the new key handlers, Tab and Shift+Tab cycle between the search input and the close button without escaping the modal on WebKit.
  </done>
</task>

</tasks>
