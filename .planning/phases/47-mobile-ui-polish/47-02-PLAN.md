---
phase: 47-mobile-ui-polish
plan: 2
type: plan
wave: 2
depends_on:
  - "47-01"
files_modified:
  - src/ui/tabs/CatalogTab.tsx
  - src/ui/tabs/CollectionTab.tsx
  - src/ui/components/TierBadge.tsx
  - src/game/data/watchModels.ts
  - src/game/catalog.ts
  - tests/catalog-expansion.spec.ts
  - tests/catalog-expansion.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "Tier badges help players quickly identify watch categories at a glance, and responsive touch targets meet 44px requirement across new catalog sections."
    - "Help copy explains catalog variety expansion to players and provides tooltip support for badge and tier meanings."
  artifacts:
    - path: "src/ui/tabs/CatalogTab.tsx"
      provides: "Tier-grouped catalog layout and filter hooks that incorporate new badges."
    - path: "src/ui/tabs/CollectionTab.tsx"
      provides: "Collection summary that references catalog variety and explains tier badges."
    - path: "src/ui/components/TierBadge.tsx"
      provides: "TierBadge component (or reuse existing) that highlights low/mid/lux tiers with brand cues."
    - path: "src/style.css"
      provides: "CSS for badge colors, responsive sizing, and background theming to match tier themes."

<objective>
Update Catalog and Collection panels to display tier badges and tooltips, ensuring new catalog variety is clearly communicated through existing help content and new badge component.
</objective>

<context>
- Requirements: MOBILE-01, MOBILE-04, MOBILE-05, MOBILE-07, MOBILE-08, MOBILE-09 (navigation, responsiveness, touch targets, help search)
- Phase 47 plan 01: tier badges and metadata that power badge rendering
- Phase 45 per-watch stats selectors and tests now exist, so badges can be derived from watch tier data
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire tier badges into Catalog card renderer</name>
  <files>
src/game/catalog.ts
src/ui/tabs/CatalogTab.tsx
  </files>
  <action>
- Update `CatalogCard` component to consume `tierBadge` prop from watch model objects.
- Update `renderCatalogGrid` helper to pass through `tierBadge` data to card renderers.
- Ensure badges render correctly at different positions (unowned vs owned cards).
- Test that badges display with correct colors and tooltips.
  </action>
  <verify>
pnpm test:unit -- tests/catalog.unit.test.tsx
  </verify>
  <done>
Catalog cards now display tier badges with proper styling and tooltip support.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tier badges to Collection summary</name>
  <files>
src/ui/tabs/CollectionTab.tsx
  </files>
  <action>
- Update Collection’s summary rendering to display tier badges for each model tier.
- Ensure new catalog varieties (low/mid/lux) are visually represented through badges.
- Wire summary to existing help copy system so tooltip text can reference tiers.
  </action>
  <verify>
pnpm test:unit -- tests/collection.unit.test.tsx
  </verify>
  <done>
Collection summary shows tier badges, making catalog variety more discoverable.
  </done>
</task>

<task type="auto">
  <name>Task 3: Help copy updates for badges</name>
  <files>
src/ui/help/helpContent.ts
src/ui/tabs/CollectionTab.tsx
  </files>
  <action>
- Update help copy to explain tier badges and their color/position meaning (Starter = entry-level, Mid-tier = main progression, Luxury = prestige goals).
- Add tooltip definitions for badge hover states explaining why each tier exists in the catalog.
- Ensure badge descriptions are consistent with catalog card copy and tier color theming.
  </action>
  <verify>
pnpm test:unit -- tests/catalog.unit.test.tsx
  </verify>
  <done>
Help system now explains tier badges with tooltips and visual cues.
  </done>
</task>

</tasks>
