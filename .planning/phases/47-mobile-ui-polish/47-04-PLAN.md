---
phase: 47-mobile-ui-polish
plan: 4
type: plan
wave: 4
depends_on:
  - "47-03"
files_modified:
  - src/ui/help/HelpModal.tsx
autonomous: true
must_haves:
  truths:
    - "Help modal traps focus within the dialog on iOS Safari (WebKit) so keyboard navigation cannot escape to the background."
    - "Background content becomes inert/aria-hidden while the modal is open so assistive tech and touch events stay within the dialog."
  artifacts:
    - path: "src/ui/help/HelpModal.tsx"
      provides: "WebKit-friendly focus management (useLayoutEffect), inert intents, and previously focused element restoration for HelpModal."

<objective>
Implement the HelpModal focus trap pattern that mirrors WindingMiniGameModal so the dialog behaves correctly on WebKit devices, keeps the background inert, and restores focus after closing.
</objective>

<context>
- UAT-07 reported focus escaping HelpModal on WebKit while Chromium browsers were fine, so our plan must address the mismatch directly.
- The existing modal already has focus sentinels, but Safari/iOS ignores the sentinels and rejects programmatic focus changes scheduled via useEffect.
- WindMiniGameModal uses useLayoutEffect, close button focus, and scroll lock; the HelpModal needs the same pattern plus an inerted background and focus restoration.
- The fix also needs to remember the previously focused element so we can restore focus when the modal closes.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden HelpModal focus trap for WebKit</name>
  <files>
src/ui/help/HelpModal.tsx
  </files>
  <action>
  Update HelpModal to replace the old focus effect with a useLayoutEffect that saves the previously focused element and focuses the close button.
  Inert the `#app`/`#root` container and set `aria-hidden` while the modal is open, then clean up (remove inert/aria-hidden and restore focus) on close.
  Keep the existing focus sentinels, keyboard handlers, and scroll lock.
  </action>
  <verify>
pnpm test:e2e -- tests/help.spec.ts
  </verify>
  <done>
HelpModal traps focus on WebKit, the background becomes inert while the modal is open, and focus is restored to the prior element on close.
  </done>
</task>

</tasks>
