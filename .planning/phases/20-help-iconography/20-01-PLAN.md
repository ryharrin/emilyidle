---
phase: 20-help-iconography
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/ui/icons/coreIcons.tsx
autonomous: true

must_haves:
  truths:
    - "The UI has a shared icon language for help/lock/prestige cues"
  artifacts:
    - path: "src/ui/icons/coreIcons.tsx"
      provides: "HelpIcon/LockIcon/PrestigeIcon wrappers"
  key_links:
    - from: "src/ui/icons/coreIcons.tsx"
      to: "lucide-react"
      via: "explicit icon imports"
      pattern: "from \\\"lucide-react\\\""
---

<objective>
Add a small, explicit icon layer so the rest of the phase can use consistent Help/Lock/Prestige glyphs without bespoke SVG/CSS.

Purpose: Establish a single source of truth for core UI cues (POLISH-05).
Output: lucide-react dependency + `HelpIcon`/`LockIcon`/`PrestigeIcon` wrappers.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/20-help-iconography/20-CONTEXT.md
@.planning/phases/20-help-iconography/20-RESEARCH.md

@package.json
@src/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lucide-react dependency</name>
  <files>package.json
pnpm-lock.yaml</files>
  <action>
Add lucide-react at the researched version and update the lockfile.

- Use: `pnpm add lucide-react@0.563.0`
- Do not add any other icon packages.
  </action>
  <verify>pnpm -s run typecheck</verify>
  <done>`pnpm -s run typecheck` succeeds with lucide-react installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create core icon wrappers</name>
  <files>src/ui/icons/coreIcons.tsx</files>
  <action>
Create `src/ui/icons/coreIcons.tsx` with explicit, tree-shakeable imports and three wrapper components:

- `HelpIcon` wraps `CircleQuestionMark` from `lucide-react`
- `LockIcon` wraps `Lock` from `lucide-react`
- `PrestigeIcon` wraps `RotateCcw` from `lucide-react`

Wrapper requirements:
- Accept `React.ComponentProps<typeof Icon>` so callers can pass `size`, `className`, etc.
- Set `aria-hidden={true}` and `focusable={false}` (icons are decorative; buttons provide accessible labels).
- No dynamic icon registry or lookup-by-name.
  </action>
  <verify>pnpm -s run typecheck</verify>
  <done>Icons compile and can be imported from `src/ui/icons/coreIcons.tsx` without pulling in a dynamic registry.</done>
</task>

</tasks>

<verification>
- `pnpm -s run typecheck`
- `pnpm -s run lint`
</verification>

<success_criteria>
- `lucide-react` is installed and typechecking passes.
- `src/ui/icons/coreIcons.tsx` exports `HelpIcon`, `LockIcon`, and `PrestigeIcon` wrappers.
</success_criteria>

<output>
After completion, create `.planning/phases/20-help-iconography/20-01-SUMMARY.md`
</output>
