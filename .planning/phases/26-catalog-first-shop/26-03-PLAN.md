---
phase: 26-catalog-first-shop
plan: 03
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - src/App.tsx
  - src/ui/tabs/CatalogTab.tsx
  - src/style.css
autonomous: true

must_haves:
  truths:
    - "Each catalog entry shows price (value-only), owned count (N owned), and a buy CTA or a clear lock reason"
    - "User can buy a watch directly from a catalog card and see ownership update inline"
    - "Purchasing from Catalog keeps the user on the Catalog flow (no forced navigation)"
  artifacts:
    - path: src/ui/tabs/CatalogTab.tsx
      provides: "Catalog card action bar (price/owned/buy/lock reason)"
      contains: "catalog-buy-"
    - path: src/App.tsx
      provides: "CatalogTab receives GameState and purchase handler"
      contains: "<CatalogTab"
  key_links:
    - from: src/ui/tabs/CatalogTab.tsx
      to: src/game/state.ts
      via: "Model purchase action + gate selectors"
      pattern: "buyWatchModel|getWatchModelPurchaseGate|getWatchModelOwnedCount|getNextDuplicateRewardMultiplier"
    - from: src/ui/tabs/CatalogTab.tsx
      to: src/App.tsx
      via: "onPurchase dispatch"
      pattern: "onPurchase\\(buyWatchModel"
---

<objective>
Turn the Catalog grid into a purchase surface by adding a per-card action bar (price + owned + buy CTA + lock reason) and wiring it into the existing purchase handler.

Purpose: This is the core of the catalog-first shop: buying happens where the player is browsing.
Output: CatalogTab can buy watches directly from cards and shows the purchase state inline.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-catalog-first-shop/26-CONTEXT.md
@src/App.tsx
@src/ui/tabs/CatalogTab.tsx
@src/ui/tabs/CollectionTab.tsx
@src/game/state.ts
@src/style.css

# Phase 26 depends on Phase 25 (watch models + model-level purchasing)
@.planning/phases/25-watch-models-and-duplicates/25-02-PLAN.md
@.planning/phases/25-watch-models-and-duplicates/25-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pass GameState + purchase handler into CatalogTab</name>
  <files>
src/App.tsx
src/ui/tabs/CatalogTab.tsx
  </files>
  <action>
 - Extend `CatalogTab` props to accept:
   - `state: GameState`
   - `onPurchase: (nextState: GameState, meta?: { prestigeTier?: "workshop" | "maison" | "nostalgia" }) => void` (match `handlePurchase` signature shape in `src/App.tsx` and other tabs)
 - Update `src/App.tsx` to pass `state` and `handlePurchase` into `CatalogTab`.
 - Ensure TypeScript types align with existing `GameState` imports.
  </action>
  <verify>
pnpm run typecheck
  </verify>
  <done>
- CatalogTab compiles with access to current `state` and can dispatch purchases via `onPurchase`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add per-card buy/action bar (price, owned, CTA, lock reason)</name>
  <files>
src/ui/tabs/CatalogTab.tsx
src/style.css
  </files>
  <action>
 - For each catalog card in both the unowned and owned grids:
   - Show an action bar at the bottom containing:
     - Owned count label formatted exactly as "N owned" (e.g. "0 owned", "2 owned")
     - Price formatted as value-only (e.g. "$123") with no "Price:" label
     - Buy button (single-click; no confirm modal)
     - Next duplicate multiplier (show next multiplier only)
- Use Phase 25 model-level APIs (assumes Phase 25 complete):
  - Treat `entry.id` as the model id.
  - Owned: `getWatchModelOwnedCount(state, entry.id)`.
  - Gate/price: `getWatchModelPurchaseGate(state, entry.id)`.
  - CTA onClick: `onPurchase(buyWatchModel(state, entry.id))`.
  - Duplicate preview: `getNextDuplicateRewardMultiplier(state, entry.id)`.
 - Lock reason UX:
   - When gated (gate is not ok), replace the CTA entirely with the lock reason (do not render a disabled Buy button).
   - Use the gate fields to render the reason text (cash deficit vs enjoyment requirement).
   - Do NOT add per-card `ExplainButton` triggers (Phase 26 wants a single Help button at the top).
 - Add stable test ids:
   - Buy button: `data-testid={\`catalog-buy-${entry.id}\`}`
   - Lock reason container (rendered in the CTA slot when gated): `data-testid={\`catalog-gate-${entry.id}\`}`
 - Keep the player on Catalog after buying (no navigation).
  </action>
  <verify>
pnpm run typecheck
  </verify>
  <done>
 - Each catalog card displays "N owned" + value-only price + CTA or lock reason.
 - Clicking Buy purchases and the owned count updates inline.
 - The buy CTA is single-click with no confirmation modal.
 - When gated, the CTA is replaced by the lock reason (no disabled Buy button).
  </done>
</task>

<task type="auto">
  <name>Task 3: Update owned/unowned filtering semantics to use model ownership</name>
  <files>
src/App.tsx
src/ui/tabs/CatalogTab.tsx
  </files>
  <action>
- Update the Catalog "View" tabs logic so "Owned" means `ownedCount > 0` for the entry/model id.
- Update any derived props that were previously tier-based (e.g. `hasOwnedCatalogTiers`) to reflect model ownership.
- Preserve the discovered/archive shelf behavior (discovery is separate from ownership and is still based on `state.discoveredCatalogEntries`).
  </action>
  <verify>
pnpm run typecheck
  </verify>
  <done>
- The Catalog owned/unowned views reflect model ownership, not tier tags.
- Discovered shelf continues to render discovered entries independent of owned/unowned filter.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit`
</verification>

<success_criteria>
- Requirements CATALOG-02 and CATALOG-03 are satisfied: catalog cards show purchase info and allow buying inline.
- Buying from Catalog updates ownership immediately without leaving the Catalog.
</success_criteria>

<output>
After completion, create `.planning/phases/26-catalog-first-shop/26-03-SUMMARY.md`
</output>
