---
phase: 26-catalog-first-shop
plan: 07
type: execute
wave: 4
depends_on:
  - 26-04
files_modified:
  - src/App.tsx
  - src/ui/tabs/CollectionTab.tsx
  - src/ui/tabs/CatalogTab.tsx
  - src/style.css
  - tests/catalog.unit.test.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Catalog cards are the only way to buy watches"
    - "Buying-relevant Vault info (current cash + enjoyment) is visible above the catalog card grid"
    - "The consolidated surface still supports owned/unowned views and purchase feedback"
  artifacts:
    - path: src/ui/tabs/CollectionTab.tsx
      provides: "Embedded catalog purchase panel inside Vault"
      contains: "catalog-"
    - path: src/App.tsx
      provides: "Catalog purchase state/props are wired into Vault"
      contains: "filteredCatalogEntries"
  key_links:
    - from: src/ui/tabs/CollectionTab.tsx
      to: src/game/actions/index.ts
      via: "buyWatchModel"
      pattern: "buyWatchModel"
    - from: src/ui/tabs/CollectionTab.tsx
      to: src/ui/help/helpContent.ts
      via: "catalog help section id"
      pattern: "HELP_SECTION_IDS\\.catalogShop"
---

<objective>
Close Phase 26 UAT gaps by consolidating watch shopping into the Vault surface: embed the catalog cards purchase experience into Vault and remove competing watch-buy entry points.

Purpose: The app currently splits watch buying/information across Vault and Catalog. This plan makes catalog cards the purchase path and keeps relevant info in the same surface.
Output: Vault contains the catalog cards purchase panel; watch-buy UI in Vault is removed or demoted; tests updated.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-catalog-first-shop/26-CONTEXT.md
@.planning/phases/26-catalog-first-shop/26-05-SUMMARY.md
@src/App.tsx
@src/ui/tabs/CollectionTab.tsx
@src/ui/tabs/CatalogTab.tsx
@src/ui/help/helpContent.ts
@src/style.css
@tests/catalog.unit.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Embed the catalog cards purchase panel into Vault (CollectionTab)</name>
  <files>
src/App.tsx
src/ui/tabs/CollectionTab.tsx
src/ui/tabs/CatalogTab.tsx
  </files>
  <action>
- Consolidation approach (prefer reuse over duplication):
  - Extract the existing catalog card grid + filters + help header into a reusable exported component **within** `src/ui/tabs/CatalogTab.tsx` (no new file), e.g. `export function CatalogPurchasePanel(...)`.
  - Use that shared component from both `CatalogTab` (temporarily) and `CollectionTab`.
  - This avoids a wave-boundary regression: while the Catalog tab still exists (until 26-06 runs), it should continue to render the same shopping UI.

- Wire the embedded panel:
  - Update `src/App.tsx` so the shopping UI renders inside Vault.
  - Pass the catalog filter state (search/filters/sort/owned view) and derived arrays (`filteredCatalogEntries`, `discoveredCatalogEntries`, etc.) into `CollectionTab`.
  - Render the embedded `CatalogPurchasePanel` near the top of `CollectionTab`.

- Ensure buying-relevant Vault info is visible above the grid:
  - Show current cash (dollars) and current enjoyment amount in the embedded panel header area (above the card grid).
  - Keep the UI compact (single line or short stack) so it doesn't crowd filters.

- Ensure buying behavior is unchanged:
  - Buy clicks must still call `onPurchase(buyWatchModel(state, entry.id))`.
  - Keep purchase micro-feedback (highlight) and keep expanded details behavior.
  </action>
  <verify>
pnpm run typecheck
  </verify>
  <done>
- Vault tab contains the catalog purchase panel with cards, filters, help, details, and purchase feedback.
- Current cash + enjoyment are visible above the catalog card grid.
- While the Catalog tab still exists (until 26-06 runs), it continues to render the same shopping UI via the shared component.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove Vault watch-buy entry points so catalog cards are the purchase path</name>
  <files>
src/ui/tabs/CollectionTab.tsx
src/style.css
  </files>
  <action>
- In `CollectionTab`, locate the existing watch model purchase list (brand-grouped Vault cards with `vault-buy-*` selectors).
- Remove or demote the watch purchase CTAs there so buying is not split:
  - Remove Buy buttons for watch models in Vault.
  - Keep non-buy actions (like dismantle) only if they are not part of the purchase flow.
  - If needed, replace the old section CTA with a simple link/button that scrolls to the embedded catalog purchase panel.
- Ensure there is a single, obvious purchase CTA per watch: the catalog card buy CTA.
  </action>
  <verify>
pnpm run typecheck
  </verify>
  <done>
- Vault no longer presents a second watch-buy UI; catalog cards are the purchase flow.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update unit tests to reflect the consolidated surface</name>
  <files>
tests/catalog.unit.test.tsx
  </files>
  <action>
- Update tests that previously asserted a Catalog tab panel exists.
- Add/adjust assertions so that when Vault is active:
  - At least one `catalog-buy-*` element exists.
  - Clicking a catalog buy CTA updates owned count inline.
  - The single catalog help button exists and opens the Help modal to the catalog shopping section.
- Remove/adjust assertions for `vault-buy-*` selectors if they no longer exist.
  </action>
  <verify>
pnpm run test:unit
  </verify>
  <done>
- Unit tests pass and validate the consolidated surface behavior.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit`
</verification>

<success_criteria>
- Phase 26 UAT issues are addressed: catalog cards are the purchase surface and Vault/Catalog are consolidated.
- No second watch-buy surface remains.
</success_criteria>

<output>
After completion, create `.planning/phases/26-catalog-first-shop/26-07-SUMMARY.md`
</output>
