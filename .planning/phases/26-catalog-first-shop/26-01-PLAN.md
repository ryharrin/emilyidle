---
phase: 26-catalog-first-shop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - tests/catalog.unit.test.tsx
autonomous: true

must_haves:
  truths:
    - "A fresh save shows the Catalog tab and lands on Catalog by default"
    - "Existing saves also land on Catalog by default"
    - "An explicit deep link overrides the default landing tab"
  artifacts:
    - path: src/App.tsx
      provides: "Default landing tab resolution and tab visibility for Catalog"
      contains: "tab=\""
    - path: tests/catalog.unit.test.tsx
      provides: "Unit coverage for Catalog default landing and tab visibility"
      contains: "primary navigation tabs"
  key_links:
    - from: src/App.tsx
      to: tests/catalog.unit.test.tsx
      via: "fresh save / deep link assertions"
      pattern: "replaceState\\(\\{\\}, \\"\\, \\"/\\?tab="
    - from: src/App.tsx
      to: src/App.tsx
      via: "URLSearchParams tab override"
      pattern: "new URLSearchParams\\(window\\.location\\.search\\)"
---

<objective>
Make Catalog the default landing tab and ensure it is visible from the start, while allowing explicit deep links to override the default.

Purpose: Phase 26 requires a catalog-first experience. This plan establishes the navigation/visibility foundation so subsequent plans can build purchase UI in the Catalog.
Output: Updated tab visibility + initial tab selection logic and updated unit coverage.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-catalog-first-shop/26-CONTEXT.md
@src/App.tsx
@tests/catalog.unit.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make Catalog visible and default the landing tab to Catalog</name>
  <files>
src/App.tsx
  </files>
  <action>
- Update tab visibility so the Catalog tab is available from the beginning (fresh save).
- Change the initial `activeTab` selection to prefer `catalog` (Catalog is the home screen).
- Implement a URL deep link override for the initial tab:
  - Use a query param `tab` (e.g. `/?tab=save`, `/?tab=collection`, `/?tab=catalog`).
  - Validate the value against known `TabId`s and only select it if that tab is visible.
  - If invalid or hidden, fall back to the default (Catalog).
- Update the "active tab became hidden" safety effect to fall back to `catalog` first (and `collection` only if Catalog is not visible).
- Do NOT add a routing library.
  </action>
  <verify>
pnpm run typecheck
  </verify>
  <done>
- Fresh saves can see the Catalog tab.
- Without a deep link, `activeTab` starts on `catalog`.
- With `/?tab=...`, the requested tab is selected when visible.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update unit tests for catalog-first landing and deep link override</name>
  <files>
tests/catalog.unit.test.tsx
  </files>
  <action>
- Update the "primary navigation tabs" test suite expectations:
  - Fresh save should include the Catalog tab in the primary tab list.
  - Default selected tab should be Catalog (not Vault).
- Add a unit test that sets `window.history.replaceState({}, "", "/?tab=save")` before rendering and asserts Save is selected.
- Keep the existing tab keyboard-focus tests working (arrow focus moves without activation; Enter/Space activates).
  </action>
  <verify>
pnpm run test:unit
  </verify>
  <done>
- Unit tests pass with Catalog visible and selected by default on a fresh save.
- Deep link override via `/?tab=...` is covered by a unit test.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit`
</verification>

<success_criteria>
- Requirement CATALOG-01 is unblocked: Catalog is a visible, default landing surface.
- Deep links can explicitly select a tab and override the default.
</success_criteria>

<output>
After completion, create `.planning/phases/26-catalog-first-shop/26-01-SUMMARY.md`
</output>
