---
phase: 26-catalog-first-shop
plan: 06
type: execute
wave: 5
depends_on:
  - 26-07
files_modified:
  - src/App.tsx
  - tests/catalog.unit.test.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Primary navigation does not expose separate Catalog and Vault tabs"
    - "Existing deep links and persisted last-tab values referencing catalog still land on the consolidated shopping surface"
  artifacts:
    - path: src/App.tsx
      provides: "Tab definitions and landing resolution without a separate Catalog tab"
      contains: "TAB_DEFINITIONS"
    - path: tests/catalog.unit.test.tsx
      provides: "Unit coverage for consolidated navigation and legacy tab aliasing"
      contains: "primary navigation tabs"
  key_links:
    - from: src/App.tsx
      to: src/App.tsx
      via: "navigation persistence"
      pattern: "emily-idle:navigation"
    - from: src/App.tsx
      to: src/App.tsx
      via: "tab deep link parsing"
      pattern: "URLSearchParams\\(window\\.location\\.search\\)"
---

<objective>
Close Phase 26 UAT gaps by removing the separate Catalog tab and ensuring navigation/landing behavior remains deterministic and backwards compatible.

Purpose: Human verification flagged that Catalog and Vault should not be separate tabs. This plan updates primary navigation and preserves deep-link + last-tab behavior.
Output: App navigation no longer exposes a Catalog tab; legacy `tab=catalog` and stored lastTabId values are gracefully handled.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-catalog-first-shop/26-CONTEXT.md
@.planning/phases/26-catalog-first-shop/26-05-SUMMARY.md
@src/App.tsx
@tests/catalog.unit.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the Catalog tab and add legacy alias handling for catalog deep links</name>
  <files>
src/App.tsx
  </files>
  <action>
- Update primary navigation tab definitions:
  - Remove the `catalog` entry from `TAB_DEFINITIONS`.
  - Ensure the remaining baseline tabs still include Vault (`collection`) and Save (`save`) on a fresh save.

- Update tab hiding infrastructure:
  - Remove `catalog` from `HIDEABLE_TAB_IDS`.
  - Ensure hidden tab sanitation continues to ignore unknown values.

- Update landing resolution to support the consolidated surface and backwards compatibility:
  - Treat `/?tab=catalog` as an alias for the consolidated surface (`collection`).
  - Treat stored `emily-idle:navigation.lastTabId === "catalog"` as `collection`.
  - Keep the existing override order:
    1) deep link (with `catalog` alias)
    2) first-session override on fresh save
    3) persisted last tab
    4) fallback to `collection`
  - IMPORTANT: deep links must not overwrite `emily-idle:navigation`.

  - Explicitly update the fresh-save / first-session override:
    - If any code path currently selects `catalog` for first-session, change it to select `collection`.

- Update the "active tab became hidden" safety effect:
  - Fallback should never try to activate `catalog` (since it no longer exists).
  - Use `collection` as the default fallback.

- Do not introduce a router.
  </action>
  <verify>
pnpm run typecheck
  </verify>
  <done>
- Catalog is no longer present as a primary navigation tab.
- `/?tab=catalog` and a stored lastTabId of `catalog` both resolve to the consolidated surface.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update navigation unit tests to reflect the single surface</name>
  <files>
tests/catalog.unit.test.tsx
  </files>
  <action>
- Update the "primary navigation tabs" suite:
  - Replace assertions that expect both Vault + Catalog tabs.
  - Assert that on a fresh save the tablist contains Vault and Save (and does NOT contain Catalog).
  - Update focus/activation tests to match the new visible tab set.

- Update last-tab and deep-link tests:
  - Add (or update) coverage that a stored lastTabId of `catalog` is treated as Vault (`collection`).
  - Update deep-link non-persistence coverage to use `/?tab=catalog` as an alias and verify `emily-idle:navigation` is unchanged.
  </action>
  <verify>
pnpm run test:unit
  </verify>
  <done>
- Unit tests pass with no Catalog tab in primary navigation.
- Backwards compatibility for `tab=catalog` and stored lastTabId values is covered.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit`
</verification>

<success_criteria>
- Phase 26 UAT issue is addressed: no separate Catalog/Vault tabs.
- Navigation behavior remains deterministic and backwards compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/26-catalog-first-shop/26-06-SUMMARY.md`
</output>
