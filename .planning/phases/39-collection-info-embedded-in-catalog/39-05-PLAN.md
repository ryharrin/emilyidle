---
phase: 39-collection-info-embedded-in-catalog
plan: 05
type: execute
wave: 3
depends_on:
  - "39-04"
files_modified:
  - tests/catalog.unit.test.tsx
  - tests/catalog-buy-buttons.spec.ts
autonomous: true

must_haves:
  truths:
    - "Automated tests assert the Catalog shopping header includes the collection context block (capacity/value)."
  artifacts:
    - path: tests/catalog.unit.test.tsx
      provides: "Unit coverage for catalog-collection-context pill presence/content."
    - path: tests/catalog-buy-buttons.spec.ts
      provides: "E2E coverage confirming the context pill renders in the real app while shopping."
  key_links:
    - from: tests/catalog.unit.test.tsx
      to: "catalog-collection-context"
      via: "unit assertion on header pill"
      pattern: "catalog-collection-context"
---

<objective>
Add regression coverage for the Catalog header collection context block.

Purpose: Protect VLT-01/VLT-02 by ensuring the new shopping context pill remains present and includes capacity + value.
Output: Unit + e2e assertions that `data-testid="catalog-collection-context"` renders with expected content.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/39-collection-info-embedded-in-catalog/39-RESEARCH.md

@tests/catalog.unit.test.tsx
@tests/catalog-buy-buttons.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unit assertions for the catalog collection context pill</name>
  <files>tests/catalog.unit.test.tsx</files>
  <action>
  - In `tests/catalog.unit.test.tsx`, add assertions that when the Catalog purchase surface is rendered, the header includes `data-testid="catalog-collection-context"`.
  - Assert the pill content includes both:
    - a "Collection" capacity string with a "/" separator (current/max)
    - a "Collection value" label (or the exact wording implemented in 39-01)
  </action>
  <verify>pnpm run test:unit -- tests/catalog.unit.test.tsx</verify>
  <done>
  - Unit tests fail if `catalog-collection-context` is removed or if it no longer includes capacity/value labeling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add e2e assertion that the context pill renders while shopping</name>
  <files>tests/catalog-buy-buttons.spec.ts</files>
  <action>
  - In `tests/catalog-buy-buttons.spec.ts`, after navigating to the Catalog tab and scrolling to `data-testid="catalog-shop"`, assert the context pill is visible via `getByTestId("catalog-collection-context")`.
  - Keep purchase selector semantics unchanged; this is an additional assertion only.
  </action>
  <verify>pnpm run test:e2e -- tests/catalog-buy-buttons.spec.ts</verify>
  <done>
  - E2E test confirms the context pill renders in the running app.
  </done>
</task>

</tasks>

<verification>
- `pnpm run test:unit -- tests/catalog.unit.test.tsx`
- `pnpm run test:e2e -- tests/catalog-buy-buttons.spec.ts`
</verification>

<success_criteria>
- Automated tests protect presence/content of the catalog collection context pill.
</success_criteria>

<output>
After completion, create `.planning/phases/39-collection-info-embedded-in-catalog/39-05-SUMMARY.md`
</output>
