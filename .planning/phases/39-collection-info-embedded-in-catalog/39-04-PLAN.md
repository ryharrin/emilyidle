---
phase: 39-collection-info-embedded-in-catalog
plan: 04
type: execute
wave: 2
depends_on:
  - "39-01"
  - "39-02"
  - "39-03"
files_modified:
  - tests/catalog.unit.test.tsx
  - tests/collection-loop.spec.ts
  - tests/unlock-clarity.spec.ts
  - tests/wear-one-bonus.spec.ts
  - tests/phase32-uat-landing-navigation.spec.ts
  - tests/phase35-uat.spec.ts
  - tests/tabs.spec.ts
  - tests/ui-screenshots.spec.ts
  - tests/uat-screenshots.spec.ts
  - tests/career-landing.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "Automated tests locate the renamed Collection tab by accessible name and continue to pass without selector changes."
  artifacts:
    - path: tests/catalog.unit.test.tsx
      provides: "Unit coverage for Collection tab name and updated visible copy assertions."
    - path: tests/collection-loop.spec.ts
      provides: "E2E navigation continues using the Collection tab accessible name."
  key_links:
    - from: tests/catalog.unit.test.tsx
      to: "catalog-collection-context"
      via: "unit assertion on header pill"
      pattern: "catalog-collection-context"
---

<objective>
Update unit and Playwright tests to match the new “Collection” naming.

Purpose: Keep regression coverage stable after renaming accessible tab labels and adding catalog shopping context UI.
Output: Tests pass under the new naming while preserving all existing selectors (ids/data-testids) used by tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/39-collection-info-embedded-in-catalog/39-RESEARCH.md

@tests/catalog.unit.test.tsx
@tests/collection-loop.spec.ts
@tests/ui-screenshots.spec.ts
@tests/uat-screenshots.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update tests that click/assert the “Vault” tab to use “Collection”</name>
  <files>tests/catalog.unit.test.tsx, tests/collection-loop.spec.ts, tests/unlock-clarity.spec.ts, tests/wear-one-bonus.spec.ts, tests/phase32-uat-landing-navigation.spec.ts, tests/phase35-uat.spec.ts, tests/tabs.spec.ts, tests/ui-screenshots.spec.ts, tests/uat-screenshots.spec.ts, tests/career-landing.unit.test.ts</files>
  <action>
  - Replace role/name selectors that expect the tab name "Vault" with "Collection".
    - Examples to update include `getByRole("tab", { name: "Vault" })` and regex variants `/Vault/i`.
  - Keep existing selectors that intentionally contain `vault-*` (e.g. `data-testid="vault-interact-*"`) unchanged.
  - Update any text assertions that explicitly expect "Vault" in visible copy (e.g. coachmark titles, stats labels, prestige summaries) to match the updated copy from 39-02.
  </action>
  <verify>pnpm run test:unit</verify>
  <done>
  - Unit tests no longer fail due to expecting the old "Vault" accessible label or old visible copy.
  </done>
</task>

</tasks>

<verification>
- `pnpm run test:unit`
- `pnpm run test:e2e -- tests/tabs.spec.ts`
</verification>

<success_criteria>
- Tests that navigate by tab name and assert visible copy pass under the new Collection naming.
</success_criteria>

<output>
After completion, create `.planning/phases/39-collection-info-embedded-in-catalog/39-04-SUMMARY.md`
</output>
