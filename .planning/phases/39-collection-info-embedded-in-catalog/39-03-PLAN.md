---
phase: 39-collection-info-embedded-in-catalog
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/data/milestones.ts
  - src/game/data/items.ts
  - src/game/data/upgrades.ts
  - src/game/model/state.ts
  - src/game/selectors/index.ts
autonomous: true

must_haves:
  truths:
    - "Displayed milestone/achievement/upgrade strings use 'Collection' instead of 'Vault' without changing any persisted ids."
  artifacts:
    - path: src/game/data/milestones.ts
      provides: "Milestone display names/descriptions align to Collection naming (ids unchanged)."
    - path: src/game/model/state.ts
      provides: "Player-visible tier/upgrade/event descriptions align to Collection naming (ids unchanged)."
    - path: src/game/selectors/index.ts
      provides: "Requirement labels shown in UI use Collection wording."
  key_links:
    - from: src/game/data/milestones.ts
      to: "Collection showcase"
      via: "rename display string only"
      pattern: "name: \"Collection showcase\""
---

<objective>
Align player-visible domain strings to “Collection” naming without changing any identifiers.

Purpose: Support VLT-04 by ensuring milestone/achievement/upgrade labels shown in the UI do not refer to “Vault”, while preserving all ids used by saves/tests.
Output: Updated milestone names/descriptions and other displayed strings; no persisted identifiers renamed.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@.planning/phases/39-collection-info-embedded-in-catalog/39-RESEARCH.md

@src/game/data/milestones.ts
@src/game/data/items.ts
@src/game/data/upgrades.ts
@src/game/model/state.ts
@src/game/selectors/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename milestone display strings to use “Collection” (ids unchanged)</name>
  <files>src/game/data/milestones.ts</files>
  <action>
  - In `src/game/data/milestones.ts`, update milestone display strings to use Collection naming.
    - Example: rename the milestone name "Vault showcase" -> "Collection showcase".
    - Do not change milestone ids (e.g. keep `id: "showcase"`).
  </action>
  <verify>pnpm run typecheck</verify>
  <done>
  - Milestone display names/descriptions no longer use "Vault".
  - No milestone ids were renamed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename player-visible domain descriptions to avoid “Vault” while keeping ids stable</name>
  <files>src/game/data/items.ts, src/game/data/upgrades.ts, src/game/model/state.ts, src/game/selectors/index.ts</files>
  <action>
  - In `src/game/data/items.ts` and `src/game/data/upgrades.ts`, update `description` strings that refer to "vault" to use "collection" wording.
    - Do not change any item ids or upgrade ids.
  - In `src/game/model/state.ts`, update player-visible copy (names/descriptions) that refer to "Vault"/"vault".
    - Do not change ids (e.g. `vault-calibration`, `six-figure-vault`, `vault-century`) because they are persisted.
  - In `src/game/selectors/index.ts`, update player-visible requirement labels (e.g. `getAchievementRequirementLabel`) to say "collection" instead of "vault".
    - Keep logic/thresholds unchanged.
  </action>
  <verify>pnpm run typecheck && ! pnpm -s exec rg "\\bVault\\b" src/game</verify>
  <done>
  - Player-visible domain strings in `src/game/**` no longer contain the word "Vault".
  - No ids/keys were renamed.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `! pnpm -s exec rg "\\bVault\\b" src/game`
</verification>

<success_criteria>
- Domain strings displayed to the player use "Collection" naming.
- All persisted identifiers remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/39-collection-info-embedded-in-catalog/39-03-SUMMARY.md`
</output>
