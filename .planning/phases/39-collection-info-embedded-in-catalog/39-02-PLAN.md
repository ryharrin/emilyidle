---
phase: 39-collection-info-embedded-in-catalog
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/ui/help/helpContent.ts
  - src/ui/prestigeOnboarding.ts
  - src/ui/prestigeSummary.ts
  - src/ui/tabs/CollectionTab.tsx
  - src/ui/tabs/StatsTab.tsx
  - src/ui/tabs/UpgradesTab.tsx
autonomous: true

must_haves:
  truths:
    - "The primary navigation tab label reads 'Collection' (tab id remains 'collection')."
    - "Player-facing UI and help copy uses 'Collection' instead of 'Vault' while keeping all ids/data-testids and persistence keys unchanged."
  artifacts:
    - path: src/App.tsx
      provides: "Primary nav uses Collection label; hero + stats copy no longer says Vault."
    - path: src/ui/tabs/CollectionTab.tsx
      provides: "Collection tab header/copy uses Collection naming without selector changes."
    - path: src/ui/help/helpContent.ts
      provides: "Help copy uses Collection naming and no longer references Vault as the loop label."
  key_links:
    - from: src/App.tsx
      to: "TAB_DEFINITIONS"
      via: "tab label update only"
      pattern: "\{ id: \"collection\", label: \"Collection\" \}"
---

<objective>
Rename player-facing “Vault” terminology to “Collection” across UI and help while preserving selectors and persistence.

Purpose: Satisfy VLT-04 by making “Collection” the consistent player-visible naming without breaking existing ids/data-testids, save formats, or localStorage keys.
Output: Updated UI copy + help text; all functional identifiers remain unchanged.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@.planning/phases/39-collection-info-embedded-in-catalog/39-RESEARCH.md

@src/App.tsx
@src/ui/tabs/CollectionTab.tsx
@src/ui/tabs/StatsTab.tsx
@src/ui/tabs/UpgradesTab.tsx
@src/ui/help/helpContent.ts
@src/game/selectors/index.ts
@src/game/model/state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the primary tab label and visible UI copy from “Vault” to “Collection” (VLT-04)</name>
  <files>src/App.tsx, src/ui/tabs/CollectionTab.tsx, src/ui/tabs/StatsTab.tsx, src/ui/tabs/UpgradesTab.tsx, src/ui/help/helpContent.ts, src/ui/prestigeOnboarding.ts, src/ui/prestigeSummary.ts</files>
  <action>
  - In `src/App.tsx`:
    - Change the `TAB_DEFINITIONS` label for `{ id: "collection" }` from "Vault" to "Collection".
    - Update hero/stats visible copy to remove "Vault" wording (e.g. hero subtitle and stats dt/heading text), but do not rename any DOM `id` attributes (e.g. keep `id="vault-stats-title"`).
    - Keep localStorage keys unchanged (`emily-idle:*`).
    - Keep coachmark ids unchanged (e.g. keep `id: "vault-basics"`) but update their visible titles/text to use "Collection".
  - In `src/ui/tabs/CollectionTab.tsx`, `src/ui/tabs/StatsTab.tsx`, `src/ui/tabs/UpgradesTab.tsx`, and `src/ui/help/helpContent.ts`:
    - Replace player-facing "Vault"/"vault" wording with "Collection"/"collection" (or remove the Vault prefix where more natural, e.g. rename "Vault dollars" -> "Dollars").
    - Do not change any `id` or `data-testid` attributes.
  - In `src/ui/prestigeOnboarding.ts` and `src/ui/prestigeSummary.ts`:
    - Replace player-facing "vault" wording with "collection" wording, keeping behavior and types unchanged.
  </action>
  <verify>pnpm run typecheck && ! pnpm -s exec rg "\\bVault\\b" src/App.tsx src/ui</verify>
  <done>
  - Primary nav tab reads "Collection".
  - No player-visible UI/help text refers to "Vault" as the loop label.
  - All ids/data-testids/localStorage keys remain unchanged.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `! pnpm -s exec rg "\\bVault\\b" src/App.tsx src/ui`
</verification>

<success_criteria>
- The tab label and player-facing copy consistently use "Collection" instead of "Vault".
- Selectors (`id`, `data-testid`) and persistence (save/localStorage) remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/39-collection-info-embedded-in-catalog/39-02-SUMMARY.md`
</output>
