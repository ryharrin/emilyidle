---
phase: 46-catalog-expansion-tiered-variety
plan: 01
type: execute
wave: 1
depends_on:
  - 45-02
files_modified:
  - src/game/catalog.ts
  - public/catalog/**/*
  - scripts/catalog/sync-catalog-images.js
  - tests/catalog-image-url-contract.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "Catalog includes new watches across starter/classic/chronograph/tourbillon tiers."
    - "Every new catalog entry has complete license metadata and a valid local image path under /catalog/."
    - "No missing catalog images remain after running the sync script."
  artifacts:
    - path: src/game/catalog.ts
      provides: "Catalog entries + tier tagging + base-aware image mapping"
      contains: "CATALOG_ENTRIES"
    - path: scripts/catalog/sync-catalog-images.js
      provides: "Automated missing-image detection and optional download"
      contains: "--download"
---

<objective>
Add a meaningful batch of new catalog entries across early/mid/late tiers and ensure all images and licensing metadata are complete.

Purpose: Deliver CAT-05/06/07/08/10 with complete assets.
Output: Expanded `CATALOG_ENTRIES` + locally available images under `public/catalog`.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/46-catalog-expansion-tiered-variety/46-CONTEXT.md

@src/game/catalog.ts
@scripts/catalog/sync-catalog-images.js
@public/catalog
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add new catalog entries (brands/models) across tiers with complete metadata</name>
  <files>src/game/catalog.ts</files>
  <action>
Add new `CATALOG_ENTRIES` items that satisfy tiered variety:

- Ensure brand expansion is reflected in `CatalogBrand` union type.
- For each new entry, include:
  - `id` (stable, slug-like)
  - `brand`, `model`, `description`, `year`
  - `tags` (include tier tag or tags that infer the tier reliably)
  - `facts` (optional but recommended)
  - `image` object with:
    - `url` (Wikimedia URL preferred)
    - `sourceUrl`
    - `licenseName` + `licenseUrl`
    - `author` + `attribution`

Prefer adding entries in a tier-balanced batch (see Phase context target counts).
  </action>
  <verify>pnpm -s run typecheck</verify>
  <done>
- Catalog compiles with new brands/entries and tier tagging is deterministic.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sync/download missing catalog images into public/catalog</name>
  <files>
public/catalog/**/*
scripts/catalog/sync-catalog-images.js
  </files>
  <action>
Use the existing sync script to ensure all referenced Wikimedia images exist locally:

- Run:
  - `node scripts/catalog/sync-catalog-images.js`
  - then `node scripts/catalog/sync-catalog-images.js --download` as needed
- If Wikimedia filenames are awkward, update the override map consistently (script + catalog mapping) rather than renaming URLs inside the catalog entries.

Goal: the script should report Missing: 0 after downloads.
  </action>
  <verify>node scripts/catalog/sync-catalog-images.js</verify>
  <done>
- All catalog image paths referenced by `src/game/catalog.ts` exist under `public/catalog`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update/extend image URL contract coverage for the new entries</name>
  <files>tests/catalog-image-url-contract.unit.test.ts</files>
  <action>
Update `tests/catalog-image-url-contract.unit.test.ts` (or add a companion test) to include new entries in its assertions.

Minimum:
- `getCatalogImageUrl(entry)` returns a base-aware local `/catalog/...` URL for Wikimedia entries.
- New entries do not regress to remote Wikimedia URLs in-app.
  </action>
  <verify>pnpm -s run test:unit -- tests/catalog-image-url-contract.unit.test.ts</verify>
  <done>
- Contract tests cover the new entries and enforce local image mapping.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `node scripts/catalog/sync-catalog-images.js` (expect Missing: 0)
- `pnpm run test:unit -- tests/catalog-image-url-contract.unit.test.ts`
</verification>

<success_criteria>
- CAT-05/06/07/08/10 satisfied: catalog has expanded tiered variety and every new watch has complete metadata and local images.
</success_criteria>

<output>
After completion, create `.planning/phases/46-catalog-expansion-tiered-variety/46-01-SUMMARY.md`
</output>

