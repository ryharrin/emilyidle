---
phase: 46-catalog-expansion-tiered-variety
plan: 1
type: plan
wave: 1
depends_on:
  - "45-03"
files_modified:
  - src/game/data/watchModels.ts
  - src/game/catalog.ts
  - public/catalog/*
autonomous: true

must_haves:
  truths:
    - "Catalog includes new low-end, mid-tier, and luxury models with unique enjoyment and cash rates so players can judge value across tiers."
    - "Each new watch entry carries full metadata (brand, movement, tags, year, image attribution) so the catalog renderer has everything it needs."
  artifacts:
    - path: "src/game/data/watchModels.ts"
      provides: "Tiered watch definitions (IDs, tiers, catalog references, base enjoyment/cash rates, reserve expectations)."
    - path: "src/game/catalog.ts"
      provides: "Catalog entries that surface the new models with licensing, tags, and imagery metadata."

<objective>
Populate the catalog data layer with a third wave of watch models that expands the low/mid/lux spectrum, anchors their enjoyment/cash math, and ensures every entry has a metadata wallet for the UI to render without missing fields.
</objective>

<context>
- Requirements: CAT-05..CAT-10 (low/mid/lux variety, appropriate stats, imagery)
- Phase 45 selectors and tests now exist, so the model layer can power the new items without recalculating multipliers.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add low/mid/lux watch definitions</name>
  <files>
src/game/data/watchModels.ts
  </files>
  <action>
- Add at least two new entries per tier (low/mid/lux) with unique IDs, tier affiliations, brand/model metadata, and references to catalog entries that will render them.
- Define each model’s base enjoyment/cash rates in cents/sec, reserve-aware multipliers, and event-multiplier sanitization so the selector math can reuse the existing helpers.
- Document any new movements or tier tags in comments so future designers understand how each tier differs.
  </action>
  <verify>
pnpm test:unit -- tests/per-watch-stats.unit.test.ts
  </verify>
  <done>
New watch models exist in `WATCH_MODELS` with distinct tier data and don’t break the per-watch stats selectors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire catalog metadata for the new models</name>
  <files>
src/game/catalog.ts
public/catalog/*
  </files>
  <action>
- Add catalog entry objects for each new model, including brand, model name, year, tags, licensing metadata, image URLs, and `catalogEntryIds` that tie back to the watch definitions.
- Ensure each entry references hosted/placeholder imagery under `/public/catalog` and that the license blocks satisfy the existing schema.
- Document why each tier’s metadata is tuned as it is (price, tags, messaging) so the UI copy knows how to present the variety.
  </action>
  <verify>
pnpm test:unit -- tests/catalog.unit.test.tsx
  </verify>
  <done>
New catalog entries resolve their metadata and images without runtime errors, allowing the Catalog tab to render them immediately.
  </done>
</task>

<task type="auto">
  <name>Task 3: Capture tier info for planners and guardrails</name>
  <files>
src/game/data/watchModels.ts
src/game/catalog.ts
  </files>
  <action>
- Annotate the data files with comments describing each tier’s intended pacing, rarity, and enjoyment/cash delta so marketing and QA know the context.
- Update any helper arrays (e.g., tier orderings) so the new tiers slot into the progression without surprising sort results.
- Run existing data integrity checks or augment tests (e.g., verifying `catalogEntryIds` are non-empty) to lock down the new entries.
  </action>
  <verify>
pnpm lint
  </verify>
  <done>
Tier metadata is documented and the data helpers still pass lint/validation, keeping catalog builders safe to consume the new watches.
  </done>
</task>

</tasks>
