---
phase: 46-catalog-expansion-tiered-variety
plan: 3
type: plan
wave: 3
depends_on:
  - "46-02"
files_modified:
  - tests/catalog-expansion.spec.ts
  - tests/catalog-expansion.unit.test.ts
  - tests/localstorage-schema.unit.test.tsx
  - tests/localstorage-keys.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "Unit and Playwright coverage prove the catalog variety is reachable, the tiers display the right stats, and the sticky controls stay attached on mobile."
    - "Guardrail/localStorage tests continue to pass despite the new data/filters so downloads remain stable."
  artifacts:
    - path: "tests/catalog-expansion.unit.test.ts"
      provides: "Vitest assertions that new tier models surface with distinct enjoyment/cash rates and the Collection call-out matches the selectors."
    - path: "tests/catalog-expansion.spec.ts"
      provides: "Playwright steps that seed the expanded catalog, open the new tiers, and confirm the sticky controls remain visible while scrolling."

<objective>
Lock the expanded catalog variety behind automated guardrails so future edits can’t regress the tier counts, rates, or sticky controls.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add unit coverage for new tiered models</name>
  <files>
tests/catalog-expansion.unit.test.ts
src/game/data/watchModels.ts
src/game/selectors/perWatchStats.ts
  </files>
  <action>
- Create a Vitest test that loads the new watch models, asserts each tier is represented, and checks their enjoyment/cash strings via `formatRateFromCentsPerSec`.
- Verify the equipped watch delta still produces the expected explanation string when one of the new models is worn.
- Ensure the tests are keyed by `modelId` so future watchers stay stable even as more entries are added.
  </action>
  <verify>
pnpm test:unit -- tests/catalog-expansion.unit.test.ts
  </verify>
  <done>
The new models have deterministic coverage and any change in tier rates fails the unit test.
  </done>
</task>

<task type="auto">
  <name>Task 2: Playwright regression for tier discovery and sticky controls</name>
  <files>
tests/catalog-expansion.spec.ts
  </files>
  <action>
- Seed the catalog/localStorage with unlocked data that includes the new low/mid/lux entries.
- Navigate to the Catalog tab, scroll through the tier lanes, and assert each `data-testid="catalog-tier-..."` section exists with enjoyment/cash columns plus sticky filter controls.
- Use a mobile viewport and confirm the sticky filters stay visible while scrolling the lanes containing the new models.
  </action>
  <verify>
pnpm test:e2e -- tests/catalog-expansion.spec.ts
  </verify>
  <done>
Playwright verifies the tier lanes exist, the sticky controls remain fixed, and the new entries render as expected.
  </done>
</task>

<task type="auto">
  <name>Task 3: Ensure guardrail tests stay green</name>
  <files>
tests/localstorage-schema.unit.test.tsx
tests/localstorage-keys.unit.test.ts
  </files>
  <action>
- Confirm no new localStorage keys were introduced by the catalog expansion (re-run schema checks).
- If new catalog filters persist user choices, register the keys in the guardrail tests and document the schema shape.
- Run the guardrail suites to ensure the new watches don’t break the schema assertions.
  </action>
  <verify>
pnpm test:unit -- tests/localstorage-keys.unit.test.ts tests/localstorage-schema.unit.test.tsx
  </verify>
  <done>
LocalStorage guardrails remain satisfied, and any future persisted preferences are explicitly documented.
  </done>
</task>

</tasks>
