---
phase: 46-catalog-expansion-tiered-variety
plan: 2
type: plan
wave: 2
depends_on:
  - "46-01"
files_modified:
  - src/ui/tabs/CatalogTab.tsx
  - src/style.css
  - src/ui/components/PerWatchStatsTable.tsx
  - src/ui/components/TierBadge.tsx
autonomous: true

must_haves:
  truths:
    - "Catalog surfaces the new low/mid/lux groups with their own discovery lanes so players can see the expanded variety at a glance."
    - "Tier badges, sticky filters, and responsive touch targets meet the 44px requirement across the new sections."
  artifacts:
    - path: "src/ui/tabs/CatalogTab.tsx"
      provides: "Tier-grouped catalog layout and filter hooks that keep the new models discoverable."
    - path: "src/style.css"
      provides: "Responsive styles that keep the new tier bands distinct and the controls sticky on mobile."

<objective>
Resurface the expanded catalog variety in the UI by grouping entries into tiered lanes, adding badges/call-outs, and keeping the new controls accessible on desktop and mobile.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Introduce tiered catalog lanes</name>
  <files>
src/ui/tabs/CatalogTab.tsx
src/ui/components/PerWatchStatsTable.tsx
  </files>
  <action>
- Refactor `CatalogTab` so it renders the per-watch table (if any) followed by lane sections dedicated to low, mid, and lux tiers.
- Ensure each lane uses stable `data-testid` values (e.g., `catalog-tier-low`) so regression tests can target the new variety.
- Keep the existing filters + sticky controls working across the new layout so sorting/filtering still applies globally.
  </action>
  <verify>
pnpm test:e2e -- tests/catalog-tier-sections.spec.ts
  </verify>
  <done>
The Catalog panel shows clear tier lanes with the new watches and the per-watch stats surface stays sticky while the lanes render below.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tier badges and discovery helper copy</name>
  <files>
src/ui/components/TierBadge.tsx
src/style.css
  </files>
  <action>
- Create a `TierBadge` component (or reuse existing UI) that highlights low/mid/lux tiers with brand cues and reward hints.
- Place the badges in each catalog lane header and ensure they respect the color palette and 44px touch targets.
- Update `src/style.css` so the badges, lane headers, and sticky controls all respond well on mobile (tap target size, background blur, spacing).
  </action>
  <verify>
pnpm test:unit -- tests/catalog.unit.test.tsx
  </verify>
  <done>
Tier badges render correctly, the lane headers stay responsive, and the new discovery copy complements the expanded catalog.
  </done>
</task>

<task type="auto">
  <name>Task 3: Surface the catalog variety in Collection and help copy</name>
  <files>
src/ui/tabs/CollectionTab.tsx
src/ui/help/helpContent.ts
  </files>
  <action>
- Update Collection’s summary to mention the expanded catalog tiers and how they feed enjoyment/cash decisions.
- Add new help copy or tooltips pointing players toward the tiered catalog lanes and the meaning of the badges.
- Make sure the new copy references the tier names used in the data layer so nothing diverges.
  </action>
  <verify>
pnpm test:unit -- tests/collection.unit.test.tsx
  </verify>
  <done>
Collection references the catalog variety cleanly and the help system knows about the new tiers.
  </done>
</task>

</tasks>
