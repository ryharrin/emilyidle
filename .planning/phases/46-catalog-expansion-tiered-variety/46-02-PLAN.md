---
phase: 46-catalog-expansion-tiered-variety
plan: 02
type: execute
wave: 2
depends_on:
  - 46-01
files_modified:
  - tests/catalog-image-rendering.spec.ts
  - tests/catalog.unit.test.tsx
  - src/game/data/items.ts
autonomous: true

must_haves:
  truths:
    - "New watches have tier-appropriate stats and feel sensible relative to progression."
    - "E2E verifies new catalog images render under the deployed base path."
  artifacts:
    - path: tests/catalog-image-rendering.spec.ts
      provides: "E2E asserts catalog images actually load in browser"
      contains: "naturalWidth"
---

<objective>
Validate that catalog expansion is not just “data added”, but also “playable and correct”: images render, UI remains usable, and new watches have tier-appropriate stats.

Purpose: Deliver CAT-09 and harden CAT-10 via runtime verification.
Output: Balanced stat adjustments + Playwright checks for image rendering.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@src/game/data/items.ts
@tests/catalog-image-rendering.spec.ts
@tests/catalog.unit.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tune tier stats if needed so new entries feel tier-appropriate</name>
  <files>src/game/data/items.ts</files>
  <action>
If Phase 45 introduces per-model variance, ensure the tier baselines remain sane for progression:

- Review `WATCH_ITEMS` baselines (price, enjoyment/sec, income/sec) and adjust minimally if expansion makes early game too slow/fast.
- Keep changes conservative; this phase is primarily “variety + assets”, not a full rebalance pass.

Document any tuning decisions in the plan summary when executed.
  </action>
  <verify>pnpm -s run test:unit</verify>
  <done>
- Baseline rates/prices remain sensible after adding new catalog variety.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend Playwright coverage to include rendering of new images</name>
  <files>tests/catalog-image-rendering.spec.ts</files>
  <action>
Extend the existing image rendering spec so it samples (or iterates) over the new entries and asserts:
- images have `naturalWidth > 0` under `/emilyidle/` base path

Keep it pragmatic:
- sample a few representative entries across tiers/brands, OR
- iterate but cap runtime (avoid a 5-minute test).
  </action>
  <verify>pnpm -s run test:e2e -- tests/catalog-image-rendering.spec.ts</verify>
  <done>
- E2E prevents “missing image” regressions for the new catalog entries.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add a quick unit/UI sanity assertion for catalog variety</name>
  <files>tests/catalog.unit.test.tsx</files>
  <action>
Add/extend a unit test that asserts:
- Catalog renders at least one card from each tier tag (starter/classic/chronograph/tourbillon) OR
- The underlying catalog data includes entries tagged into each tier.

Prefer stable assertions (avoid “exact count” expectations).
  </action>
  <verify>pnpm -s run test:unit -- tests/catalog.unit.test.tsx</verify>
  <done>
- Unit test prevents accidentally collapsing all new entries into a single tier/tag.
  </done>
</task>

</tasks>

<verification>
- `pnpm run test:unit`
- `pnpm run test:e2e -- tests/catalog-image-rendering.spec.ts`
</verification>

<success_criteria>
- CAT-09 satisfied: new watches’ tier baselines remain appropriate (with per-model variance on top).
- CAT-10 strengthened: new images are verified to render in-browser.
</success_criteria>

<output>
After completion, create `.planning/phases/46-catalog-expansion-tiered-variety/46-02-SUMMARY.md`
</output>

