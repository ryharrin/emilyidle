---
phase: 49-mobile-ux-polish
plan: 4
type: execute
wave: 3
depends_on:
  - 49-02
files_modified:
  - src/App.tsx
  - src/game/selectors/statsBreakdown.ts
  - src/game/state.ts
  - src/ui/components/StatsHeader.tsx
  - src/ui/tabs/StatsTab.tsx
  - src/style.css
  - tests/collection-loop.spec.ts
  - tests/dual-currency.unit.test.tsx
autonomous: true

must_haves:
  truths:
    - "Top-level stats are grouped into clearer sub-sections rather than one flat block."
    - "Detailed header stats can be collapsed/expanded to reduce noise on smaller screens."
    - "Stat modifiers are presented with subtotals in breakdown views."
    - "Softcap details move out of the primary header and into breakdown content."
  artifacts:
    - path: "src/ui/components/StatsHeader.tsx"
      provides: "Grouped and collapsible header stats component"
    - path: "src/game/selectors/statsBreakdown.ts"
      provides: "Selector-backed subtotal breakdown structure"
    - path: "src/ui/tabs/StatsTab.tsx"
      provides: "Breakdown rendering that includes grouped modifier subtotals"
  key_links:
    - from: "src/App.tsx"
      to: "src/ui/components/StatsHeader.tsx"
      via: "header replacement"
      pattern: "StatsHeader"

<objective>
Implement `STATS-01`, `HEADER-01`, `BREAK-01`, and `SOFTCAP-01` by restructuring the stats information architecture around grouped primary metrics and selector-backed breakdowns.

Purpose: Make key numbers scannable while keeping deep detail accessible on demand.
Output: new header component + subtotal selector helpers + StatsTab/UI/test updates.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/49-mobile-ux-polish/49-02-PLAN.md
@src/App.tsx
@src/ui/tabs/StatsTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract grouped/collapsible stats header component</name>
  <files>
src/ui/components/StatsHeader.tsx
src/App.tsx
src/style.css
  </files>
  <action>
Create `StatsHeader` and replace the inline App header stats grid.

Requirements:
- Group metrics into logical buckets (primary economy, progression, system).
- Add expand/collapse affordance for detail-heavy metrics.
- Keep stable ids currently used by tests (`#currency`, `#income`, `#collection-value`, etc.) or provide backward-compatible anchors.
  </action>
  <verify>pnpm typecheck</verify>
  <done>
Stats header is grouped, collapsible, and App uses the new component with stable anchors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add selector-backed stat modifier subtotals and softcap relocation</name>
  <files>
src/game/selectors/statsBreakdown.ts
src/game/state.ts
src/ui/tabs/StatsTab.tsx
tests/dual-currency.unit.test.tsx
  </files>
  <action>
Add selector helpers that compute subtotal groups for key stat modifiers.

Move softcap emphasis from primary header chips into the breakdown section so the main header stays focused.

Keep all math selector-driven (no duplicate calculations in component render code).
  </action>
  <verify>pnpm test:unit -- tests/dual-currency.unit.test.tsx</verify>
  <done>
Stats breakdown includes grouped subtotals and softcap context in the correct location.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update high-level regression checks for stats surfaces</name>
  <files>
tests/collection-loop.spec.ts
  </files>
  <action>
Refresh e2e assertions to align with grouped/collapsible header behavior while preserving existing anchor coverage.

Avoid brittle copy assertions; prefer ids/data-testids and role-based checks.
  </action>
  <verify>pnpm test:e2e -- tests/collection-loop.spec.ts</verify>
  <done>
Stats architecture changes are covered by end-to-end contracts.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/dual-currency.unit.test.tsx`
- `pnpm test:e2e -- tests/collection-loop.spec.ts`
</verification>

<success_criteria>
- `STATS-01` + `HEADER-01`: grouped, collapsible header improves scanability.
- `BREAK-01` + `SOFTCAP-01`: subtotals and softcap details are moved to breakdown surfaces backed by selectors.
</success_criteria>

<output>
After completion, create `.planning/phases/49-mobile-ux-polish/49-04-SUMMARY.md`
</output>
