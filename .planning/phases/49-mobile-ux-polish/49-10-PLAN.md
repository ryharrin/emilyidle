---
phase: 49-mobile-ux-polish
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/components/CareerTimeline.tsx
  - src/ui/tabs/career/CareerPanel.tsx
  - src/ui/tabs/career/CareerMap.tsx
  - src/style.css
  - tests/career-landing.spec.ts
  - tests/career-map.spec.ts
  - tests/career-progression.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "Career view includes a clear timeline that shows current position and upcoming milestones."
    - "Timeline entries explain the impact of permanent career choices on progression."
    - "Timeline remains usable on mobile and does not disrupt existing stage-map interactions."
  artifacts:
    - path: "src/ui/components/CareerTimeline.tsx"
      provides: "Reusable career timeline visualization with current and upcoming milestones"
    - path: "src/ui/tabs/career/CareerPanel.tsx"
      provides: "Career panel integration for timeline view and responsive layout"
    - path: "tests/career-landing.spec.ts"
      provides: "E2E verification for timeline rendering and milestone visibility"
  key_links:
    - from: "src/ui/tabs/career/CareerPanel.tsx"
      to: "src/ui/components/CareerTimeline.tsx"
      via: "timeline panel wiring and state props"
      pattern: "CareerTimeline"

<objective>
Implement `CAREER-01` by adding a timeline visualization that shows progression milestones, current position, and permanent choice impact.

Purpose: Make career progression legible at a glance so users can understand what is next and why prior choices matter.
Output: reusable timeline component + Career panel/map integration + regression coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/49-mobile-ux-polish/49-CONTEXT.md
@src/ui/tabs/career/CareerPanel.tsx
@src/ui/tabs/career/CareerMap.tsx
@tests/career-landing.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build reusable career timeline model and component</name>
  <files>
src/ui/components/CareerTimeline.tsx
src/ui/tabs/career/CareerMap.tsx
src/style.css
  </files>
  <action>
Create a `CareerTimeline` component that renders stage milestones in sequence with:
- current stage highlighting,
- locked/unlocked/upcoming states,
- concise milestone hints sourced from existing career stage metadata.

Keep timeline derivation based on existing career data/constants so no duplicate progression math is introduced in UI.

Add additive timeline test anchors (for example `data-testid="career-timeline"`, `data-testid="career-timeline-node-{id}"`) without renaming existing career-map anchors.
  </action>
  <verify>pnpm typecheck</verify>
  <done>
Timeline component exists, renders deterministic stage states, and preserves existing map selectors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate timeline into Career panel with responsive layout</name>
  <files>
src/ui/tabs/career/CareerPanel.tsx
src/style.css
  </files>
  <action>
Wire `CareerTimeline` into the Career stages surface so users can see current progress and upcoming milestones alongside the existing map.

Requirements:
- Keep stage/upgrades toggle behavior (`career-view-stages`, `career-view-upgrades`) intact.
- Preserve session card flows and salary alert anchors.
- Ensure mobile layout keeps timeline readable without pushing core controls off-screen.

Do not alter persistence keys or viewport storage behavior while adjusting layout.
  </action>
  <verify>pnpm test:e2e -- tests/career-landing.spec.ts tests/career-map.spec.ts</verify>
  <done>
Career panel shows timeline context in stages view and remains stable across desktop/mobile interactions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Cover timeline milestones and choice impact expectations</name>
  <files>
tests/career-progression.unit.test.ts
tests/career-landing.spec.ts
tests/career-map.spec.ts
  </files>
  <action>
Extend tests to validate:
- timeline nodes appear in deterministic order,
- current stage indicator updates as progression advances,
- milestone copy includes permanent-choice impact context,
- existing career map pan/zoom and stage-card checks still pass.

Use role/test-id assertions rather than brittle full-text matching.
  </action>
  <verify>pnpm test:unit -- tests/career-progression.unit.test.ts && pnpm test:e2e -- tests/career-landing.spec.ts tests/career-map.spec.ts</verify>
  <done>
Timeline behavior and choice-impact signals are regression-covered without breaking existing career contracts.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/career-progression.unit.test.ts`
- `pnpm test:e2e -- tests/career-landing.spec.ts tests/career-map.spec.ts`
</verification>

<success_criteria>
- `CAREER-01`: career timeline visualization shows current position, milestones, and choice impact.
- Timeline integrates with existing career surfaces while preserving selector, accessibility, and viewport persistence contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/49-mobile-ux-polish/49-10-SUMMARY.md`
</output>
