---
phase: 49-mobile-ux-polish
plan: 8
type: execute
wave: 4
depends_on:
  - 49-04
files_modified:
  - src/App.tsx
  - src/ui/icons/coreIcons.tsx
  - src/ui/components/ValueTicker.tsx
  - src/ui/components/FloatingDelta.tsx
  - src/ui/components/ToastStack.tsx
  - src/ui/tabs/NostalgiaTab.tsx
  - src/style.css
  - tests/collection-loop.spec.ts
  - tests/nostalgia-prestige.spec.ts
  - tests/ui-screenshots.spec.ts
autonomous: true

must_haves:
  truths:
    - "Currency/readout values animate via count-up transitions when values change."
    - "User actions show local +X feedback near the interaction point."
    - "Nostalgia reset confirmation appears as dismissible toast feedback instead of modal-only text."
    - "Currency/lock/upgrade iconography is standardized across key surfaces."
  artifacts:
    - path: "src/ui/components/ValueTicker.tsx"
      provides: "Reusable count-up value renderer with reduced-motion fallback"
    - path: "src/ui/components/FloatingDelta.tsx"
      provides: "Floating +X feedback component for action confirmation"
    - path: "src/ui/components/ToastStack.tsx"
      provides: "Dismissible toast host for nostalgia and future event feedback"
  key_links:
    - from: "src/App.tsx"
      to: "src/ui/components/ToastStack.tsx"
      via: "top-level feedback host integration"
      pattern: "ToastStack"

<objective>
Implement `ANIM-01`, `FLOAT-01`, `NOSTALGIA-01`, and `ICON-01` by adding reusable feedback primitives and standardized icon wrappers.

Purpose: Improve perceived responsiveness and consistency without destabilizing existing selectors or reduced-motion behavior.
Output: value ticker + floating delta + toast stack + iconography unification + tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/49-mobile-ux-polish/49-04-PLAN.md
@src/App.tsx
@src/ui/icons/coreIcons.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reusable value ticker and iconography wrappers</name>
  <files>
src/ui/components/ValueTicker.tsx
src/ui/icons/coreIcons.tsx
src/App.tsx
src/style.css
  </files>
  <action>
Create `ValueTicker` for animated count-up display and wire it into top-level readouts where values change frequently.

Standardize icons for currencies, locks, and upgrades through `coreIcons.tsx` wrappers so visual language is consistent.

Respect reduced-motion preferences by falling back to instant value updates.
  </action>
  <verify>pnpm typecheck</verify>
  <done>
Core readouts animate safely and icon usage is standardized through shared wrappers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add floating deltas and nostalgia toast feedback</name>
  <files>
src/ui/components/FloatingDelta.tsx
src/ui/components/ToastStack.tsx
src/ui/tabs/NostalgiaTab.tsx
src/App.tsx
src/style.css
  </files>
  <action>
Implement action feedback primitives:
- `FloatingDelta` near triggering controls for +X reward cues.
- `ToastStack` with dismissible messages.

Apply `NOSTALGIA-01` by routing nostalgia reset feedback into toasts (while preserving necessary modal confirmation flows where still required for destructive confirmation).
  </action>
  <verify>pnpm test:unit -- tests/nostalgia-prestige.unit.test.tsx</verify>
  <done>
Floating and toast feedback is live, dismissible, and does not break nostalgia flow contracts.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update visual and flow regressions for feedback surfaces</name>
  <files>
tests/collection-loop.spec.ts
tests/nostalgia-prestige.spec.ts
tests/ui-screenshots.spec.ts
  </files>
  <action>
Extend regression tests/snapshots to validate:
- Animated readouts render stable final values.
- Floating deltas appear for representative actions.
- Nostalgia toasts are visible, dismissible, and non-blocking.
  </action>
  <verify>pnpm test:e2e -- tests/collection-loop.spec.ts tests/nostalgia-prestige.spec.ts tests/ui-screenshots.spec.ts</verify>
  <done>
Feedback motion and toast UX updates are guarded by automated checks.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/nostalgia-prestige.unit.test.tsx`
- `pnpm test:e2e -- tests/collection-loop.spec.ts tests/nostalgia-prestige.spec.ts tests/ui-screenshots.spec.ts`
</verification>

<success_criteria>
- `ANIM-01` + `FLOAT-01`: value and action feedback are visibly improved.
- `NOSTALGIA-01`: nostalgia reset feedback is toast-driven and dismissible.
- `ICON-01`: key iconography is standardized via shared wrappers.
</success_criteria>

<output>
After completion, create `.planning/phases/49-mobile-ux-polish/49-08-SUMMARY.md`
</output>
