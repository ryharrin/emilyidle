---
phase: 49-mobile-ux-polish
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/ui/navigation/tabMeta.ts
  - src/ui/navigation/PageTabRail.tsx
  - src/ui/navigation/pageTabRail.css
  - tests/mobile-navigation.spec.ts
  - tests/tabs.spec.ts
autonomous: true

must_haves:
  truths:
    - "Primary navigation renders as a single-row horizontal tab rail with swipe/scroll-snap behavior on mobile."
    - "Tabs are visually grouped into primary/progression/system buckets for faster scanning."
    - "Existing tab accessibility semantics (tablist/tab roles and roving focus behavior) remain intact."
  artifacts:
    - path: "src/ui/navigation/PageTabRail.tsx"
      provides: "Reusable grouped tab rail component with tab semantics"
    - path: "src/ui/navigation/tabMeta.ts"
      provides: "Tab metadata including stable order and bucket grouping"
    - path: "tests/mobile-navigation.spec.ts"
      provides: "Mobile nav regression coverage for sticky + snap behavior"
  key_links:
    - from: "src/App.tsx"
      to: "src/ui/navigation/PageTabRail.tsx"
      via: "render + active/focus state wiring"
      pattern: "PageTabRail"

<objective>
Implement `TAB-02` and `TAB-03` by replacing the current tab row with a grouped single-row horizontal rail that stays mobile-friendly and keyboard-correct.

Purpose: Improve scan speed and touch ergonomics without breaking established selector/a11y contracts.
Output: Grouped tab metadata + reusable rail component + App wiring + regression updates.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/49-mobile-ux-polish/49-CONTEXT.md
@.planning/phases/49-mobile-ux-polish/49-RESEARCH.md

@src/App.tsx
@tests/mobile-navigation.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build grouped tab rail primitives</name>
  <files>
src/ui/navigation/tabMeta.ts
src/ui/navigation/PageTabRail.tsx
src/ui/navigation/pageTabRail.css
  </files>
  <action>
Create a tab metadata module that encodes stable tab order plus a `bucket` classification (`primary`, `progression`, `system`).

Implement `PageTabRail` that:
- Renders one `tablist` row with horizontal scrolling and scroll-snap.
- Visually labels bucket boundaries while keeping one continuous rail.
- Keeps tab ids/labels deterministic and additive-only (do not rename existing tab ids).

Use component-scoped CSS for the rail so this phase can evolve styles without expanding monolithic global styling.
  </action>
  <verify>pnpm typecheck</verify>
  <done>
Grouped tab rail primitives exist and preserve role/aria semantics.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire grouped rail into App navigation</name>
  <files>
src/App.tsx
  </files>
  <action>
Replace inline nav rendering with `PageTabRail` while preserving current behavior:
- Keep existing tab activation and roving-focus flows.
- Keep existing test anchors (`data-testid="help-open"`, `data-testid="nostalgia-tab"`).
- Keep help button placement in the navigation affordance.

Do not alter save/settings key usage while touching App wiring.
  </action>
  <verify>pnpm test:unit -- tests/mobile-responsive.unit.test.tsx</verify>
  <done>
App renders the grouped rail and existing tab interactions still work.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refresh mobile and tab regression coverage</name>
  <files>
tests/mobile-navigation.spec.ts
tests/tabs.spec.ts
  </files>
  <action>
Update e2e checks to validate grouped rail behavior without hard-coding brittle copy:
- Verify single-row tablist stays sticky.
- Verify horizontal snap/scroll still works on mobile viewports.
- Verify tab semantics and selection state remain stable.
  </action>
  <verify>pnpm test:e2e -- tests/mobile-navigation.spec.ts tests/tabs.spec.ts</verify>
  <done>
Navigation rail behavior is guarded in Playwright and contract stability is preserved.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/mobile-responsive.unit.test.tsx`
- `pnpm test:e2e -- tests/mobile-navigation.spec.ts tests/tabs.spec.ts`
</verification>

<success_criteria>
- `TAB-02`: single-row horizontal tab rail is live and sticky on mobile.
- `TAB-03`: tabs are visibly grouped by information bucket without breaking tab semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/49-mobile-ux-polish/49-01-SUMMARY.md`
</output>
