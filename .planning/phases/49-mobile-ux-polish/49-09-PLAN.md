---
phase: 49-mobile-ux-polish
plan: 9
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/tabs/CollectionTab.tsx
  - src/ui/components/CollectionSectionNav.tsx
  - src/ui/components/OnboardingCoachmark.tsx
  - src/style.css
  - tests/collection-loop.spec.ts
  - tests/help.spec.ts
  - tests/localstorage-keys.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "Collection view is segmented into sticky sections with in-page navigation for quick jumps."
    - "First-time section guidance appears as progressive onboarding tooltips."
    - "Onboarding dismissal state uses existing settings storage contracts (no new storage keys)."
  artifacts:
    - path: "src/ui/components/CollectionSectionNav.tsx"
      provides: "Sticky in-page section navigation for Collection"
    - path: "src/ui/components/OnboardingCoachmark.tsx"
      provides: "Reusable progressive onboarding tooltip"
    - path: "src/ui/tabs/CollectionTab.tsx"
      provides: "Collection section anchors + onboarding wiring"
  key_links:
    - from: "src/ui/tabs/CollectionTab.tsx"
      to: "src/ui/components/OnboardingCoachmark.tsx"
      via: "existing settings.coachmarksDismissed persistence"
      pattern: "coachmarksDismissed"

<objective>
Implement `VAULT-01` and `ONBOARD-01` by improving Collection navigation structure and progressive in-context guidance.

Purpose: Reduce navigation friction in dense Collection surfaces while preserving save/settings contracts.
Output: sticky section nav + onboarding tooltip system + persistence-safe tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/49-mobile-ux-polish/49-CONTEXT.md
@src/ui/tabs/CollectionTab.tsx
@tests/collection-loop.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sticky Collection section navigation with anchor links</name>
  <files>
src/ui/components/CollectionSectionNav.tsx
src/ui/tabs/CollectionTab.tsx
src/style.css
  </files>
  <action>
Break Collection into explicit sections with stable anchor ids (overview, milestones, achievements, events, set bonuses, crafting, etc.).

Add a sticky subnav component for in-page jumps that:
- Uses accessible link/button semantics.
- Accounts for sticky header offsets.
- Keeps existing `data-testid` anchors intact.
  </action>
  <verify>pnpm typecheck</verify>
  <done>
Collection sections are navigable via sticky in-page subnav with stable anchors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add progressive onboarding coachmarks per section</name>
  <files>
src/ui/components/OnboardingCoachmark.tsx
src/ui/tabs/CollectionTab.tsx
  </files>
  <action>
Implement lightweight onboarding tooltips that appear when players first encounter key sections.

Persistence constraints:
- Reuse existing `settings.coachmarksDismissed` storage field.
- Do not introduce new localStorage keys.

Ensure tooltips are dismissible, keyboard reachable, and non-blocking.
  </action>
  <verify>pnpm test:unit -- tests/localstorage-keys.unit.test.ts</verify>
  <done>
Onboarding guidance appears progressively and dismiss state is persisted without storage key drift.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Collection/help regressions for section nav and onboarding</name>
  <files>
tests/collection-loop.spec.ts
tests/help.spec.ts
  </files>
  <action>
Add or update e2e checks for:
- Sticky section subnav visibility and anchor jumps.
- Onboarding coachmark appearance and dismissal.
- No regressions in existing Collection/help interaction flows.
  </action>
  <verify>pnpm test:e2e -- tests/collection-loop.spec.ts tests/help.spec.ts</verify>
  <done>
Collection IA and onboarding improvements are covered by e2e checks.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/localstorage-keys.unit.test.ts`
- `pnpm test:e2e -- tests/collection-loop.spec.ts tests/help.spec.ts`
</verification>

<success_criteria>
- `VAULT-01`: Collection is segmented with sticky in-page navigation.
- `ONBOARD-01`: progressive onboarding guidance is visible and persistently dismissible.
</success_criteria>

<output>
After completion, create `.planning/phases/49-mobile-ux-polish/49-09-SUMMARY.md`
</output>
