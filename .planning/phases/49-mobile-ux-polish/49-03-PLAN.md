---
phase: 49-mobile-ux-polish
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/tabs/SaveTab.tsx
  - src/style.css
  - tests/touch-targets.spec.ts
  - tests/settings-clear-save.spec.ts
  - tests/localstorage-keys.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "Settings fieldsets and legends feel cohesive with dark theme polish while staying readable in light theme."
    - "Interactive controls meet the 44px tap-target minimum across settings and shared control patterns."
    - "Light theme contrast remains accessible for settings labels, inputs, and control states."
    - "Persistence key contracts remain unchanged."
  artifacts:
    - path: "src/ui/tabs/SaveTab.tsx"
      provides: "Refined settings structure and semantic grouping"
    - path: "src/style.css"
      provides: "Shared touch-target and contrast-safe settings styling"
    - path: "tests/localstorage-keys.unit.test.ts"
      provides: "Guardrail against accidental storage key drift"
  key_links:
    - from: "src/ui/tabs/SaveTab.tsx"
      to: "src/style.css"
      via: "fieldset/legend/control class styling"
      pattern: "settings|controls"

<objective>
Deliver `SETTINGS-01`, `TOUCH-01`, and `LIGHT-01` through settings-surface polish plus shared control ergonomics.

Purpose: Improve mobile usability and readability without touching save schema/key behavior.
Output: refined settings UI, 44px baseline controls, contrast updates, and contract-safe test updates.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/49-mobile-ux-polish/49-CONTEXT.md
@src/ui/tabs/SaveTab.tsx
@tests/touch-targets.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle settings fieldsets and legend hierarchy</name>
  <files>
src/ui/tabs/SaveTab.tsx
src/style.css
  </files>
  <action>
Refine Settings visual structure so fieldsets/legends read as intentional sections:
- Improve spacing, hierarchy, and grouping for export/import, audio, preferences, and visibility controls.
- Keep existing settings behavior and key usage unchanged.
- Preserve all existing test ids (`settings-clear-save`, audio/settings toggles, tab visibility ids).
  </action>
  <verify>pnpm typecheck</verify>
  <done>
Settings sections are clearer and visually aligned with dark UI polish.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enforce touch target and light-theme contrast contracts</name>
  <files>
src/style.css
tests/touch-targets.spec.ts
tests/settings-clear-save.spec.ts
  </files>
  <action>
Apply or tighten shared control styles so tappable elements are at least 44px high/wide where appropriate.

Update light-theme tokens/states in settings controls to keep readable contrast for labels, hints, and control boundaries.

Do not reduce existing focus-visible treatment while adjusting control dimensions.
  </action>
  <verify>pnpm test:e2e -- tests/touch-targets.spec.ts tests/settings-clear-save.spec.ts</verify>
  <done>
Touch-target and contrast expectations are explicitly tested and pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-run persistence key guardrails</name>
  <files>
tests/localstorage-keys.unit.test.ts
  </files>
  <action>
Adjust tests only if selectors/paths changed, but keep the asserted key strings unchanged.

The purpose is to prove this UI polish did not introduce persistence contract drift.
  </action>
  <verify>pnpm test:unit -- tests/localstorage-keys.unit.test.ts</verify>
  <done>
LocalStorage key contracts remain stable after settings UI changes.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:e2e -- tests/touch-targets.spec.ts tests/settings-clear-save.spec.ts`
- `pnpm test:unit -- tests/localstorage-keys.unit.test.ts`
</verification>

<success_criteria>
- `SETTINGS-01`: fieldsets/legends are polished and coherent.
- `TOUCH-01`: control targets satisfy 44px minimum in tested surfaces.
- `LIGHT-01`: light theme contrast/readability improves without selector/persistence regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/49-mobile-ux-polish/49-03-SUMMARY.md`
</output>
