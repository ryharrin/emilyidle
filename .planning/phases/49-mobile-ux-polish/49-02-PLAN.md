---
phase: 49-mobile-ux-polish
plan: 2
type: execute
wave: 2
depends_on:
  - 49-01
files_modified:
  - src/App.tsx
  - src/ui/navigation/tabReadiness.ts
  - src/ui/navigation/TabSwitchSkeleton.tsx
  - src/ui/navigation/PageTabRail.tsx
  - src/ui/navigation/pageTabRail.css
  - tests/mobile-navigation.spec.ts
  - tests/tabs.spec.ts
  - tests/selectors-contract.spec.ts
autonomous: true

must_haves:
  truths:
    - "Tabs display ready-indicator badges when key actions are available (sessions, upgrades, prestige, etc.)."
    - "Keyboard shortcuts 1-8 switch tabs without hijacking typing in inputs/search/modal controls."
    - "Tab switching shows a lightweight skeleton/loading affordance and retains visible keyboard focus states."
  artifacts:
    - path: "src/ui/navigation/tabReadiness.ts"
      provides: "Selector-backed tab readiness map"
    - path: "src/ui/navigation/TabSwitchSkeleton.tsx"
      provides: "Reusable tab switch skeleton affordance"
    - path: "tests/tabs.spec.ts"
      provides: "Shortcut and badge regression checks"
  key_links:
    - from: "src/App.tsx"
      to: "src/ui/navigation/tabReadiness.ts"
      via: "state-derived readiness passed to rail"
      pattern: "tabReadiness"

<objective>
Implement `TAB-01`, `KEY-01`, `SKELETON-01`, and `FOCUS-01` as additive UX behavior on top of the grouped rail foundation.

Purpose: Make tab transitions faster to parse for both keyboard and touch users while preserving existing contracts.
Output: readiness mapper + numeric shortcut routing + switch skeleton + coverage updates.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/49-mobile-ux-polish/49-01-PLAN.md
@.planning/phases/49-mobile-ux-polish/49-CONTEXT.md

@src/App.tsx
@src/ui/navigation/PageTabRail.tsx
@tests/tabs.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add selector-backed readiness badges to tabs</name>
  <files>
src/ui/navigation/tabReadiness.ts
src/ui/navigation/PageTabRail.tsx
src/App.tsx
  </files>
  <action>
Create a readiness helper that derives per-tab status from existing state/selectors (no duplicated formulas in UI).

Render badge indicators in the tab rail with additive anchors such as `data-testid="tab-ready-{tabId}"`.
Requirements:
- Keep badges purely additive; never remove existing tab ids/test ids.
- Provide accessible text labels for badge state.
- Keep non-ready tabs visually calm (no noisy animation loops).
  </action>
  <verify>pnpm typecheck</verify>
  <done>
Ready-indicator badges are rendered from selector-backed state and are test-addressable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add numeric shortcuts and tab switch skeletons</name>
  <files>
src/App.tsx
src/ui/navigation/TabSwitchSkeleton.tsx
src/ui/navigation/pageTabRail.css
  </files>
  <action>
Add keyboard shortcuts `1-8` mapped to visible tab order.

Guardrails:
- Ignore shortcuts when focus is inside input/select/textarea/contentEditable.
- Ignore shortcuts while modal dialogs are open.
- Preserve existing Arrow/Home/End/Enter/Space tab behavior.

Add a lightweight tab-switch skeleton component that appears during tab transitions and respects reduced-motion preferences.
  </action>
  <verify>pnpm test:unit -- tests/mobile-responsive.unit.test.tsx</verify>
  <done>
Numeric tab shortcuts work safely and tab switches have a stable skeleton/loading affordance.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update nav contract tests for badges, focus, and shortcuts</name>
  <files>
tests/mobile-navigation.spec.ts
tests/tabs.spec.ts
tests/selectors-contract.spec.ts
  </files>
  <action>
Extend Playwright assertions to cover:
- Numeric tab switching across visible tabs.
- Badge anchor presence for readiness states.
- Visible focus ring behavior on tab controls and help button.

Keep tests resilient by targeting roles/data-testids rather than exact prose.
  </action>
  <verify>pnpm test:e2e -- tests/tabs.spec.ts tests/mobile-navigation.spec.ts tests/selectors-contract.spec.ts</verify>
  <done>
Shortcut, readiness, and focus behavior is covered by e2e contracts.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/mobile-responsive.unit.test.tsx`
- `pnpm test:e2e -- tests/tabs.spec.ts tests/mobile-navigation.spec.ts tests/selectors-contract.spec.ts`
</verification>

<success_criteria>
- `TAB-01`: readiness badges appear on applicable tabs.
- `KEY-01`: shortcuts 1-8 switch tabs without interfering with text input.
- `SKELETON-01` + `FOCUS-01`: tab transitions have skeleton affordance and visible keyboard focus.
</success_criteria>

<output>
After completion, create `.planning/phases/49-mobile-ux-polish/49-02-SUMMARY.md`
</output>
