---
phase: 49-mobile-ux-polish
plan: 6
type: execute
wave: 2
depends_on:
  - 49-05
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/ui/tabs/CatalogTab.tsx
  - src/ui/components/catalog/CatalogCardDetailsSheet.tsx
  - src/ui/hooks/useCatalogVirtualizer.ts
  - src/style.css
  - tests/catalog.unit.test.tsx
  - tests/catalog-actionable-visual.spec.ts
  - tests/touch-targets.spec.ts
autonomous: true

must_haves:
  truths:
    - "Desktop users can preview watch stats on hover/focus before opening full details."
    - "Affordable watches are visually highlighted with a subtle glow/border cue."
    - "Long catalog lists virtualize rendering while preserving lane/selector contracts."
    - "Mobile users can read card details in a bottom-sheet flow."
    - "Dismantle action stays hidden until workshop unlock conditions are met."
  artifacts:
    - path: "src/ui/hooks/useCatalogVirtualizer.ts"
      provides: "Virtualization hook wrapper around TanStack Virtual"
    - path: "src/ui/components/catalog/CatalogCardDetailsSheet.tsx"
      provides: "Mobile bottom-sheet detail surface for catalog cards"
    - path: "src/ui/tabs/CatalogTab.tsx"
      provides: "Hover preview, glow cues, virtualized rendering, and dismantle gating"
  key_links:
    - from: "src/ui/tabs/CatalogTab.tsx"
      to: "src/ui/hooks/useCatalogVirtualizer.ts"
      via: "virtual row rendering"
      pattern: "useCatalogVirtualizer"

<objective>
Deliver `HOVER-01`, `GLOW-01`, `VIRTUAL-01`, `BOTTOM-01`, and `DISMANTLE-01` on top of the compact filter/sort foundation.

Purpose: Improve catalog clarity and performance on both desktop and mobile without changing data contracts.
Output: catalog affordance upgrades + virtualization integration + bottom-sheet details + coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/49-mobile-ux-polish/49-RESEARCH.md
@.planning/phases/49-mobile-ux-polish/49-05-PLAN.md

@src/ui/tabs/CatalogTab.tsx
@tests/catalog-actionable-visual.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add catalog actionability affordances and dismantle gating</name>
  <files>
src/ui/tabs/CatalogTab.tsx
src/style.css
tests/catalog.unit.test.tsx
  </files>
  <action>
Add desktop and purchase-readiness affordances:
- Hover/focus stat preview for catalog entries.
- Subtle glow/border cue when an entry is currently affordable.
- Keep these states additive to existing card classes/data-testids.

Apply `DISMANTLE-01` by hiding dismantle controls entirely until workshop unlock criteria are met (not disabled placeholders shown early).
  </action>
  <verify>pnpm test:unit -- tests/catalog.unit.test.tsx</verify>
  <done>
Hover/glow affordances and workshop-gated dismantle visibility are implemented and unit-covered.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate virtualization and mobile bottom-sheet details</name>
  <files>
package.json
pnpm-lock.yaml
src/ui/hooks/useCatalogVirtualizer.ts
src/ui/components/catalog/CatalogCardDetailsSheet.tsx
src/ui/tabs/CatalogTab.tsx
  </files>
  <action>
Add virtualization using `@tanstack/react-virtual`.

Implementation requirements:
- Install dependency via `pnpm add @tanstack/react-virtual`.
- Virtualize only when result counts exceed a threshold so small lists remain simple.
- Preserve lane/test-id semantics (`catalog-grid`, card-level anchors) even when rows are virtualized.

Add mobile bottom-sheet details that open from catalog cards on small viewports, reusing existing detail content and maintaining keyboard close/focus return behavior.
  </action>
  <verify>pnpm typecheck</verify>
  <done>
Catalog list virtualization and mobile detail sheets are integrated with stable contracts.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refresh catalog visual/mobile regressions</name>
  <files>
tests/catalog-actionable-visual.spec.ts
tests/touch-targets.spec.ts
  </files>
  <action>
Update e2e coverage for:
- Affordable glow visibility.
- Hover/focus preview behavior.
- Bottom-sheet open/close and touch-target sizing on mobile.
- No regressions in catalog actionability selectors.
  </action>
  <verify>pnpm test:e2e -- tests/catalog-actionable-visual.spec.ts tests/touch-targets.spec.ts</verify>
  <done>
Catalog UX/performance enhancements are validated in mobile and visual regression flows.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/catalog.unit.test.tsx`
- `pnpm test:e2e -- tests/catalog-actionable-visual.spec.ts tests/touch-targets.spec.ts`
</verification>

<success_criteria>
- `HOVER-01` + `GLOW-01`: actionable visual affordances are present.
- `VIRTUAL-01`: long lists are virtualized with preserved selectors.
- `BOTTOM-01`: mobile detail access uses a bottom sheet.
- `DISMANTLE-01`: dismantle remains hidden until workshop unlock.
</success_criteria>

<output>
After completion, create `.planning/phases/49-mobile-ux-polish/49-06-SUMMARY.md`
</output>
