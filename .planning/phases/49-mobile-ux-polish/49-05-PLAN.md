---
phase: 49-mobile-ux-polish
plan: 5
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/tabs/CatalogTab.tsx
  - src/style.css
  - tests/catalog.unit.test.tsx
  - tests/catalog-buy-buttons.spec.ts
  - tests/collection-loop.spec.ts
autonomous: true

must_haves:
  truths:
    - "Catalog filters can be collapsed behind one compact control with an active-filter count on mobile."
    - "Catalog default sort behavior is price ascending to support early discovery."
    - "Filter and sorting interactions remain keyboard-accessible and test-stable."
  artifacts:
    - path: "src/ui/tabs/CatalogTab.tsx"
      provides: "Collapsible filter UI + default sort behavior"
    - path: "tests/catalog.unit.test.tsx"
      provides: "Unit assertions for filter state/count and sorting"
    - path: "tests/catalog-buy-buttons.spec.ts"
      provides: "E2E coverage for filter control behavior"
  key_links:
    - from: "src/ui/tabs/CatalogTab.tsx"
      to: "tests/catalog-buy-buttons.spec.ts"
      via: "catalog filter control and result rendering"
      pattern: "catalog-filters|catalog-results-count"

<objective>
Implement `FILTER-01` and `SORT-01` by making filters compact and making default catalog discovery flow start with price-ascending order.

Purpose: Reduce mobile UI noise while improving initial browsing behavior.
Output: collapsible filter control + default sort update + regression coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/49-mobile-ux-polish/49-CONTEXT.md
@src/ui/tabs/CatalogTab.tsx
@tests/catalog-buy-buttons.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collapse catalog filters behind a compact trigger with active count</name>
  <files>
src/ui/tabs/CatalogTab.tsx
src/style.css
  </files>
  <action>
Replace always-expanded filter controls with a compact disclosure pattern:
- Add a single trigger button (for mobile-first layouts) that opens/closes filter controls.
- Show active-filter count in the trigger label.
- Keep filter controls fully keyboard accessible and preserve existing control ids/test ids.

Do not remove existing catalog filter inputs/selects; wrap them in the new disclosure behavior.
  </action>
  <verify>pnpm typecheck</verify>
  <done>
Catalog filters are compact by default and expose an active-filter count.
  </done>
</task>

<task type="auto">
  <name>Task 2: Change default sort behavior to price ascending</name>
  <files>
src/ui/tabs/CatalogTab.tsx
tests/catalog.unit.test.tsx
  </files>
  <action>
Update sorting logic so default catalog ordering is ascending by price.

Requirements:
- Keep explicit sort options available.
- Ensure default behavior applies consistently after search/filter changes.
- Keep deterministic ordering for same-price ties (secondary stable key).
  </action>
  <verify>pnpm test:unit -- tests/catalog.unit.test.tsx</verify>
  <done>
Default catalog rows load in ascending price order and remain deterministic.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refresh filter/sort e2e contracts</name>
  <files>
tests/catalog-buy-buttons.spec.ts
tests/collection-loop.spec.ts
  </files>
  <action>
Update Playwright tests to validate:
- Compact filter disclosure behavior.
- Active-filter count updates as controls change.
- Default result ordering is stable and price-ascending.
  </action>
  <verify>pnpm test:e2e -- tests/catalog-buy-buttons.spec.ts tests/collection-loop.spec.ts</verify>
  <done>
Filter and sorting UX updates are protected by e2e coverage.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/catalog.unit.test.tsx`
- `pnpm test:e2e -- tests/catalog-buy-buttons.spec.ts tests/collection-loop.spec.ts`
</verification>

<success_criteria>
- `FILTER-01`: filters collapse behind one button with active count.
- `SORT-01`: default catalog order is price ascending and test-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/49-mobile-ux-polish/49-05-SUMMARY.md`
</output>
