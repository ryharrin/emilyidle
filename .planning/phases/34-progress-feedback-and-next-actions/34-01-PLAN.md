---
phase: 34-progress-feedback-and-next-actions
plan: 01
type: execute
wave: 1
depends_on:
  - 33-03
files_modified:
  - src/game/selectors/careerProgress.ts
  - src/game/selectors/index.ts
  - src/ui/components/CareerProgressCard.tsx
  - src/ui/tabs/CareerTab.tsx
  - tests/career-progress.unit.test.ts
autonomous: true
must_haves:
  truths:
    - "Career shows a progress bar and a next-unlock callout based on pure selectors."
    - "If an unlocked permanent choice is available, the next-unlock callout points to it; otherwise it points to the next stage threshold."
  artifacts:
    - path: src/ui/components/CareerProgressCard.tsx
      provides: "Progress bar + next unlock UI with stable test IDs"
      contains: "career-progress-card"
    - path: src/game/selectors/careerProgress.ts
      provides: "Pure selectors for next unlock + progress computation"
      contains: "getCareerNextUnlock"
---

<objective>
Add explicit career progress feedback on the Career tab (progress bar + next-unlock callout) using pure selectors and stable UI anchors.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-progress-feedback-and-next-actions/34-CONTEXT.md

@src/game/selectors/index.ts
@src/game/selectors/careerStages.ts
@src/game/actions/index.ts
@src/ui/tabs/CareerTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add career progress selectors (next unlock + stage progress)</name>
  <files>
src/game/selectors/careerProgress.ts
src/game/selectors/index.ts
  </files>
  <action>
- Create `src/game/selectors/careerProgress.ts` with pure selectors:
  - `getCareerNextUnlock(state)` returning one of:
    - `{ kind: "choice", stageId, label }` when a permanent choice is available now
    - `{ kind: "stage", stageId, label, unlockLevel }` for the next locked stage
    - `null` when at max stage and no pending choices
  - `getCareerNextStageProgress(state)` returning `{ currentLevel, nextUnlockLevel, progress01, levelsRemaining }`.
- Export these via `src/game/selectors/index.ts`.
- Keep computations deterministic and independent from wall clock.
  </action>
  <verify>pnpm run typecheck</verify>
  <done>
  - Progress and next unlock data is available via selectors and stable across renders.
  </done>
</task>

<task type="auto">
  <name>Task 2: Render progress card on Career tab with stable test IDs</name>
  <files>
src/ui/components/CareerProgressCard.tsx
src/ui/tabs/CareerTab.tsx
tests/career-progress.unit.test.ts
  </files>
  <action>
- Create `src/ui/components/CareerProgressCard.tsx`:
  - Uses selectors from `src/game/state` to render:
    - progress bar: `data-testid="career-progress-bar"`
    - next unlock callout: `data-testid="career-next-unlock"`
    - levels remaining: `data-testid="career-next-unlock-levels"`
  - Displays an explicit message for `{kind:"choice"}` vs `{kind:"stage"}`.
- Mount the card near the top of `src/ui/tabs/CareerTab.tsx` (above stages is preferred).
- Add `tests/career-progress.unit.test.ts` covering:
  - next unlock prefers an available choice
  - otherwise points to the next stage threshold
  - progress01 is within [0, 1]
  </action>
  <verify>pnpm run test:unit</verify>
  <done>
  - Career tab shows a clear progress bar and next-unlock callout.
  - Unit tests protect selector behavior.
  </done>
</task>

</tasks>

<verification>
- pnpm run typecheck
- pnpm run test:unit
</verification>

<output>
After completion, create `.planning/phases/34-progress-feedback-and-next-actions/34-01-SUMMARY.md`
</output>
