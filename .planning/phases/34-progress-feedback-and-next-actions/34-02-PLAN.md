---
phase: 34-progress-feedback-and-next-actions
plan: 02
type: execute
wave: 2
depends_on:
  - 34-01
files_modified:
  - src/game/selectors/careerNextAction.ts
  - src/game/selectors/index.ts
  - src/ui/components/CareerNextActionCard.tsx
  - src/ui/tabs/CareerTab.tsx
  - tests/career-next-action.unit.test.ts
autonomous: true
---

<objective>
Add a deterministic "next action" cue on the Career tab to nudge the player toward the fastest available progress.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add next-action selector logic (pure)</name>
  <files>
src/game/selectors/careerNextAction.ts
src/game/selectors/index.ts
tests/career-next-action.unit.test.ts
  </files>
  <action>
- Create `getCareerNextActionCue(state)` returning `{ id, label, detail }`.
- Priority order:
  1) available permanent stage choice (track/modality/style/focus)
  2) if sessions are supported and currently available: perform a session
  3) otherwise: passive XP guidance
- Add unit tests asserting the priority order.
  </action>
  <verify>pnpm run test:unit</verify>
</task>

<task type="auto">
  <name>Task 2: Render next-action card with stable selectors</name>
  <files>
src/ui/components/CareerNextActionCard.tsx
src/ui/tabs/CareerTab.tsx
  </files>
  <action>
- Create `CareerNextActionCard` with:
  - `data-testid="career-next-action"`
  - a concise single recommendation
- Mount near the progress card.
  </action>
  <verify>pnpm run test:e2e</verify>
</task>

</tasks>

<output>
After completion, create `.planning/phases/34-progress-feedback-and-next-actions/34-02-SUMMARY.md`
</output>
