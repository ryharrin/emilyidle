---
phase: 25-watch-models-and-duplicates
plan: 07
type: execute
wave: 1
depends_on:
  - 25-02
  - 25-06
files_modified:
  - src/App.tsx
autonomous: true
must_haves:
  truths:
    - "Auto-buy no longer purchases tier items via buyItem(); it purchases watch models via buyWatchModel()"
    - "Auto-buy purchases update state.watchModels so duplicate scaling and ownership stay consistent"
  artifacts:
    - path: src/App.tsx
      provides: "Auto-buy loop uses model purchasing"
      contains: "autoBuy"
  key_links:
    - from: src/App.tsx
      to: src/game/actions/index.ts
      via: "Auto-buy calls buyWatchModel"
      pattern: "buyWatchModel\\("
    - from: src/App.tsx
      to: src/game/actions/index.ts
      via: "Auto-buy no longer calls buyItem for watch purchases"
      pattern: "buyItem\\("
---
<objective>
Fix Phase 25 purchase-path gaps by updating the auto-buy loop to purchase watch models (not tier items), ensuring all watch purchasing paths are model-based.
Purpose: The verifier flagged auto-buy as a tier-only purchase path that bypasses model ownership and duplicate scaling. This plan makes purchasing consistent across the app.
Output: Auto-buy uses buyWatchModel deterministically (one model per tier) and updates watchModels as purchases happen.
</objective>
<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-watch-models-and-duplicates/25-watch-models-and-duplicates-VERIFICATION.md
@src/App.tsx
@src/game/state.ts
@src/game/actions/index.ts
@src/game/data/watchModels.ts
</context>
<tasks>
<task type="auto">
  <name>Task 1: Replace tier-only auto-buy purchases with model purchases</name>
  <files>
src/App.tsx
  </files>
  <action>
- In the auto-buy effect (currently iterating `watchItems` and calling `buyItem(nextState, item.id, purchaseQty)`), replace the purchase logic so it buys watch models:
  - Build a deterministic tierId -> modelId mapping once (e.g. in a `useMemo`), derived from `getWatchModels()`:
    - For each tierId, choose a single default model (first encountered is fine).
  - Keep iterating tiers (`watchItems`) for unlock ordering, but when tier is unlocked:
    - Look up the default model id for that tier; if none, continue.
    - Attempt to purchase up to `min(10, ...)` copies via repeated `buyWatchModel(nextState, modelId)` calls:
      - Loop up to 10 times; stop early if `buyWatchModel` returns the same state (gate failed).
- Remove (or stop using) `getMaxAffordableItemCount(nextState, item.id)` for watch auto-buy, since that is tier-based; model affordability can be handled by “attempt until gate fails”.
- Ensure the behavior remains non-destructive:
  - If model purchase is blocked by enjoyment/cash, auto-buy should simply skip (no errors).
- Confirm there is no remaining watch auto-buy call site that purchases via `buyItem()` for watch tiers.
  </action>
  <verify>
pnpm run typecheck
  </verify>
  <done>
- Auto-buy purchases increment `state.watchModels[modelId]` (and `state.items[tierId]` stays in sync via buyWatchModel).
- Auto-buy no longer purchases watch tiers directly via buyItem().
  </done>
</task>
</tasks>
<verification>
- `pnpm run typecheck`
- `pnpm run test:unit`
- `pnpm run test:e2e` (recommended after changing App purchase behavior)
</verification>
<success_criteria>
- Verification truth #5 is unblocked: all watch purchasing paths are model-based (auto-buy included).
- WATCH-01 and WATCH-02 are no longer blocked by a tier-only purchase path.
</success_criteria>
<output>
After completion, create `.planning/phases/25-watch-models-and-duplicates/25-07-SUMMARY.md`
</output>
