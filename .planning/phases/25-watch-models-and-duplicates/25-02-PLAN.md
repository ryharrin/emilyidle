---
phase: 25-watch-models-and-duplicates
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - src/game/selectors/watchModels.ts
  - src/game/selectors/enjoyment.ts
  - src/game/model/state.ts
  - src/game/actions/index.ts
  - src/game/state.ts
  - tests/enjoyment.unit.test.tsx
  - tests/purchase-gates.unit.test.tsx
autonomous: true

must_haves:
  truths:
    - "Buying a watch uses a specific model id and increments that model's owned count"
    - "Buying duplicate copies yields diminishing enjoyment + memories contributions (with 0.10x floor)"
    - "Point-of-purchase gating still blocks by enjoyment or cash (tier-derived)"
  artifacts:
    - path: src/game/actions/index.ts
      provides: "Model-level buy/dismantle actions"
      contains: "buyWatchModel"
    - path: src/game/selectors/enjoyment.ts
      provides: "Enjoyment rate derived from model ownership with duplicate diminishing returns"
      contains: "watchModels"
    - path: src/game/model/state.ts
      provides: "Collection value (memories) derived from model ownership with diminishing returns"
      contains: "getCollectionValueCents"
  key_links:
    - from: src/game/actions/index.ts
      to: src/game/model/state.ts
      via: "Keep tier totals in sync when buying/dismantling"
      pattern: "items: \{[^}]*\[tierId\]"
    - from: src/game/actions/index.ts
      to: src/game/actions/index.ts
      via: "Discover purchased catalog entry id"
      pattern: "discoverCatalogEntries\(.*\[modelId\]"
---

<objective>
Switch core economy and actions to model-level ownership while keeping existing tier totals for unlocks and set bonuses.

Purpose: Make Phase 25 real: specific models are the purchasable items, and duplicates have diminishing returns on enjoyment + memories.
Output: New model-level selectors and actions (`buyWatchModel`, `dismantleWatchModel`), updated enjoyment + collection value calculations, and updated unit tests.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-watch-models-and-duplicates/25-CONTEXT.md
@src/game/data/watchModels.ts
@src/game/data/items.ts
@src/game/model/state.ts
@src/game/selectors/index.ts
@src/game/selectors/enjoyment.ts
@src/game/actions/index.ts
@tests/enjoyment.unit.test.tsx
@tests/purchase-gates.unit.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add model-level selectors for pricing and purchase gates</name>
  <files>
src/game/selectors/watchModels.ts
src/game/state.ts
  </files>
  <action>
- Create model-level selectors built on `WATCH_MODELS`:
  - `getWatchModelOwnedCount(state, modelId)` (from `state.watchModels`).
  - `requireWatchModel(modelId)` and `getWatchModelTierId(modelId)`.
  - `getWatchModelPriceCents(state, modelId)` using the model's tier base price + tier price growth, applied per-model owned count.
  - `getWatchModelPurchaseGate(state, modelId)` mirroring `getWatchPurchaseGate` semantics (cash vs enjoyment), but keyed on model id.
  - `getNextDuplicateRewardMultiplier(state, modelId)` for UI display.
- Re-export these selectors from `src/game/state.ts`.
- Do not remove existing tier-based selectors yet; keep API stable for non-watch systems.
  </action>
  <verify>
pnpm run typecheck
  </verify>
  <done>
- There is a model-level purchase gate that returns `{ok, cashPriceCents, enjoymentRequiredCents}` and blocks by `cash|enjoyment`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement model-level buy/dismantle actions and keep tier totals in sync</name>
  <files>
src/game/actions/index.ts
src/game/state.ts
  </files>
  <action>
- Add actions:
  - `buyWatchModel(state, modelId)`
  - `dismantleWatchModel(state, modelId, quantity=1)`
- Buying a model:
  - Enforce unlock via the model's tier (reuse existing tier unlock rules).
  - Enforce gate using `getWatchModelPurchaseGate`.
  - Deduct cash by gate price.
  - Increment `state.watchModels[modelId]`.
  - ALSO increment `state.items[tierId]` to keep tier totals consistent for existing UI (set bonuses, milestones, etc.).
  - Discover the purchased catalog entry id directly: `discoverCatalogEntries(nextState, [modelId])`.
- Dismantling a model:
  - Decrement `state.watchModels[modelId]` and `state.items[tierId]`.
  - Award crafting parts based on tier (reuse existing parts-per-watch mapping).
- Update any prestige reset paths to clear `watchModels` alongside existing `items` resets (Vault/Workshop/Maison/Nostalgia paths as appropriate).
  </action>
  <verify>
pnpm run typecheck
  </verify>
  <done>
- Buying a model updates both model owned count and tier total.
- Dismantling a model decrements both and awards parts.
- Purchased model immediately counts as a discovered catalog entry.
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply duplicate diminishing returns to enjoyment and memories</name>
  <files>
src/game/selectors/enjoyment.ts
src/game/model/state.ts
tests/enjoyment.unit.test.tsx
tests/purchase-gates.unit.test.tsx
  </files>
  <action>
- Update enjoyment rate computation to derive from `state.watchModels`:
  - For each owned model, look up tier rates (enjoymentCentsPerSec) and multiply by `getDuplicateRewardSum(ownedCount)`.
  - Preserve existing prestige legacy multiplier behavior.
- Update collection value (memories) computation to derive from `state.watchModels` similarly:
  - Each model contributes tier `collectionValueCents * getDuplicateRewardSum(ownedCount)`.
  - This makes duplicates contribute less to displayed memories.
- Update unit tests:
  - Add/adjust a test verifying that the delta in enjoyment rate for the second purchase of the same model is lower than for the first.
  - Update purchase gate test to use `getWatchModelPurchaseGate` with a stable model id (pick a known catalog entry id that is classic-tier, e.g. a Rolex GMT entry).
  </action>
  <verify>
pnpm run test:unit
  </verify>
  <done>
- Duplicate purchases show diminishing enjoyment + memories contributions with a 0.10 floor.
- Purchase gate unit tests pass using model-level gating.
  </done>
</task>

</tasks>

<verification>
- `pnpm run test:unit`
- `pnpm run typecheck`
</verification>

<success_criteria>
- There is a first-class model purchase API (`buyWatchModel`) and model purchase gating.
- Enjoyment rate and memories (collection value) reflect diminishing returns for duplicates.
- Tier totals remain consistent for existing tier-based systems.
</success_criteria>

<output>
After completion, create `.planning/phases/25-watch-models-and-duplicates/25-02-SUMMARY.md`
</output>
