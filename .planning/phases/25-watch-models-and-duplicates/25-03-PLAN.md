---
phase: 25-watch-models-and-duplicates
plan: 03
type: execute
wave: 3
depends_on:
  - 25-02
files_modified:
  - src/ui/tabs/CollectionTab.tsx
  - src/ui/tabs/CatalogTab.tsx
  - src/style.css
autonomous: true

must_haves:
  truths:
    - "Vault shows purchasable watches as specific models grouped by brand"
    - "Each model row shows owned count and the next duplicate reward multiplier"
    - "Buy CTA changes from 'Buy' to 'Buy another' after the first purchase and provides visible feedback"
  artifacts:
    - path: src/ui/tabs/CollectionTab.tsx
      provides: "Brand-grouped model purchase list and per-model CTAs"
      contains: "getWatchModels"
    - path: src/style.css
      provides: "Purchase highlight animation (disabled under prefers-reduced-motion)"
      contains: "purchase-flash"
  key_links:
    - from: src/ui/tabs/CollectionTab.tsx
      to: src/game/state.ts
      via: "Model purchase selectors/actions"
      pattern: "buyWatchModel|getWatchModelPurchaseGate|getNextDuplicateRewardMultiplier"
---

<objective>
Update the Vault (Collection tab) purchase UI to list and buy watch models (not generic tiers), grouped by brand, with clear duplicate multiplier messaging and purchase feedback.

Purpose: Deliver the player-facing part of Phase 25 before Phase 26 catalog-first UX: users buy specific models and immediately see ownership and duplicate scaling.
Output: Updated Vault watch list UI + lightweight animation feedback; copy updates where Vault/Catalog references still mention tiers.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-watch-models-and-duplicates/25-CONTEXT.md
@src/ui/tabs/CollectionTab.tsx
@src/ui/tabs/CatalogTab.tsx
@src/style.css
@src/game/state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Vault watch list with brand-grouped watch models</name>
  <files>
src/ui/tabs/CollectionTab.tsx
  </files>
  <action>
- Replace the watch purchase list that currently iterates `getWatchItems()` with a list derived from `getWatchModels()`.
- Group by brand (avoid one flat list): render a section per brand with a header.
- For each model row/card, show:
  - display name (includes reference #)
  - owned count
  - next duplicate multiplier label (e.g. "Duplicate: 0.70x rewards" and/or "Owned 2 | Next: 0.55x")
  - buy CTA with dynamic copy:
    - owned=0 => "Buy"
    - owned>0 => "Buy another"
- Use model purchase gate + action:
  - disable buy if `!gate.ok` or locked
  - `onClick` => `onPurchase(buyWatchModel(state, modelId))`
- Keep dismantle and interact buttons, but rewire them to model-level actions (dismantle uses `dismantleWatchModel`).
- Preserve stable selectors where possible; introduce per-model `data-testid` values:
  - `data-testid={\`vault-buy-${modelId}\`}`
  - `data-testid={\`purchase-gate-${modelId}\`}`
- Avoid adding complex search/virtualization; a brand grouping is enough for this phase.
  </action>
  <verify>
pnpm run typecheck
  </verify>
  <done>
- Vault purchase list shows brand sections and model rows with owned count and next multiplier.
- Buy/Dismantle operate on model ids.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add purchase highlight/animation feedback on the purchased model row</name>
  <files>
src/ui/tabs/CollectionTab.tsx
src/style.css
  </files>
  <action>
- Add a lightweight highlight animation when a model is purchased:
  - Store the last purchased model id in component state.
  - Apply a CSS class (e.g. `.purchase-flash`) to that row.
  - Clear after ~600-900ms.
- Ensure `prefers-reduced-motion: reduce` disables the animation (CSS should no-op there).
- Keep the styling consistent with existing UI polish primitives.
  </action>
  <verify>
pnpm run test:unit
  </verify>
  <done>
- Buying a model triggers a visible row highlight and it clears automatically.
- Reduced motion disables the animation.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Vault/Catalog copy that still implies tier-only purchasing</name>
  <files>
src/ui/tabs/CatalogTab.tsx
  </files>
  <action>
- Update any short copy strings that explicitly mention buying "tiers" so they read as buying "models" or "watches".
- Keep Phase 26 scope deferred: do not redesign Catalog layouts beyond wording.
  </action>
  <verify>
pnpm run typecheck
  </verify>
  <done>
- No user-facing copy contradicts model-based purchasing.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit`
</verification>

<success_criteria>
- Users can buy specific models from the Vault, grouped by brand.
- Duplicate multiplier is explicitly shown per model at point of purchase.
- Purchase feedback is visible and not obnoxious.
</success_criteria>

<output>
After completion, create `.planning/phases/25-watch-models-and-duplicates/25-03-SUMMARY.md`
</output>
