---
phase: 25-watch-models-and-duplicates
plan: 04
type: execute
wave: 4
depends_on:
  - 25-03
files_modified:
  - tests/nostalgia-unlocks.spec.ts
  - tests/purchase-gates.unit.test.tsx
  - tests/enjoyment.unit.test.tsx
autonomous: true

must_haves:
  truths:
    - "Automated tests validate model-based purchasing and gating"
    - "E2E coverage no longer depends on tier-only buy selectors"
  artifacts:
    - path: tests/nostalgia-unlocks.spec.ts
      provides: "E2E assertions for buy gating using model-based selectors"
      contains: "vault-buy"
    - path: tests/purchase-gates.unit.test.tsx
      provides: "Unit tests for model-level purchase gates"
      contains: "getWatchModelPurchaseGate"
  key_links:
    - from: tests/nostalgia-unlocks.spec.ts
      to: src/ui/tabs/CollectionTab.tsx
      via: "data-testid selectors"
      pattern: "vault-buy-"
---

<objective>
Repair and extend automated coverage to match the new model-based purchase surface, including at least one flow that exercises duplicate diminishing returns.

Purpose: Keep regression protection as Vault purchasing transitions from 4 tiers to many model rows.
Output: Updated unit and Playwright tests that use stable model ids and verify duplicate diminishing returns behaviors.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@tests/nostalgia-unlocks.spec.ts
@tests/purchase-gates.unit.test.tsx
@tests/enjoyment.unit.test.tsx
@src/game/catalog.ts
@src/game/state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update unit tests to use model-level purchase gating</name>
  <files>
tests/purchase-gates.unit.test.tsx
tests/enjoyment.unit.test.tsx
  </files>
  <action>
- Update unit tests that referenced tier IDs to use model-level selectors/actions.
- Pick a stable model id from `src/game/catalog.ts` that is known to exist (e.g. a Rolex GMT entry) and use it in tests.
- Ensure the tests assert:
  - gating blocks by enjoyment/cash as before
  - the second purchase produces a smaller enjoyment delta than the first (duplicate diminishing returns)
  </action>
  <verify>
pnpm run test:unit
  </verify>
  <done>
- Unit tests pass using model ids.
- Duplicate diminishing returns are exercised in at least one unit test.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Playwright nostalgia unlock flow for model-based Vault buy selectors</name>
  <files>
tests/nostalgia-unlocks.spec.ts
  </files>
  <action>
- Update any `vault-buy-classic` references to use a model id selector that exists and is classic-tier.
- Ensure the test can find the model row and assert the buy button is enabled once the classic tier is unlocked.
- If the Vault brand sections become long, rely on scrolling to bring the target button into view rather than fragile DOM structure.
  </action>
  <verify>
pnpm run test:e2e
  </verify>
  <done>
- Playwright nostalgia unlock flow passes and uses model-based buy selectors.
  </done>
</task>

</tasks>

<verification>
- `pnpm run test:unit`
- `pnpm run test:e2e`
</verification>

<success_criteria>
- Unit and e2e tests are green after the model-based purchase UI change.
- Duplicate diminishing returns behavior is protected by at least one automated test.
</success_criteria>

<output>
After completion, create `.planning/phases/25-watch-models-and-duplicates/25-04-SUMMARY.md`
</output>
