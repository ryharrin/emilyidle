---
phase: 25-watch-models-and-duplicates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/data/watchModels.ts
  - src/game/model/types.ts
  - src/game/model/state.ts
  - src/game/selectors/duplicates.ts
  - src/game/state.ts
  - tests/duplicate-rewards.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "The game has a stable, deterministic roster of purchasable watch models derived from catalog entries"
    - "Game state tracks owned counts per watch model id (not just per tier)"
    - "Duplicate reward multiplier is visible as a numeric factor and never drops below 0.10x"
  artifacts:
    - path: src/game/data/watchModels.ts
      provides: "Watch model roster derived from catalog entries"
      contains: "export const WATCH_MODELS"
    - path: src/game/model/types.ts
      provides: "GameState includes watchModels ownership map"
      contains: "watchModels"
    - path: src/game/selectors/duplicates.ts
      provides: "Duplicate diminishing-returns helpers (floor 0.10)"
      contains: "DUPLICATE_REWARD_FLOOR"
    - path: tests/duplicate-rewards.unit.test.ts
      provides: "Unit coverage for duplicate multiplier shape"
      contains: "duplicate rewards"
  key_links:
    - from: src/game/data/watchModels.ts
      to: src/game/catalog.ts
      via: "CATALOG_ENTRIES mapping"
      pattern: "CATALOG_ENTRIES"
    - from: src/game/model/state.ts
      to: src/game/model/types.ts
      via: "watchModels initialization"
      pattern: "watchModels"
---

<objective>
Introduce a model-level watch roster and state tracking, plus a transparent duplicate diminishing-returns curve (floor 10%), without changing gameplay wiring yet.

Purpose: Establish the core data + state primitives needed for Phase 25 so later plans can switch purchasing and economy calculations to model-level ownership.
Output: Watch model definitions derived from catalog entries, a `watchModels` ownership map in state, and tested duplicate reward-multiplier helpers.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-watch-models-and-duplicates/25-CONTEXT.md
@src/game/catalog.ts
@src/game/data/items.ts
@src/game/model/types.ts
@src/game/model/state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create purchasable watch model roster from catalog</name>
  <files>
src/game/data/watchModels.ts
  </files>
  <action>
- Create a new module that builds a deterministic list of purchasable models from `CATALOG_ENTRIES`.
- Each model MUST map to at least one catalog entry id; start with 1:1 mapping (`catalogEntryIds: [entry.id]`).
- Determine tier by using `getCatalogEntryTags(entry)` and selecting the tier tag (starter/classic/chronograph/tourbillon). Treat that value as the model's tier.
- Include these fields on each model:
  - `id`: stable, unique id derived from the catalog entry (use `entry.id` as the model id).
  - `brand`, `model`.
  - `tierId` (one of the existing tier ids).
  - `referenceNumber`: deterministic per-brand numbering (based on order in `CATALOG_ENTRIES` filtered to that brand).
  - `displayName`: include the reference number (e.g. "Rolex GMT Master #12").
- Export `WATCH_MODELS` as a `ReadonlyArray` and a `getWatchModels()` accessor.
- Avoid adding new dependencies.
  </action>
  <verify>
pnpm run typecheck
  </verify>
  <done>
- `src/game/data/watchModels.ts` exists and exports a deterministic `WATCH_MODELS` derived from `CATALOG_ENTRIES`.
- Each model has a tierId and displayName includes a reference number.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add watch model ownership to GameState</name>
  <files>
src/game/model/types.ts
src/game/model/state.ts
src/game/state.ts
  </files>
  <action>
- Extend `GameState` to include `watchModels: Record<string, number>` (sparse map; missing key = 0).
- Extend `PersistedGameState` to optionally include `watchModels?: Record<string, number>`.
- Update initial state creation to initialize `watchModels` to `{}`.
- Update save restore (`createStateFromSave`) to accept `saved.watchModels`:
  - Only retain entries whose key corresponds to a known model id (validate by checking against `WATCH_MODELS`).
  - Sanitize values to finite integers >= 0.
- Keep existing tier-based `items` map intact for now (later plans will keep it in sync).
- Re-export new model helpers from `src/game/state.ts` so UI can import via the facade.
  </action>
  <verify>
pnpm run typecheck
  </verify>
  <done>
- `GameState` persists a `watchModels` map and default state includes it.
- Loading a save with unknown model ids drops them safely.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement and test duplicate reward multiplier helpers</name>
  <files>
src/game/selectors/duplicates.ts
tests/duplicate-rewards.unit.test.ts
  </files>
  <action>
- Implement a "medium" diminishing returns curve with a 10% floor for duplicates.
- Export:
  - `DUPLICATE_REWARD_FLOOR = 0.1`.
  - `getDuplicateRewardMultiplierForCopy(copyIndex: number): number` where `copyIndex=0` => 1.0.
  - `getDuplicateRewardMultiplierForNextPurchase(ownedCount: number): number` (same curve, based on current owned).
  - `getDuplicateRewardSum(ownedCount: number): number` which sums per-copy multipliers for `ownedCount` copies.
- Ensure outputs are finite, monotonic non-increasing as copies increase, and never below the floor.
- Add unit tests asserting:
  - first copy multiplier is exactly 1.0
  - second copy is ~0.70x (within a reasonable tolerance)
  - multipliers decrease and floor at 0.10
  - sum(0)=0, sum(1)=1, sum(2)=1+secondMultiplier
  </action>
  <verify>
pnpm run test:unit
  </verify>
  <done>
- Duplicate multiplier helpers exist and are covered by unit tests.
- Floor is enforced at 0.10x.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit`
</verification>

<success_criteria>
- A model roster exists for all catalog entries with stable ids and per-brand reference numbers.
- GameState can track owned counts per model.
- Duplicate reward multiplier helpers are implemented and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/25-watch-models-and-duplicates/25-01-SUMMARY.md`
</output>
