---
phase: 32-career-landing-and-defaults
plan: 02
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - src/App.tsx
  - src/ui/navigation/landing.ts
  - tests/career-landing.spec.ts
autonomous: true
must_haves:
  truths:
    - "With no existing save, the app lands on the Career tab by default."
    - "A deep link via /?tab=... opens that tab for that navigation only and does not overwrite last-tab persistence."
    - "With an existing save, refresh/navigation without query params restores the last user-selected tab."
  artifacts:
    - path: src/App.tsx
      provides: "Initial landing selection uses resolver and preserves persistence rules"
      contains: "resolveInitialTabSelection"
    - path: tests/career-landing.spec.ts
      provides: "Playwright smoke coverage for landing + deep-link + last-tab persistence"
      contains: "Career landing"
  key_links:
    - from: src/App.tsx
      to: src/ui/navigation/landing.ts
      via: "initial landing selection"
      pattern: "resolveLandingTab"
    - from: tests/career-landing.spec.ts
      to: src/App.tsx
      via: "role=tab assertions on initial landing"
      pattern: "getByRole\(\"tab\""
---

<objective>
Wire the landing resolver into the app and verify the behavior end-to-end with Playwright.

Purpose: Deliver CAREER-LAND-01/02 without breaking deep links, selectors, or existing-save last-tab behavior.
Output: Fresh-save default landing on Career + an e2e regression test for deep link + persistence.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-career-landing-and-defaults/32-CONTEXT.md
@.planning/phases/32-career-landing-and-defaults/32-01-SUMMARY.md

@src/App.tsx
@src/ui/navigation/landing.ts
@tests/tabs.spec.ts
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Use resolver for initial tab selection (fresh save -> Career)</name>
  <files>
src/App.tsx
src/ui/navigation/landing.ts
  </files>
  <action>
- In `src/App.tsx`, update the `resolveInitialTabSelection` callback to delegate selection to `src/ui/navigation/landing.ts`:
  - Import `resolveLandingTab` from `src/ui/navigation/landing.ts`.
  - Keep the existing `TabActivationSource` union in App (`"user" | "deep-link" | "system"`) and map the resolver's source into it.
  - Keep the deep link behavior: `tab` query param overrides for that navigation only.
  - Keep persistence behavior: deep-link selection MUST NOT write to NAVIGATION_KEY.
    - This should remain true because `activateTab` only persists when `source === "user"`; do not change that rule.

- Implement the concrete policy:
  - Determine `hasSave` using the existing localStorage key: `window.localStorage.getItem("emily-idle:save") !== null`.
  - Read `navigationState` using the existing `loadNavigationState()`.
  - Call the resolver with:
    - `search: window.location.search`
    - `hasSave`
    - `navigationState`
    - `isVisible: (tabId) => combinedTabVisibility[tabId]`
  - For `tabId` returned by the resolver, if it is not visible in App (defensive), fall back to `collection`.

- Keep selectors stable: do not change any `id`, `data-testid`, role attributes, or tab labels.
  </action>
  <verify>pnpm run typecheck</verify>
  <done>
- Fresh load with no save lands on Career.
- Existing saves still restore last-tab on refresh with no query params.
- Deep link selection still does not persist as last-tab.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Playwright smoke tests for landing policy</name>
  <files>
tests/career-landing.spec.ts
  </files>
  <action>
- Create `tests/career-landing.spec.ts` with Playwright tests covering:

  1) Fresh save defaults to Career
     - `page.addInitScript(() => localStorage.clear())`
     - `page.goto("/")`
     - Expect the "Career" tab to have `aria-selected="true"`.

  2) Deep link does not overwrite last-tab persistence
     - Seed localStorage with:
       - `emily-idle:save` (use the same minimal seeded state pattern as `tests/tabs.spec.ts`).
       - `emily-idle:navigation` set to `{ lastTabId: "save" }`.
     - `page.goto("/?tab=career")` and expect Career selected.
     - Assert `localStorage.getItem("emily-idle:navigation")` still points to `save` (not overwritten).
     - `page.goto("/")` and expect the "Save" tab selected.

  3) Existing save restores last-tab
     - Seed `emily-idle:save` and `emily-idle:navigation` with `{ lastTabId: "upgrades" }`.
     - `page.goto("/")` and expect the "Upgrades" tab selected.

- Prefer roles/names over brittle selectors:
  - Use `page.getByRole("tab", { name: "Career" })` etc.
  - Do not add new `data-testid` attributes.

- Keep tests fast and deterministic:
  - No real timeouts/sleeps; wait via `expect(...).toHaveAttribute(...)`.
  </action>
  <verify>pnpm run test:e2e</verify>
  <done>
- Playwright tests cover all three behaviors and pass on CI-style headless run.
- Tests demonstrate deep link override without persistence overwrite.
  </done>
</task>

</tasks>

<verification>
- pnpm run typecheck
- pnpm run test:unit
- pnpm run test:e2e
</verification>

<success_criteria>
1. CAREER-LAND-01: Fresh saves land on Career; deep links override without persisting.
2. CAREER-LAND-02: Career is reachable immediately and remains a stable primary navigation tab.
</success_criteria>

<output>
After completion, create `.planning/phases/32-career-landing-and-defaults/32-02-SUMMARY.md`
</output>
