---
phase: 32-career-landing-and-defaults
plan: 03
type: execute
wave: 3
depends_on:
  - 32-02
files_modified: []
autonomous: false

must_haves:
  truths:
    - "On a fresh save, the first landing is Career (desktop + mobile)."
    - "Deep link /?tab=... opens that tab for that navigation only and does not overwrite last-tab persistence."
    - "On an existing save, refresh without query params returns to the last user-selected tab."
  artifacts: []
  key_links: []
---

<objective>
Human verification of landing + navigation rules (fresh save default, deep links, last-tab persistence) on desktop and mobile.

Purpose: Landing behavior is UX-sensitive; a short pass catches edge cases that unit/e2e might miss.
Output: Approved landing/navigation behavior or a concrete issue list for follow-up.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-career-landing-and-defaults/32-CONTEXT.md
@.planning/phases/32-career-landing-and-defaults/32-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Career-first landing for fresh saves, deep link override that does not persist, and predictable last-tab restore for existing saves.
  </what-built>
  <how-to-verify>
1. Run: `pnpm dev`.
2. Open the app in the browser.

Fresh save landing
3. Clear site data/localStorage for the dev origin.
4. Visit `/` (no query params).
5. Confirm you land on the Career tab.

Deep link override does not persist
6. Click the Save tab.
7. Refresh (no query params) and confirm you land back on Save.
8. Navigate directly to `/?tab=career` and confirm Career opens.
9. Refresh again (no query params) and confirm you land back on Save (deep link did not overwrite last-tab).

Existing save last-tab behavior
10. Without clearing site data, click Upgrades.
11. Refresh (no query params) and confirm you land back on Upgrades.

Deep link validity and alias behavior
12. Navigate to `/?tab=catalog` and confirm it opens Vault (tab alias preserved).
13. Navigate to `/?tab=not-a-real-tab` and confirm the app lands on the expected default for your current save state:
    - With an existing save: last-tab restores.
    - With no save: Career default.

Mobile pass
14. Toggle responsive/mobile mode and repeat steps 3-9 quickly:
    - Tabs remain usable.
    - Landing behavior matches desktop.
  </how-to-verify>
  <resume-signal>Type "approved" or paste a short issue list.</resume-signal>
</task>

</tasks>

<verification>
- Manual verification only (see checkpoint).
</verification>

<success_criteria>
- Fresh save default landing is Career.
- Deep links work but do not change last-tab persistence.
- Existing saves restore last-tab on refresh without query params.
- No selector churn (roles/ids/testids unchanged).
</success_criteria>

<output>
After completion, create `.planning/phases/32-career-landing-and-defaults/32-03-SUMMARY.md`
</output>
