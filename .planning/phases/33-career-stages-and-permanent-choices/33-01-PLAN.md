---
phase: 33-career-stages-and-permanent-choices
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/model/types.ts
  - src/game/model/state.ts
  - src/game/persistence.ts
autonomous: true
must_haves:
  truths:
    - "Saves can persist permanent career stage choices (primary track, modality, operating style, expansion focus) without breaking older saves."
    - "Existing saves with therapistCareer.activeTrackId set are treated as already having a locked primary track."
    - "Career stage choices are optional in persisted state; missing fields default safely."
  artifacts:
    - path: src/game/model/types.ts
      provides: "TherapistCareerState includes fields for permanent stage choices"
      contains: "primaryTrackId"
    - path: src/game/model/state.ts
      provides: "createInitialState + createStateFromSave initialize/sanitize permanent choice fields"
      contains: "createStateFromSave"
    - path: src/game/persistence.ts
      provides: "sanitizeState accepts optional therapistCareer choice fields"
      contains: "therapistCareer"
  key_links:
    - from: src/game/persistence.ts
      to: src/game/model/state.ts
      via: "createStateFromSave(persisted)"
      pattern: "createStateFromSave"
    - from: src/game/model/state.ts
      to: src/game/model/types.ts
      via: "TherapistCareerState schema"
      pattern: "primaryTrackId"
---

<objective>
Extend the therapist career save schema to include stage permanence fields and migrate existing saves safely.

Purpose: Enable CAREER-STAGE-03 persistence for permanent stage choices while keeping old saves loadable.
Output: TherapistCareerState includes permanent choice fields; persistence + createStateFromSave sanitize/migrate them.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-career-stages-and-permanent-choices/33-CONTEXT.md

@src/game/model/types.ts
@src/game/model/state.ts
@src/game/persistence.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add permanent stage choice fields to therapist career state</name>
  <files>
src/game/model/types.ts
src/game/model/state.ts
  </files>
  <action>
- In src/game/model/types.ts:
  - Add explicit ID types for stage choices:
    - CareerModalityId (stage 3)
    - CareerOperatingStyleId (stage 4)
    - CareerExpansionFocusId (stage 5)
  - Extend TherapistCareerState with persisted permanence fields:
    - primaryTrackId: CareerTrackId | null
    - modalityId: CareerModalityId | null
    - operatingStyleId: CareerOperatingStyleId | null
    - expansionFocusId: CareerExpansionFocusId | null
  - Extend PersistedGameState.therapistCareer with optional persisted fields:
    - primaryTrackId?: string | null
    - modalityId?: string | null
    - operatingStyleId?: string | null
    - expansionFocusId?: string | null
- In src/game/model/state.ts:
  - Update createInitialState() to initialize the new therapistCareer fields to null.
  - Do NOT store a stage id in state; stages are derived from level in a later plan.
  </action>
  <verify>pnpm run typecheck</verify>
  <done>
  - TherapistCareerState includes explicit fields for permanent career stage choices.
  - Fresh saves start with all permanence fields set to null.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement save-field migration and sanitization for permanence fields</name>
  <files>
src/game/persistence.ts
src/game/model/state.ts
  </files>
  <action>
- In src/game/persistence.ts sanitizeState():
  - Accept optional therapistCareer fields for permanence and pass them through to PersistedGameState:
    - primaryTrackId, modalityId, operatingStyleId, expansionFocusId
  - Treat invalid shapes as undefined (mirror existing sanitize pattern).
- In src/game/model/state.ts createStateFromSave():
  - Parse primaryTrackId from save (string -> CareerTrackId, null -> null; invalid -> null).
  - Migration rule for existing saves:
    - If primaryTrackId is missing/invalid but activeTrackId is valid and non-null, set primaryTrackId = activeTrackId.
  - Enforce a basic invariant:
    - If primaryTrackId is non-null, set activeTrackId to primaryTrackId (do not allow a mismatch).
  - Parse choice ids (modalityId / operatingStyleId / expansionFocusId) from save as string-or-null; if missing/invalid type, default to null.
  - Keep older saves safe: absence of these fields must not prevent state restoration.
  </action>
  <verify>pnpm run test:unit</verify>
  <done>
  - Older saves without permanence fields still load successfully.
  - Existing saves with activeTrackId set now load with primaryTrackId set and activeTrackId pinned to it.
  - New permanence fields are preserved end-to-end through encode/decode.
  </done>
</task>

</tasks>

<verification>
- pnpm run typecheck
- pnpm run test:unit
- pnpm run test:e2e
</verification>

<success_criteria>
1. GameState loads for both old and new saves, with permanent-choice fields defaulted safely when missing.
2. Existing saves that already selected a track are treated as locked to that track on load.
</success_criteria>

<output>
After completion, create `.planning/phases/33-career-stages-and-permanent-choices/33-01-SUMMARY.md`
</output>
