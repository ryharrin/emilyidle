---
phase: 33-career-stages-and-permanent-choices
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - src/game/data/career.ts
  - src/game/data/careerStages.ts
  - src/game/selectors/careerStages.ts
  - src/game/selectors/index.ts
  - src/game/actions/therapistCareer.ts
  - src/game/actions/index.ts
  - src/game/state.ts
  - src/ui/components/CareerStageChoices.tsx
  - src/ui/tabs/CareerTab.tsx
autonomous: true
must_haves:
  truths:
    - "Career progression exposes 5 stages (grad student -> private practice owner) based on therapist career level."
    - "Each stage after the first offers a permanent choice with clear before/after previews (salary + session policy) and the choice persists."
    - "Primary track selection becomes permanent once chosen (existing saves with activeTrackId are treated as already locked)."
    - "Cash economy remains career-driven; stage choices only modify therapist salary/sessions and do not introduce non-career cash sources."
  artifacts:
    - path: src/game/data/careerStages.ts
      provides: "Stage thresholds + choice definitions (ids, labels, effect multipliers)"
      contains: "STAGE"
    - path: src/game/selectors/careerStages.ts
      provides: "Selectors for current stage, available choices, and before/after previews"
      contains: "getTherapistCareerStage"
    - path: src/game/actions/therapistCareer.ts
      provides: "Actions enforcing permanent choices (track + stage choices)"
      contains: "selectPrimaryCareerTrack"
    - path: src/ui/components/CareerStageChoices.tsx
      provides: "Stage + permanent choice UI with stable test ids and previews"
      contains: "data-testid=\"career-stages-card\""
  key_links:
    - from: src/ui/tabs/CareerTab.tsx
      to: src/ui/components/CareerStageChoices.tsx
      via: "render stage card + choice options"
      pattern: "CareerStageChoices"
    - from: src/ui/components/CareerStageChoices.tsx
      to: src/game/selectors/careerStages.ts
      via: "preview selectors"
      pattern: "getCareerChoicePreview"
    - from: src/game/selectors/index.ts
      to: src/game/selectors/careerStages.ts
      via: "therapist salary/session policy uses choice modifiers"
      pattern: "modalityId|operatingStyleId|expansionFocusId"
---

<objective>
Implement career stages and permanent choices, apply their effects to salary/session cadence, and wire UI with before/after previews.

Purpose: Satisfy CAREER-STAGE-01/02/03 by making stages explicit and choices permanent + previewable while keeping cash career-driven.
Output: Stage/choice definitions + selectors; actions enforcing permanence; Career UI shows stage/choice surfaces with stable test ids.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-career-stages-and-permanent-choices/33-CONTEXT.md

@src/game/data/career.ts
@src/game/selectors/index.ts
@src/game/actions/index.ts
@src/ui/tabs/CareerTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define stages + choice effects and apply them to therapist salary and session policy</name>
  <files>
src/game/data/careerStages.ts
src/game/data/career.ts
src/game/selectors/careerStages.ts
src/game/selectors/index.ts
  </files>
  <action>
- Create src/game/data/careerStages.ts defining:
  - Stage ids and unlock levels:
    - grad-student: level 1+
    - licensed-associate: level 3+
    - specialist-certification: level 6+
    - practice-builder: level 10+
    - private-practice-owner: level 15+
  - Choice definitions for stages 2-5 with stable ids, labels, short descriptions, and effect multipliers.
  - Constraint: every permanent choice MUST change at least one money-related term (salary rate and/or session payout/cooldown/cost), but must not add any new cash sources.
  - Recommended effect schema per choice:
    - salaryMultiplier
    - sessionCashPayoutMultiplier
    - sessionCooldownMultiplier
    - sessionEnjoymentCostMultiplier
  - Ensure each choice has a non-1 salaryMultiplier so previews are testable even on salary-only tracks.
- In src/game/data/career.ts:
  - Keep TRACK_CHOICE_UNLOCK_LEVEL = 3; treat it as stage 2 unlock.
  - Do not remove existing tracks/nodes; these remain compatible.
- Create src/game/selectors/careerStages.ts with pure selectors:
  - getTherapistCareerStageId(level) and getTherapistCareerStage(state)
  - getTherapistCareerStageUnlockLevel(stageId)
  - getTherapistCareerChoiceStatus(state): which stage choices are available/locked
  - getCareerChoicePreview(state, { stageId, choiceId }): returns before/after numbers for:
    - salary cents/sec
    - session payout/cooldown/enjoyment cost (or "unavailable")
  - getTherapistCareerEffectMultipliers(state): merges multipliers from chosen modality/style/focus.
- In src/game/selectors/index.ts:
  - Apply effect multipliers (from getTherapistCareerEffectMultipliers) to:
    - getTherapistCashRateCentsPerSec (salary)
    - getTherapistSessionCooldownMs
    - getTherapistSessionEnjoymentCostCents
    - getTherapistSessionCashPayoutCents
  - Keep selector purity rules.
  - Keep cash career-driven: do NOT incorporate watches, upgrades, workshop, maison, etc into getTotalCashRateCentsPerSec.
  </action>
  <verify>pnpm run typecheck</verify>
  <done>
  - Stage ids and unlock levels exist and are derived from therapist career level.
  - Permanent stage choices affect therapist salary/session terms via selectors only.
  - A preview selector can compute before/after salary + session policy for any offered choice.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add actions + UI wiring for permanent choices with stable previews and selectors</name>
  <files>
src/game/actions/therapistCareer.ts
src/game/actions/index.ts
src/game/state.ts
src/ui/components/CareerStageChoices.tsx
src/ui/tabs/CareerTab.tsx
  </files>
  <action>
- Create src/game/actions/therapistCareer.ts implementing actions that enforce permanence:
  - selectPrimaryCareerTrack(state, trackId):
    - Requires career.level >= 3.
    - If state.therapistCareer.primaryTrackId is non-null, return state (no switching).
    - Otherwise set both primaryTrackId and activeTrackId to trackId.
  - chooseCareerModality(state, modalityId): requires level >= 6 and modalityId unset.
  - chooseCareerOperatingStyle(state, operatingStyleId): requires level >= 10 and operatingStyleId unset.
  - chooseCareerExpansionFocus(state, expansionFocusId): requires level >= 15 and expansionFocusId unset.
  - All actions must be pure and defensive (invalid ids -> return state).
- Re-export these actions from src/game/actions/index.ts and src/game/state.ts.
- Create src/ui/components/CareerStageChoices.tsx:
  - Render a "Career stages" card showing:
    - current stage label + level
    - upcoming stages with required level
    - for each stage with an available permanent choice: show option buttons/cards
    - show before/after previews for each option using selector results
  - Add stable selectors:
    - card: data-testid="career-stages-card"
    - current stage: data-testid="career-stage-current"
    - per-stage block: data-testid="career-stage-block-{stageId}"
    - per-option button: data-testid="career-choice-option-{choiceId}"
    - preview before/after salary: data-testid="career-choice-salary-before" / "career-choice-salary-after"
    - preview before/after session payout/cooldown/cost (if sessions supported):
      - data-testid="career-choice-session-payout-before" / "career-choice-session-payout-after"
      - data-testid="career-choice-session-cooldown-before" / "career-choice-session-cooldown-after"
      - data-testid="career-choice-session-cost-before" / "career-choice-session-cost-after"
    - locked summary per stage: data-testid="career-choice-locked-{stageId}"
  - Show a clear "Permanent" label after a choice is made.
- Update src/ui/tabs/CareerTab.tsx:
  - Replace direct track mutation with selectPrimaryCareerTrack().
  - If primaryTrackId is set, disable other track buttons and display a lock indicator.
  - Keep node spending and respec behavior unchanged; ensure respec does NOT touch permanence fields.
  - Mount CareerStageChoices near the top of the Career panel (above tracks is preferred for discoverability).
  </action>
  <verify>pnpm run test:e2e</verify>
  <done>
  - Track selection is permanent once chosen and is enforced by actions + UI.
  - Stages 3-5 permanent choices can be selected once and persist; UI shows locked state.
  - Each choice shows clear before/after previews.
  - Existing tests still pass; no selector churn for existing anchors (career-panel, career-action, etc).
  </done>
</task>

</tasks>

<verification>
- pnpm run typecheck
- pnpm run test:unit
- pnpm run test:e2e
</verification>

<success_criteria>
1. Career has 5 stages derived from level thresholds and surfaces them on the Career tab.
2. Stage 2-5 permanent choices exist, are one-way, persist in saves, and have before/after previews.
3. Cash remains career-driven: stage choices only modify therapist salary/sessions.
</success_criteria>

<output>
After completion, create `.planning/phases/33-career-stages-and-permanent-choices/33-02-SUMMARY.md`
</output>
