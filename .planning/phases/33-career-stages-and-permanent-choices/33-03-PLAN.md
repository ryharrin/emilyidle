---
phase: 33-career-stages-and-permanent-choices
plan: 03
type: execute
wave: 3
depends_on:
  - 33-02
files_modified:
  - tests/career-stages.unit.test.ts
  - tests/career-permanent-choices.unit.test.ts
  - tests/career-permanent-choices.spec.ts
autonomous: true
must_haves:
  truths:
    - "Unit tests cover stage unlock mapping and permanence rules (choices are one-way and affect salary/session terms)."
    - "Unit tests cover persistence/migration: older saves load and existing activeTrackId becomes locked primaryTrackId."
    - "E2E test verifies choosing at least one permanent choice shows before/after previews, locks the choice, and persists across refresh."
  artifacts:
    - path: tests/career-stages.unit.test.ts
      provides: "Stage unlock threshold + derived stage coverage"
      contains: "career stages"
    - path: tests/career-permanent-choices.unit.test.ts
      provides: "Action + selector unit coverage for permanence and preview deltas"
      contains: "selectPrimaryCareerTrack"
    - path: tests/career-permanent-choices.spec.ts
      provides: "Playwright coverage for UI previews + persistence"
      contains: "career-stages-card"
  key_links:
    - from: tests/career-permanent-choices.spec.ts
      to: src/ui/components/CareerStageChoices.tsx
      via: "data-testid selectors"
      pattern: "career-stages-card"
    - from: tests/career-permanent-choices.unit.test.ts
      to: src/game/persistence.ts
      via: "decode/restore save"
      pattern: "createStateFromSave"
---

<objective>
Add unit and e2e coverage for career stages and permanent choices (unlock rules, previews, and persistence).

Purpose: Lock in CAREER-STAGE-01/02/03 behaviors and prevent regressions in permanence and previews.
Output: Focused unit tests for stage/choice logic + a Playwright spec exercising the UI and persistence.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-career-stages-and-permanent-choices/33-CONTEXT.md

@tests/career-first-economy.unit.test.ts
@tests/career-progression.unit.test.ts
@tests/career-upgrades.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unit tests for stage unlocks, permanence rules, and preview deltas</name>
  <files>
tests/career-stages.unit.test.ts
tests/career-permanent-choices.unit.test.ts
  </files>
  <action>
- Create tests/career-stages.unit.test.ts:
  - Cover stage mapping boundaries (levels exactly at thresholds and just below):
    - 1, 2 => grad student
    - 3-5 => licensed associate
    - 6-9 => specialist certification
    - 10-14 => practice builder
    - 15+ => private practice owner
- Create tests/career-permanent-choices.unit.test.ts:
  - Track migration and lock behavior:
    - createStateFromSave where therapistCareer.activeTrackId is set but primaryTrackId absent -> resulting state has primaryTrackId set and activeTrackId pinned.
  - Action enforcement:
    - selectPrimaryCareerTrack cannot change the track once primaryTrackId is set.
    - chooseCareerModality/OperatingStyle/ExpansionFocus are one-way (second attempt returns unchanged state).
  - Effect application:
    - Choosing a modality/style/focus changes getTherapistCashRateCentsPerSec (salary) by a non-1 factor.
    - Preview selector returns before/after salary where after != before for the tested choice.
  - Keep assertions resilient: compare relative changes and invariants, not exact balancing numbers.
  </action>
  <verify>pnpm run test:unit</verify>
  <done>
  - Unit tests fail if stage thresholds change unexpectedly, if permanence can be bypassed, or if previews stop reflecting deltas.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Playwright coverage for UI previews + persistence across refresh</name>
  <files>
tests/career-permanent-choices.spec.ts
  </files>
  <action>
- Create tests/career-permanent-choices.spec.ts:
  - Seed a save (version: 2) with therapistCareer.level high enough to unlock at least stage 3 (>= 6) and enough enjoyment for sessions if needed.
  - Navigate to / and assert Career is visible and stage card exists:
    - expect(getByTestId("career-stages-card")).toBeVisible()
  - Choose the primary track via a stage choice option button (stage 2 choice) if not already locked.
  - Choose a modality (stage 3) that has a salary multiplier != 1.
  - Verify previews exist (Before/After salary test ids) and salary-after differs from salary-before.
  - After choosing, verify locked UI appears (career-choice-locked-{stageId}).
  - Reload the page and verify the locked UI is still present and option buttons are no longer available.
- Selector stability rules:
  - Use data-testid anchors defined in 33-02 and avoid brittle text-only selectors.
  </action>
  <verify>pnpm run test:e2e</verify>
  <done>
  - E2E test proves permanent choices can be made, previews are visible, and choices persist across refresh.
  </done>
</task>

</tasks>

<verification>
- pnpm run typecheck
- pnpm run test:unit
- pnpm run test:e2e
</verification>

<success_criteria>
1. Unit tests cover stage mapping, permanence enforcement, and preview deltas.
2. Playwright test verifies UI previews and persistence across refresh using stable selectors.
</success_criteria>

<output>
After completion, create `.planning/phases/33-career-stages-and-permanent-choices/33-03-SUMMARY.md`
</output>
