---
phase: 21-explanations-and-rate-transparency
plan: 05
type: execute
wave: 2
depends_on:
  - 21-02
files_modified:
  - src/ui/tabs/NostalgiaTab.tsx
autonomous: true

must_haves:
  truths:
    - "User can understand why Nostalgia unlocks are ordered and why an item is Locked"
    - "User can open that explanation directly from the unlock store"
  artifacts:
    - path: "src/ui/tabs/NostalgiaTab.tsx"
      provides: "Unlock-store Explain trigger for nostalgia unlock order"
  key_links:
    - from: "src/ui/tabs/NostalgiaTab.tsx"
      to: "src/ui/help/helpContent.ts"
      via: "HELP_SECTION_IDS.nostalgiaUnlocks"
      pattern: "HELP_SECTION_IDS\\.nostalgiaUnlocks"
---

<objective>
Add point-of-use explanations for Nostalgia unlock order and lock reasons so the unlock store reads as an intentional progression path.

Purpose: Satisfy GUIDE-03 for the Nostalgia unlock store.
Output: Explain affordance in Nostalgia unlock UI wired to Help.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/21-explanations-and-rate-transparency/21-RESEARCH.md

@src/ui/tabs/NostalgiaTab.tsx
@src/ui/help/ExplainButton.tsx
@src/ui/help/helpContent.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unlock-store ExplainButton for nostalgia unlock order</name>
  <files>src/ui/tabs/NostalgiaTab.tsx</files>
  <action>
Update `src/ui/tabs/NostalgiaTab.tsx` in the unlock store section:

- In the `nostalgia-unlocks-header` (near "Unlock store" title/description), add an inline ExplainButton:
  - `sectionId={HELP_SECTION_IDS.nostalgiaUnlocks}`
  - `data-testid` should come from ExplainButton (explain-nostalgia-unlocks).

- Add (or adjust) one short line of copy in the unlock store description making the rule explicit, e.g. "Unlocks are purchased in order." (keep it short).

Do NOT change the unlock gating logic or the lockReason logic; this is explanatory wiring only.
  </action>
  <verify>pnpm -s run typecheck</verify>
  <done>
- Unlock store has a tap/click explain affordance.
- Clicking it opens Help to the Nostalgia unlocks section.
  </done>
</task>

</tasks>

<verification>
- `pnpm -s run typecheck`
- `pnpm -s run test:unit`
</verification>

<success_criteria>
- The unlock order rule is explicit at point of use.
- A dedicated explanation opens from the unlock store.
</success_criteria>

<output>
After completion, create `.planning/phases/21-explanations-and-rate-transparency/21-05-SUMMARY.md`
</output>
