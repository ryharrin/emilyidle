---
phase: 30-workshop-atelier-and-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/selectors/enjoyment.ts
  - src/game/selectors/index.ts
  - src/game/actions/index.ts
  - src/ui/tabs/WorkshopTab.tsx
  - src/ui/tabs/CollectionTab.tsx
  - tests/workshop-atelier.unit.test.ts
autonomous: true
must_haves:
  truths:
    - "When Atelier is locked, dismantle is not usable anywhere and shows a clear locked explanation."
    - "In Atelier, user can see enjoyment remaining to the next +1 Blueprint, an ETA based on current enjoyment rate, and a dollars-related hint that is derivable (not misleading)."
    - "After first Atelier reset, prestige legacy + upgrades make the next vault run meaningfully faster (target ~3x to next Atelier reset) and the multiplier is reflected in rates."
  artifacts:
    - path: src/game/selectors/index.ts
      provides: "Selector helper(s) for next Blueprint threshold + ETA + cash hint"
    - path: src/ui/tabs/WorkshopTab.tsx
      provides: "Atelier reset panel includes next-Blueprint progress readout"
    - path: src/ui/tabs/CollectionTab.tsx
      provides: "Dismantle affordances gated behind Atelier unlock"
  key_links:
    - from: src/ui/tabs/WorkshopTab.tsx
      to: src/game/state.ts
      via: "imports selector helper(s)"
      pattern: "getWorkshop"
    - from: src/ui/tabs/CollectionTab.tsx
      to: src/game/state.ts
      via: "Atelier unlock gating"
      pattern: "canWorkshopPrestige|workshopPrestigeCount|workshopBlueprints"
---

<objective>
Tighten Workshop/Atelier UX clarity and pacing foundations: gate dismantle behind Atelier unlock, show actionable “next blueprint” progress (remaining + ETA + cash hint), and tune the legacy multiplier so the 2nd run accelerates.

Purpose: Make the Workshop loop readable and meaningfully rewarding immediately after the first reset.
Output: Updated Workshop/Vault dismantle behavior, new selector helpers + unit tests, and tuned prestige legacy pacing.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/30-workshop-atelier-and-docs/30-CONTEXT.md

@src/ui/tabs/WorkshopTab.tsx
@src/ui/tabs/CollectionTab.tsx
@src/game/selectors/index.ts
@src/game/selectors/enjoyment.ts
@src/game/actions/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add next-Blueprint progress selector + tune prestige legacy pace</name>
  <files>
src/game/selectors/enjoyment.ts
src/game/selectors/index.ts
tests/workshop-atelier.unit.test.ts
  </files>
  <action>
- In src/game/selectors/index.ts, add an exported helper for the Atelier reset panel that answers: “how much enjoyment until the next +1 Blueprint in reset gain?”
  - Implement by searching integer base-gain values upward and converting to an enjoyment threshold using WORKSHOP_PRESTIGE_THRESHOLD_CENTS * baseGain^2.
  - Include: currentBlueprintGain, nextBlueprintGain (= current + 1), nextEnjoymentThresholdCents, enjoymentRemainingCents.
  - Include ETA seconds using current enjoyment rate (use getEnjoymentRateCentsPerSec(state) and guard division-by-zero).
  - Include a dollars-related hint derived from that ETA (recommended: cashEarnedDuringEtaCents = getTotalCashRateCentsPerSec(state, eventMultiplier=1) * etaSeconds).
- In src/game/selectors/enjoyment.ts, tune getPrestigeLegacyMultiplier(state) to make the 2nd vault run meaningfully faster (target ~3x to next Atelier reset).
  - Keep the existing “workshop + maison” structure, but make the FIRST workshop prestige provide a large immediate jump, then smaller compounding.
  - Recommended shape (keep cap): if workshopPrestigeCount >= 1, multiply by a one-time jump (e.g. ~2.0-2.5x), then apply the existing per-prestige compounding for additional resets.
- Add tests in tests/workshop-atelier.unit.test.ts:
  - next-Blueprint threshold helper is monotonic: increasing enjoyment never increases enjoymentRemainingCents.
  - When enjoymentRate is 0, ETA is null/Infinity-safe and cash hint is 0.
  - Legacy multiplier after first workshop prestige is >= 2 (or whatever the chosen one-time jump is), and multiplier remains capped.
  </action>
  <verify>pnpm run test:unit</verify>
  <done>
- New selector helper exists and is exported via src/game/state.ts.
- Tests cover next-Blueprint computations and the tuned prestige legacy shape.
  </done>
</task>

<task type="auto">
  <name>Task 2: Gate dismantle + add Atelier progress readout in UI</name>
  <files>
src/game/actions/index.ts
src/ui/tabs/WorkshopTab.tsx
src/ui/tabs/CollectionTab.tsx
  </files>
  <action>
- Enforce dismantle last-copy policy in src/game/actions/index.ts:
  - Update dismantleItem() so it refuses to reduce owned count below 1 (block dismantling the last owned copy).
  - Keep quantity=1 per click behavior in UI; still enforce in the action for safety.
- Update src/ui/tabs/WorkshopTab.tsx:
  - Hide the entire dismantle list (and any dismantle affordance) until the Atelier is unlocked (use the existing showWorkshopPanel prop).
  - When locked: render a teaser/locked placeholder in the crafting panel explaining dismantle unlocks with Atelier, and point the player at the reset threshold progress.
  - When unlocked: keep dismantle as a visible section (not collapsed), quantity=1 per click, and show gain-only messaging (“+N parts”).
  - Add a “Next blueprint” readout near the reset threshold/current gain block using the new selector helper:
    - Show enjoyment remaining.
    - Show ETA (human-friendly; seconds/minutes are fine).
    - Show a cash hint derived from ETA (cash earned during ETA).
- Update src/ui/tabs/CollectionTab.tsx:
  - Gate the per-watch dismantle button behind Atelier unlock (match App’s showWorkshopPanel logic locally: canWorkshopPrestige(state) || state.workshopPrestigeCount > 0 || state.workshopBlueprints > 0).
  - When locked: replace dismantle with a clear locked placeholder (disabled button + short text).
  - When unlocked: disable dismantle when owned <= 1 to reflect the last-copy policy.
  </action>
  <verify>pnpm run typecheck</verify>
  <done>
- Dismantle is not available before Atelier unlock (Workshop + Vault affordances).
- Dismantle never reduces an owned watch to 0.
- Atelier reset panel shows “next blueprint” remaining + ETA + cash hint.
  </done>
</task>

</tasks>

<verification>
- pnpm run test:unit
- pnpm run typecheck
</verification>

<success_criteria>
1. Workshop dismantle UI is hidden until Atelier is unlocked; Vault dismantle controls show a clear locked placeholder instead of functioning.
2. Atelier reset panel includes next-Blueprint remaining enjoyment, ETA, and a cash hint derived from ETA.
3. Second run acceleration is visibly supported by the tuned prestige legacy multiplier (and reflected in rate breakdowns).
</success_criteria>

<output>
After completion, create `.planning/phases/30-workshop-atelier-and-docs/30-01-SUMMARY.md`
</output>
