---
phase: 42-winding-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/components/WindingMiniGameModal.tsx
  - src/ui/components/winding/windingMath.ts
  - src/ui/components/winding/useWindingRun.ts
  - src/ui/components/winding/WindingCrown.tsx
  - src/style.css
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "Player can start winding and sees a visible winding animation that responds to progress"
    - "Player can stop winding at a chosen moment and immediately sees an outcome message (including 'Over-wound!' for >95%)"
    - "Winding input control is clear: progress (amount) + crown/tension speed (pace) reflect where the marker is"
    - "Reduced-motion users still get clear non-rotating feedback (tension/progress) during the run"
    - "On mobile, the winding interaction prevents background scroll and provides a large tap target"
    - "Screen readers get announcements for start, stop position, and outcome; Escape closes the modal"
  artifacts:
    - path: src/ui/components/WindingMiniGameModal.tsx
      provides: "Winding modal UI with band rules, animation wiring, a11y, and stable testids"
    - path: src/ui/components/winding/windingMath.ts
      provides: "Band mapping (under/good/perfect/over) + domain tier mapping (miss|good|perfect)"
    - path: src/ui/components/winding/useWindingRun.ts
      provides: "RAF-driven progress + crown angle/tension with reduced-motion stepping and stop easing"
    - path: src/ui/components/winding/WindingCrown.tsx
      provides: "Presentational crown rendering (rotate or reduced-motion feedback)"
    - path: src/style.css
      provides: "Winding visuals: 72px crown, track bands, tension glow, mobile touch behavior"
  key_links:
    - from: src/App.tsx
      to: src/game/actions/interactions.ts
      via: applyWindingReward(state, itemId, nowMs, outcome.tier)
      pattern: "applyWindingReward\\(.*outcome\\.tier"
    - from: src/ui/components/WindingMiniGameModal.tsx
      to: src/ui/components/winding/windingMath.ts
      via: getWindingBand(progress) -> tier mapping
      pattern: "getWindingBand\\("
    - from: src/ui/components/WindingMiniGameModal.tsx
      to: src/ui/components/winding/useWindingRun.ts
      via: requestAnimationFrame progress + crown motion
      pattern: "useWindingRun"
    - from: src/ui/components/WindingMiniGameModal.tsx
      to: tests
      via: stable selectors
      pattern: "data-testid=\"winding-(modal|track|stop|outcome|done)\""
---

<objective>
Refresh the winding mini-game to use Phase 42 band rules (under/good/perfect/over), add a visible crown + tension animation that responds to progress and eases to a stop, and meet mobile + accessibility requirements without changing domain reward types.

Purpose: Make winding feel more interactive and readable while keeping the existing timing-based mechanic.
Output: Updated winding modal behavior + visuals, with no new dependencies and stable test selectors.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-winding-refresh/42-CONTEXT.md
@.planning/phases/42-winding-refresh/42-RESEARCH.md

@src/ui/components/WindingMiniGameModal.tsx
@src/style.css
@src/ui/help/HelpModal.tsx
@src/game/actions/interactions.ts
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add band mapping + RAF run helpers (with over-wind UI band)</name>
  <files>src/ui/components/winding/windingMath.ts, src/ui/components/winding/useWindingRun.ts</files>
  <action>
Create `src/ui/components/winding/windingMath.ts` with:
- `export type WindingBand = "under" | "good" | "perfect" | "over"`.
- `export function clamp01(n: number): number`.
- `export function getWindingBand(progress01: number): WindingBand` implementing Phase 42 rules:
  - <0.30 => under
  - <0.70 => good
  - <=0.95 => perfect
  - >0.95 => over
- `export function getOutcomeTierFromBand(band: WindingBand): "miss" | "good" | "perfect"` mapping `under` and `over` -> `miss` (domain stays unchanged).
- `export function getWindingBandLabel(band: WindingBand): string` returning copy used in the UI ("Under-wound", "Good wind", "Perfect tension", "Over-wound!").
- Keep this file pure and independent of React.

Create `src/ui/components/winding/useWindingRun.ts` that owns the winding run loop:
- Inputs: `{ open, runDurationMs, prefersReducedMotion, stepMsReducedMotion }`.
- Outputs: `{ progress01, crownAngleDeg, tension01, phase, stop }` where:
  - `progress01` advances from 0..1 over `runDurationMs` using `requestAnimationFrame`.
  - Reduced motion uses stepped progress (no CSS keyframes reliance).
  - `crownAngleDeg` updates continuously while running; on `stop()`, keep `progress01` frozen but continue animating `crownAngleDeg` through a short deceleration (mechanical easing) and then stop.
  - `tension01` increases toward the perfect band and spikes in the over-wind danger zone; it should be derived from `progress01` (so reduced-motion users still see feedback).
- MUST cancel RAF on close/unmount and never update state after unmount.
- Do NOT delay the stop result; `stop()` is for visuals only.
  </action>
  <verify>pnpm typecheck</verify>
  <done>Helpers exist, are exported, and TypeScript builds with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update winding modal behavior, a11y, and persisted tap hint wiring</name>
  <files>src/ui/components/WindingMiniGameModal.tsx, src/ui/components/winding/WindingCrown.tsx, src/App.tsx</files>
  <action>
Update `src/ui/components/WindingMiniGameModal.tsx` to implement Phase 42 behavior:
- Change run duration to ~5-6 seconds (pick a single constant, e.g. `5_600ms`).
- Replace the existing distance-from-center scoring with band rules from `getWindingBand()`.
- Introduce a UI-only band field on the internal result state so you can show "Over-wound!" while still calling `onComplete` with `tier: "miss"`.
- Ensure `onComplete(outcome)` is called immediately on stop (do not wait for animations).
- Add a visible progress/amount readout while running (e.g. "Tension: 82%" or "Wind: 82%") so the player's control is obvious.

Animation requirements:
- Replace the CSS-keyframe spin with a crown rendered via a new presentational component `src/ui/components/winding/WindingCrown.tsx`.
- `WindingCrown` takes `{ prefersReducedMotion, angleDeg, tension01, band, phase }` and renders:
  - Normal motion: `transform: rotate(angleDeg)` and a tension glow based on `tension01`.
  - Reduced motion: do NOT rotate; instead use scale/opacity and a tension glow that still changes (driven by JS styles).
- The crown visibly speeds up as progress approaches 70-95% and feels dangerous in 95-100%.
- On stop, crown decelerates to rest (mechanical easing).

Mobile + interaction surface:
- Make the stop action accessible via both the stop button (`data-testid="winding-stop"`) and tapping the track/primary surface.
- Add `touch-action: none` / overscroll containment on the primary interaction surface so tapping doesn\'t scroll the page.

Accessibility:
- Add Escape-to-close using the `HelpModal` pattern.
- Add body scroll lock while open using the `HelpModal` pattern.
- Add an `aria-live="polite"` status region that announces: "Winding...", "Stopped at XX%", and the outcome label.
  - Add `data-testid="winding-live"` to this live region so unit tests can assert announcements without screen reader tooling.
- Ensure focus lands on the stop button when the modal opens and tab focus stays inside the modal (focus trap).
  - Initial focus: on open, call `stopButtonRef.current?.focus()`.
  - Trap strategy (no deps): add two invisible focus sentinels inside the modal (top + bottom, `tabIndex={0}`) and on focus:
    - top sentinel focuses the last focusable element inside the modal
    - bottom sentinel focuses the first focusable element inside the modal
  - Focusable set: all enabled buttons/links/inputs within the winding modal card.
  - On close, restore focus to the element that was focused before opening (store `document.activeElement` on open).

Persisted first-play tap hint (no new localStorage keys):
- Add optional props to `WindingMiniGameModal` to control showing a tap hint and dismissing it.
- Update `src/App.tsx` to use `settings.coachmarksDismissed["winding:tap-hint"]`:
  - Pass `showTapHint` as `!settings.coachmarksDismissed["winding:tap-hint"]`.
  - On first successful stop (or on close), call `persistSettings({...settings, coachmarksDismissed: {..., ["winding:tap-hint"]: true}})`.
- Do NOT introduce a new storage key; this must only touch existing settings persistence.

Keep these `data-testid` values unchanged: `winding-modal`, `winding-track`, `winding-stop`, `winding-outcome`, `winding-done`.
  </action>
  <verify>pnpm test:unit -- tests/catalog.unit.test.tsx -t "wind minigame"</verify>
  <done>
 The winding modal uses Phase 42 band rules, over-wind shows "Over-wound!", `onComplete` receives only miss/good/perfect, and unit tests referencing winding selectors still pass.
 
 A11y+interaction behaviors are in place:
 - Escape closes the modal.
 - While open, `document.body.style.overflow` is set to `hidden` and restored on close.
 - On open, focus is on the stop button; tabbing does not allow focus to leave the modal.
 - The `aria-live` region (with `data-testid="winding-live"`) updates for winding, stop position, and outcome.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update winding visuals and mobile touch ergonomics in global CSS</name>
  <files>src/style.css</files>
  <action>
Update `src/style.css` winding styles to match Phase 42 context:
- Increase crown size to ~72px and style it to feel more like a crown (keep existing theme).
- Replace the single sweetspot window with band visuals across the track:
  - Under-wind (0-30%): muted
  - Good (30-69%): warm
  - Perfect (70-95%): brighter / highlight
  - Over-wind (95-100%): danger accent
- Increase the primary interaction touch target (track height and/or a surrounding hit area) to be comfortable on mobile.
- Ensure the primary interaction surface uses `touch-action: none` and prevents background scroll interference when the modal is open.
- Ensure reduced-motion feedback does not rely on CSS keyframes (the crown component should drive visual state through inline styles).

Keep existing class names where possible and avoid refactoring unrelated modal styles.
  </action>
  <verify>pnpm lint</verify>
  <done>Winding UI has visible bands, a larger crown, and mobile-friendly interaction surface without lint errors.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm lint`
- `pnpm test:unit -- tests/catalog.unit.test.tsx -t "wind minigame"`

Manual verification (a11y + mobile-ish behavior):
1. `pnpm dev` and open a winding modal from Catalog.
2. Try to scroll the background while the modal is open; page should not scroll.
3. Press `Escape`; modal closes.
4. Re-open the modal and confirm keyboard focus lands on the Stop button.
5. Press `Tab` repeatedly (and `Shift+Tab`); focus should stay within the modal controls.
6. In devtools Elements, observe `[data-testid="winding-live"]`:
   - after open contains "Winding"
   - after stop contains "Stopped at"
   - after outcome contains the outcome label (e.g. "Perfect tension" / "Over-wound!").
</verification>

<success_criteria>
- Winding run lasts ~5-6s and the crown/tension visibly respond during the run.
- Band rules match Phase 42 boundaries and over-wind shows distinct messaging while mapping to `miss` for rewards.
- Reduced-motion users get progress/tension feedback without rotation.
- Background scroll is locked while the modal is open; Escape closes the modal.
- Existing `winding-*` test IDs remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/42-winding-refresh/42-01-SUMMARY.md`
</output>
