---
phase: 42-winding-refresh
plan: 01
type: execute
wave: 1
depends_on:
  - 41-04
files_modified:
  - src/ui/components/WindingMiniGameModal.tsx
  - src/style.css
  - src/ui/help/helpContent.ts
autonomous: true

must_haves:
  truths:
    - "Winding keeps the timing-based stop mechanic but uses the Phase 42 zones (Under/Good/Perfect/Over-wind)."
    - "Winding crown animation shows increasing tension and visually reacts to the stop/outcome."
    - "Reduced-motion users do not see continuous spinning; feedback remains clear without motion."
  artifacts:
    - path: src/ui/components/WindingMiniGameModal.tsx
      provides: "Winding mini-game mechanic + outcome messaging"
      contains: "winding-modal"
    - path: src/style.css
      provides: "Winding crown/track styling + reduced-motion overrides"
      contains: ".winding-crown"
    - path: src/ui/help/helpContent.ts
      provides: "Player-facing description of the winding interaction"
      contains: "Winding"
  key_links:
    - from: src/App.tsx
      to: src/ui/components/WindingMiniGameModal.tsx
      via: "renders winding modal + handles onComplete"
      pattern: "<WindingMiniGameModal"
    - from: src/ui/components/WindingMiniGameModal.tsx
      to: src/style.css
      via: "winding-* classnames"
      pattern: "winding-"
    - from: src/App.tsx
      to: src/game/state.ts
      via: "applies winding reward"
      pattern: "applyWindingReward"
---

<objective>
Refresh the winding mini-game to feel more like a watch interaction by adding richer animation + feedback and an over-wind penalty zone, while preserving the existing timing-based stop mechanic.

Purpose: Deliver WATCH-01 (Phase 42) as defined in `42-CONTEXT.md`.
Output: Updated winding modal mechanics + styling that are readable on mobile, accessible, and consistent with existing UI conventions.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-winding-refresh/42-CONTEXT.md

@src/App.tsx
@src/ui/components/WindingMiniGameModal.tsx
@src/style.css
@src/ui/help/helpContent.ts
@tests/catalog.unit.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Phase 42 zone thresholds and outcome messaging</name>
  <files>src/ui/components/WindingMiniGameModal.tsx</files>
  <action>
Update the winding run to match the Phase 42 decision doc exactly:

- Change the run duration to ~5–6 seconds (target: 5.5s; allow test-mode override if needed later).
- Replace the centered sweet-spot math with zone thresholds:
  - Under-wind: 0–30% → Miss (25 enjoyment)
  - Good wind: 30–69% → Good (75 enjoyment)
  - Perfect: 70–95% → Perfect (150 enjoyment)
  - Over-wind: >95% → Miss (25 enjoyment) with distinct copy ("Over-wound!")
- Keep `WindingOutcomeTier` as `"miss" | "good" | "perfect"` for compatibility with `applyWindingReward`.
  - Add an internal `missReason`/`zone` discriminator for player-facing messaging only (under vs over), without changing reward plumbing.

Feedback requirements:
- While running, show a concise instruction and a visible “tension rising” cue (text + visuals).
- On stop, show:
  - the zone label (Under-wound / Good wind / Perfect tension / Over-wound!)
  - the reward amount (+X enjoyment) and cooldown note (existing copy is fine)
  - an aria-live announcement that includes the stopped percentage and result.

Keep all existing `data-testid` values stable (`winding-modal`, `winding-track`, `winding-stop`, `winding-outcome`, `winding-done`, `winding-close`).
  </action>
  <verify>pnpm -s run typecheck</verify>
  <done>
- Winding outcomes match the Phase 42 threshold table and communicate under/over misses distinctly.
- No selector/test-id churn.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tension-driven crown animation + penalty band visuals</name>
  <files>
src/ui/components/WindingMiniGameModal.tsx
src/style.css
  </files>
  <action>
Implement visual polish driven by the run progress (deterministic; no randomness):

- Crown:
  - Increase crown size to ~72px and make it visually prominent.
  - While running, animate rotation with speed that increases as progress approaches the Perfect zone.
    - Recommended implementation: set a CSS custom property (e.g. `--winding-spin-ms`) inline and use it as the animation duration.
  - Add a “tension” glow/pulse that intensifies in the Perfect/Over-wind zones (use theme-appropriate gold and a warning tint near over-wind).
  - On stop, remove the continuous rotation and apply a short easing “settle” transform so it doesn’t feel like it snaps off.

- Track:
  - Visually represent:
    - Good band (30–70%) as a subtle safe region
    - Perfect band (70–95%) as the primary highlighted region (gold)
    - Over-wind penalty (95–100%) as a narrow warning region (red)
  - Keep click-to-stop working on the track (touch-friendly).

Reduced-motion:
- Under `prefers-reduced-motion: reduce`, disable continuous spinning and instead use discrete state styling (e.g. opacity/scale pulses or static state changes).
  </action>
  <verify>pnpm -s run test:unit -- tests/catalog.unit.test.tsx</verify>
  <done>
- Winding has a visible “winding feel” (speed/tension) and a clearly visible penalty zone.
- Reduced-motion does not use continuous rotation.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit -- tests/catalog.unit.test.tsx`
</verification>

<success_criteria>
- WATCH-01 satisfied: winding is more interactive/animated and includes an over-wind penalty zone while preserving the timing stop mechanic.
- Winding remains readable and usable on mobile and with reduced motion enabled.
</success_criteria>

<output>
After completion, create `.planning/phases/42-winding-refresh/42-01-SUMMARY.md`
</output>

