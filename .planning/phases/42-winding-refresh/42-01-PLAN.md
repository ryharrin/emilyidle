---
phase: 42-winding-refresh
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/components/winding/useWindingRun.ts
  - src/ui/components/winding/windingMath.ts
  - src/ui/components/winding/WindingCrown.tsx
  - src/ui/components/WindingMiniGameModal.tsx
  - src/style.css
autonomous: true

must_haves:
  truths:
    - "Player can start the winding interaction and sees the crown/track animate responsively during the run"
    - "Player can stop at a chosen moment and sees live progress + band/tension feedback before stopping"
    - "Over-wind (>95%) is visibly distinct and communicated as a penalty state"
    - "Reduced motion disables rotation and still shows reactive feedback"
    - "Primary tap targets in the mini-game meet a 44px minimum"
  artifacts:
    - path: "src/ui/components/winding/useWindingRun.ts"
      provides: "winding run state + telemetry (progress/band/tension/velocity) and stop lifecycle"
    - path: "src/ui/components/WindingMiniGameModal.tsx"
      provides: "winding UI: legend, track, stop/done controls, and conditional outcome"
    - path: "src/ui/components/winding/WindingCrown.tsx"
      provides: "crown visuals driven by band + telemetry"
    - path: "src/style.css"
      provides: "winding animations + reduced-motion behavior + touch target sizing"
  key_links:
    - from: "src/ui/components/WindingMiniGameModal.tsx"
      to: "src/ui/components/winding/useWindingRun.ts"
      via: "hook"
      pattern: "useWindingRun\\("
    - from: "src/ui/components/WindingMiniGameModal.tsx"
      to: "src/ui/components/winding/WindingCrown.tsx"
      via: "props: band + telemetry"
      pattern: "<WindingCrown"
    - from: "src/ui/components/winding/useWindingRun.ts"
      to: "src/ui/components/winding/windingMath.ts"
      via: "band computation"
      pattern: "windingMath|getWindingBand|band"

<objective>
Refresh the winding mini-game so it communicates tension and pace live and animates the crown/track responsively while preserving the existing timing-based stop mechanic.

Purpose: Deliver WATCH-01 by making winding feel tactile and readable (legend + tension + animated crown) without changing the core interaction.
Output: Updated winding run telemetry, modal rendering/state flow, crown component props, and CSS animations (with reduced-motion support).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/42-winding-refresh/42-CONTEXT.md
@.planning/phases/42-winding-refresh/42-RESEARCH.md

@src/ui/components/winding/useWindingRun.ts
@src/ui/components/winding/windingMath.ts
@src/ui/components/winding/WindingCrown.tsx
@src/ui/components/WindingMiniGameModal.tsx
@src/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make useWindingRun expose stable progress + telemetry for animation</name>
  <files>
src/ui/components/winding/useWindingRun.ts
src/ui/components/winding/windingMath.ts
  </files>
  <action>
- Ensure the run starts with deterministic initial state: progress at 0, band derived from 0%, and outcome/result unset until stop.
- Keep the existing timing-based track (total duration 5-6 seconds) and band thresholds, including the penalty zone at >95%.
- Ensure telemetry is consistent and suitable for animation without recomputing in the modal:
  - `progress01` in [0, 1] while running.
  - `band` derived only from progress and the defined thresholds.
  - `tension01` and/or `tensionPercent` derived from progress (0..1 and/or 0..100) and stable at t=0.
  - `velocity01` (or similar) is non-negative and increases as progress approaches the sweet spot (70-95%), and stays high (or spikes) in the penalty zone.
- Keep any browser timing (RAF/nowMs sampling) inside the hook. Keep math helpers pure.
  </action>
  <verify>
pnpm typecheck
pnpm test:unit -- tests/winding-bands.unit.test.ts
  </verify>
  <done>
- The hook returns progress + band + tension + velocity (or equivalent) with deterministic initial values.
- Band thresholds include the >95% over-wind penalty.
- Typecheck and winding band unit tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire the modal UI: legend highlight, live tension/progress, and correct initial state</name>
  <files>
src/ui/components/WindingMiniGameModal.tsx
  </files>
  <action>
- Render the band legend and visually highlight the current band while the run is active.
- Display live progress (percent) and live tension (percent) while running.
- Preserve the stop mechanic: user stops the run by tapping/clicking stop; stopping freezes progress/telemetry and transitions to an outcome view.
- Ensure the outcome UI is NOT rendered until stop (initial outcome must be null/undefined).
- Ensure the over-wind zone (>95%) uses a distinct label/message (e.g. "Over-wound!") once stopped in that zone.
- Ensure touch targets meet 44px using CSS classes (no inline sizing): stop/done and any primary tap target within the mini-game.
- Stabilize test contracts:
  - Stop button: stable accessible name ("Stop") and stable `data-testid="winding-stop"`.
  - Outcome container: stable `data-testid="winding-outcome"`, absent from the DOM until stop.
  </action>
  <verify>
pnpm test:unit -- tests/winding-modal-a11y.unit.test.tsx
  </verify>
  <done>
- Band legend renders and highlights the current band during the run.
- Outcome is absent until stop.
- Stop button is discoverable by role/name and the expected test id.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add responsive crown/track animation with reduced-motion support</name>
  <files>
src/ui/components/winding/WindingCrown.tsx
src/style.css
  </files>
  <action>
- Make `WindingCrown` accept the telemetry needed for animation (band + velocity/tension/progress) and set them as CSS variables on the crown root element (e.g. `--winding-velocity`, `--winding-tension`, `--winding-progress`).
- Update `src/style.css` winding styles to animate the crown and track using those variables:
  - Crown rotates continuously while running; rotation speed increases as velocity increases.
  - Add a subtle glow/pulse whose intensity scales with tension.
  - Over-wind band (>95%) has a danger treatment (color + pulse).
- Add `@media (prefers-reduced-motion: reduce)` overrides:
  - Disable rotation.
  - Keep feedback via scale/opacity pulse tied to tension/velocity.
- Enforce >=44px touch targets via CSS for winding controls/track.
  </action>
  <verify>
pnpm build
  </verify>
  <done>
- Crown/track visuals respond to telemetry via CSS variables.
- Reduced-motion disables rotation but still communicates pace/tension.
- Build passes.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/winding-bands.unit.test.ts tests/winding-modal-a11y.unit.test.tsx`
- `pnpm build`
</verification>

<success_criteria>
- While winding is active, the UI shows live progress + tension and the crown/track animation clearly changes with pace.
- Band legend highlights the current band and the penalty zone is visibly distinct.
- Reduced-motion users see no rotation but still see responsive feedback.
- Primary tap targets meet 44px minimum via CSS.
</success_criteria>

<output>
After completion, create `.planning/phases/42-winding-refresh/42-01-SUMMARY.md`
</output>
