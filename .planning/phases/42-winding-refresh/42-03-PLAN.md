---
phase: 42-winding-refresh
plan: 3
type: execute
wave: 3
  - 42-02
files_modified:
  - src/ui/components/winding/windingMath.ts
  - src/ui/components/WindingMiniGameModal.tsx
  - src/ui/components/winding/WindingCrown.tsx
  - src/style.css
gap_closure: true

must_haves:
  truths:
    - "Over-wind penalties appear only after the run crosses a softer stop margin (≈97%), giving players more room to hit the sweet spot."
    - "Players still see a clear `Over-wound!` warning, crown/track danger styling, and penalty messaging as soon as the harder limit engages."
    - "Reduced-motion and touch-target guarantees remain intact despite the recalibrated penalty spread."
  artifacts:
    - path: "src/ui/components/winding/windingMath.ts"
      provides: "band threshold constants for safe/penalty zones"
    - path: "src/ui/components/WindingMiniGameModal.tsx"
      provides: "banner text + message that references the new penalty margin and warning tone"
    - path: "src/ui/components/winding/WindingCrown.tsx"
      provides: "danger/penalty CSS variables that still light up once the stricter cap is reached"
    - path: "src/style.css"
      provides: "over-wind styles + reduced-motion overrides tied to the new thresholds"
  key_links:
    - from: "WindingMiniGameModal.tsx"
      to: "src/ui/components/winding/windingMath.ts"
      via: "winding band helpers"
      pattern: "getWindingBand"
    - from: "WindingMiniGameModal.tsx"
      to: "src/ui/components/winding/WindingCrown.tsx"
      via: "model"
      pattern: "<WindingCrown"

<objective>
Relax the penalty margin so the winding run feels more forgiving while keeping the over-wind warning visually obvious and the reduced-motion/touch target contracts intact.

Purpose: Tune threshold boundaries so the penalty does not trigger the instant the run crosses the sweet spot but still rewards players who carefully time their stops.
Output: Updated band math, a clearer warning message, and regression coverage aligned with the softer margin.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/42-winding-refresh/42-01-PLAN.md
@.planning/phases/42-winding-refresh/42-02-PLAN.md

@src/ui/components/winding/windingMath.ts
@src/ui/components/WindingMiniGameModal.tsx
@src/ui/components/winding/WindingCrown.tsx
@src/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calibrate over-wind thresholds for a softer stop margin</name>
  <files>
src/ui/components/winding/windingMath.ts
  </files>
  <action>
- Adjust the constants (percent thresholds) so the “good/perfect” bands extend up through ≈97% before the penalty takes effect, while still marking >98.5% as the over-wind zone for the visible warning.
- Ensure the helper outputs (progress%, band, tension, velocity) remain deterministic, and add derived labels/flags that the modal can read to display the softer margin.
- Keep the >95% penalty path available (for the warning copy) but make the strict penalty flag require the new higher threshold.
  </action>
  <verify>
pnpm test:unit -- tests/winding-bands.unit.test.ts
  </verify>
  <done>
- Threshold helper still classifies progress as expected with the new cut points and exposes a `softPenalty` flag for the modal.
- Tests cover the updated boundaries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update modal/UX copy to describe the relaxed margin</name>
  <files>
src/ui/components/WindingMiniGameModal.tsx
src/style.css
  </files>
  <action>
- Mention the new “stop before the red glow” messaging so players understand they now have an easier window before penalty styling appears.
- Tie the message to the CSS variables (e.g., use the `softPenalty` flag to trigger a “closing in on over-wind” highlight) while the full penalty still shows the `Over-wound!` warning.
- Keep reduced-motion classes and 44px touch target rules unchanged.
  </action>
  <verify>
pnpm test:unit -- tests/winding-modal-a11y.unit.test.tsx
  </verify>
  <done>
- Modal copy references the relaxed margin and the light warning state once `softPenalty` is true without stealing focus.
- Over-wind warning appears as soon as the stricter threshold is reached.
  </done>
</task>

<task type="auto">
  <name>Task 3: Harden regression coverage around the new margins</name>
  <files>
tests/winding-bands.unit.test.ts
tests/winding-modal-a11y.unit.test.tsx
tests/catalog.unit.test.tsx
  </files>
  <action>
- Extend the band tests to assert the new cut points (e.g., 0.969 remains perfect, 0.971 still perfect, 0.985 triggers `softPenalty`, and 0.986+ triggers `over`).
- Update the modal/catalog tests to expect the “soft warning” copy (if present) before the full penalty and ensure the `data-testid="winding-outcome"` remains hidden until stop.
- Ensure the catalog regression still fails if the warning copy appears before the user stops.
  </action>
  <verify>
pnpm test:unit -- tests/winding-bands.unit.test.ts tests/winding-modal-a11y.unit.test.tsx tests/catalog.unit.test.tsx
  </verify>
  <done>
- Tests fail if the soft margin triggers the penalty copy too early.
- All relevant test suites pass with the new thresholds.
  </done>
</task>

</tasks>

<verification>
- `pnpm test:unit -- tests/winding-bands.unit.test.ts tests/winding-modal-a11y.unit.test.tsx tests/catalog.unit.test.tsx`
</verification>

<success_criteria>
- Penalty styling now requires ≈97% progress before the `softPenalty` warning appears, and `Over-wound!` still triggers after 98.5%.
- Modal copy/legend clue users about the relaxed margin while still highlighting the hard limit.
- Regression tests guard the new boundaries and warn if the warning appears too early.
</success_criteria>

<output>
After completion, create `.planning/phases/42-winding-refresh/42-03-SUMMARY.md`
</output>
