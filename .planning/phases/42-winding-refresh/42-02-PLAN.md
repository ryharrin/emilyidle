---
phase: 42-winding-refresh
plan: 2
type: execute
wave: 2
depends_on:
  - 42-01
files_modified:
  - src/ui/components/WindingMiniGameModal.tsx
  - tests/winding-modal-a11y.unit.test.tsx
  - tests/catalog.unit.test.tsx
  - tests/winding-bands.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "On open, the winding modal renders a discoverable Stop control"
    - "Before stopping, the winding outcome UI is not present (result is null/hidden)"
    - "After stopping, the winding outcome UI appears and reflects the stopped band"
    - "Catalog does not show a winding outcome until the player stops the run"
  artifacts:
    - path: "tests/winding-modal-a11y.unit.test.tsx"
      provides: "a11y and DOM contract coverage for winding modal controls"
    - path: "tests/catalog.unit.test.tsx"
      provides: "regression coverage for winding outcome visibility in catalog flows"
    - path: "tests/winding-bands.unit.test.ts"
      provides: "regression coverage for winding band thresholds (incl. over-wind)"
  key_links:
    - from: "tests/winding-modal-a11y.unit.test.tsx"
      to: "src/ui/components/WindingMiniGameModal.tsx"
      via: "role/name and test ids"
      pattern: "WindingMiniGameModal|winding-stop|winding-outcome"
    - from: "tests/catalog.unit.test.tsx"
      to: "src/ui/components/WindingMiniGameModal.tsx"
      via: "outcome visibility"
      pattern: "winding-outcome"

<objective>
Fix current Phase 42 test failures and add tight regression coverage so the refreshed winding mini-game is stable: stop button is discoverable and outcome remains hidden until the user stops.

Purpose: Lock in WATCH-01 UX contracts and prevent regressions around initialization/conditional rendering.
Output: Updated tests + any minimal modal wiring fixes required to satisfy those contracts.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/42-winding-refresh/42-01-PLAN.md
@src/ui/components/WindingMiniGameModal.tsx
@tests/winding-modal-a11y.unit.test.tsx
@tests/catalog.unit.test.tsx
@tests/winding-bands.unit.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and fix premature outcome initialization (result must be null until stop)</name>
  <files>
src/ui/components/WindingMiniGameModal.tsx
tests/catalog.unit.test.tsx
  </files>
  <action>
- Reproduce the failing unit test and identify where the modal/catalog code sets the winding result/outcome too early (e.g., derived from initial progress/band instead of a "stopped" snapshot).
- Change modal state flow so that:
  - Outcome/result is `null` (or equivalent) on first render.
  - Outcome/result only becomes non-null when the user activates Stop.
  - Outcome UI uses the stopped snapshot (progress/band/tension captured at stop), not a live value.
- Update `tests/catalog.unit.test.tsx` to assert the correct contract:
  - On initial open/render, outcome element is absent (`queryBy...` returns null).
  - After triggering stop (simulated click), outcome element appears.
  </action>
  <verify>
pnpm test:unit -- tests/catalog.unit.test.tsx
  </verify>
  <done>
- `tests/catalog.unit.test.tsx` passes and explicitly guards "outcome is hidden until stop".
- The modal no longer sets or renders outcome on open.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix winding-stop discoverability and stabilize a11y/test selectors</name>
  <files>
src/ui/components/WindingMiniGameModal.tsx
tests/winding-modal-a11y.unit.test.tsx
  </files>
  <action>
- Reproduce the a11y test failure and align the implementation + test on an explicit, stable contract.
- Ensure the Stop control is present in the DOM while the run is active and is discoverable by:
  - `role="button"` and accessible name including "Stop".
  - `data-testid="winding-stop"` (if the test suite expects it).
- Ensure the outcome container uses `data-testid="winding-outcome"` and is absent until stop.
- Update `tests/winding-modal-a11y.unit.test.tsx` to prefer role/name queries where possible, while still anchoring on stable `data-testid` for contract-critical nodes.
  </action>
  <verify>
pnpm test:unit -- tests/winding-modal-a11y.unit.test.tsx
  </verify>
  <done>
- The Stop button is found by the test without relying on fragile text placement.
- Outcome container remains absent until stop.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend winding band math regression tests (include penalty zone and sweet spot edges)</name>
  <files>
tests/winding-bands.unit.test.ts
  </files>
  <action>
- Add explicit boundary-case tests around the band thresholds described in phase context:
  - Under-wind upper bound and Good lower bound (around 30%).
  - Good/Perfect boundary (around 70%).
  - Perfect/Over-wind boundary (around 95%).
- Include at least one test asserting that >95% maps to the over-wind/penalty band.
- Keep tests resilient to minor floating precision by using representative percent inputs (e.g., 0.299, 0.301, 0.699, 0.701, 0.949, 0.951).
  </action>
  <verify>
pnpm test:unit -- tests/winding-bands.unit.test.ts
  </verify>
  <done>
- Band math tests cover all three boundaries and the penalty zone mapping.
  </done>
</task>

</tasks>

<verification>
- `pnpm test:unit`
</verification>

<success_criteria>
- `tests/winding-modal-a11y.unit.test.tsx` passes and reliably finds the Stop button.
- `tests/catalog.unit.test.tsx` passes and asserts outcome is absent until stop.
- `tests/winding-bands.unit.test.ts` covers threshold boundaries including over-wind.
- Full unit suite passes.
</success_criteria>

<output>
After completion, create `.planning/phases/42-winding-refresh/42-02-SUMMARY.md`
</output>
