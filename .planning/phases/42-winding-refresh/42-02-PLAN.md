---
phase: 42-winding-refresh
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - tests/winding-bands.unit.test.ts
  - tests/winding-modal-a11y.unit.test.tsx
  - tests/catalog.unit.test.tsx
  - tests/collection-loop.spec.ts
autonomous: true

must_haves:
  truths:
    - "Band boundary rules (0-30, 30-69, 70-95, >95) are locked by unit tests"
    - "Escape closes the winding modal, scroll is locked while open, and focus stays inside the modal (unit test)"
    - "Winding aria-live announcements update for start/stop/outcome (unit test)"
    - "Winding flow remains stable in unit + e2e tests via the existing winding testids"
  artifacts:
    - path: tests/winding-bands.unit.test.ts
      provides: "Regression coverage for Phase 42 band mapping + domain tier mapping"
    - path: tests/winding-modal-a11y.unit.test.tsx
      provides: "Regression coverage for Escape close, scroll lock, focus trap, and aria-live announcements"
  key_links:
    - from: tests/winding-bands.unit.test.ts
      to: src/ui/components/winding/windingMath.ts
      via: direct imports of band mapping functions
      pattern: "from .*windingMath"
---

<objective>
Add regression tests for the Phase 42 winding band boundaries and confirm unit + e2e winding flows remain stable with unchanged test selectors.

Purpose: Prevent future regressions to winding scoring bands and keep the existing test contract intact.
Output: A small unit test suite plus updated tests only if needed.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-winding-refresh/42-CONTEXT.md
@.planning/phases/42-winding-refresh/42-RESEARCH.md

@src/ui/components/winding/windingMath.ts
@tests/catalog.unit.test.tsx
@tests/collection-loop.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unit tests for Phase 42 band mapping boundaries</name>
  <files>tests/winding-bands.unit.test.ts</files>
  <action>
Create `tests/winding-bands.unit.test.ts` to lock the Phase 42 band rules and domain tier mapping:
- Import `getWindingBand` and `getOutcomeTierFromBand` from `src/ui/components/winding/windingMath.ts`.
- Assert boundary behavior with representative inputs:
  - `0`, `0.299` => `under`
  - `0.3`, `0.69` => `good`
  - `0.7`, `0.95` => `perfect`
  - `0.951`, `1` => `over`
- Assert tier mapping:
  - `under` => `miss`
  - `good` => `good`
  - `perfect` => `perfect`
  - `over` => `miss`

Keep the test fast and pure (no DOM).
  </action>
  <verify>pnpm test:unit -- tests/winding-bands.unit.test.ts</verify>
  <done>Band mapping boundaries are covered and the test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix up any test assertions affected by copy/structure changes</name>
  <files>tests/catalog.unit.test.tsx, tests/collection-loop.spec.ts</files>
  <action>
Run the existing winding tests and adjust ONLY if needed:
- Keep all `winding-*` selectors stable; do not change tests to new selectors.
- If assertions are too copy-specific, update them to assert via stable anchors (testids) and resilient regex (e.g., `/enjoyment/i`).
- Ensure Playwright winding test still:
  - opens modal
  - clicks `winding-stop`
  - sees `winding-outcome`
  - clicks `winding-done`
  - observes enjoyment increased and cooldown disables interaction
  </action>
  <verify>pnpm test:unit -- tests/catalog.unit.test.tsx -t "wind minigame" && pnpm test:e2e -- tests/collection-loop.spec.ts</verify>
  <done>Unit and e2e winding flows pass without changing testid contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for winding modal a11y + focus/scroll behavior</name>
  <files>tests/winding-modal-a11y.unit.test.tsx</files>
  <action>
Create `tests/winding-modal-a11y.unit.test.tsx` (Testing Library + user-event) to assert key must-haves from `42-01`:
- Render `<App />` and navigate to Catalog (follow the setup pattern in `tests/catalog.unit.test.tsx` wind tests).
- Open the winding modal by clicking an enabled `vault-interact-*` button.
- Assert scroll lock behavior (matches `HelpModal`): `document.body.style.overflow` becomes `hidden` while open and is restored after close.
- Assert initial focus behavior: after open, `document.activeElement` is the Stop button (`data-testid="winding-stop"`).
- Assert focus trap behavior:
  - Press `Tab` repeatedly and assert `document.activeElement` always remains within the modal container (`data-testid="winding-modal"`).
  - Press `Shift+Tab` and assert the same (no focus escape to background).
- Assert aria-live text updates via the dedicated live region test hook:
  - Find `[data-testid="winding-live"]` and assert it contains "Winding" shortly after open.
  - Click `winding-stop`; assert live region contains "Stopped at".
  - Assert the outcome label is present either in the live region OR in `winding-outcome` (prefer live region).
- Assert Escape closes: dispatch `Escape` keydown and ensure `winding-modal` unmounts.

Keep the test resilient:
- Prefer queries by role/testid; avoid relying on exact copy beyond small stable substrings ("Winding", "Stopped at").
- Do not change existing `winding-*` test ids.
  </action>
  <verify>pnpm test:unit -- tests/winding-modal-a11y.unit.test.tsx</verify>
  <done>Unit test passes and covers Escape close, scroll lock, focus trap, and aria-live updates.</done>
</task>

</tasks>

<verification>
- `pnpm test:unit -- tests/winding-bands.unit.test.ts`
- `pnpm test:unit -- tests/winding-modal-a11y.unit.test.tsx`
- `pnpm test:unit -- tests/catalog.unit.test.tsx -t "wind minigame"`
- `pnpm test:e2e -- tests/collection-loop.spec.ts`
</verification>

<success_criteria>
- Band boundary rules are enforced by unit tests.
- Winding unit + e2e coverage passes without changing any `winding-*` test IDs.
</success_criteria>

<output>
After completion, create `.planning/phases/42-winding-refresh/42-02-SUMMARY.md`
</output>
