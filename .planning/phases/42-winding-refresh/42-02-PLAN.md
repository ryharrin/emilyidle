---
phase: 42-winding-refresh
plan: 02
type: execute
wave: 2
depends_on:
  - 42-01
files_modified:
  - tests/winding-refresh.unit.test.ts
  - tests/ui-screenshots.spec.ts
autonomous: true

must_haves:
  truths:
    - "Unit tests cover the winding zone boundaries (under/good/perfect/over) to prevent regressions."
    - "A visual artifact exists for the refreshed winding modal (desktop and/or mobile)."
  artifacts:
    - path: tests/winding-refresh.unit.test.ts
      provides: "Deterministic boundary tests for winding outcome mapping"
      contains: "over-wind"
    - path: tests/ui-screenshots.spec.ts
      provides: "Screenshot captures for winding modal UI"
      contains: "winding"
  key_links:
    - from: tests/winding-refresh.unit.test.ts
      to: src/ui/components/WindingMiniGameModal.tsx
      via: "imports exported outcome helper"
      pattern: "WindingMiniGameModal"
---

<objective>
Add regression coverage and visual artifacts for the winding refresh so the new zones and styling do not drift over time.

Purpose: Protect WATCH-01 once Phase 42 is executed.
Output: Deterministic unit boundary tests + at least one screenshot capture path for review.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/42-winding-refresh/42-CONTEXT.md
@src/ui/components/WindingMiniGameModal.tsx
@tests/catalog.unit.test.tsx
@tests/ui-screenshots.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic unit tests for winding outcome zones</name>
  <files>
tests/winding-refresh.unit.test.ts
src/ui/components/WindingMiniGameModal.tsx
  </files>
  <action>
Add pure, deterministic unit coverage for the new threshold mapping without relying on RAF timing:

- Extract (or add) a small pure helper in `WindingMiniGameModal.tsx` that maps `progress (0..1)` to:
  - `tier` (`miss|good|perfect`)
  - `zone` (`under|good|perfect|over`) or `missReason` for messaging
- Export the helper (or move it to a small `src/ui/interactions/winding.ts` module) so tests can import it.

Create `tests/winding-refresh.unit.test.ts` and assert boundary behavior at representative points:
- 0.0, 0.29 → under miss
- 0.30, 0.69 → good
- 0.70, 0.95 → perfect
- 0.96, 1.0 → over miss

Keep expectations stable and small (avoid snapshots).
  </action>
  <verify>pnpm -s run test:unit -- tests/winding-refresh.unit.test.ts</verify>
  <done>
- Tests fail if the Phase 42 thresholds drift or the over-wind zone is removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Capture winding modal screenshots for review (desktop + mobile)</name>
  <files>tests/ui-screenshots.spec.ts</files>
  <action>
Extend `tests/ui-screenshots.spec.ts` to capture the refreshed winding modal as a manual-review artifact:

- Seed localStorage with a save that owns at least one manual watch tier (chronograph).
- Navigate to Catalog, click the interact button to open the winding modal.
- Capture a screenshot for desktop and mobile:
  - `/tmp/ui-screenshots/13-winding-modal.png`
  - `/tmp/ui-screenshots/14-mobile-winding-modal.png`

Notes:
- This test is an artifact generator; avoid brittle pixel assertions.
- If a deterministic “over-wind” screenshot is desired, add a test-mode hook only when necessary (prefer not adding new global hooks).
  </action>
  <verify>pnpm -s run test:e2e -- tests/ui-screenshots.spec.ts</verify>
  <done>
- Screenshots exist on disk and reflect the new track bands + crown visuals.
  </done>
</task>

</tasks>

<verification>
- `pnpm run test:unit -- tests/winding-refresh.unit.test.ts`
- `pnpm run test:e2e -- tests/ui-screenshots.spec.ts`
</verification>

<success_criteria>
- The winding refresh has both a deterministic unit guardrail and a visual artifact for future comparison.
</success_criteria>

<output>
After completion, create `.planning/phases/42-winding-refresh/42-02-SUMMARY.md`
</output>

