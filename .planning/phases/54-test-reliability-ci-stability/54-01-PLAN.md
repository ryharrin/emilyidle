---
phase: 54-test-reliability-ci-stability
plan: 1
type: execute
depends_on: []
files_modified:
  - tests/collection-loop.spec.ts
  - tests/achievements-toast.spec.ts
---

<objective>
Eliminate strict-mode selector ambiguity in high-failure e2e specs by switching to unique,
intent-specific locator anchors.
</objective>

<tasks>
- Replace ambiguous `getByRole(... name: "Import")` with unique save-import trigger selectors.
- Replace broad text matches (`Auction weekend`, `Filters`) with specific `data-testid` or scoped locators.
- Keep behavior assertions intact while removing selector collisions.
</tasks>

<verification>
- `pnpm test:e2e --project=chromium -- tests/collection-loop.spec.ts -g "export and import save round trip|achievements and events panels render"`
- `pnpm test:e2e --project=chromium-mobile-pixel5 -- tests/achievements-toast.spec.ts`
</verification>
