---
phase: 48-session-atelier
plan: 7
type: execute
wave: 2
depends_on:
  - 48-06
files_modified:
  - src/game/selectors/index.ts
  - src/game/state.ts
  - src/ui/tabs/WorkshopTab.tsx
  - src/ui/tabs/UpgradesTab.tsx
  - tests/workshop-atelier.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "Blueprints display the current cost and the next cost side-by-side."
    - "The cost values are computed in selectors and used consistently across Workshop and Upgrades UI."
    - "Tooltip copy lists the specific rewards unlocked, without cluttering the main panel."
  artifacts:
    - path: "src/game/selectors/index.ts"
      provides: "Blueprint cost detail helper (current + next)"
    - path: "src/ui/tabs/WorkshopTab.tsx"
      provides: "Workshop reset panel shows current and next blueprint cost"
    - path: "src/ui/tabs/UpgradesTab.tsx"
      provides: "Workshop upgrades surface blueprint cost detail consistently"
  key_links:
    - from: "src/ui/tabs/UpgradesTab.tsx"
      to: "src/game/state.ts"
      via: "imports blueprint cost helper"
      pattern: "getWorkshop.*Blueprint"

<objective>
Improve blueprint clarity by showing the current and next blueprint cost side-by-side, with reward details in a tooltip.

Purpose: ATELIER-02 helps players plan upgrades and understand the next threshold.
Output: Selector helper + Workshop/Upgrades UI wiring + tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/48-session-atelier/48-CONTEXT.md
@src/ui/tabs/WorkshopTab.tsx
@src/ui/tabs/UpgradesTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add blueprint cost detail selector helper (current + next)</name>
  <files>
src/game/selectors/index.ts
src/game/state.ts
tests/workshop-atelier.unit.test.ts
  </files>
  <action>
Add a pure helper that returns blueprint cost details used by UI.

Requirements:
- Output includes:
  - `currentCostCents`
  - `nextCostCents`
  - `deltaCents` (optional)
  - `hasNext: boolean`
- Costs must be derived from the authoritative blueprint threshold/cost model (do not duplicate formulas in UI).
- Re-export from `src/game/state.ts`.

Add unit tests verifying:
- `nextCostCents >= currentCostCents` (monotonic).
- Costs change when blueprint count changes.
  </action>
  <verify>pnpm test:unit -- tests/workshop-atelier.unit.test.ts</verify>
  <done>
Selector helper exists, exported via facade, and unit tests cover monotonic + state-dependent behavior.
  </done>
</task>

<task type="auto">
  <name>Task 2: Show current + next blueprint cost in Workshop and Upgrades UI</name>
  <files>
src/ui/tabs/WorkshopTab.tsx
src/ui/tabs/UpgradesTab.tsx
  </files>
  <action>
Update Workshop UI:
- Add a side-by-side cost display (two columns or stacked rows) labeled "Current" and "Next".
- Add tooltip content listing the specific rewards unlocked (enjoyment %, cash %, new tier) as specified in Phase 48 context.

Update Upgrades UI:
- Wherever blueprint cost is shown for workshop-related upgrades, render the same current/next detail.

Avoid changing or adding new localStorage keys.
  </action>
  <verify>pnpm typecheck</verify>
  <done>
Blueprint costs are displayed consistently in Workshop and Upgrades with current/next visibility.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/workshop-atelier.unit.test.ts`
</verification>

<success_criteria>
- Players can see current and next blueprint costs and understand rewards via tooltip copy.
</success_criteria>

<output>
After completion, create `.planning/phases/48-session-atelier/48-07-SUMMARY.md`
</output>
