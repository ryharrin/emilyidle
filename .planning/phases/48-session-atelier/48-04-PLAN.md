---
phase: 48-session-atelier
plan: 4
type: execute
wave: 2
depends_on:
  - 48-03
files_modified:
  - src/game/selectors/interactions.ts
  - src/game/state.ts
  - src/ui/tabs/CatalogTab.tsx
  - src/App.tsx
  - tests/catalog.unit.test.tsx
  - tests/quartz-alignment.spec.ts
autonomous: true

must_haves:
  truths:
    - "Winding is only available on hand-wind and quartz watches; automatic models do not expose winding controls."
    - "UI shows a clear disabled reason when winding is not applicable (movement gating)."
    - "Gating is enforced in selectors (not only UI) so actions remain consistent under tests and future UI refactors."
  artifacts:
    - path: "src/game/selectors/interactions.ts"
      provides: "Movement-aware availability helper(s) and disabled reason"
    - path: "src/ui/tabs/CatalogTab.tsx"
      provides: "Catalog interaction buttons honor movement gating + show reason"
  key_links:
    - from: "src/ui/tabs/CatalogTab.tsx"
      to: "src/game/selectors/interactions.ts"
      via: "availability + reason rendering"
      pattern: "isInteractionAvailable|getInteraction"

<objective>
Add movement-aware gating so only hand-wind/quartz watches can wind; automatic models disable the control and explain why.

Purpose: WIND-02 ensures the new drag-based winding experience is only offered when it makes sense.
Output: Selector gating + UI wiring + regression coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/48-session-atelier/48-CONTEXT.md
@.planning/phases/48-session-atelier/48-RESEARCH.md

@src/game/selectors/interactions.ts
@src/ui/tabs/CatalogTab.tsx
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add selector-level movement gating + disabled reason</name>
  <files>
src/game/selectors/interactions.ts
src/game/state.ts
tests/catalog.unit.test.tsx
  </files>
  <action>
Implement a pure helper that answers whether winding is applicable based on the watch movement type.

Requirements:
- Only allow winding for movement types: hand-wind and quartz.
- Automatic movement returns unavailable with a reason string (e.g. "Automatic watches don’t wind by crown drag").
- Export via `src/game/state.ts` so UI consumes the game facade.

Update unit coverage in `tests/catalog.unit.test.tsx` (or a more appropriate existing test file if there is a dedicated interactions unit test):
- For an automatic model, the helper returns `{ available: false, reason: string }`.
- For hand-wind/quartz, helper returns available.
  </action>
  <verify>
pnpm typecheck
pnpm test:unit -- tests/catalog.unit.test.tsx
  </verify>
  <done>
Selector gating exists, exports from facade, and unit tests cover movement cases.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire gating into Catalog interactions + quartz wiring stays intact</name>
  <files>
src/ui/tabs/CatalogTab.tsx
src/App.tsx
tests/quartz-alignment.spec.ts
  </files>
  <action>
Update Catalog interaction button(s) to:
- Disable winding affordances for automatic watches.
- Surface the selector-provided reason string in the same UX pattern used for other disabled purchase/interaction explanations.

Ensure quartz interaction still routes to the quartz modal and that its tests remain stable.
- If existing quartz/winding buttons share UI, keep `data-testid` values stable and adjust only copy where needed.

Update Playwright `tests/quartz-alignment.spec.ts` only if required due to new gating behavior/copy; do not weaken the test.
  </action>
  <verify>
pnpm test:e2e -- tests/quartz-alignment.spec.ts
  </verify>
  <done>
- Automatic watches do not show/enable winding.
- Quartz wiring still works and e2e coverage remains passing.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/catalog.unit.test.tsx`
- `pnpm test:e2e -- tests/quartz-alignment.spec.ts`
</verification>

<success_criteria>
- Winding is only offered for hand-wind/quartz and disabled for automatic with a clear reason.
</success_criteria>

<output>
After completion, create `.planning/phases/48-session-atelier/48-04-SUMMARY.md`
</output>
