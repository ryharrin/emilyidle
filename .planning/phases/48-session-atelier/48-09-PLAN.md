---
phase: 48-session-atelier
plan: 9
type: execute
wave: 3
depends_on:
  - 48-02
files_modified:
  - src/game/selectors/therapistSalary.ts
  - src/game/state.ts
  - src/ui/tabs/career/CareerPanel.tsx
  - tests/career-salary-window.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "When the therapist salary window is close to expiring, the UI shows a clear alert."
    - "Alert timing is deterministic and driven by nowMs (no Date.now leakage)."
    - "The alert helps the player act (refresh salary window) without being noisy when far from expiration."
  artifacts:
    - path: "src/game/selectors/therapistSalary.ts"
      provides: "Selector helper(s) for salary remaining ms + alert severity"
    - path: "src/ui/tabs/career/CareerPanel.tsx"
      provides: "Salary expiration alert rendered in the Career panel"
  key_links:
    - from: "src/ui/tabs/career/CareerPanel.tsx"
      to: "src/game/selectors/therapistSalary.ts"
      via: "alert detail read from selector"
      pattern: "therapistSalary"

<objective>
Add a salary window expiration alert so players know when their income is about to drop and can refresh the window proactively.

Purpose: SALARY-01 reduces "why did my cash stop" surprises.
Output: Selector-based remaining/alert logic + Career UI banner/row + unit tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/game/selectors/therapistSalary.ts
@src/ui/tabs/career/CareerPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add selector helper for salary remaining + alert level</name>
  <files>
src/game/selectors/therapistSalary.ts
src/game/state.ts
tests/career-salary-window.unit.test.ts
  </files>
  <action>
Add pure helpers:
- `getTherapistSalaryRemainingMs(state, nowMs)` -> `number` (0 if inactive)
- `getTherapistSalaryExpirationAlert(state, nowMs)` -> `{ level: 'none' | 'soon' | 'urgent', remainingMs: number }`

Alert thresholds (can be tuned, but must be explicit):
- soon: remaining <= 2 minutes
- urgent: remaining <= 30 seconds

Update unit tests to cover:
- inactive salary => none
- active with remaining above thresholds => none
- active within soon/urgent => correct level + remainingMs

Re-export from `src/game/state.ts`.
  </action>
  <verify>
pnpm typecheck
pnpm test:unit -- tests/career-salary-window.unit.test.ts
  </verify>
  <done>
Selector helper returns deterministic alert detail and tests lock in behavior.
  </done>
</task>

<task type="auto">
  <name>Task 2: Render salary expiration alert in Career panel</name>
  <files>
src/ui/tabs/career/CareerPanel.tsx
  </files>
  <action>
Render an alert row/banner in the Career header or Sessions card when the selector says level is soon/urgent.

Requirements:
- Use a compact style that does not dominate the panel.
- Include remaining seconds/minutes in the message.
- Add stable anchor `data-testid="career-salary-expiration"`.
- Ensure it only shows when salary is active (avoid confusing new players before career start).
  </action>
  <verify>pnpm typecheck</verify>
  <done>
Salary expiration alert appears at the right time and is test-anchorable.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/career-salary-window.unit.test.ts`
</verification>

<success_criteria>
- Players are warned shortly before salary expires, with deterministic logic and clear copy.
</success_criteria>

<output>
After completion, create `.planning/phases/48-session-atelier/48-09-SUMMARY.md`
</output>
