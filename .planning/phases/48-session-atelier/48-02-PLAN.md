---
phase: 48-session-atelier
plan: 2
type: execute
wave: 2
depends_on:
  - 48-01
files_modified:
  - src/ui/components/CooldownRing.tsx
  - src/style.css
  - src/ui/tabs/career/CareerPanel.tsx
  - tests/modal-interactions.spec.ts
autonomous: false

must_haves:
  truths:
    - "While a therapist session is on cooldown, the UI shows a circular progress ring next to the action button."
    - "The ring progresses smoothly and stays consistent with the numeric cooldown seconds label."
    - "Switching tabs or closing overlays does not pause the ring; it catches up based on nowMs."
  artifacts:
    - path: "src/ui/components/CooldownRing.tsx"
      provides: "Reusable SVG ring component that renders progress via stroke dash offset"
    - path: "src/ui/tabs/career/CareerPanel.tsx"
      provides: "Session button includes a ring + cooldown label wiring"
    - path: "src/style.css"
      provides: "Cooldown ring sizing/transition styling + reduced-motion behavior"
  key_links:
    - from: "src/ui/tabs/career/CareerPanel.tsx"
      to: "src/ui/components/CooldownRing.tsx"
      via: "renders ring using cooldown progress ratio"
      pattern: "<CooldownRing"

<objective>
Add a visual circular cooldown timer for therapist sessions, rendered next to the session action button and driven by the same nowMs-based cooldown math.

Purpose: SESSION-02 makes cooldown urgency readable at a glance and reinforces that cooldown keeps running outside the view.
Output: Reusable SVG ring component + Career UI wiring + regression anchor.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/48-session-atelier/48-CONTEXT.md
@.planning/phases/48-session-atelier/48-RESEARCH.md

@src/ui/tabs/career/CareerPanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable SVG cooldown ring component</name>
  <files>
src/ui/components/CooldownRing.tsx
src/style.css
  </files>
  <action>
Create `CooldownRing` as a small SVG component that renders a circular track + progress arc via `strokeDasharray`/`strokeDashoffset`.

Requirements:
- Props: `progress01: number` (clamped 0..1), `sizePx?: number`, `strokeWidthPx?: number`, `label?: string`.
- Render a background track circle plus a foreground progress circle rotated -90deg.
- Apply a short CSS transition on dashoffset for smoothing (avoid a JS animation loop).
- Add `@media (prefers-reduced-motion: reduce)` styling to disable the transition.
- Keep colors themeable via CSS variables (don’t hardcode in the component).
  </action>
  <verify>pnpm typecheck</verify>
  <done>
CooldownRing renders deterministically and can be styled via CSS variables.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire ring into session action button with stable test anchor</name>
  <files>
src/ui/tabs/career/CareerPanel.tsx
tests/modal-interactions.spec.ts
  </files>
  <action>
Update the Sessions card action row:
- When on cooldown (`career.nextAvailableAtMs > nowMs`), show the ring next to the "Run session" button.
- Compute progress using the same underlying `remainingMs` as the numeric label:
  - `remainingMs = max(0, nextAvailableAtMs - nowMs)`
  - `progress01 = clamp01(1 - remainingMs / cooldownMs)`
- Keep the numeric cooldown seconds label behavior unchanged.
- Add `data-testid="career-session-cooldown-ring"` on the ring wrapper.

Update Playwright coverage minimally:
- In `tests/modal-interactions.spec.ts`, add a small assertion that the ring appears when the session is disabled due to cooldown (seed a state where career has `nextAvailableAtMs` in the future).
  </action>
  <verify>pnpm test:e2e -- tests/modal-interactions.spec.ts</verify>
  <done>
- Ring appears during cooldown and is derived from the same `remainingMs` as the label.
- Playwright assertion guards the ring DOM anchor.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Cooldown ring next to the therapist session action</what-built>
  <how-to-verify>
1. Run `pnpm dev` and open the Career tab.
2. Perform a session so the button enters cooldown.
3. Confirm:
   - The ring animates forward smoothly.
   - The numeric "Cooldown Ns" label decreases and reaches 0 when the ring finishes.
   - Switch to another tab for ~5 seconds, then return: the ring has advanced (no pause).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what feels off (timing mismatch, jumpiness, styling).</resume-signal>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:e2e -- tests/modal-interactions.spec.ts`
</verification>

<success_criteria>
- Cooldown UI communicates time remaining both numerically and visually, and stays consistent across tab switches.
</success_criteria>

<output>
After completion, create `.planning/phases/48-session-atelier/48-02-SUMMARY.md`
</output>
