---
phase: 48-session-atelier
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/components/NextUnlockPanel.tsx
  - src/ui/components/UnlockHint.tsx
  - src/ui/tabs/CollectionTab.tsx
  - tests/selectors-contract.spec.ts
autonomous: false

must_haves:
  truths:
    - "Players can preview the next unlock (what it is + what it does) before it happens."
    - "The preview stays stable and does not rely on brittle copy-only selectors in tests."
    - "Unlock preview content comes from selector-backed detail (no duplicated requirement math)."
  artifacts:
    - path: "src/ui/components/NextUnlockPanel.tsx"
      provides: "Next unlock preview card with concise effect preview"
    - path: "src/ui/components/UnlockHint.tsx"
      provides: "Inline unlock hint supports preview rendering"
    - path: "src/ui/tabs/CollectionTab.tsx"
      provides: "Collection tab wires next unlock preview in a visible place"
  key_links:
    - from: "src/ui/tabs/CollectionTab.tsx"
      to: "src/ui/components/NextUnlockPanel.tsx"
      via: "render"
      pattern: "NextUnlockPanel"

<objective>
Add a richer "next unlock" preview so players understand what they are working toward and what the unlock will change.

Purpose: UNLOCK-01 improves motivation and reduces surprise unlocks with unclear effects.
Output: Expanded preview UI + stable test anchor + e2e contract update.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/ui/components/NextUnlockPanel.tsx
@src/ui/tabs/CollectionTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand next-unlock preview content with effect summary</name>
  <files>
src/ui/components/NextUnlockPanel.tsx
src/ui/components/UnlockHint.tsx
  </files>
  <action>
Update the next unlock preview to include a short effect summary (1-2 lines) alongside the requirement/progress.

Guidelines:
- Prefer structured rendering (icon + title + effect line) over long paragraphs.
- Add a stable DOM anchor for tests: `data-testid="next-unlock-preview"`.
- Keep the requirement/progress calculation sourced from existing selector helpers (do not duplicate requirement math in UI).
  </action>
  <verify>pnpm typecheck</verify>
  <done>
Next unlock preview includes a concise effect summary and stable test anchor.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire preview into Collection tab and update selector contract test</name>
  <files>
src/ui/tabs/CollectionTab.tsx
tests/selectors-contract.spec.ts
  </files>
  <action>
Ensure the next unlock preview is visible in the Collection tab in a consistent place.

Update `tests/selectors-contract.spec.ts` to assert the presence of `data-testid="next-unlock-preview"` when an unlock is pending.
  </action>
  <verify>pnpm test:e2e -- tests/selectors-contract.spec.ts</verify>
  <done>
Preview is present and e2e contract guards the anchor.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Next unlock effect preview</what-built>
  <how-to-verify>
1. Run `pnpm dev` and open Collection.
2. Find the next unlock panel.
3. Confirm the preview answers:
   - What unlock is next?
   - What will it change (effect summary)?
   - How close am I (progress)?
  </how-to-verify>
  <resume-signal>Type "approved" or describe missing/unclear preview details.</resume-signal>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:e2e -- tests/selectors-contract.spec.ts`
</verification>

<success_criteria>
- Players can see a motivating, clear next unlock preview with stable test coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/48-session-atelier/48-10-SUMMARY.md`
</output>
