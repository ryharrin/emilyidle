---
phase: 48-session-atelier
plan: 6
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/selectors/enjoyment.ts
  - src/game/selectors/index.ts
  - src/game/state.ts
  - src/ui/tabs/WorkshopTab.tsx
  - tests/workshop-atelier.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "Atelier/legacy bonuses scale so the second run feels meaningfully stronger than the first."
    - "UI explains the bonus with multiplier math (in a tooltip or compact note) without cluttering the main panel."
    - "Bonus calculations are selector-driven (single source of truth), not duplicated in UI."
  artifacts:
    - path: "src/game/selectors/enjoyment.ts"
      provides: "Legacy/atelier multiplier function(s) used by the economy"
    - path: "src/game/selectors/index.ts"
      provides: "Display-focused helper exposing bonus breakdown for UI"
    - path: "src/ui/tabs/WorkshopTab.tsx"
      provides: "Workshop/Atelier UI surfaces the bonus explanation"
  key_links:
    - from: "src/ui/tabs/WorkshopTab.tsx"
      to: "src/game/state.ts"
      via: "imports helper for atelier bonus breakdown"
      pattern: "getWorkshop|getPrestige"

<objective>
Scale atelier/legacy bonuses so the second run feels notably improved, and clarify the multiplier math in the Workshop UI.

Purpose: ATELIER-01 makes the reset loop feel rewarding and understandable.
Output: Tuned multiplier function(s) + selector-backed breakdown + UI tooltip/note + unit tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/48-session-atelier/48-CONTEXT.md
@.planning/phases/48-session-atelier/48-RESEARCH.md

@src/game/selectors/enjoyment.ts
@src/ui/tabs/WorkshopTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tune scaling + expose selector breakdown for atelier bonus</name>
  <files>
src/game/selectors/enjoyment.ts
src/game/selectors/index.ts
src/game/state.ts
tests/workshop-atelier.unit.test.ts
  </files>
  <action>
Implement a clear scaling model for atelier/legacy bonuses and expose a display-friendly breakdown.

Requirements:
- Keep the authoritative multiplier function(s) in `src/game/selectors/enjoyment.ts` (or the existing owning file).
- Expose a helper in `src/game/selectors/index.ts` that returns:
  - `multiplier01`: overall multiplier
  - `components`: named parts (e.g. "First prestige jump", "Additional prestiges", "Upgrades") with numeric values
  - `asPercentStrings` or raw ratios suitable for UI formatting (prefer raw numbers; UI formats)
- Re-export via `src/game/state.ts`.

Tuning target:
- After the first relevant reset/prestige, the multiplier should create a noticeable acceleration (don’t require multiple resets before it feels good).

Unit tests:
- Multiplier is monotonic with prestige count.
- After first prestige, multiplier >= a meaningful threshold (pick the exact target in implementation and lock it in tests).
- Breakdown sums/multiplies consistently with the final multiplier.
  </action>
  <verify>pnpm test:unit -- tests/workshop-atelier.unit.test.ts</verify>
  <done>
Selectors provide a single authoritative multiplier and a UI-ready breakdown, covered by unit tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Clarify atelier bonus math in Workshop UI (tooltip-first)</name>
  <files>
src/ui/tabs/WorkshopTab.tsx
  </files>
  <action>
Update Workshop/Atelier UI to clarify the bonus:
- Add a compact label near the reset/blueprint panel (e.g. "Atelier bonus").
- Put the detailed breakdown in a tooltip/expander text to avoid clutter.
- Copy should explicitly include multiplier math (e.g. "+8% enjoyment, +12% cash") sourced from the selector breakdown.

Keep existing help/ExplainButton patterns intact.
  </action>
  <verify>pnpm typecheck</verify>
  <done>
Workshop UI clearly explains the atelier bonus using selector-backed math.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/workshop-atelier.unit.test.ts`
</verification>

<success_criteria>
- Second run bonuses are noticeably stronger and the UI explains why with concrete multiplier math.
</success_criteria>

<output>
After completion, create `.planning/phases/48-session-atelier/48-06-SUMMARY.md`
</output>
