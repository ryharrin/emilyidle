---
phase: 48-session-atelier
plan: 5
type: execute
wave: 3
depends_on:
  - 48-03
files_modified:
  - src/ui/components/winding/WindingCrown.tsx
  - src/ui/components/WindingMiniGameModal.tsx
  - src/style.css
  - tests/winding-band-legend.unit.test.tsx
autonomous: false

must_haves:
  truths:
    - "While dragging, a spring-like gauge fills to show tension build-up in real time."
    - "On release, the user sees a glow + tiered outcome presentation tied to the achieved band."
    - "Reduced-motion users still get clear state changes without relying on continuous motion."
  artifacts:
    - path: "src/ui/components/winding/WindingCrown.tsx"
      provides: "Crown/gauge visuals driven by telemetry via CSS variables"
    - path: "src/style.css"
      provides: "Gauge + glow styling (with reduced-motion overrides)"
  key_links:
    - from: "src/ui/components/WindingMiniGameModal.tsx"
      to: "src/ui/components/winding/WindingCrown.tsx"
      via: "telemetry props for gauge fill"
      pattern: "<WindingCrown"

<objective>
Add richer, more physical-feeling winding feedback: a spring gauge that fills while dragging and a stronger outcome glow/reward presentation.

Purpose: WIND-03 makes the drag interaction feel like building real tension.
Output: Gauge arc + glow animation, reduced-motion safe, and test-anchored legend/outcome state.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/48-session-atelier/48-CONTEXT.md
@.planning/phases/48-session-atelier/48-RESEARCH.md

@src/ui/components/winding/WindingCrown.tsx
@src/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add spring gauge fill arc driven by drag telemetry</name>
  <files>
src/ui/components/winding/WindingCrown.tsx
src/style.css
  </files>
  <action>
Implement a gauge fill visualization that tracks tension/progress.

Guidelines:
- Drive visuals via CSS variables set on the crown root (e.g. `--winding-tension`, `--winding-progress`, `--winding-velocity`).
- Render a small arc gauge (SVG preferred for determinism) that fills from 0..1.
- Apply a "spring" feel by smoothing the displayed tension slightly (UI-only smoothing; keep domain math intact).
- Add a distinct over-wind/penalty treatment when the band indicates danger.

Reduced motion:
- No continuous oscillation; use instant state changes + subtle opacity/scale to communicate tension.
  </action>
  <verify>pnpm typecheck</verify>
  <done>
Gauge renders and responds to drag telemetry; styling is reduced-motion safe.
  </done>
</task>

<task type="auto">
  <name>Task 2: Strengthen tiered outcome glow + keep legend contracts stable</name>
  <files>
src/ui/components/WindingMiniGameModal.tsx
src/style.css
tests/winding-band-legend.unit.test.tsx
  </files>
  <action>
Update the resolved outcome presentation:
- Add a tier-tinted glow effect (miss/good/perfect/overwind) around the crown or outcome badge.
- Ensure the band legend still highlights the current band while dragging and transitions to the achieved band when resolved.

Update `tests/winding-band-legend.unit.test.tsx` only if necessary to account for the new tier/glow markup; keep selectors stable.
  </action>
  <verify>
pnpm test:unit -- tests/winding-band-legend.unit.test.tsx
  </verify>
  <done>
Outcome tier glow is visible and tests guarding the legend remain passing.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Spring gauge + richer tiered glow while winding</what-built>
  <how-to-verify>
1. Run `pnpm dev`.
2. Open winding modal and drag the crown slowly vs quickly.
3. Confirm:
   - Gauge fill feels responsive but stable (no jitter).
   - Tier glow is readable and distinct per outcome.
   - Reduced-motion mode (system setting) disables continuous motion but still shows outcome clarity.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looks wrong (jitter, unclear tiers, motion too strong).</resume-signal>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/winding-band-legend.unit.test.tsx`
</verification>

<success_criteria>
- Winding has a satisfying, readable tension gauge and tiered outcome feedback without breaking reduced-motion or test contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/48-session-atelier/48-05-SUMMARY.md`
</output>
