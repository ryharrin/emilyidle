---
phase: 48-session-atelier
plan: 8
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/selectors/interactions.ts
  - src/game/state.ts
  - src/ui/tabs/CatalogTab.tsx
  - src/ui/components/PowerReserveHint.tsx
  - src/style.css
  - tests/catalog.unit.test.tsx
autonomous: true

must_haves:
  truths:
    - "Power reserve is explained clearly and consistently (what it is, what it affects, how it changes)."
    - "Players can see a stable, movement-appropriate power reserve readout for applicable watches."
    - "Copy and calculations are selector-backed and do not diverge between surfaces."
  artifacts:
    - path: "src/ui/components/PowerReserveHint.tsx"
      provides: "Reusable power reserve readout + tooltip copy"
    - path: "src/ui/tabs/CatalogTab.tsx"
      provides: "Catalog surfaces power reserve clarity for applicable watches"
    - path: "src/game/selectors/interactions.ts"
      provides: "Selector helper(s) for reserve ratio/labels"
  key_links:
    - from: "src/ui/tabs/CatalogTab.tsx"
      to: "src/ui/components/PowerReserveHint.tsx"
      via: "renders reserve readout"
      pattern: "PowerReserveHint"

<objective>
Make power reserve clarity feel obvious: show a consistent reserve readout and explain what it affects without forcing players to infer from hidden mechanics.

Purpose: POWER-01 reduces confusion about why automatic watches behave differently and how reserve relates to enjoyment/rewards.
Output: Selector-backed reserve helper + reusable UI component + catalog wiring + unit guard.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/game/selectors/interactions.ts
@src/ui/tabs/CatalogTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add selector helper for reserve readout detail</name>
  <files>
src/game/selectors/interactions.ts
src/game/state.ts
tests/catalog.unit.test.tsx
  </files>
  <action>
Add a pure helper that returns reserve display details for a watch interaction, including:
- `reserve01` (0..1)
- `reservePercent` (0..100 integer)
- `label` (movement-appropriate copy)
- `explanation` (short tooltip string)

Re-export via `src/game/state.ts` and add a unit test that the helper is deterministic and clamps values.
  </action>
  <verify>
pnpm typecheck
pnpm test:unit -- tests/catalog.unit.test.tsx
  </verify>
  <done>
Selector helper exists, is pure, and is guarded by unit coverage.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add PowerReserveHint component and wire into Catalog UI</name>
  <files>
src/ui/components/PowerReserveHint.tsx
src/ui/tabs/CatalogTab.tsx
src/style.css
  </files>
  <action>
Create a compact `PowerReserveHint` component:
- Accepts selector-provided detail and renders "Power reserve: X%" with an inline tooltip/explanation affordance.
- Use existing typography + muted copy patterns; keep it compact.

Wire into Catalog watch details:
- Show reserve hint where it’s currently shown for automatic watches, and ensure copy explains what it affects.
- Keep stable selectors (`data-testid`) if already present; add a new stable anchor if none exists (e.g. `data-testid="power-reserve-hint"`).
  </action>
  <verify>pnpm typecheck</verify>
  <done>
Catalog shows a clear power reserve readout with consistent explanation copy.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/catalog.unit.test.tsx`
</verification>

<success_criteria>
- Players can quickly understand power reserve and see a reliable readout where applicable.
</success_criteria>

<output>
After completion, create `.planning/phases/48-session-atelier/48-08-SUMMARY.md`
</output>
