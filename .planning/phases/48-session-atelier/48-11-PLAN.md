---
phase: 48-session-atelier
plan: 11
type: execute
wave: 3
depends_on:
  - 48-07
files_modified:
  - src/ui/tabs/UpgradesTab.tsx
  - src/game/actions/index.ts
  - src/game/state.ts
  - tests/upgrades-preview.unit.test.tsx
autonomous: true

must_haves:
  truths:
    - "Upgrades show an effect preview (before/after) that is derived from real selector math."
    - "Preview does not mutate live state; it uses pure state delta simulation."
    - "Preview stays consistent across upgrades and does not require owning the upgrade to understand it."
  artifacts:
    - path: "src/ui/tabs/UpgradesTab.tsx"
      provides: "Upgrade rows render effect previews consistently"
    - path: "tests/upgrades-preview.unit.test.tsx"
      provides: "Unit coverage for upgrade effect preview behavior"
  key_links:
    - from: "src/ui/tabs/UpgradesTab.tsx"
      to: "src/game/actions/index.ts"
      via: "state-delta simulation for preview"
      pattern: "purchase|buy|upgrade"

<objective>
Add consistent upgrade effect previews so players can see what an upgrade will do before buying.

Purpose: UPGRADE-01 strengthens decision-making by making upgrade deltas explicit.
Output: UpgradesTab preview UI + unit coverage + stable selector-based computations.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/ui/tabs/UpgradesTab.tsx
@src/game/actions/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement upgrade effect preview via pure state delta</name>
  <files>
src/ui/tabs/UpgradesTab.tsx
src/game/actions/index.ts
src/game/state.ts
  </files>
  <action>
Extend the existing preview pattern (already used for some rate previews) to cover upgrade effects consistently:
- For each upgrade row, compute `nextState` by applying the relevant pure action to `state` (without committing it).
- Compute before/after values using selectors (rates, caps, cooldown reductions, etc.).
- Render a concise preview line (e.g. "+0.8 enjoyment/sec" or "Cooldown: 20s -> 16s").

Keep layering clean:
- UI imports selectors/actions from `src/game/state.ts`.
- No selector/action uses Date.now.
  </action>
  <verify>pnpm typecheck</verify>
  <done>
Upgrade rows show consistent before/after effect previews without mutating state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit coverage for upgrade preview output</name>
  <files>
tests/upgrades-preview.unit.test.tsx
  </files>
  <action>
Create a focused unit test file that renders `UpgradesTab` (or the extracted row component, if it exists) with a small seeded state and asserts:
- Preview line exists for a representative workshop upgrade.
- Preview line exists for a representative career upgrade.
- Preview values change between state and nextState (not always 0 / identical).

Keep tests stable by targeting `data-testid` anchors rather than exact copy where possible.
  </action>
  <verify>pnpm test:unit -- tests/upgrades-preview.unit.test.tsx</verify>
  <done>
Unit tests guard that upgrade effect previews render and reflect real selector deltas.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/upgrades-preview.unit.test.tsx`
</verification>

<success_criteria>
- Upgrade list consistently shows effect previews derived from selector math, guarded by unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/48-session-atelier/48-11-SUMMARY.md`
</output>
