---
phase: 29-interactions-and-mini-games
plan: 03
type: execute
wave: 3
depends_on: ["29-02"]
files_modified:
  - src/game/sim.ts
  - src/App.tsx
  - src/ui/tabs/CollectionTab.tsx
  - src/ui/components/AutomaticMiniGameModal.tsx
  - src/style.css
  - tests/interactions.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "Automatic watches have a distinct interaction mini-game (not winding)"
    - "Automatic interaction reward is communicated as power reserve and has a measurable gameplay effect"
    - "Per-item cooldown messaging works for the automatic interaction button"
  artifacts:
    - path: src/ui/components/AutomaticMiniGameModal.tsx
      provides: "Automatic interaction mini-game modal"
    - path: src/game/sim.ts
      provides: "Time-based decay for power reserve"
  key_links:
    - from: src/ui/components/AutomaticMiniGameModal.tsx
      to: src/App.tsx
      via: "onComplete -> applyAutomaticReward"
      pattern: "applyAutomaticReward"
    - from: src/game/sim.ts
      to: src/game/selectors/enjoyment.ts
      via: "powerReserveByItem affects earned enjoyment"
      pattern: "powerReserveByItem"

---

<objective>
Implement the automatic-only interaction mini-game that fills a per-item power reserve meter and makes its reward meaningful via a visible, testable enjoyment-rate effect.

Purpose: Satisfy ACT-05 and complete the watch-type interaction set with distinct mechanics.
Output: AutomaticMiniGameModal + power reserve decay/effect + Vault wiring.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-interactions-and-mini-games/29-CONTEXT.md

@src/game/sim.ts
@src/game/selectors/enjoyment.ts
@src/App.tsx
@src/ui/tabs/CollectionTab.tsx
@src/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement power reserve decay in simulation and surface reserve in UI</name>
  <files>
src/game/sim.ts
src/ui/tabs/CollectionTab.tsx
  </files>
  <action>
- Add a simple decay model for `powerReserveByItem[itemId]` in `src/game/sim.ts`:
  - On each `step`, reduce reserve by `dtMs` at a fixed rate (ex: 1.0 drains over 120s), clamped to [0, 1].
  - Keep the function pure; no side effects.
  - Only touch items that currently have reserve > 0.

- In `src/ui/tabs/CollectionTab.tsx`, show a compact power reserve indicator on automatic watch cards:
  - Example: `Power reserve: 62%` (or `Charged: 62%`)
  - Keep it muted and non-dominating.
  </action>
  <verify>
pnpm -s run typecheck
  </verify>
  <done>
- Power reserve decays over time during simulation ticks and the UI surfaces its current value on automatic watch cards.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add automatic mini-game modal and wire the automatic interaction button</name>
  <files>
src/ui/components/AutomaticMiniGameModal.tsx
src/App.tsx
src/ui/tabs/CollectionTab.tsx
src/style.css
tests/interactions.unit.test.ts
  </files>
  <action>
- Create `src/ui/components/AutomaticMiniGameModal.tsx`:
  - Theme: rotor/automatic winding; duration 8-15s.
  - Mechanic must be distinct from the timing-bar tap.
    - Example mechanic: maintain a spinning rotor “balance” by alternating inputs to keep a needle in a target band.
  - Outcomes: Miss/Good/Perfect (or equivalent) and partial success.
  - Communicate reward: show “Power reserve +X%” clearly on completion.
  - Respect reduced motion.

- Update `src/App.tsx` wiring so clicking the automatic interaction button opens this modal.
  - On completion, call `applyAutomaticReward(state, itemId, nowMs, outcome)` and close.

- Update `src/ui/tabs/CollectionTab.tsx` automatic button behavior:
  - Label: "Charge rotor"
  - Disabled states:
    - owned <= 0
    - cooldown active (show `Cooldown Ns`)

- Update/extend `tests/interactions.unit.test.ts` to cover:
  - `applyAutomaticReward` increases reserve
  - reserve affects enjoyment rate (already introduced in Plan 29-01)
  - decay in `step()` reduces reserve
  </action>
  <verify>
pnpm -s run test:unit
  </verify>
  <done>
- Automatic watches have a working, distinct interaction mini-game that fills power reserve.
- Reward is communicated and has a measurable effect (rate change + decays over time).
  </done>
</task>

</tasks>

<verification>
- `pnpm run test:unit`
</verification>

<success_criteria>
- ACT-05 satisfied: automatic watches have a distinct mini-game and rewards are communicated.
- Power reserve is visible, affects enjoyment meaningfully, and decays over time.
</success_criteria>

<output>
After completion, create `.planning/phases/29-interactions-and-mini-games/29-03-SUMMARY.md`
</output>
