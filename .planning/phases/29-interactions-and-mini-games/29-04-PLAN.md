---
phase: 29-interactions-and-mini-games
plan: 04
type: execute
wave: 4
depends_on: ["29-03"]
files_modified:
  - src/App.tsx
  - src/ui/tabs/CollectionTab.tsx
  - src/ui/components/QuartzMiniGameModal.tsx
  - src/style.css
  - tests/catalog.unit.test.tsx
autonomous: true

must_haves:
  truths:
    - "Quartz watches have a distinct interaction (not winding)"
    - "Quartz interaction clearly communicates its reward and uses consistent feedback cues"
    - "Vault/Collection only shows the interaction appropriate for each movement type"
  artifacts:
    - path: src/ui/components/QuartzMiniGameModal.tsx
      provides: "Quartz time-setting mini-game modal"
  key_links:
    - from: src/ui/components/QuartzMiniGameModal.tsx
      to: src/App.tsx
      via: "onComplete -> applyQuartzReward"
      pattern: "applyQuartzReward"

---

<objective>
Add the Quartz-specific time-setting mini-game and complete movement-type interaction gating so each watch type has a distinct, clearly rewarded interaction.

Purpose: Round out Phase 29 interactions beyond winding and ensure no watch shows the wrong interaction.
Output: QuartzMiniGameModal + Vault wiring + unit test updates.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-interactions-and-mini-games/29-CONTEXT.md

@src/App.tsx
@src/ui/tabs/CollectionTab.tsx
@src/style.css
@tests/catalog.unit.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Quartz time-setting mini-game modal</name>
  <files>
src/ui/components/QuartzMiniGameModal.tsx
src/style.css
  </files>
  <action>
- Create `src/ui/components/QuartzMiniGameModal.tsx` with a time-setting themed mini-game.
- Keep feedback cues consistent with winding/automatic:
  - Clear run instructions
  - Outcome tier (Miss/Good/Perfect or similar)
  - Explicit reward messaging

- Reward should be distinct from winding’s enjoyment burst and automatic’s power reserve.
  - Default: a small cash burst (currency cents) based on outcome tier.
  - Display the range preview (min/max) in the modal.

- Include stable test selectors:
  - modal root `data-testid="quartz-modal"`
  - close button `data-testid="quartz-close"`
  - action button(s) `data-testid="quartz-action"` or equivalent
  </action>
  <verify>
pnpm -s run typecheck
  </verify>
  <done>
- Quartz mini-game modal exists, runs deterministically, and communicates a distinct reward.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Quartz interaction button and update unit tests</name>
  <files>
src/App.tsx
src/ui/tabs/CollectionTab.tsx
tests/catalog.unit.test.tsx
  </files>
  <action>
- In `src/ui/tabs/CollectionTab.tsx`:
  - Ensure quartz items show the "Set time" interaction button.
  - Ensure manual items show "Wind crown".
  - Ensure automatic items show "Charge rotor".
  - Ensure only one interaction button is shown per watch card.
  - Cooldown disabled reason should be shown consistently.

- In `src/App.tsx`:
  - Implement routing for quartz interaction: open `QuartzMiniGameModal` and apply `applyQuartzReward` on completion.

- Update the existing wind minigame unit tests in `tests/catalog.unit.test.tsx` to avoid assuming only one interaction exists:
  - Target the specific manual/quartz watch interaction using `vault-interact-${itemId}`.
  - Add a small quartz smoke test (open modal + complete + verify reward reflected).
  </action>
  <verify>
pnpm -s run test:unit
  </verify>
  <done>
- Quartz interaction works end-to-end and movement-type gating is complete in the Vault UI.
  </done>
</task>

</tasks>

<verification>
- `pnpm run test:unit`
</verification>

<success_criteria>
- Quartz watches have a distinct, working time-setting interaction.
- Vault/Collection shows correct interaction per movement type.
</success_criteria>

<output>
After completion, create `.planning/phases/29-interactions-and-mini-games/29-04-SUMMARY.md`
</output>
