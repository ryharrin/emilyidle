---
phase: 29-interactions-and-mini-games
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - src/App.tsx
  - src/ui/tabs/CollectionTab.tsx
  - src/ui/components/WindingMiniGameModal.tsx
  - src/style.css
  - tests/catalog.unit.test.tsx
autonomous: true

must_haves:
  truths:
    - "Winding interaction is only offered for manual (non-automatic) watch items"
    - "Winding is timing/skill-based and shows Miss/Good/Perfect feedback"
    - "Winding includes a visible animation and clearly communicates rewards"
  artifacts:
    - path: src/ui/components/WindingMiniGameModal.tsx
      provides: "Timing-bar winding mini-game modal"
    - path: src/ui/tabs/CollectionTab.tsx
      provides: "Per-item interaction button labels + disabled reasons"
  key_links:
    - from: src/ui/tabs/CollectionTab.tsx
      to: src/App.tsx
      via: "onInteract(itemId) opens winding modal"
      pattern: "onInteract\\(item\\.id\\)"
    - from: src/App.tsx
      to: src/game/state.ts
      via: "applyWindingReward"
      pattern: "applyWindingReward"

---

<objective>
Replace the old Steady/Push wind session with a short timing-bar winding mini-game that has animation, outcome tiers, reward messaging, and per-item cooldown feel.

Purpose: Satisfy ACT-01..ACT-04 for manual/non-automatic watches with a modernized interaction.
Output: WindingMiniGameModal + Vault/Collection wiring + updated unit coverage.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-interactions-and-mini-games/29-CONTEXT.md

@src/App.tsx
@src/ui/tabs/CollectionTab.tsx
@src/ui/components/PrestigeOnboardingModal.tsx
@src/ui/help/HelpModal.tsx
@src/style.css
@tests/catalog.unit.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create timing-bar winding mini-game modal with animation + outcome tiers</name>
  <files>
src/ui/components/WindingMiniGameModal.tsx
src/style.css
  </files>
  <action>
- Create `src/ui/components/WindingMiniGameModal.tsx` following existing modal patterns (`role="dialog"`, `aria-modal="true"`, close button).
- Mini-game design (from phase context):
  - Duration: 3-5 seconds
  - Input: tap/click when indicator is in a sweet spot on a horizontal bar
  - Outcomes: Miss / Good / Perfect
  - Failure: partial success (Miss still yields something)
  - Reward: enjoyment burst (communicate exact enjoyment gained after completion)
  - Preview: the reward range is displayed in the modal header/subtext (passed in as props from selectors/actions)

- Animation requirement (ACT-02):
  - Add a visible winding animation (ex: crown icon/shape rotating or a winding arc filling) that reacts to the run and outcome.
  - Respect reduced motion: if `prefers-reduced-motion` is active, avoid continuous motion and instead use discrete state changes.

- Component API (keep it simple for App wiring):
  - Props: `{ open, itemLabel, rewardRangeLabel, cooldownLabel, onComplete(outcome), onClose }`
  - Outcome should include a scalar performance value (0..1) and derived tier string.

- Add minimal CSS in `src/style.css` for the timing track + indicator + sweet spot + outcome banner.
  </action>
  <verify>
pnpm -s run typecheck
  </verify>
  <done>
- Modal renders with an animated timing bar, produces Miss/Good/Perfect outcomes, and can run end-to-end without relying on randomness.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire winding into Vault/Collection interaction entry point and update unit tests</name>
  <files>
src/App.tsx
src/ui/tabs/CollectionTab.tsx
tests/catalog.unit.test.tsx
  </files>
  <action>
- Remove the legacy wind session state (`windRound`, `windTension`, Steady/Push UI) from `src/App.tsx` and replace with a generic “active interaction” state that can open the winding modal for manual watches.
- In `src/ui/tabs/CollectionTab.tsx`:
  - Replace the generic "Interact" label with a specific label per movement type:
    - manual: "Wind crown"
    - automatic: "Charge rotor" (implemented in Plan 29-03)
    - quartz: "Set time" (implemented in Plan 29-04)
  - Add a stable selector for tests: `data-testid={`vault-interact-${item.id}`}` on the interaction button.
  - Button enable/disable rules:
    - Disabled if `owned <= 0`
    - Disabled if per-item cooldown is active; show reason text next to/under button: `Cooldown {Ns}`
    - For this plan, keep automatic/quartz buttons disabled with a clear reason (e.g., "Unlocks next: Automatic interaction" / "Quartz interaction coming") but do not show winding for them.

- In `src/App.tsx`:
  - On click of a manual watch interaction button, open `WindingMiniGameModal`.
  - On completion, call the new pure action(s) from `src/game/state.ts` to apply winding reward + cooldown using `Date.now()` as `nowMs`.
  - Close modal after applying reward.

- Update `tests/catalog.unit.test.tsx` wind minigame suite to:
  - Find the interaction button using `data-testid` instead of role name.
  - Assert opening/closing resets state.
  - Assert completing a run applies a visible reward cue in UI OR results in a saved-state change (choose whichever is stable).
  </action>
  <verify>
pnpm -s run test:unit
  </verify>
  <done>
- Manual watches show a "Wind crown" interaction that opens the new timing mini-game.
- Automatic/quartz do not show winding.
- Unit tests pass with stable selectors.
  </done>
</task>

</tasks>

<verification>
- `pnpm run test:unit`
</verification>

<success_criteria>
- ACT-01..ACT-04 are satisfied for manual winding (feedback, animation, skill, gating).
- Vault/Collection uses specific interaction labels and cooldown disabled reason.
</success_criteria>

<output>
After completion, create `.planning/phases/29-interactions-and-mini-games/29-02-SUMMARY.md`
</output>
