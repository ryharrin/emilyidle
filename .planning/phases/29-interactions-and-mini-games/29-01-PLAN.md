---
phase: 29-interactions-and-mini-games
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/model/types.ts
  - src/game/model/state.ts
  - src/game/data/items.ts
  - src/game/persistence.ts
  - src/game/selectors/enjoyment.ts
  - src/game/selectors/index.ts
  - src/game/actions/index.ts
  - src/game/state.ts
  - tests/interactions.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "Game can classify each watch item as quartz/manual/automatic for interaction gating"
    - "Game can compute per-item interaction availability (cooldown + owned gating)"
    - "Game can apply interaction rewards in a pure, testable way"
  artifacts:
    - path: src/game/model/types.ts
      provides: "Movement + interaction state types"
    - path: src/game/persistence.ts
      provides: "Save/load support for interaction state"
    - path: tests/interactions.unit.test.ts
      provides: "Unit coverage for interaction gating + rewards"
  key_links:
    - from: src/game/data/items.ts
      to: src/game/model/types.ts
      via: "WatchItemDefinition.movement"
      pattern: "movement:\\s*(\\\"quartz\\\"|\\\"manual\\\"|\\\"automatic\\\")"
    - from: src/game/actions/index.ts
      to: src/game/model/types.ts
      via: "updates GameState interaction fields"
      pattern: "interaction"

---

<objective>
Add interaction foundations (movement typing + per-item cooldown/power reserve state + pure reward application) so Phase 29 UI can gate interactions correctly and persist interaction state.

Purpose: Phase 29 needs watch-type gated mini-games with clear availability rules and per-watch cooldown feel.
Output: Interaction-ready domain model (types + persistence + selectors/actions) with unit tests.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-interactions-and-mini-games/29-CONTEXT.md

@src/game/model/types.ts
@src/game/model/state.ts
@src/game/data/items.ts
@src/game/persistence.ts
@src/game/selectors/enjoyment.ts
@src/game/actions/index.ts
@src/game/state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add movement typing to watch items</name>
  <files>
src/game/model/types.ts
src/game/data/items.ts
  </files>
  <action>
- In `src/game/model/types.ts`, introduce a movement discriminator for watch items:
  - Add `export type WatchMovement = "quartz" | "manual" | "automatic";`
  - Extend `WatchItemDefinition` with `movement: WatchMovement;`
- In `src/game/data/items.ts`, set movement for existing ids (do not rename ids):
  - `starter`: quartz
  - `classic`: automatic
  - `chronograph`: manual
  - `tourbillon`: manual
- Keep this a pure data/model change; do not touch UI in this task.
- Ensure TypeScript compiles (all WatchItemDefinition literals updated).
  </action>
  <verify>
pnpm -s run typecheck
  </verify>
  <done>
- Watch items compile with explicit `movement` and can be used for interaction gating without relying on name strings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add persistent per-item interaction state + reward application actions</name>
  <files>
src/game/model/types.ts
src/game/model/state.ts
src/game/persistence.ts
src/game/actions/index.ts
src/game/state.ts
  </files>
  <action>
- Add interaction state to `GameState` (and `PersistedGameState`) to support Phase 29 decisions:
  - Per-item cooldown timestamp: `interactionNextAvailableAtMsByItem: Partial<Record<WatchItemId, number>>`
  - Per-item power reserve (0..1): `powerReserveByItem: Partial<Record<WatchItemId, number>>`
  - Keep values clamped/sanitized when loading.

- Update `createInitialState()` (or equivalent constructor in `src/game/model/state.ts`) to include defaults:
  - cooldown map empty
  - power reserve map empty

- Update `src/game/persistence.ts` so save v2 load:
  - Accepts missing fields (old saves)
  - Coerces unknown keys to known `WatchItemId` where possible (ignore unknown)
  - Clamps reserve into [0, 1]
  - Coerces timestamps into finite non-negative numbers

- Add actions in `src/game/actions/index.ts` (pure, no Date.now inside):
  - `getInteractionCooldownRemainingMs(state, itemId, nowMs)` or equivalent helper
  - `setInteractionCooldown(state, itemId, nextAvailableAtMs)`
  - `applyWindingReward(state, itemId, nowMs, outcome)` that:
    - grants an enjoyment burst based on outcome tier (Miss/Good/Perfect)
    - sets per-item cooldown
    - (optional) can also call existing `activateManualEvent(..., "wind-up", ...)` if you decide to keep the wind-up event as an extra communicated reward; if you do, make it deterministic based on outcome tier
  - `applyAutomaticReward(state, itemId, nowMs, outcome)` that:
    - increases power reserve (additive, clamped to [0, 1])
    - sets per-item cooldown
  - `applyQuartzReward(state, itemId, nowMs, outcome)` that:
    - grants a distinct reward from winding (default: small cash burst), and sets cooldown

- Export the new actions through `src/game/state.ts` (facade) so UI can call them later.
  </action>
  <verify>
pnpm -s run typecheck
  </verify>
  <done>
- GameState includes persisted per-item cooldown + power reserve.
- Pure actions exist to apply winding/automatic/quartz rewards and set cooldown without UI involvement.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update selectors for power reserve effect and add unit tests</name>
  <files>
src/game/selectors/enjoyment.ts
src/game/selectors/index.ts
tests/interactions.unit.test.ts
  </files>
  <action>
- Implement an enjoyment effect for automatic power reserve (so the reward is meaningful and measurable):
  - In `src/game/selectors/enjoyment.ts`, apply a multiplier to the automatic watch item(s) enjoyment contribution when `powerReserveByItem[itemId] > 0`.
  - Keep the math simple and communicable:
    - Example: `effectiveEnjoyment = baseEnjoyment * (1 + 0.5 * reserve)` where `reserve` is 0..1.
  - Do not change the existing prestige legacy multiplier logic; apply reserve at the per-item contribution layer.
- Export any new helper selectors from `src/game/selectors/index.ts` as needed by UI in later plans (e.g., `getPowerReserveForItem`, `getInteractionCooldownRemainingMs`).

- Add `tests/interactions.unit.test.ts` covering:
  - Movement mapping exists on all watch items.
  - Cooldown remaining helper returns expected values.
  - Applying winding reward increases enjoyment and sets cooldown.
  - Applying automatic reward increases reserve (clamped) and increases enjoyment rate vs baseline.
  - Applying quartz reward changes state in the expected way (cash or other distinct reward).
  </action>
  <verify>
pnpm -s run test:unit
  </verify>
  <done>
- Unit tests pass and demonstrate interaction rewards and power reserve effect are working at the selector/action level.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit`
</verification>

<success_criteria>
- Watch items have explicit movement typing.
- GameState persists per-item cooldown and power reserve.
- Pure actions and selectors exist for Phase 29 UI to gate interactions and apply rewards.
</success_criteria>

<output>
After completion, create `.planning/phases/29-interactions-and-mini-games/29-01-SUMMARY.md`
</output>
