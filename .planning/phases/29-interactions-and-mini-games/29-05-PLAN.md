---
phase: 29-interactions-and-mini-games
plan: 05
type: execute
wave: 5
depends_on: ["29-04"]
files_modified:
  - tests/collection-loop.spec.ts
  - tests/catalog.unit.test.tsx
autonomous: true

must_haves:
  truths:
    - "Automated tests verify winding and automatic interactions complete and apply rewards"
    - "Tests use stable selectors (data-testid), not brittle button labels"
  artifacts:
    - path: tests/collection-loop.spec.ts
      provides: "E2E coverage for interaction flows"
  key_links:
    - from: tests/collection-loop.spec.ts
      to: src/ui/tabs/CollectionTab.tsx
      via: "vault-interact-* selectors"
      pattern: "vault-interact-"

---

<objective>
Update and extend automated test coverage to lock in Phase 29 interaction behavior (winding + automatic + cooldown messaging).

Purpose: Prevent regressions in the interaction entry point and reward wiring.
Output: Updated unit and Playwright tests using stable selectors.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@tests/catalog.unit.test.tsx
@tests/collection-loop.spec.ts
@src/ui/tabs/CollectionTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Playwright wind interaction test to the new mini-game</name>
  <files>
tests/collection-loop.spec.ts
  </files>
  <action>
- Update the existing "wind session completes" e2e test to:
  - Locate the manual watch interaction button via `data-testid="vault-interact-chronograph"` (or whichever watch is manual per Plan 29-01 mapping).
  - Open the winding modal.
  - Complete the mini-game deterministically (avoid relying on randomness):
    - Prefer a test-only deterministic mode (e.g. set indicator position by clicking at a known time) or expose a small test hook if needed.
  - Verify:
    - Modal closes
    - Saved `enjoymentCents` increased OR UI shows a clear reward message
    - Interaction button becomes disabled with a cooldown reason
  </action>
  <verify>
pnpm -s run test:e2e
  </verify>
  <done>
- E2E asserts the winding interaction completes and applies a reward using stable selectors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add/extend e2e coverage for automatic interaction + power reserve</name>
  <files>
tests/collection-loop.spec.ts
  </files>
  <action>
- Add a new Playwright test (or extend the existing one) to cover the automatic interaction:
  - Seed state with `classic: 1` (automatic) and ensure interaction is available.
  - Click `data-testid="vault-interact-classic"` to open the automatic modal.
  - Complete the mini-game deterministically.
  - Verify:
    - Power reserve increases (read from UI label or localStorage state field)
    - Enjoyment rate increases while reserve > 0 (UI stats or derived effect)
    - Cooldown reason appears after completion
  </action>
  <verify>
pnpm -s run test:e2e
  </verify>
  <done>
- E2E covers the automatic mini-game and power reserve effect end-to-end.
  </done>
</task>

</tasks>

<verification>
- `pnpm run test:unit`
- `pnpm run test:e2e`
</verification>

<success_criteria>
- Unit + e2e suites pass.
- Interactions are exercised in e2e using stable selectors.
</success_criteria>

<output>
After completion, create `.planning/phases/29-interactions-and-mini-games/29-05-SUMMARY.md`
</output>
