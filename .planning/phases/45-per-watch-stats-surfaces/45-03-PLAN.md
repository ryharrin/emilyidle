---
phase: 45-per-watch-stats-surfaces
plan: 3
type: execute
wave: 3
depends_on: ["45-02"]
files_modified:
  - tests/per-watch-stats-ui.unit.test.tsx
  - tests/per-watch-stats.spec.ts
  - tests/localstorage-schema.unit.test.tsx
  - tests/localstorage-keys.unit.test.ts
  - src/ui/components/PerWatchStatsTable.tsx
  - src/ui/tabs/CatalogTab.tsx
  - src/ui/tabs/CollectionTab.tsx
autonomous: true

must_haves:
  truths:
    - "Per-watch stats UI remains stable under sorting/filtering: expanding a row stays attached to the same watch after reorder."
    - "On mobile viewport, the per-watch stats sticky controls remain visible while scrolling through the stats list."
    - "E2E coverage asserts the per-watch stats surface can be reached with seeded storage and shows both enjoyment and cash rate columns." 
  artifacts:
    - path: "tests/per-watch-stats-ui.unit.test.tsx"
      provides: "Vitest coverage for controlled expansion + sort behavior"
    - path: "tests/per-watch-stats.spec.ts"
      provides: "Playwright coverage for sticky header + surface presence in Catalog"
  key_links:
    - from: "tests/per-watch-stats.spec.ts"
      to: "src/ui/components/PerWatchStatsTable.tsx"
      via: "data-testid anchors"
      pattern: "per-watch-stats"

<objective>
Lock Phase 45 behavior with regression coverage: unit tests for expansion stability and end-to-end verification that the per-watch stats surface works on mobile and stays navigable.

Purpose: Prevent regressions in the most fragile parts (sticky header + sort/filter + expansion) and ensure selectors/UI stay wired as intended.
Output: New Vitest + Playwright tests and any minimal fixes required for testability.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/ui/components/PerWatchStatsTable.tsx
@src/ui/tabs/CatalogTab.tsx
@src/ui/tabs/CollectionTab.tsx
@tests/selectors-contract.spec.ts
@tests/localstorage-schema.unit.test.tsx
@tests/localstorage-keys.unit.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a Vitest UI test for sort + controlled expansion stability</name>
  <files>
tests/per-watch-stats-ui.unit.test.tsx
  </files>
  <action>
- Add a Testing Library/Vitest test that renders `PerWatchStatsTable` with a small set of fake rows keyed by stable `modelId` values.
- Expand one row, change sort order, and assert the same row (same `data-testid="per-watch-row-${modelId}"`) remains expanded after reorder.
- Assert numeric columns render via `formatRateFromCentsPerSec` and that the cash column label indicates its semantics (career-based if applicable).
  </action>
  <verify>
pnpm test:unit -- tests/per-watch-stats-ui.unit.test.tsx
  </verify>
  <done>
- Unit test fails if expansion state becomes position-based or if data-testid anchors regress.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add a Playwright test for mobile sticky controls and surface presence</name>
  <files>
tests/per-watch-stats.spec.ts
  </files>
  <action>
- Add Playwright test that:
  - Seeds localStorage with a valid save that includes multiple watch models (follow `tests/selectors-contract.spec.ts` patterns).
  - Navigates to the Catalog tab and scrolls to the per-watch stats section.
  - Asserts `data-testid="per-watch-stats"` exists and includes columns for enjoyment and cash.
  - Uses a mobile-ish viewport and scrolls the per-watch list, asserting the sticky control header stays visible (e.g., bounding box remains within viewport or a fixed element remains attached).
- Keep the test anchored on `data-testid` values added in Plan 45-02.
  </action>
  <verify>
pnpm test:e2e -- tests/per-watch-stats.spec.ts
  </verify>
  <done>
- E2E test passes locally and fails if the sticky header stops sticking or if the section is missing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update any guardrail tests impacted by new storage/test seeding</name>
  <files>
tests/localstorage-schema.unit.test.tsx
tests/localstorage-keys.unit.test.ts
  </files>
  <action>
- If new UI state requires localStorage keys (e.g., per-watch sort preference), ensure keys are explicitly registered and guardrail tests are updated.
- If no new keys are introduced, do not touch these files.
  </action>
  <verify>
pnpm test:unit -- tests/localstorage-keys.unit.test.ts tests/localstorage-schema.unit.test.tsx
  </verify>
  <done>
- LocalStorage key/schema guardrails remain green and accurately reflect any newly introduced persisted UI preferences.
  </done>
</task>

</tasks>

<verification>
- `pnpm test:unit -- tests/per-watch-stats-ui.unit.test.tsx tests/per-watch-stats.unit.test.ts`
- `pnpm test:e2e -- tests/per-watch-stats.spec.ts`
</verification>

<success_criteria>
- Unit and E2E tests prevent regressions in per-watch stats UI behavior, especially on mobile.
- Any new localStorage usage is explicitly guarded or avoided.
</success_criteria>

<output>
After completion, create `.planning/phases/45-per-watch-stats-surfaces/45-03-SUMMARY.md`
</output>
