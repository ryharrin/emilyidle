---
phase: 45-per-watch-stats-surfaces
plan: 02
type: execute
wave: 2
depends_on:
  - 45-01
files_modified:
  - src/ui/tabs/CatalogTab.tsx
  - src/ui/tabs/CollectionTab.tsx
  - src/style.css
  - tests/catalog.unit.test.tsx
  - tests/wear-one-bonus.spec.ts
autonomous: true

must_haves:
  truths:
    - "Catalog shows per-watch enjoyment/sec and cash/sec without requiring purchase."
    - "Collection view shows the equipped watch’s contribution/breakdown clearly."
  artifacts:
    - path: src/ui/tabs/CatalogTab.tsx
      provides: "Catalog card rendering includes per-watch stats"
      contains: "catalog-card"
    - path: src/ui/tabs/CollectionTab.tsx
      provides: "Worn watch summary includes contribution breakdown"
      contains: "worn watch"
---

<objective>
Surface per-watch stats in the Catalog and show equipped watch contribution in Collection, so players can make informed purchase/equip decisions.

Purpose: Deliver STATS-01/02/04/05 in the UI layer after the domain work in 45-01.
Output: UI rate lines + worn contribution breakdown + regression tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@src/ui/tabs/CatalogTab.tsx
@src/ui/tabs/CollectionTab.tsx
@src/style.css
@tests/catalog.unit.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-watch enjoyment/sec + cash/sec lines to catalog cards</name>
  <files>
src/ui/tabs/CatalogTab.tsx
src/style.css
tests/catalog.unit.test.tsx
  </files>
  <action>
Update catalog card UI to show per-watch rates:

- For each catalog entry card, render two compact lines (even when unowned):
  - `Enjoyment/sec: +X`
  - `Cash/sec: +Y` (or the chosen cash model label)

Notes:
- Keep the display concise; avoid adding a large breakdown UI here (Stats tab can handle deep breakdowns later).
- Prefer using existing formatting helpers (`formatRateFromCentsPerSec`) and new selectors exported via `src/game/state.ts`.
- Keep existing test ids stable. If adding new test ids, use `data-testid="catalog-watch-rates-${entry.id}"` (or similar).

Update `tests/catalog.unit.test.tsx` to assert that at least one catalog card renders the new rate lines.
  </action>
  <verify>pnpm -s run test:unit -- tests/catalog.unit.test.tsx</verify>
  <done>
- Catalog surfaces per-watch enjoyment/cash rates without requiring purchase.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add equipped watch contribution breakdown in Collection</name>
  <files>
src/ui/tabs/CollectionTab.tsx
src/style.css
tests/wear-one-bonus.spec.ts
  </files>
  <action>
Make equipped watch impact legible:

- In the worn watch panel in Collection:
  - Show the worn model’s per-watch enjoyment/sec and cash/sec.
  - Show a small “Contribution” breakdown:
    - e.g., “Adds +X enjoyment/sec (base) · xY worn bonus · …”
  - Keep it short; the goal is “can I tell what this does?”

Update the existing wear-one Playwright coverage (`tests/wear-one-bonus.spec.ts`) to assert:
- the worn watch panel updates when switching watches, and
- the displayed contribution/rate line changes accordingly (text assertion; avoid pixel checks).
  </action>
  <verify>pnpm -s run test:e2e -- tests/wear-one-bonus.spec.ts</verify>
  <done>
- Equipped watch contribution is visible and verified by e2e coverage.
  </done>
</task>

</tasks>

<verification>
- `pnpm run test:unit -- tests/catalog.unit.test.tsx`
- `pnpm run test:e2e -- tests/wear-one-bonus.spec.ts`
</verification>

<success_criteria>
- STATS-01/02/05 satisfied: per-watch rates are visible in Catalog without purchase.
- STATS-04 satisfied: Collection shows equipped contribution clearly and updates on switch.
</success_criteria>

<output>
After completion, create `.planning/phases/45-per-watch-stats-surfaces/45-02-SUMMARY.md`
</output>

