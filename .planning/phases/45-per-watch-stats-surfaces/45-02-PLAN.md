---
phase: 45-per-watch-stats-surfaces
plan: 2
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - src/ui/components/PerWatchStatsTable.tsx
  - src/ui/tabs/CatalogTab.tsx
  - src/ui/tabs/CollectionTab.tsx
  - src/style.css
autonomous: true

must_haves:
  truths:
    - "Catalog includes a per-watch stats surface (table/detail list) that shows each watch's enjoyment rate and the explicitly-labeled cash rate, without requiring purchase."
    - "Player can sort/filter the per-watch rows (enjoyment primary, cash secondary) and expanded rows remain stable across reorders."
    - "Collection view highlights the equipped (worn) watch contribution in a clear call-out using the same underlying selector math."
  artifacts:
    - path: "src/ui/components/PerWatchStatsTable.tsx"
      provides: "Reusable per-watch stats table/list with sticky controls + expandable rows"
    - path: "src/ui/tabs/CatalogTab.tsx"
      provides: "Catalog wiring for per-watch stats surface"
    - path: "src/ui/tabs/CollectionTab.tsx"
      provides: "Equipped watch contribution call-out"
    - path: "src/style.css"
      provides: "Sticky header + table/list styling and mobile layout rules"
  key_links:
    - from: "src/ui/components/PerWatchStatsTable.tsx"
      to: "src/game/state.ts"
      via: "selector consumption"
      pattern: "getPerWatchStatsRows|getEquippedWatchContribution"
    - from: "src/ui/tabs/CatalogTab.tsx"
      to: "src/ui/components/PerWatchStatsTable.tsx"
      via: "render per-watch stats section"
      pattern: "PerWatchStatsTable"

<objective>
Render Phase 45's per-watch stats UI: a sortable, mobile-friendly table/detail list with a sticky control header, plus an equipped watch contribution call-out in Collection.

Purpose: Make per-watch rates visible and actionable (compare/sort/filter) while staying consistent with existing Catalog UI patterns.
Output: New reusable component + Catalog/Collection integration + styling.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-per-watch-stats-surfaces/45-CONTEXT.md
@.planning/phases/45-per-watch-stats-surfaces/45-RESEARCH.md

@src/ui/tabs/CatalogTab.tsx
@src/ui/tabs/CollectionTab.tsx
@src/ui/tabs/StatsTab.tsx
@src/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a reusable per-watch stats table component</name>
  <files>
src/ui/components/PerWatchStatsTable.tsx
  </files>
  <action>
- Create `PerWatchStatsTable` that renders a semantic table-like list (either `<table>` or a list with CSS grid) with:
  - Sticky header row containing sorting + minimal filtering controls (desktop + mobile friendly).
  - Rows keyed by `modelId` (stable key) and expandable details using controlled `<details>` (persist expansion per `modelId` even after sorting).
  - Columns: Watch name, Tier/movement, Enjoyment / sec, Cash / sec.
  - Ensure cash column label makes semantics explicit (e.g. "Cash / sec (career)") if cash is not watch-dependent.
- Add stable anchors for tests:
  - `data-testid="per-watch-stats"` on the root
  - `data-testid="per-watch-row-${modelId}"` per row
  - `data-testid="per-watch-sort"` and similar for controls.
- Keep component under ~300 LOC; extract small helpers if needed.
  </action>
  <verify>
pnpm typecheck
  </verify>
  <done>
- Component renders with stable test ids and controlled expansions keyed by model id.
- Sticky controls are structured to work with CSS `position: sticky` (no transforms on the sticky ancestor).
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire per-watch stats into Catalog and the equipped contribution call-out into Collection</name>
  <files>
src/ui/tabs/CatalogTab.tsx
src/ui/tabs/CollectionTab.tsx
  </files>
  <action>
- In `src/ui/tabs/CatalogTab.tsx` (inside `CatalogPurchasePanel`), compute per-watch rows via the new selector exports (from `src/game/state.ts`) using the existing `state`, `nowMs`, and `currentEventMultiplier` props already available in the purchase panel.
- Render a new section near the top of the catalog shop (above the unowned/owned cards) titled "Per-watch stats" (or similar) containing `PerWatchStatsTable`.
- In `src/ui/tabs/CollectionTab.tsx`, add an equipped watch contribution call-out near the worn-watch picker:
  - Show worn watch label (if equipped)
  - Show enjoyment delta + worn multiplier (e.g. "+$X/s enjoyment (x1.05)")
  - Show cash contribution as 0 / career-based explanation consistent with Phase 45 semantics.
- Keep existing ids and `data-testid` selectors stable; prefer adding new ones rather than changing existing.
  </action>
  <verify>
pnpm typecheck
  </verify>
  <done>
- Catalog renders the per-watch table without requiring purchase.
- Collection shows a clear equipped watch contribution call-out tied to selector math.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add styling for sticky controls and table/list layout (desktop + mobile)</name>
  <files>
src/style.css
  </files>
  <action>
- Add minimal new classes used by `PerWatchStatsTable`:
  - Sticky controls background, z-index, and border/shadow so content doesn't bleed under it.
  - Table/list spacing, column alignment, and readable numeric alignment.
  - Mobile rules: ensure controls remain reachable while scrolling, rows remain tap-friendly (>=44px), and expanded details are readable.
- Respect `prefers-reduced-motion` for any transitions (disable where appropriate).
  </action>
  <verify>
pnpm lint
  </verify>
  <done>
- Sticky header works in the intended scroll container and remains legible on mobile.
- Touch targets meet the 44px standard for primary row actions/expand toggles.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm lint`
</verification>

<success_criteria>
- Per-watch stats surface exists in Catalog and is sortable/filterable with stable expansions.
- Equipped watch contribution call-out exists in Collection.
- Styling is mobile-friendly with a sticky control header.
</success_criteria>

<output>
After completion, create `.planning/phases/45-per-watch-stats-surfaces/45-02-SUMMARY.md`
</output>
