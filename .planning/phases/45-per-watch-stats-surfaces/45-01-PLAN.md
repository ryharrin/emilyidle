---
phase: 45-per-watch-stats-surfaces
plan: 01
type: execute
wave: 1
depends_on:
  - 44-02
files_modified:
  - src/game/data/watchModels.ts
  - src/game/selectors/watchModels.ts
  - src/game/selectors/enjoyment.ts
  - src/game/selectors/index.ts
  - src/game/sim.ts
  - src/game/state.ts
  - tests/rate-breakdowns.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "Per-model enjoyment/sec is deterministic and varies within a tier."
    - "Per-model cash/sec is defined, deterministic, and varies within a tier."
    - "Displayed per-model stats reflect real underlying calculations (no fake numbers)."
  artifacts:
    - path: src/game/selectors/watchModels.ts
      provides: "Per-model rate helpers used by UI"
      contains: "getWatchModel"
    - path: src/game/selectors/enjoyment.ts
      provides: "Enjoyment rate uses per-model variance for owned watches"
      contains: "watchModels"
    - path: src/game/selectors/index.ts
      provides: "Cash rate calculation updated per chosen cash model"
      contains: "getEffectiveCashRateCentsPerSec"
---

<objective>
Implement per-watch (model-level) rate computation with meaningful within-tier variance and a clear definition for “cash/sec”, so UI can surface truthful per-watch stats.

Purpose: Deliver STATS-01/02/03/05 foundations in the domain layer.
Output: Deterministic per-model rate selectors and updated aggregate rate calculations.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/45-per-watch-stats-surfaces/45-CONTEXT.md

@src/game/data/watchModels.ts
@src/game/data/items.ts
@src/game/selectors/enjoyment.ts
@src/game/selectors/index.ts
@src/game/sim.ts
@src/game/state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic per-model multipliers (hash-based)</name>
  <files>
src/game/data/watchModels.ts
src/game/selectors/watchModels.ts
  </files>
  <action>
Add deterministic within-tier variance using a stable hash of the model id:

- Implement a small `hashStringToUnitInterval(id) -> 0..1` helper (no dependencies).
- Derive two multipliers from that hash:
  - `enjoymentMultiplier` (e.g. 0.9–1.1)
  - `cashMultiplier` (e.g. 0.9–1.1) OR a single shared multiplier used for both.
- Expose selectors usable by UI without requiring ownership:
  - `getWatchModelEnjoymentRateCentsPerSec(modelId): number`
  - `getWatchModelCashRateCentsPerSec(modelId): number`

Do not persist these values; they must be derivable from ids so they remain stable across saves.
  </action>
  <verify>pnpm -s run typecheck</verify>
  <done>
- Per-model multipliers are deterministic and stable for a given model id.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make aggregate enjoyment/cash rates use per-model variance (truthful stats)</name>
  <files>
src/game/selectors/enjoyment.ts
src/game/selectors/index.ts
src/game/sim.ts
src/game/state.ts
tests/rate-breakdowns.unit.test.ts
  </files>
  <action>
Update calculations so “per-watch stats” are real:

- Enjoyment:
  - Update the owned-model enjoyment accumulation to include the per-model multiplier (instead of tier-uniform rate).
  - Ensure existing multipliers (prestige legacy, upgrades, worn watch bonus, power reserve) still apply correctly.

- Cash:
  - Pick the cash model from `45-CONTEXT.md` and implement it:
    - If “Market cash/sec”: incorporate watch model cash contributions into `getTotalCashRateCentsPerSec` and ensure `step()` accrues it.
    - If “Career cash bonus”: implement a worn-watch cash multiplier and compute the resulting cash rate accordingly.
  - Keep the implementation deterministic and testable.

- Exports:
  - Export the new selectors via `src/game/state.ts` so UI can use them.

- Tests:
  - Extend/add assertions in `tests/rate-breakdowns.unit.test.ts` (or a new unit test if better) that:
    - two models in the same tier can have different enjoyment/sec
    - the cash model is deterministic and consistent with selector outputs
  </action>
  <verify>pnpm -s run test:unit -- tests/rate-breakdowns.unit.test.ts</verify>
  <done>
- Aggregate rates reflect the per-model variance and the chosen cash model is test-protected.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit -- tests/rate-breakdowns.unit.test.ts`
</verification>

<success_criteria>
- STATS-03 satisfied at the domain level: within-tier variance exists and is deterministic.
- The chosen “cash/sec per watch” definition is implemented in real calculations (not only displayed).
</success_criteria>

<output>
After completion, create `.planning/phases/45-per-watch-stats-surfaces/45-01-SUMMARY.md`
</output>

