---
phase: 45-per-watch-stats-surfaces
plan: 1
type: execute
wave: 1
depends_on: ["44-01"]
files_modified:
  - src/game/selectors/perWatchStats.ts
  - src/game/selectors/index.ts
  - src/game/state.ts
  - tests/per-watch-stats.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "Player can derive per-watch enjoyment and cash rates for every catalog watch (including unowned) using pure selectors (no UI math)."
    - "Equipped watch contribution is computable as a clear delta or multiplier explanation based on the worn-watch multiplier selector."
    - "Cash-rate semantics are explicit: cash is currently career-based, so per-watch cash rows are anchored to the global cash rate and labeled accordingly (no invented allocations)."
  artifacts:
    - path: "src/game/selectors/perWatchStats.ts"
      provides: "Per-watch stats view-model selectors + equipped contribution summary"
    - path: "tests/per-watch-stats.unit.test.ts"
      provides: "Regression coverage for per-watch row outputs and equipped contribution math"
    - path: "src/game/state.ts"
      provides: "UI-safe facade exports for per-watch stats selectors"
  key_links:
    - from: "src/game/selectors/perWatchStats.ts"
      to: "src/game/selectors/index.ts"
      via: "re-export + shared rate helpers"
      pattern: "getCashRateBreakdown|getEnjoymentRateBreakdown"
    - from: "tests/per-watch-stats.unit.test.ts"
      to: "src/game/selectors/perWatchStats.ts"
      via: "selector contract assertions"
      pattern: "getPerWatchStats"

<objective>
Create a selector-backed per-watch stats view model (rows + equipped contribution) so Phase 45 UI can render sortable per-watch rates without duplicating domain math in React.

Purpose: Satisfy STATS-01/03/04/05 foundations by producing a single, testable source of truth for per-watch rates and equipped contribution.
Output: New selectors + unit tests + facade exports.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-per-watch-stats-surfaces/45-CONTEXT.md
@.planning/phases/45-per-watch-stats-surfaces/45-RESEARCH.md

@src/game/selectors/index.ts
@src/game/selectors/enjoyment.ts
@src/game/selectors/watchModels.ts
@src/game/state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a pure per-watch stats selector (view model rows + equipped contribution)</name>
  <files>
src/game/selectors/perWatchStats.ts
  </files>
  <action>
- Create `getPerWatchStatsRows(state, nowMs, eventMultiplier?)` that returns an array of rows keyed by stable `modelId` with at least:
  - `modelId`, `tierId`, `ownedCount`
  - `enjoymentCentsPerSec` (the watch's per-copy enjoyment rate at current multipliers; keep a raw/base field if needed for UI)
  - `cashCentsPerSec` (career-based cash rate value; same for all rows) and a `cashSource: "career"` marker
  - optional fields needed for sorting/filtering (tier label, movement, etc.) but avoid JSX/DOM concerns.
- Add `getEquippedWatchContribution(state, nowMs, eventMultiplier?)` that returns:
  - worn watch id (if any)
  - worn enjoyment multiplier (e.g. x1.05)
  - enjoyment delta in cents/sec computed from selector math (effective-with-worn minus effective-without-worn)
  - cash delta (should be 0 with a clear explanation field) because cash is currently career-derived.
- Keep selectors pure (no Date.now, no localStorage). Thread `nowMs` in for career cash.
- Do NOT invent per-watch cash allocations: either use the global cash rate, or return 0 + an explicit explanation string; choose one and keep it consistent with tests.
  </action>
  <verify>
pnpm typecheck
  </verify>
  <done>
- New selectors compile in strict TS and return stable, UI-ready rows keyed by model id.
- Equipped contribution includes a deterministic enjoyment delta and an explicit cash semantics marker.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire selectors through the domain facade for UI consumption</name>
  <files>
src/game/selectors/index.ts
src/game/state.ts
  </files>
  <action>
- Export the new per-watch selectors from `src/game/selectors/index.ts` (or the closest existing barrel pattern).
- Re-export them from `src/game/state.ts` so UI can import from the facade (avoid deep imports in UI).
- Keep naming consistent with existing selector naming (`getX`, `type X`).
  </action>
  <verify>
pnpm typecheck
  </verify>
  <done>
- UI can import the per-watch selectors from `src/game/state.ts` without circular deps.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for per-watch row outputs and equipped contribution math</name>
  <files>
tests/per-watch-stats.unit.test.ts
  </files>
  <action>
- Add unit tests that build a small seeded state with:
  - multiple watch models owned across tiers
  - a worn watch set (to exercise worn multiplier)
  - a deterministic `nowMs`
- Assert:
  - rows are returned for catalog models (at least the ones referenced by the seeded state)
  - enjoyment cents/sec values are finite and increase with higher enjoyment tiers
  - cash cents/sec equals the same career-based value for all rows (or equals 0 + explanation, depending on the chosen semantics)
  - equipped contribution enjoyment delta is > 0 when worn multiplier > 1
- Keep the test contract focused on outputs, not internal helper calls.
  </action>
  <verify>
pnpm test:unit -- tests/per-watch-stats.unit.test.ts
  </verify>
  <done>
- Tests fail if per-watch enjoyment rows drift or if equipped contribution math regresses.
- Tests explicitly lock down the cash-rate semantics chosen for Phase 45.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/per-watch-stats.unit.test.ts`
</verification>

<success_criteria>
- A single selector-backed view model exists for per-watch rows and equipped contribution, exported via `src/game/state.ts`.
- Unit tests lock down per-watch enjoyment math and the chosen cash-rate semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/45-per-watch-stats-surfaces/45-01-SUMMARY.md`
</output>
