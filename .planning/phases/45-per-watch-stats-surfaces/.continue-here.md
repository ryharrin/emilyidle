---
phase: 45-per-watch-stats-surfaces
task: 2
total_tasks: 5
status: in_progress
last_updated: 2026-02-03T17:35:00Z
---

<current_state>
Phase 45 research/context are complete and the phase has not yet been broken down into executable plans; the current work is verifying whether STATS-02 (per-watch cash rate) already exists in the domain before drafting the plan outline.
</current_state>

<completed_work>

- Task 1: Gathered the phase's context and documented the key UI/UX decisions plus pitfalls in `45-CONTEXT.md` and `45-RESEARCH.md` so the next planner can work from a shared understanding. - Done
- Task 2: Captured the mobile polish research for Phase 47 and aligned the quartz modal reward flow (App tap hint wiring, quartz reward moving from cash to enjoyment, and tests) so all branches stay in sync before the new stats surface is designed. - Done
- Task 3: Framed the STATS-02 question about whether per-watch cash contributions exist in the current economic selectors and flagged it in `45-RESEARCH.md`. - In progress
</completed_work>

<remaining_work>

- Task 3: Determine how STATS-02 should be satisfied (trace `getCashRateBreakdown`, confirm if watches contribute cash, or decide on an explicit exception). - Not finished
- Task 4: Draft the executable plan for Phase 45 (per-watch selectors, sortable table/list, sticky header, filter controls, equipped watch call-out). - Not started
- Task 5: Once the plan is approved, stage the implementation (selectors + UI + tests). - Not started
</remaining_work>

<decisions_made>

- Present the stats as a detail list/table with expandable rows, sorted/filtered via sticky header controls that stay usable on mobile (per `45-CONTEXT.md`).
- Keep the per-watch data coming from new selectors that reuse `formatRateFromCentsPerSec`/existing multiplier helpers rather than re-implementing currency math in React.
- Reuse the sticky header, `<details>` expansion, and filter patterns already used in `CatalogTab.tsx` so the new surface fits the established UI vocabulary.
</decisions_made>

<blockers>

- STATS-02 per-watch cash rate: the current cash breakdown seems to originate exclusively from career salary/event multipliers, so the plan must explicitly decide whether to extend the domain or surface a placeholder/clarification. - waiting on trace
</blockers>

<context>
Thinking through Phase 45 planning while keeping the quartz modal reward/enjoyment change (+tap hint logic in `src/App.tsx`, `src/game/actions/interactions.ts`, `tests/interactions.unit.test.ts`) and the latest research files (Phase 45 + Phase 47) in mind; need to wrap the existing dirty files into the next commit once the plan moves forward.
</context>

<next_action>
Start with: trace `getCashRateBreakdown` / related selectors to confirm whether per-watch cash contributions exist (resolve the STATS-02 open question) before drafting the Phase 45 plan.
</next_action>
