---
phase: 51-quality-of-life-events
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/actions/index.ts
  - src/game/selectors/perWatchStats.ts
  - src/ui/tabs/CatalogTab.tsx
  - src/ui/tabs/CollectionTab.tsx
  - tests/catalog-favorites.unit.test.tsx
  - tests/purchase-undo.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "Players can mark and unmark favorite watches, then filter catalog/collection views by favorites only."
    - "The most recent purchase can be undone within a short window and restores prior resource totals."
    - "Expired or invalid undo attempts fail gracefully without corrupting progression state."
  artifacts:
    - path: "src/game/actions/index.ts"
      provides: "Deterministic undo-last-purchase transition with bounded validity window"
    - path: "src/ui/tabs/CatalogTab.tsx"
      provides: "Favorite toggle + favorites-only filter UI in catalog browsing"
    - path: "src/ui/tabs/CollectionTab.tsx"
      provides: "Favorite-aware collection filtering and quick-reference surfacing"
  key_links:
    - from: "src/ui/tabs/CatalogTab.tsx"
      to: "src/game/actions/index.ts"
      via: "favorite and undo triggers wired through existing action handlers"
      pattern: "favorite|undo"
    - from: "tests/purchase-undo.unit.test.ts"
      to: "src/game/actions/index.ts"
      via: "state transition assertions for undo window and balance restoration"
      pattern: "undo"
---

<objective>
Deliver quick-reference quality-of-life controls: favorites and short-window purchase undo.

Purpose: `FAVE-01` and `UNDO-01` reduce accidental friction and speed up repeated browsing/purchasing loops.
Output: Favorite filter/toggle interactions plus deterministic undo behavior with unit test coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/51-quality-of-life-events/51-CONTEXT.md
@src/game/actions/index.ts
@src/ui/tabs/CatalogTab.tsx
@src/ui/tabs/CollectionTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement bounded undo for latest purchase</name>
  <files>
src/game/actions/index.ts
tests/purchase-undo.unit.test.ts
  </files>
  <action>
Add an undoable purchase envelope in domain actions for the most recent purchase only.

Implementation requirements:
- Capture enough purchase metadata to reverse the latest purchase safely.
- Enforce a short undo validity window (time-bounded) and single-use semantics.
- Reject undo when inventory, currency, or ownership invariants would break.
- Keep undo transition pure and deterministic with `nowMs` passed in.
  </action>
  <verify>pnpm test:unit -- tests/purchase-undo.unit.test.ts</verify>
  <done>
- Undo reverts only the latest purchase within the validity window.
- Expired/invalid undo attempts return unchanged state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add favorite toggles and favorites-only filtering</name>
  <files>
src/game/selectors/perWatchStats.ts
src/ui/tabs/CatalogTab.tsx
src/ui/tabs/CollectionTab.tsx
tests/catalog-favorites.unit.test.tsx
  </files>
  <action>
Add favorite affordances in both Catalog and Collection with additive filter controls.

Implementation requirements:
- Add favorite toggle controls with stable additive `data-testid` anchors.
- Add favorites-only filter in Catalog and Collection without changing default sort/filter semantics.
- Keep selectors/view-model logic deterministic and avoid UI-only data duplication.
- Ensure favorite display state remains consistent across both tabs in the same session.
  </action>
  <verify>pnpm test:unit -- tests/catalog-favorites.unit.test.tsx</verify>
  <done>
- Players can mark favorites and quickly filter to favorite items in both browsing surfaces.
- Existing tab and selector contracts remain intact.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/purchase-undo.unit.test.ts`
- `pnpm test:unit -- tests/catalog-favorites.unit.test.tsx`
</verification>

<success_criteria>
- `UNDO-01`: latest purchase undo works within a short bounded window.
- `FAVE-01`: favorite toggles + favorites-only filtering work in catalog and collection.
</success_criteria>

<output>
After completion, create `.planning/phases/51-quality-of-life-events/51-02-SUMMARY.md`
</output>
