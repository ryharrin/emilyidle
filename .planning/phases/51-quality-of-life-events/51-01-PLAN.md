---
phase: 51-quality-of-life-events
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/runtime/useGameRuntime.ts
  - src/game/persistence.ts
  - src/App.tsx
  - src/ui/tabs/SaveTab.tsx
  - tests/persistence.unit.test.ts
  - tests/persistence-compat.unit.test.ts
  - tests/localstorage-schema.unit.test.tsx
autonomous: true

must_haves:
  truths:
    - "Returning after time away grants capped offline gains with a clear explanation of elapsed and capped duration."
    - "Players can export saves and import via paste/file without breaking current save compatibility."
    - "Invalid import payloads fail with explicit error feedback and do not mutate active progress."
  artifacts:
    - path: "src/game/runtime/useGameRuntime.ts"
      provides: "Offline elapsed-time calculation and capped gain application entrypoint"
    - path: "src/game/persistence.ts"
      provides: "Import/export-safe persistence helpers preserving v2 + legacy contracts"
    - path: "src/ui/tabs/SaveTab.tsx"
      provides: "Save export/import controls for paste + file workflows"
  key_links:
    - from: "src/game/runtime/useGameRuntime.ts"
      to: "src/game/persistence.ts"
      via: "last-simulated timestamps used for capped offline credit"
      pattern: "lastSimulatedAtMs"
    - from: "src/ui/tabs/SaveTab.tsx"
      to: "src/App.tsx"
      via: "import/export handlers + status messaging"
      pattern: "onExport|onImport|saveStatus"
---

<objective>
Implement offline-gain resilience and robust save import/export flows.

Purpose: `OFFLINE-01` and `SAVE-01` are foundational quality-of-life contracts that must be stable before higher-level progression features.
Output: Capped offline gain handling plus validated export/import UX covered by persistence guardrail tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/51-quality-of-life-events/51-CONTEXT.md
@.planning/phases/51-quality-of-life-events/51-RESEARCH.md

@src/game/runtime/useGameRuntime.ts
@src/game/persistence.ts
@src/ui/tabs/SaveTab.tsx
@tests/localstorage-schema.unit.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add capped offline gain application path</name>
  <files>
src/game/runtime/useGameRuntime.ts
src/App.tsx
  </files>
  <action>
Add offline progress calculation on save load using `lastSimulatedAtMs` and current wall clock.

Implementation requirements:
- Compute elapsed offline ms from persisted timestamp.
- Cap applied duration to a fixed maximum window (for example, 8h).
- Apply the same sim stepping path as live runtime (not a separate economy branch).
- Surface a concise UI message/toast summarizing elapsed, capped time, and net gains.
- Keep all domain math deterministic and avoid duplicating selector logic.
  </action>
  <verify>pnpm test:unit -- tests/persistence.unit.test.ts tests/persistence-compat.unit.test.ts</verify>
  <done>
- Returning players receive bounded offline gains and explanatory feedback.
- Offline application does not bypass existing simulation invariants.
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand SaveTab import/export for paste and file workflows</name>
  <files>
src/game/persistence.ts
src/ui/tabs/SaveTab.tsx
tests/localstorage-schema.unit.test.tsx
  </files>
  <action>
Extend save UX to support both copy/paste and file-based import/export using existing persistence decode/encode contracts.

Implementation requirements:
- Add file upload import path in SaveTab without removing current paste import flow.
- Keep export output compatible with current decode path.
- Route all imports through validation in `persistence.ts` (no blind UI parse).
- Emit explicit status messages for success/failure and leave state unchanged on failure.
- Update schema guardrail tests for any additive persisted fields introduced for offline bookkeeping.
  </action>
  <verify>pnpm test:unit -- tests/localstorage-schema.unit.test.tsx tests/persistence.unit.test.ts</verify>
  <done>
- Save import/export supports paste and file workflows safely.
- Invalid payloads are rejected with clear messaging and no data loss.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/persistence.unit.test.ts tests/persistence-compat.unit.test.ts`
- `pnpm test:unit -- tests/localstorage-schema.unit.test.tsx`
</verification>

<success_criteria>
- `OFFLINE-01`: capped offline gains are applied and explained.
- `SAVE-01`: save import/export works for paste + file with validation and compatibility preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/51-quality-of-life-events/51-01-SUMMARY.md`
</output>
