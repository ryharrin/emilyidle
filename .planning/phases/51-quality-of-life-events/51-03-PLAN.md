---
phase: 51-quality-of-life-events
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/ui/components/ToastStack.tsx
  - src/ui/tabs/SaveTab.tsx
  - tests/achievement-toast.unit.test.tsx
  - tests/notifications-preferences.unit.test.tsx
  - tests/achievements-toast.spec.ts
autonomous: true

must_haves:
  truths:
    - "Players can opt in/out of notifications for sessions, prestige readiness, achievements, and events."
    - "Newly unlocked achievements trigger toast feedback when achievement notifications are enabled."
    - "Notification preferences persist and are respected across reloads."
  artifacts:
    - path: "src/ui/tabs/SaveTab.tsx"
      provides: "Notification preference controls in settings"
    - path: "src/App.tsx"
      provides: "Preference persistence + toast gating logic for unlock/session/prestige/event signals"
    - path: "src/ui/components/ToastStack.tsx"
      provides: "Achievement/event/session toast rendering path with additive metadata support"
  key_links:
    - from: "src/ui/tabs/SaveTab.tsx"
      to: "src/App.tsx"
      via: "persisted settings update callbacks"
      pattern: "notification"
    - from: "src/App.tsx"
      to: "src/ui/components/ToastStack.tsx"
      via: "toast queue generation from unlocked achievements and readiness signals"
      pattern: "toast"
---

<objective>
Add notification controls and achievement unlock toasts tied to user preferences.

Purpose: `NOTIF-01` and `ACHIEVE-01` require preference-controlled feedback loops that reinforce progress without spamming users.
Output: Persisted notification toggles and gated achievement toasts with unit/e2e regression coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/51-quality-of-life-events/51-CONTEXT.md
@src/App.tsx
@src/ui/tabs/SaveTab.tsx
@src/ui/components/ToastStack.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add persisted notification preference settings</name>
  <files>
src/ui/tabs/SaveTab.tsx
src/App.tsx
tests/notifications-preferences.unit.test.tsx
  </files>
  <action>
Add a notification preferences block in Settings with explicit toggles for:
- sessions ready,
- prestige readiness,
- achievement unlock,
- event start/end.

Implementation requirements:
- Persist preferences through existing settings storage flow (no new localStorage key).
- Default preferences should be additive and backward-compatible for existing saves/settings.
- Keep accessibility labels and touch targets consistent with current settings patterns.
  </action>
  <verify>pnpm test:unit -- tests/notifications-preferences.unit.test.tsx</verify>
  <done>
- Notification preferences are user-configurable and survive reloads.
- Existing settings schema remains backward-compatible.
  </done>
</task>

<task type="auto">
  <name>Task 2: Gate achievement/session/event toasts by notification preferences</name>
  <files>
src/App.tsx
src/ui/components/ToastStack.tsx
tests/achievement-toast.unit.test.tsx
tests/achievements-toast.spec.ts
  </files>
  <action>
Integrate preference-aware toast emission for achievement unlocks and notification signals.

Implementation requirements:
- Detect newly unlocked achievements and enqueue one-time unlock toasts.
- Respect per-channel notification toggles before enqueueing.
- Preserve existing toast queue behavior, dismissal semantics, and accessibility announcements.
- Add deterministic tests for enabled/disabled cases and no-duplicate behavior.
  </action>
  <verify>pnpm test:e2e -- tests/achievements-toast.spec.ts</verify>
  <done>
- Achievement and readiness toasts appear only when relevant toggles are enabled.
- Duplicate unlock toasts are prevented.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`
- `pnpm test:unit -- tests/notifications-preferences.unit.test.tsx tests/achievement-toast.unit.test.tsx`
- `pnpm test:e2e -- tests/achievements-toast.spec.ts`
</verification>

<success_criteria>
- `NOTIF-01`: notification preferences exist and gate notifications by channel.
- `ACHIEVE-01`: achievement unlock toasts are emitted and preference-controlled.
</success_criteria>

<output>
After completion, create `.planning/phases/51-quality-of-life-events/51-03-SUMMARY.md`
</output>
