---
phase: 38-catalog-lock-disabled-explanations
plan: 01
type: execute
wave: 1
files_modified:
  - src/ui/tabs/CatalogTab.tsx
  - src/ui/components/catalog/CatalogPurchaseGate.tsx
  - src/ui/components/catalog/CatalogDisabledExplanation.tsx
autonomous: true

must_haves:
  truths:
    - "Undiscovered catalog cards remain visible and show a lock icon overlay while using the existing greyed-out treatment."
    - "When purchase is unavailable (tier locked or insufficient resources), the card shows a gate chip plus an in-place 'Why can't I buy?' explainer without rendering any `catalog-buy-*` control."
    - "When both cash and enjoyment deficits apply, the expanded explanation lists both requirements/deficits (not just the primary `blocksBy`)."
  artifacts:
    - path: src/ui/tabs/CatalogTab.tsx
      provides: "Catalog purchase cards render lock overlay for undiscovered entries and delegate buy/gate rendering to a shared component."
    - path: src/ui/components/catalog/CatalogPurchaseGate.tsx
      provides: "Single-source rendering for `catalog-buy-*` vs `catalog-gate-*` plus the explanation affordance."
    - path: src/ui/components/catalog/CatalogDisabledExplanation.tsx
      provides: "Expandable, testable inline explanation UI with stable selectors."
  key_links:
    - from: src/ui/tabs/CatalogTab.tsx
      to: src/ui/components/catalog/CatalogPurchaseGate.tsx
      via: "component render in the catalog action bar"
      pattern: "<CatalogPurchaseGate"
    - from: src/ui/components/catalog/CatalogPurchaseGate.tsx
      to: "catalog-buy-*"
      via: "conditional render when unlocked && gate.ok"
      pattern: "gate\\.ok"
    - from: src/ui/components/catalog/CatalogPurchaseGate.tsx
      to: "catalog-gate-*"
      via: "conditional render when !unlocked || !gate.ok"
      pattern: "catalog-gate-\\$\\{"
---

<objective>
Surface lock and disabled explanations directly on catalog cards while keeping existing buy/gate selector semantics stable.

Purpose: Satisfy CAT-02/CAT-03 by making undiscovered items clearly visible (locked styling + lock icon) and providing an in-context "Why can't I buy?" explanation for disabled purchase states.
Output: A lock icon overlay for undiscovered cards and a reusable purchase-gate UI that adds an expandable explanation panel next to `catalog-gate-*` chips without changing when `catalog-buy-*` renders.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@.planning/phases/38-catalog-lock-disabled-explanations/38-RESEARCH.md

@src/ui/tabs/CatalogTab.tsx
@src/ui/icons/coreIcons.tsx
@src/game/selectors/watchModels.ts
@tests/catalog.unit.test.tsx
@tests/collection-loop.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a lock icon overlay for undiscovered catalog cards (CAT-02)</name>
  <files>src/ui/tabs/CatalogTab.tsx</files>
  <action>
  - In `src/ui/tabs/CatalogTab.tsx` (CatalogPurchasePanel), for both the Unowned and Owned card render paths, add a lock icon overlay inside `.catalog-media` when `!discovered`.
  - Use the existing `LockIcon` from `src/ui/icons/coreIcons.tsx` (do not introduce new icon libs).
  - Add a stable selector for tests: render `data-testid={\`catalog-lock-${entry.id}\`}` on the lock overlay element.
  - Add a stable CSS hook for 38-02: ensure the overlay element also has `className="catalog-lock-icon"`.
  - Keep existing discovery/buy semantics stable:
    - Do not hide undiscovered entries.
    - Keep the existing `.catalog-locked` class behavior (grayscale) and the existing `Undiscovered` badge.
    - Do not change any `catalog-buy-*` / `catalog-gate-*` rendering logic in this task.
  </action>
  <verify>pnpm run typecheck</verify>
  <done>
  - At least one card on a fresh save (no discoveries) renders `data-testid="catalog-lock-..."` and is still visible in the catalog grid.
  - Existing `data-testid="catalog-card"`, `catalog-buy-*`, and `catalog-gate-*` selectors remain unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add an inline "Why can't I buy?" explainer for disabled purchase states (CAT-03)</name>
  <files>src/ui/components/catalog/CatalogPurchaseGate.tsx, src/ui/components/catalog/CatalogDisabledExplanation.tsx, src/ui/tabs/CatalogTab.tsx</files>
  <action>
  - Create `src/ui/components/catalog/CatalogDisabledExplanation.tsx`:
    - Render an expandable inline explanation using `<details>` / `<summary>`.
    - Use stable selectors:
      - `summary` gets `data-testid={\`catalog-why-${entryId}\`}` and visible label text exactly `Why can't I buy?`.
      - the `<details>` root gets `data-testid={\`catalog-explain-${entryId}\`}`.
    - Add a stable CSS hook for 38-02: set `className="catalog-disabled-explanation"` on the `<details>` root.
    - Accept a typed `reasons: Array<{ title: string; body: React.ReactNode }>` and render them as a short list.
    - Keep this component presentation-only: do not recompute gate math here.

  - Create `src/ui/components/catalog/CatalogPurchaseGate.tsx`:
    - Inputs (minimum): `entryId`, `discovered`, `unlocked`, `unlockDetail?`, `unlockCurrentLabel?`, `unlockThresholdLabel?`, `gate`, `buyLabel`, `onBuy`.
    - Preserve selector semantics (hard requirement):
      - Render `data-testid={\`catalog-buy-${entryId}\`}` ONLY when `unlocked && gate.ok`.
      - Render `data-testid={\`catalog-gate-${entryId}\`}` when `!unlocked || !gate.ok`.
    - Keep the existing gate-chip copy consistent with current behavior:
      - If `!unlocked`: chip text is `Locked`.
      - Else if `gate.blocksBy === "enjoyment"`: chip includes `Requires {enjoymentRequired}` and optionally `({enjoymentDeficit} more)`.
      - Else if `gate.blocksBy === "cash"`: chip includes `Need {cashDeficit} more`.
    - When the chip is shown, render `CatalogDisabledExplanation` directly under/next-to the chip (in the same general action area) and build `reasons` by combining existing computations:
      - If `!discovered`: include a reason explaining the reference is undiscovered (copy should not claim purchase is blocked by discovery).
      - If `!unlocked`: include a reason using `unlockDetail.label` and, when labels are provided, include the `current/threshold` values.
      - If `!gate.ok`: include reasons for BOTH deficits when present:
        - enjoyment deficit: show required enjoyment and deficit (use the `gate.enjoymentRequiredCents` + `gate.enjoymentDeficitCents`).
        - cash deficit: show price and deficit (use the `gate.cashPriceCents` + `gate.cashDeficitCents`).
      - Design for future reasons: allow `extraReasons?: reasons[]` (e.g. capacity) without changing rendering structure.
    - Use `formatMoneyFromCents()` for all cents formatting; do not duplicate or re-derive purchase math.

    - Add a stable wrapper CSS hook for 38-02 around the gate chip + explainer:
      - Wrap the chip and `CatalogDisabledExplanation` in an element with `className="catalog-gate-stack"`.
  
  - In `src/ui/tabs/CatalogTab.tsx`, replace the duplicated inline buy/gate rendering blocks in CatalogPurchasePanel (both Unowned and Owned sections) with `CatalogPurchaseGate`.
    - Keep all existing `unlocked` / `unlockDetail` / `unlockCurrentLabel` / `unlockThresholdLabel` computations in place; just pass them through.
    - Do not touch `CatalogTabLegacy` unless it breaks typecheck; focus on the active CatalogPurchasePanel code paths.
  </action>
  <verify>pnpm run typecheck && pnpm run test:unit -- tests/catalog.unit.test.tsx</verify>
  <done>
  - Disabled purchase states still render `catalog-gate-*` and do not render any `catalog-buy-*` controls.
  - A visible `Why can't I buy?` summary renders for disabled states and expands to show at least one reason.
  - When both `gate.enjoymentDeficitCents` and `gate.cashDeficitCents` exist, the expanded explanation includes both.
  </done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit -- tests/catalog.unit.test.tsx`
</verification>

<success_criteria>
- Undiscovered cards show a lock icon overlay (`data-testid="catalog-lock-*"`) while remaining visible.
- Disabled purchase states show `catalog-gate-*` plus a stable in-place explanation affordance (`catalog-why-*` / `catalog-explain-*`).
- `catalog-buy-*` rendering remains unchanged: only when purchase is possible.
</success_criteria>

<output>
After completion, create `.planning/phases/38-catalog-lock-disabled-explanations/38-01-SUMMARY.md`
</output>
