---
phase: 38-catalog-lock-disabled-explanations
plan: 02
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - src/style.css
autonomous: true

must_haves:
  truths:
    - "Undiscovered cards look clearly locked (greyed) and the lock icon overlay is legible in both dark and light themes."
    - "The disabled purchase explanation affordance is visually subordinate to the gate chip but easy to find and readable when expanded."
  artifacts:
    - path: src/style.css
      provides: "Lock overlay and disabled explanation styling for catalog cards in dark/light themes."
  key_links:
    - from: src/style.css
      to: ".catalog-media"
      via: "positioned overlay within catalog media"
      pattern: "\\.catalog-media"
    - from: src/style.css
      to: ".catalog-disabled-explanation"
      via: "details/summary styling"
      pattern: "\\.catalog-disabled-explanation"
---

<objective>
Add styling for the Phase 38 lock overlay and disabled-purchase explanation UI so the new affordances are readable and consistent with the existing catalog card aesthetic.

Purpose: Ensure CAT-02/CAT-03 improvements are perceptible at a glance and do not regress actionable/non-actionable styling tests.
Output: CSS rules for `.catalog-lock-icon` and `.catalog-disabled-explanation` (plus any minimal helper classes introduced by 38-01).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-catalog-lock-disabled-explanations/38-RESEARCH.md

@src/style.css
@src/ui/tabs/CatalogTab.tsx
@tests/catalog-actionable-visual.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Style the undiscovered lock icon overlay</name>
  <files>src/style.css</files>
  <action>
  - In `src/style.css`, add a new `.catalog-lock-icon` style intended to live inside `.catalog-media` (which is already `position: relative`).
  - The lock overlay should:
    - be positioned in the top-left (do not overlap the existing `.catalog-badge` top-right),
    - have a small circular pill/background with border consistent with existing catalog accents,
    - size the SVG to ~16-18px.
  - Add a light theme variant under `[data-theme="light"]` for proper contrast.
  - Do not change existing `.catalog-card.catalog-actionable` / `.catalog-card.catalog-nonactionable` rules; Playwright checks computed styles.
  </action>
  <verify>pnpm run build</verify>
  <done>
  - Lock overlay is visible over images in both themes.
  - Existing actionable/non-actionable visual styling still applies (no CSS changes to those rules).
  </done>
</task>

<task type="auto">
  <name>Task 2: Style the disabled purchase explanation affordance</name>
  <files>src/style.css</files>
  <action>
  - In `src/style.css`, add styles for `.catalog-disabled-explanation` and its `summary`:
    - Summary reads like a small inline link/button (uppercase is optional; keep it consistent with existing catalog typography).
    - Expanded content reads like a small inset panel (border + subtle background) with a compact list.
  - If 38-01 introduced any helper wrapper class around the gate + explainer (recommended: `.catalog-gate-stack`), style it as a small vertical stack aligned to the right within the action bar.
  - Ensure focus-visible is clearly visible on the summary for keyboard users.
  </action>
  <verify>pnpm run build</verify>
  <done>
  - The explanation control is discoverable but does not visually compete with the primary buy button.
  - Expanded explanation is readable and does not break the card layout on narrow widths.
  </done>
</task>

</tasks>

<verification>
- `pnpm run build`
</verification>

<success_criteria>
- Lock overlay and disabled explanation UI are styled consistently across dark/light themes.
- No regressions to existing actionable/non-actionable styles used by `tests/catalog-actionable-visual.spec.ts`.
</success_criteria>

<output>
After completion, create `.planning/phases/38-catalog-lock-disabled-explanations/38-02-SUMMARY.md`
</output>
