---
phase: 38-catalog-lock-disabled-explanations
plan: 03
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - tests/catalog.unit.test.tsx
  - tests/catalog-disabled-explanations.spec.ts
autonomous: true

must_haves:
  truths:
    - "Automated tests assert that disabled purchase states render `catalog-gate-*` (not `catalog-buy-*`) and that the new explainer can be opened."
    - "Automated tests assert that undiscovered cards include a stable lock overlay selector (`catalog-lock-*`)."
  artifacts:
    - path: tests/catalog.unit.test.tsx
      provides: "Unit coverage for lock overlay and explanation UI behavior without changing existing selector semantics."
    - path: tests/catalog-disabled-explanations.spec.ts
      provides: "E2E smoke coverage for in-context disabled explanations in the Catalog tab."
  key_links:
    - from: tests/catalog-disabled-explanations.spec.ts
      to: "catalog-why-*"
      via: "click to expand explanation"
      pattern: "catalog-why-"
    - from: tests/catalog.unit.test.tsx
      to: "catalog-lock-*"
      via: "assert lock overlay exists for undiscovered"
      pattern: "catalog-lock-"
---

<objective>
Add unit and e2e regression coverage for Phase 38 so selector stability is preserved and the new lock/explanation affordances are protected.

Purpose: Prevent future refactors from removing lock/explanation UI or accidentally rendering disabled buy buttons that break `catalog-buy-*` semantics.
Output: New tests covering `catalog-lock-*`, `catalog-why-*`, and ensuring `catalog-buy-*` remains absent when gated.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-catalog-lock-disabled-explanations/38-RESEARCH.md

@tests/catalog.unit.test.tsx
@tests/collection-loop.spec.ts
@src/ui/tabs/CatalogTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Vitest coverage for lock overlay and disabled purchase explanation</name>
  <files>tests/catalog.unit.test.tsx</files>
  <action>
  - In `tests/catalog.unit.test.tsx`, add a new describe block for Phase 38 catalog gating explanations.
  - Seed localStorage save state similarly to existing tests (use `createInitialState()` and override fields) so that:
    - `discoveredCatalogEntries` is an empty array (ensures `!discovered`).
    - `currencyCents` and `enjoymentCents` are low enough to gate a known model.
  - Navigate to the Catalog tab via the rendered `App`, then use the existing catalog filters (`data-testid="catalog-filters"` / `catalog-search") to filter to a single known model used in Playwright (`rolex-rolex-gmt-master-ii-ref-126713grnr`).
  - Assertions (must keep selector semantics stable):
    - `queryAllByTestId(/catalog-buy-/)` does NOT include `catalog-buy-<modelId>` for that model.
    - `getByTestId(\`catalog-gate-${modelId}\`)` is visible.
    - `getByTestId(\`catalog-lock-${modelId}\`)` exists (since undiscovered).
    - `getByTestId(\`catalog-why-${modelId}\`)` exists; clicking it expands `catalog-explain-${modelId}` and the expanded content mentions BOTH deficits when both are present.
  - Keep the assertions resilient: assert on key phrases you control in the new explainer copy (avoid brittle full-sentence matching).
  </action>
  <verify>pnpm run test:unit -- tests/catalog.unit.test.tsx</verify>
  <done>
  - Unit tests fail if `catalog-buy-*` ever renders for a gated card.
  - Unit tests fail if the lock overlay or explainer selectors disappear.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add a Playwright smoke test for the in-context explainer in Catalog</name>
  <files>tests/catalog-disabled-explanations.spec.ts</files>
  <action>
  - Create `tests/catalog-disabled-explanations.spec.ts`.
  - Follow the established pattern from `tests/collection-loop.spec.ts`:
    - Use `page.addInitScript` to seed `emily-idle:save` with a state that has insufficient cash + enjoyment and an empty `discoveredCatalogEntries`.
    - Navigate to `/`, click the "Catalog" tab, and scroll to `data-testid="catalog-shop"`.
    - Filter to the classic model id `rolex-rolex-gmt-master-ii-ref-126713grnr` using the catalog search input.
  - Assertions:
    - `catalog-gate-<modelId>` is visible.
    - `catalog-buy-<modelId>` has count 0 (do not assert disabled button).
    - `catalog-lock-<modelId>` is visible.
    - Clicking `catalog-why-<modelId>` expands the explainer (`catalog-explain-<modelId>`) and shows at least one reason.
  </action>
  <verify>pnpm run test:e2e -- tests/catalog-disabled-explanations.spec.ts</verify>
  <done>
  - The new Playwright test passes and protects CAT-02/CAT-03 behaviors using stable selectors.
  </done>
</task>

</tasks>

<verification>
- `pnpm run test:unit -- tests/catalog.unit.test.tsx`
- `pnpm run test:e2e -- tests/catalog-disabled-explanations.spec.ts`
</verification>

<success_criteria>
- Tests enforce that `catalog-buy-*` only appears when purchase is possible and that gated cards render an explainer.
- Tests enforce that undiscovered cards expose a stable lock overlay selector.
</success_criteria>

<output>
After completion, create `.planning/phases/38-catalog-lock-disabled-explanations/38-03-SUMMARY.md`
</output>
