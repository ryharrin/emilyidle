---
phase: 58-catalog-control-density-affordability-signals
plan: 1
type: execute
depends_on:
  - 57-session-policy-and-guidance-cleanup
files_modified:
  - src/ui/tabs/CatalogTab.tsx
  - src/ui/navigation/tabReadiness.ts
  - tests/catalog.unit.test.tsx
  - tests/catalog-actionable-visual.spec.ts
  - tests/tabs.spec.ts
  - tests/selectors-contract.spec.ts
---

<objective>
Close Phase 58 by making catalog affordability cues and navigation readiness cues truthful,
scannable, and contract-tested.
</objective>

<tasks>
- FILTER-02 closure: verify and retain collapsed filter toggle + active-count badge as default control model.
- CATALOG-11 closure: scope affordable card highlight treatment to discovered, unowned, buyable references.
- NAV-01 closure: compute catalog readiness from true new-watch affordability (discovered + unowned + unlocked + affordable).
- Add deterministic unit/e2e guardrails for highlight and readiness behaviors.
</tasks>

<acceptance_criteria>
1. Catalog filters remain collapsed by default (non-test) and expose active filter count.
2. Affordable highlight appears on buyable new-watch cards and does not leak onto non-actionable cards.
3. `tab-ready-catalog` appears only when at least one discovered unowned model is actually affordable.
4. Existing navigation and selector contracts remain stable.
</acceptance_criteria>

<verification>
- `pnpm exec vitest run --config vitest.config.ts tests/catalog.unit.test.tsx -t "catalog purchase CTA"`
- `pnpm exec playwright test --project=chromium tests/catalog-actionable-visual.spec.ts tests/tabs.spec.ts tests/selectors-contract.spec.ts`
- `pnpm -s lint`
- `pnpm -s typecheck`
</verification>
