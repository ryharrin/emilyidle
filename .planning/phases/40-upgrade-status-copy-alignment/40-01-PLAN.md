---
phase: 40-upgrade-status-copy-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/catalog/upgradeContext.ts
  - src/ui/tabs/CatalogTab.tsx
  - tests/catalog.unit.test.tsx
  - tests/catalog-buy-buttons.spec.ts
autonomous: true

must_haves:
  truths:
    - "While shopping in Catalog, the player can see a read-only summary of their upgrade status"
    - "Catalog continues to show the existing collection context pill without selector changes"
  artifacts:
    - path: src/ui/catalog/upgradeContext.ts
      provides: "Pure derived upgrade status summary for Catalog"
      exports: [getCatalogUpgradeContext]
    - path: src/ui/tabs/CatalogTab.tsx
      provides: "Catalog header renders upgrade status in the shopping context"
      contains: "data-testid=\"catalog-upgrade-context\""
    - path: tests/catalog.unit.test.tsx
      provides: "Unit coverage for Catalog header upgrade context"
    - path: tests/catalog-buy-buttons.spec.ts
      provides: "E2E coverage that Catalog upgrade context pill renders"
  key_links:
    - from: src/ui/tabs/CatalogTab.tsx
      to: src/ui/catalog/upgradeContext.ts
      via: getCatalogUpgradeContext(state)
      pattern: "getCatalogUpgradeContext\\(state\\)"
    - from: tests/catalog-buy-buttons.spec.ts
      to: src/ui/tabs/CatalogTab.tsx
      via: data-testid selector
      pattern: "catalog-upgrade-context"
---

<objective>
Add a read-only upgrade status summary to the Catalog shopping header so players can see upgrade progress while browsing watches.

Purpose: Satisfy VLT-03 by surfacing upgrade status in the shopping context without adding a new purchase surface.
Output: A new Catalog-derived upgrade context helper, a new Catalog header element with a stable test id, and regression coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-upgrade-status-copy-alignment/40-RESEARCH.md

@src/ui/catalog/collectionContext.ts
@src/ui/tabs/CatalogTab.tsx
@tests/catalog.unit.test.tsx
@tests/catalog-buy-buttons.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a Catalog upgrade context helper</name>
  <files>src/ui/catalog/upgradeContext.ts</files>
  <action>
Create a pure helper module mirroring the existing `src/ui/catalog/collectionContext.ts` pattern.

- Implement `getCatalogUpgradeContext(state: GameState)` that derives a compact summary from existing selectors/definitions (do not hand-roll math):
  - total upgrade levels across `state.upgrades` using `getUpgrades()` definitions
  - workshop upgrades owned vs total using `getWorkshopUpgrades()` and `state.workshopUpgrades`
  - maison upgrades owned vs total using `getMaisonUpgrades()` and `state.maisonUpgrades`
  - maison lines active vs total using `getMaisonLines()` and `state.maisonLines`
- Keep the function pure (no Date.now, no localStorage access).
- Export only the helper (and an internal type if needed) to keep the module small.

Important: This is a read-only surface; do not introduce any new buy actions or gating logic.
  </action>
  <verify>pnpm run typecheck</verify>
  <done>`src/ui/catalog/upgradeContext.ts` exists and returns a stable summary from existing selectors without side effects.</done>
</task>

<task type="auto">
  <name>Task 2: Render upgrade status in the Catalog header without changing existing selectors</name>
  <files>src/ui/tabs/CatalogTab.tsx</files>
  <action>
Update the Catalog header actions area (where `data-testid="catalog-collection-context"` lives) to include a new, adjacent, read-only upgrade status element.

- Import `getCatalogUpgradeContext` from `src/ui/catalog/upgradeContext.ts`.
- Derive the upgrade summary from `state` and render it in a new element with `data-testid="catalog-upgrade-context"`.
- Keep `data-testid="catalog-collection-context"` unchanged and preserve its content structure.
- Display should be concise and status-only (e.g. upgrade levels + owned counts); do not add any new purchase buttons in Catalog.
- If adding a navigation affordance, it must be a simple link/button that routes to the existing Upgrades tab via `onNavigate("upgrades")` (no new purchase UI).

Do not change any localStorage keys, ids, or existing testids.
  </action>
  <verify>pnpm run test:unit -- tests/catalog.unit.test.tsx</verify>
  <done>Catalog header shows a stable `catalog-upgrade-context` element while shopping, with no selector regressions.</done>
</task>

<task type="auto">
  <name>Task 3: Add unit + e2e regression coverage for the upgrade status pill</name>
  <files>tests/catalog.unit.test.tsx, tests/catalog-buy-buttons.spec.ts</files>
  <action>
Extend existing coverage that already asserts the Catalog header context pill is present.

- In `tests/catalog.unit.test.tsx`, in the existing Catalog header context test, add assertions that:
  - `screen.getByTestId("catalog-upgrade-context")` exists
  - it contains a stable label like "Upgrades" (avoid asserting exact counts)
- In `tests/catalog-buy-buttons.spec.ts`, add an expectation that `page.getByTestId("catalog-upgrade-context")` is visible.

Keep existing assertions for `catalog-collection-context` intact.
  </action>
  <verify>pnpm run test:e2e -- tests/catalog-buy-buttons.spec.ts</verify>
  <done>Unit and e2e tests both fail if the `catalog-upgrade-context` element is removed or renamed.</done>
</task>

</tasks>

<verification>
- `pnpm run typecheck`
- `pnpm run test:unit -- tests/catalog.unit.test.tsx`
- `pnpm run test:e2e -- tests/catalog-buy-buttons.spec.ts`
</verification>

<success_criteria>
- Catalog shows upgrade status while shopping via `data-testid="catalog-upgrade-context"` (VLT-03)
- No changes to save/localStorage keys or existing selectors (notably `catalog-collection-context`)
</success_criteria>

<output>
After completion, create `.planning/phases/40-upgrade-status-copy-alignment/40-01-SUMMARY.md`
</output>
