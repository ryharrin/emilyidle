---
phase: 40-upgrade-status-copy-alignment
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/tabs/UpgradesTab.tsx
  - tests/upgrades-preview.unit.test.tsx
autonomous: true

must_haves:
  truths:
    - "Upgrade previews show enjoyment changes without implying cash changes when cash is unaffected"
    - "When an upgrade does not affect cash, the preview omits cash before/after lines"
  artifacts:
    - path: src/ui/tabs/UpgradesTab.tsx
      provides: "Preview details render only affected stat lines"
    - path: tests/upgrades-preview.unit.test.tsx
      provides: "Unit coverage that cash preview lines are omitted when unchanged"
  key_links:
    - from: src/ui/tabs/UpgradesTab.tsx
      to: src/game/selectors/index.ts
      via: getEffectiveCashRateCentsPerSec
      pattern: "getEffectiveCashRateCentsPerSec"
    - from: src/ui/tabs/UpgradesTab.tsx
      to: src/game/selectors/enjoyment.ts
      via: getEnjoymentRateCentsPerSec
      pattern: "getEnjoymentRateCentsPerSec"
---

<objective>
Adjust the upgrade preview UI so it only renders stat lines that actually change, preventing cash lines from implying cash multipliers.

Purpose: Satisfy UPG-02 by keeping previews truthful to the enjoyment-only accrual behavior.
Output: Conditional preview rendering + unit test coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-upgrade-status-copy-alignment/40-RESEARCH.md

@src/ui/tabs/UpgradesTab.tsx
@tests/upgrades-preview.unit.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render cash preview lines only when cash changes</name>
  <files>src/ui/tabs/UpgradesTab.tsx</files>
  <action>
Update the preview details UI to follow the existing principle used for delta chips: only show lines that can change.

- In `renderPreviewDetails`, always render enjoyment before/after lines.
- Render cash before/after lines only when `preview.beforeCash !== preview.afterCash`.
- Do not change the underlying preview computations (still use `getEffectiveCashRateCentsPerSec` and `getEnjoymentRateCentsPerSec` with the event multiplier).
- Keep existing test ids and structure of the upgrade cards intact.

Important: This is UI-only; do not change buy actions, upgrade costs, or selector behavior.
  </action>
  <verify>pnpm run test:unit -- tests/upgrades-preview.unit.test.tsx</verify>
  <done>For a typical upgrade purchase preview where cash is unaffected, the details omit cash lines and still show enjoyment before/after values.</done>
</task>

<task type="auto">
  <name>Task 2: Update unit test to assert cash lines are omitted when unchanged</name>
  <files>tests/upgrades-preview.unit.test.tsx</files>
  <action>
Extend the existing unit test to lock in the new preview behavior.

- After rendering the first upgrade card, assert that no preview line starting with "Cash " is present in that card when `currentEventMultiplier={1}`.
- Keep the existing assertions that:
  - an enjoyment delta chip exists
  - there is no cash delta chip
  - the before/after enjoyment preview values differ

Keep the test robust: avoid asserting exact numeric values.
  </action>
  <verify>pnpm run test:unit -- tests/upgrades-preview.unit.test.tsx</verify>
  <done>The test fails if cash preview lines are rendered when cash is unchanged.</done>
</task>

</tasks>

<verification>
- `pnpm run test:unit -- tests/upgrades-preview.unit.test.tsx`
</verification>

<success_criteria>
- Upgrade preview details only show stat lines that actually change, preventing misleading cash implications (UPG-02)
</success_criteria>

<output>
After completion, create `.planning/phases/40-upgrade-status-copy-alignment/40-03-SUMMARY.md`
</output>
