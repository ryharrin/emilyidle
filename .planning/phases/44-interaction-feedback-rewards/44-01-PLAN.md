---
phase: 44-interaction-feedback-rewards
plan: 1
type: execute
wave: 1
  - 43-01
files_modified:
  - src/ui/components/WindingMiniGameModal.tsx
  - src/ui/components/QuartzMiniGameModal.tsx
  - src/ui/components/AutomaticMiniGameModal.tsx
  - src/style.css
  - tests/winding-bands.unit.test.ts
  - tests/quartz-outcome.unit.test.ts
  - tests/automatic-minigame.unit.test.ts
autonomous: true

must_haves:
  truths:
    - "Each mini-game (winding, quartz time-setting, automatic runs) reports a clear success or failure outcome immediately after the player stops interacting and never shows outcome hints while the animation is running."
    - "Rewards scale with precision and tier so better timing yields higher cash/enjoyment, and the UI highlights the tier earned (Miss/Good/Perfect) for every interaction."
    - "The modal copy, live readouts, and CSS reinforces the scaled rewards without introducing regressions in reduced-motion or touch targets."
  artifacts:
    - path: "src/ui/components/WindingMiniGameModal.tsx"
      provides: "Telemetry-driven crown + modal that shows tiered outcomes"
    - path: "src/ui/components/QuartzMiniGameModal.tsx"
      provides: "Target time prompt + outcome messaging"
    - path: "src/ui/components/AutomaticMiniGameModal.tsx"
      provides: "Automatic interaction outcome + reward preview"
    - path: "src/style.css"
      provides: "Consistent outcome styling + reduced-motion fallbacks"
  key_links:
    - from: "src/ui/components/WindingMiniGameModal.tsx"
      to: "src/style.css"
      via: "outcome visual states"
      pattern: "winding-outcome"
    - from: "src/ui/components/QuartzMiniGameModal.tsx"
      to: "tests/quartz-outcome.unit.test.ts"
      via: "outcome math + helper exports"
      pattern: "getOutcomeTier"
    - from: "src/ui/components/AutomaticMiniGameModal.tsx"
      to: "tests/automatic-minigame.unit.test.ts"
      via: "reward expectations"
      pattern: "automatic"

<objective>
Ensure every mini-game interaction communicates its success/failure clearly, ties rewards to timing precision, and keeps reduced-motion/touch targets intact so players learn to scale their engagement across Winding, Quartz, and Automatic flows.

Purpose: Deliver Phase 44 goals (consistent outcomes and tier-scaled rewards) by tying UI copy/testing/karma to the underlying math without regressions.
Output: Updated modals/animations, reward math adjustments, and regression tests covering the new feedback.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/ui/components/winding/useWindingRun.ts
@src/ui/components/WindingMiniGameModal.tsx
@src/ui/components/QuartzMiniGameModal.tsx
@src/ui/components/AutomaticMiniGameModal.tsx
@tests/winding-bands.unit.test.ts
@tests/quartz-outcome.unit.test.ts
@tests/automatic-minigame.unit.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock outcome visibility until the player finishes each run</name>
  <files>
src/ui/components/WindingMiniGameModal.tsx
src/ui/components/QuartzMiniGameModal.tsx
src/ui/components/AutomaticMiniGameModal.tsx
  </files>
  <action>
- Ensure each modal keeps the outcome node hidden during the animation and only renders it once the user taps the stop/action control.
- Add live-region copy (e.g., "Keep going..." vs "Stopped at ...") so players clearly see the difference between running and resolved states.
- Keep reduced-motion classes untouched and maintain >=44px touch targets for all primary buttons.
  </action>
  <verify>
pnpm test:unit -- tests/quartz-outcome.unit.test.ts tests/automatic-minigame.unit.test.ts
  </verify>
  <done>
- Outcome containers remain absent until completion in all three modals, and live-region text updates accordingly.
- Reduced-motion and touch targets still meet accessibility requirements.
  </done>
</task>

<task type="auto">
  <name>Task 2: Scale rewards/tier copy with precision across interactions</name>
  <files>
src/ui/components/WindingMiniGameModal.tsx
src/ui/components/QuartzMiniGameModal.tsx
src/ui/components/AutomaticMiniGameModal.tsx
src/style.css
  </files>
  <action>
- Adjust the reward messaging and CSS (chip colors, border glows) so the earned tier (Miss/Good/Perfect) is obvious right after outcome appears.
- Emphasize timing precision in copy (e.g., "Perfect timing pays 2x" vs "Good hits reward stable cash").
- Keep the reward math (cents/enjoyment) consistent with the tier names used in tests.
  </action>
  <verify>
pnpm test:unit -- tests/quartz-outcome.unit.test.ts tests/winding-bands.unit.test.ts tests/automatic-minigame.unit.test.ts
  </verify>
  <done>
- All three modals show reward copy tied to Perfect/Good/Miss.
- CSS states (e.g., `.quartz-outcome-perfect`) highlight the earned tier.
- Reward math remains deterministic and test-covered.
  </done>
</task>

<task type="auto">
  <name>Task 3: Expand regression coverage across mini-games</name>
  <files>
tests/winding-bands.unit.test.ts
tests/quartz-outcome.unit.test.ts
tests/automatic-minigame.unit.test.ts
  </files>
  <action>
- Confirm the tests assert that tier boundaries align with the new messaging and that `automatic` rewards respond to precision.
- Add assertions that outcomes only appear after stopping (no pre-emptive `winding-outcome`/`quartz-outcome`).
- Ensure tests fail if the wrong tier is shown for a given progress value.
  </action>
  <verify>
pnpm test:unit -- tests/winding-bands.unit.test.ts tests/quartz-outcome.unit.test.ts tests/automatic-minigame.unit.test.ts
  </verify>
  <done>
- Tests guard outcome visibility and reward messaging for all mini-games.
- Running the suite catches any drift in tier thresholds or copy.
  </done>
</task>

</tasks>

<verification>
- `pnpm test:unit -- tests/winding-bands.unit.test.ts tests/quartz-outcome.unit.test.ts tests/automatic-minigame.unit.test.ts`
</verification>

<success_criteria>
- Outcomes only appear after the player stops each interaction.
- Tier reward copy/visuals align with precision and remain accessible.
- Regression tests cover outcome visibility and reward scaling for winding, quartz, and automatic flows.
</success_criteria>

<output>
After completion, create `.planning/phases/44-interaction-feedback-rewards/44-01-SUMMARY.md`
</output>
