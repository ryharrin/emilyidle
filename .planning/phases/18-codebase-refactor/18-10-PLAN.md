---
phase: 18-codebase-refactor
plan: 10
type: execute
wave: 5
depends_on: ["18-03"]
files_modified:
  - src/game/model/state.ts
autonomous: true

must_haves:
  truths:
    - "Lint passes without unused type/import errors"
    - "No gameplay or persistence behavior changes from cleanup"
  artifacts:
    - path: "src/game/model/state.ts"
      provides: "State construction helpers with no unused imports"
  key_links:
    - from: "src/game/model/state.ts"
      to: "src/game/model/types.ts"
      via: "type import"
      pattern: "from\\s+\"\\./types\""
---

<objective>
Resolve the lint failure in `src/game/model/state.ts` caused by unused type imports.

Purpose: Unblock Phase 18 gate checks without changing runtime behavior.
Output: Lint-clean state model file.
</objective>

<execution_context>
@~/.opencode/get-shit-done/workflows/execute-plan.md
@~/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/18-codebase-refactor/18-CONTEXT.md
@.planning/phases/18-codebase-refactor/18-RESEARCH.md

@src/game/model/state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused type imports in src/game/model/state.ts</name>
  <files>
src/game/model/state.ts
  </files>
  <action>
- Remove unused type imports (or use them if required) in `src/game/model/state.ts`.
- Keep runtime behavior identical; do not change state construction logic.
  </action>
  <verify>pnpm run lint</verify>
  <done>
- `pnpm run lint` succeeds with no unused-vars errors.
  </done>
</task>

</tasks>

<verification>
- `pnpm run lint`
</verification>

<success_criteria>
- Lint passes without unused type/import errors in the model layer.
</success_criteria>

<output>
After completion, create `.planning/phases/18-codebase-refactor/18-10-SUMMARY.md`
</output>
