---
phase: 37-catalog-purchase-surface
plan: 01
type: execute
wave: 1
depends_on:
  - 36-01
files_modified:
  - src/App.tsx
  - src/ui/navigation/landing.ts
  - src/ui/tabs/CollectionTab.tsx
  - src/ui/tabs/CatalogTab.tsx
  - src/ui/help/helpContent.ts
  - tests/catalog.unit.test.tsx
  - tests/phase35-uat.spec.ts
autonomous: true

must_haves:
  truths:
    - "Player can open the Catalog tab and buy a watch directly from a catalog card."
    - "Vault/Collection tab no longer renders a buyable catalog grid (no purchase buttons outside Catalog)."
    - "Any 'Buy watches' CTA navigates to Catalog and scrolls to the catalog purchase grid via the 'catalog-shop' anchor."
  artifacts:
    - path: src/App.tsx
      provides: "Primary navigation includes visible 'Catalog' tab and renders <CatalogTab>."
    - path: src/ui/tabs/CollectionTab.tsx
      provides: "Does not render <CatalogPurchasePanel> or any catalog buy buttons."
    - path: src/ui/tabs/CatalogTab.tsx
      provides: "Owns the purchase surface and contains an element with id/data-testid 'catalog-shop'."
    - path: src/ui/navigation/landing.ts
      provides: "Deep link '?tab=catalog' resolves to the Catalog tab (not Vault)."
    - path: tests/catalog.unit.test.tsx
      provides: "Unit tests reflect Catalog tab visibility and CTA scroll behavior."
    - path: tests/phase35-uat.spec.ts
      provides: "E2E asserts purchase surface location and copy expectations after consolidation."
  key_links:
    - from: src/ui/tabs/CollectionTab.tsx
      to: src/App.tsx
      via: "onNavigate('catalog', 'catalog-shop')"
      pattern: "onNavigate\\(\\\"catalog\\\",\\s*\\\"catalog-shop\\\"\\)"
    - from: src/App.tsx
      to: "#catalog-shop"
      via: "navigateTo() scroll + buy-button special-case"
      pattern: "scrollTargetId === \\\"catalog-shop\\\""
    - from: src/ui/navigation/landing.ts
      to: "Catalog tab selection"
      via: "resolveTabAlias"
      pattern: "resolveTabAlias\\(value\\)"
---

<objective>
Make catalog cards the sole watch purchase flow by promoting the Catalog tab as the shop surface and removing the embedded Vault/Collection shop panel.

Purpose: Satisfy CAT-01 by removing duplicate purchase entry points while keeping navigation/help affordances stable.
Output: Catalog tab is visible and contains the purchase grid; Vault tab no longer contains a purchase grid; tests and help copy match the new surface.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/37-catalog-purchase-surface/37-RESEARCH.md

@src/App.tsx
@src/ui/navigation/landing.ts
@src/ui/tabs/CatalogTab.tsx
@src/ui/tabs/CollectionTab.tsx
@src/ui/help/helpContent.ts
@tests/catalog.unit.test.tsx
@tests/phase35-uat.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire a visible Catalog tab and enable catalog deep links</name>
  <files>src/App.tsx, src/ui/navigation/landing.ts</files>
  <action>
  - In `src/App.tsx`, add the Catalog tab to primary navigation (`TAB_DEFINITIONS`) with label "Catalog".
  - In `src/App.tsx`, set `tabVisibility.catalog` and `combinedTabVisibility.catalog` to `true` so the tab can render and be selected.
  - In `src/App.tsx`, render `CatalogTab` as a first-class tabpanel (parallel to `CollectionTab`, `CareerTab`, etc.) and pass the same catalog filter + entry props already computed for `CollectionTab`.
  - Preserve stable selectors: do not rename `data-testid="catalog-card"`, `data-testid={\`catalog-buy-${id}\`}`, or `data-testid={\`catalog-gate-${id}\`}`.
  - In `src/ui/navigation/landing.ts`, update `resolveTabAlias("catalog")` to return `"catalog"` so `/?tab=catalog` opens the Catalog tab now that it is visible.
  </action>
  <verify>pnpm run typecheck</verify>
  <done>
  - "Catalog" appears in the primary tablist and selecting it shows a visible `role="tabpanel"` for Catalog.
  - Visiting `/?tab=catalog` selects the Catalog tab on load (when visible).
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove the embedded Vault shop purchase surface and move the catalog-shop anchor to Catalog</name>
  <files>src/ui/tabs/CollectionTab.tsx, src/ui/tabs/CatalogTab.tsx, src/App.tsx</files>
  <action>
  - In `src/ui/tabs/CollectionTab.tsx`, remove the embedded purchase surface `CatalogPurchasePanel` section that currently owns `id/data-testid="catalog-shop"`.
  - Replace that removed section with a non-purchase callout (no buy buttons) that directs players to the Catalog tab to buy watches (use `onNavigate("catalog", "catalog-shop")`).
  - Ensure there is exactly one `id="catalog-shop"` in the DOM after this change. The anchor should live in the Catalog purchase surface (in `src/ui/tabs/CatalogTab.tsx`).
  - In `src/ui/tabs/CollectionTab.tsx`, update all "Buy watches" CTAs that currently navigate to `("collection", "catalog-shop")` so they navigate to the Catalog tab instead.
  - In `src/ui/tabs/CatalogTab.tsx`, ensure the purchase grid section includes `id="catalog-shop"` and `data-testid="catalog-shop"` so existing scroll logic can still scroll to the first buy button.
  - In `src/App.tsx`, keep the existing `navigateTo()` special-case for `scrollTargetId === "catalog-shop"` so CTA scroll continues to prefer the first `[data-testid^="catalog-buy-"]` inside the anchor.
  </action>
  <verify>pnpm run typecheck</verify>
  <done>
  - Vault/Collection tab no longer renders `CatalogPurchasePanel`.
  - Clicking any "Buy watches" CTA navigates to the Catalog tab and scrolls the purchase grid into view.
  - The Catalog tab contains the unique `catalog-shop` anchor.
  </done>
</task>

<task type="auto">
  <name>Task 3: Align help copy and tests with Catalog-as-shop purchase surface</name>
  <files>src/ui/help/helpContent.ts, tests/catalog.unit.test.tsx, tests/phase35-uat.spec.ts</files>
  <action>
  - In `src/ui/help/helpContent.ts`, update any sections that say "Shop lives inside the Vault tab" or "buy watches from the Shop in Vault" to reflect that Catalog is now the primary purchase surface.
  - Keep help section IDs stable (do not rename `HELP_SECTION_IDS.catalogShop` / `HELP_SECTION_IDS.catalogFirst`).
  - In `tests/catalog.unit.test.tsx`, update navigation expectations:
    - Fresh save should now include a visible Catalog tab (and it should have stable `id`/`aria-controls`).
    - `/?tab=catalog` deep link should now select Catalog (and should still not persist last-tab when source is deep-link).
    - The "scrolls buy watch CTAs to the catalog shop" test should navigate to Catalog (not Vault) and still assert `scrollIntoView` is called and `#catalog-shop` exists.
  - In `tests/phase35-uat.spec.ts`, update the "Shop vs Catalog surface clarity" test so it asserts that:
    - Vault tab does NOT contain the embedded `catalog-shop` purchase panel.
    - Catalog tab DOES contain the `catalog-shop` purchase panel and buying affordances (presence of a `data-testid^='catalog-buy-'` button when affordable, or `catalog-gate-*` when not).
    - Any text assertions about "Shop in Vault" are updated to match the new surface.
  </action>
  <verify>pnpm run test:unit && pnpm run test:e2e</verify>
  <done>
  - Help modal no longer claims the shop lives inside Vault.
  - Unit tests and Playwright e2e pass with Catalog as the purchase surface.
  </done>
</task>

</tasks>

<verification>
- `pnpm run lint`
- `pnpm run typecheck`
- `pnpm run test:unit`
- `pnpm run test:e2e`
</verification>

<success_criteria>
- Catalog cards are the sole purchase flow for watches (no separate Vault/Collection purchase panel remains).
- Catalog tab is visible and contains the `catalog-shop` anchor used by CTAs/help navigation.
- All updated tests reflect the new purchase entry point.
</success_criteria>

<output>
After completion, create `.planning/phases/37-catalog-purchase-surface/37-01-SUMMARY.md`.
</output>
