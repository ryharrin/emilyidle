---
milestone: "v3.2 (Catalog/Vault Consolidation)"
audited_at: "2026-02-02T08:57:23Z"
status: "tech_debt"
scores:
  integration_wiring: 5
  e2e_flows: 5
  storage_stability: 5
  catalog_images: 5
  naming_consistency: 5
  overall: 5
gaps: []
tech_debt:
  - id: "LINT-FAIL"
    severity: "medium"
    area: "tooling"
    description: "ESLint currently fails due to pre-existing @typescript-eslint/no-unused-vars and @typescript-eslint/no-explicit-any errors (52 total) across career-map selectors/components and some Playwright tests; phases 37-41 verification passes, but repo does not currently have a green lint gate."
    evidence:
      command: "pnpm run lint"
      timestamp_utc: "2026-02-02T08:57:23Z"
      sample_files:
        - "src/game/selectors/careerStages.ts"
        - "src/ui/tabs/CollectionTab.tsx"
        - "src/ui/components/careerMap/CareerMapCanvas.tsx"
        - "tests/career-permanent-choices.spec.ts"
---

## Milestone Audit: Cross-Phase Integration (Phases 37-41)

This audit focuses on wiring and end-to-end flow continuity across phases 37-41. Phase-level verification reports were all `passed`; this checks that the features connect as a system (UI -> state -> persistence -> regression guardrails).

### Requirements / Outcomes

| Area | Requirement | Status | Evidence (wiring + tests) |
|---|---|---:|---|
| Purchase flow | Buy a watch from a Catalog card (Catalog is the sole purchase surface) | PASS | `src/ui/tabs/CatalogTab.tsx` renders `data-testid="catalog-buy-*"` only via `CatalogPurchaseGate` and calls `buyWatchModel()`; unit `tests/catalog.unit.test.tsx` and e2e `tests/catalog-buy-buttons.spec.ts` |
| Gated states | Undiscovered/disabled cards explain why purchase is unavailable | PASS | `src/ui/components/catalog/CatalogPurchaseGate.tsx` renders `catalog-gate-*` + `CatalogDisabledExplanation` (`catalog-why-*`/`catalog-explain-*`); `src/ui/tabs/CatalogTab.tsx` renders `catalog-lock-*`; e2e `tests/catalog-disabled-explanations.spec.ts` |
| Catalog header context | Collection context pill present while shopping | PASS | `src/ui/catalog/collectionContext.ts` -> `src/ui/tabs/CatalogTab.tsx` renders `data-testid="catalog-collection-context"`; unit `tests/catalog.unit.test.tsx` |
| Catalog header context | Upgrade status pill present while shopping | PASS | `src/ui/catalog/upgradeContext.ts` -> `src/ui/tabs/CatalogTab.tsx` renders `data-testid="catalog-upgrade-context"`; unit `tests/catalog.unit.test.tsx` |
| Naming | “Collection” naming used in UI copy (no player-facing “Vault”) | PASS | Phase 39 copy sweep + tests; remaining `vault-*` tokens are persisted ids/testids (expected) per `.planning/phases/39-collection-info-embedded-in-catalog/39-VERIFICATION.md` |
| Storage stability | localStorage keys preserved; save compat and legacy migration preserved | PASS | `src/game/persistence.ts` keeps `emily-idle:save` + migrates `watch-idle:save`; key-string contract `tests/localstorage-keys.unit.test.ts`; migration contract `tests/persistence-compat.unit.test.ts`; schema boot `tests/localstorage-schema.unit.test.tsx` |
| Catalog images | Base-aware mapping and actual rendering under deployed base path | PASS | `src/game/catalog.ts` maps to `${import.meta.env.BASE_URL}catalog/` via `getCatalogImageUrl()`; unit `tests/catalog-image-url-contract.unit.test.ts`; e2e `tests/catalog-image-rendering.spec.ts` visits `/emilyidle/` and asserts image `naturalWidth > 0` |

### Phase Integration Table (37-41)

| Phase | Goal | Cross-phase connections verified | Status |
|---:|---|---|---:|
| 37 | Catalog becomes the sole purchase surface + `catalog-shop` deep link anchor | Collection/CTA -> `onNavigate("catalog","catalog-shop")` -> `src/App.tsx:navigateTo()` -> `src/ui/tabs/CatalogTab.tsx#catalog-shop` -> `catalog-buy-*` | PASS |
| 38 | Add lock overlay + inline disabled explanations without breaking selectors | `src/ui/tabs/CatalogTab.tsx` -> `CatalogPurchaseGate` -> `CatalogDisabledExplanation` + stable testids (`catalog-lock-*`, `catalog-gate-*`, `catalog-why-*`) | PASS |
| 39 | Add Catalog collection context and standardize “Collection” naming | `src/ui/tabs/CatalogTab.tsx` imports `getCatalogCollectionContext()` and renders `catalog-collection-context`; UI/help/domain strings updated without id changes | PASS |
| 40 | Add Catalog upgrade status and align upgrade copy/previews with enjoyment-only economy | `src/ui/tabs/CatalogTab.tsx` imports `getCatalogUpgradeContext()` and renders `catalog-upgrade-context`; `UpgradesTab` preview hides unchanged cash lines | PASS |
| 41 | Guardrails for storage keys, save compatibility, selector stability, and catalog images | Contract tests enforce key literals + base-aware catalog mapping; Playwright asserts key UI anchors reachable + images load under `/emilyidle/` | PASS |

### End-to-End Flow Trace (Wiring)

| Flow | End-to-end path | Result |
|---|---|---:|
| Buy from Catalog card | `CollectionTab` CTA -> `onNavigate("catalog","catalog-shop")` -> `App.navigateTo()` scroll to first `catalog-buy-*` -> `CatalogPurchaseGate` renders buy button -> `buyWatchModel()` -> `App.handlePurchase()` persists save | PASS |
| Gated catalog explanation | `CatalogTab` card (undiscovered or gated) -> `CatalogPurchaseGate` renders `catalog-gate-*` + `CatalogDisabledExplanation` -> user expands `catalog-why-*` -> reasons list renders | PASS |
| Catalog context pills | `CatalogTab` header -> `getCatalogCollectionContext(state)` + `getCatalogUpgradeContext(state)` -> `catalog-collection-context` + `catalog-upgrade-context` render | PASS |
| Storage migration | `loadSaveFromLocalStorage()` reads `emily-idle:save` else `watch-idle:save` -> decodes -> writes to new key + removes legacy key | PASS |
| Catalog images under base path | `getCatalogImageUrl()` -> `${import.meta.env.BASE_URL}catalog/<relative>` -> `<img src=...>` loads under `/emilyidle/` | PASS |

### Verification Commands Run (Audit)

```bash
pnpm run test:unit -- \
  tests/catalog.unit.test.tsx \
  tests/localstorage-keys.unit.test.ts \
  tests/localstorage-schema.unit.test.tsx \
  tests/persistence-compat.unit.test.ts \
  tests/catalog-image-url-contract.unit.test.ts \
  tests/upgrades-preview.unit.test.tsx

pnpm run test:e2e -- \
  tests/catalog-buy-buttons.spec.ts \
  tests/catalog-disabled-explanations.spec.ts \
  tests/catalog-image-rendering.spec.ts \
  tests/selectors-contract.spec.ts \
  tests/catalog-actionable-visual.spec.ts

pnpm run lint
```
